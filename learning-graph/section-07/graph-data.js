/* GENERATED FILE — do not edit.
   Source of truth: learning-graph/section-07/data/*.json
   Rebuild with: node learning-graph/scripts/build-web-data.mjs section-07 */
window.CCAF_GRAPH = {"version":1,"builtAt":"2026-08-28","subject":"Claude Code in Action","section":"Section 7 · Claude Code in Action","sectionShort":"Section 7","storageKey":"ccaf_learning_graph_07_v1","notesPath":"07-claude-code-in-action/notes.md","clusters":[{"id":"cl_foundations","name":"The loop, in Claude Code's hands","subject":"Claude Code in Action","summary":"The same agentic loop as everywhere else in this graph, but run by the CLI on your actual files and terminal: the Explore → Plan → Code → Commit workflow that gives each phase of the loop a name, and the habit of a fresh subagent reviewing the diff before it lands.","color":"#3f6b86","colorDark":"#5f9cc0","notesPart":"What Claude Code is · Explore → Plan → Code → Commit"},{"id":"cl_control_surface","name":"Matching control to risk","subject":"Claude Code in Action","summary":"The six permission modes as a risk spectrum, what Auto's classifier actually checks (intent, not correctness), when plan mode earns its keep over direct execution, and the two techniques — the interview pattern and the Explore subagent — that make plan mode's discovery phase safe to trust.","color":"#8f3f3f","colorDark":"#c67878","notesPart":"Permission modes · Course-derived operating checklist"},{"id":"cl_claudemd","name":"CLAUDE.md that Claude follows","subject":"Claude Code in Action","summary":"What CLAUDE.md actually is — guidance loaded into every prompt, not enforced configuration — the four stacking locations, @import for sharing conventions across packages, .claude/rules/ for path-scoped conventions, and the phrasing habits that keep a growing file from drowning out its own best rules.","color":"#7a4a6b","colorDark":"#a8779a","notesPart":"CLAUDE.md — the basic model · A CLAUDE.md that Claude actually follows"},{"id":"cl_mechanism","name":"The right mechanism for the rule","subject":"Claude Code in Action","summary":"The three-way split that runs through the whole section — durable context in CLAUDE.md, reusable procedures in a Skill, unskippable enforcement in a hook — plus the concrete surfaces that split maps onto: shared vs. personal slash commands, skill frontmatter, and the verification skill worth building first.","color":"#5f6b3a","colorDark":"#8ba055","notesPart":"Verification skills · Put each rule in the right mechanism"},{"id":"cl_sessions","name":"Steering a long session","subject":"Claude Code in Action","summary":"The habits that keep a multi-hour session legible: /compact with a direction versus /clear's clean slate, the rewind menu's five checkpoints, /goal and /loop for work that spans turns, and worktrees for running parallel sessions without their file changes colliding.","color":"#3d7a5a","colorDark":"#63b088","notesPart":"Steering long sessions"},{"id":"cl_hooks","name":"Hooks: deterministic enforcement","subject":"Claude Code in Action","summary":"The event lifecycle a hook can attach to, the exit-code contract that decides whether Claude even sees the objection (and the exit-1 trap that catches almost everyone once), the PreToolUse permissionDecision field, and updatedInput's whole-object replacement for rewriting a call instead of blocking it.","color":"#a84e31","colorDark":"#d4805f","notesPart":"Hooks — the deeper model"},{"id":"cl_mcp_tools","name":"MCP and tool design in the CLI","subject":"Claude Code in Action","summary":"Where an MCP server is scoped (local, user, or project via .mcp.json), the context-cost tax every configured server pays whether or not it's used, the 10%-of-context threshold that flips Claude Code into tool search mode, and why Claude Code's own toolset stays generic instead of hyper-specialized.","color":"#4a5a8a","colorDark":"#8091c4","notesPart":"MCP and Hooks — the basic model · MCP inside Claude Code"},{"id":"cl_automation","name":"Routines, headless, and plugins","subject":"Claude Code in Action","summary":"The three ways to run Claude Code without someone watching each turn — a managed Routine, a headless -p invocation, or the Agent SDK — the flags that make headless output machine-parseable, managed Code Review versus the GitHub Action, and what a plugin actually bundles.","color":"#8a5a2b","colorDark":"#c08a4d","notesPart":"Automating repeat work · GitHub Actions and Code Review · Plugins"},{"id":"cl_verification","name":"Trusting an unsupervised run","subject":"Claude Code in Action","summary":"Verifying in proportion to how little you watched, turning a promise into a gate with a Stop hook, why a cold second opinion catches what the original session rationalized past, and the seven-step evidence checklist that ties the whole section together.","color":"#2f6b6b","colorDark":"#4f9c9c","notesPart":"Trust it: verifying unsupervised runs · Verification and trust"}],"standards":[{"id":"ccaf:d1","family":"ccaf","code":"Domain 1","name":"Agentic Architecture & Orchestration","weight":0.27,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d2","family":"ccaf","code":"Domain 2","name":"Tool Design & MCP Integration","weight":0.18,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d3","family":"ccaf","code":"Domain 3","name":"Prompt Engineering & Structured Output","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d4","family":"ccaf","code":"Domain 4","name":"Claude Code Configuration & Workflows","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d5","family":"ccaf","code":"Domain 5","name":"Context Management & Reliability","weight":0.15,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf-glossary:t9","family":"ccaf-glossary","code":"t9","name":"Programmatic enforcement vs. prompt guidance","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t21","family":"ccaf-glossary","code":"t21","name":".mcp.json","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t22","family":"ccaf-glossary","code":"t22","name":"~/.claude.json","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t23","family":"ccaf-glossary","code":"t23","name":"Environment variable expansion","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t25","family":"ccaf-glossary","code":"t25","name":"Grep (built-in tool)","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t26","family":"ccaf-glossary","code":"t26","name":"Glob (built-in tool)","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t28","family":"ccaf-glossary","code":"t28","name":"CLAUDE.md hierarchy","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t29","family":"ccaf-glossary","code":"t29","name":"@import syntax","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t30","family":"ccaf-glossary","code":"t30","name":".claude/rules/","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t31","family":"ccaf-glossary","code":"t31","name":"/memory command","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t32","family":"ccaf-glossary","code":"t32","name":".claude/commands/","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t33","family":"ccaf-glossary","code":"t33","name":"~/.claude/commands/","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t34","family":"ccaf-glossary","code":"t34","name":"SKILL.md frontmatter","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t35","family":"ccaf-glossary","code":"t35","name":"context: fork","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t36","family":"ccaf-glossary","code":"t36","name":"allowed-tools (skill frontmatter)","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t37","family":"ccaf-glossary","code":"t37","name":"argument-hint","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t38","family":"ccaf-glossary","code":"t38","name":"Plan mode","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t39","family":"ccaf-glossary","code":"t39","name":"Direct execution","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t40","family":"ccaf-glossary","code":"t40","name":"Explore subagent","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t41","family":"ccaf-glossary","code":"t41","name":"Interview pattern","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t42","family":"ccaf-glossary","code":"t42","name":"-p / --print flag","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t43","family":"ccaf-glossary","code":"t43","name":"--output-format json / --json-schema","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t60","family":"ccaf-glossary","code":"t60","name":"/compact","source":"study-reference/ccaf_study_reference.md"}],"topics":[{"id":"mt_agentic_loop_cc","type":"CONCEPTUAL","cluster":"cl_foundations","name":"The agentic loop, run by the CLI","description":"Claude Code runs the same agentic loop as any other Claude-based agent, but the tools it calls act on your real files and terminal: prompt in, Claude gathers context and produces text or a tool call, the action runs, the result is checked against the original intent, and it stops or loops back.","core":true,"estMinutes":5,"evidence":["State the five-step shape of the loop from prompt to stop-or-retry","Name the three concepts (context window, permissions, fallibility) that make the loop 'agentic' rather than a single completion"],"assessmentPrompt":"What happens between the moment you submit a prompt to Claude Code and the moment it stops and waits for your next one?","standards":["ccaf:d4"],"sources":[{"label":"Notes · What Claude Code is","href":"../../07-claude-code-in-action/notes.md"}],"centrality":1,"layer":0,"x":120,"y":46,"knowledgePoints":[{"id":"kp_agentic_loop_cc_1","name":"Prompt in, verified action out","keyPrerequisites":[],"teach":"<p class='kp-key'>Prompt in → gather context and produce text or a tool call → take the action → verify against the original intent → stop or loop back.</p><p>Claude Code is the same agentic loop you'd find anywhere else Claude calls tools, except the tools that come back with results act on your actual files, terminal, and codebase — an edit really lands, a command really runs. Three things make that loop \"agentic\" rather than a single completion: a <strong>context window</strong> that's finite, so Claude has to strategically find information instead of loading everything; <strong>permissions</strong>, which ask before edits and commands by default; and the plain fact that Claude <strong>can make mistakes</strong> — the loop's own verification step, and you staying in it, are what catch those early.</p>","questions":[{"id":"q_agentic_loop_cc_1a","q":"A developer notices that after asking Claude Code to add input validation to a form handler, Claude read the handler file, added a validation check, then re-read the file and compared the change against the original request before replying 'done.' Which concept does this re-read-and-compare step demonstrate?","options":["The loop's verification phase, where the result is checked against the original intent before the loop stops or retries","The permissions system, which requires explicit approval before any file edit is allowed to proceed","Context compaction, which summarizes the conversation and discards older tool output once too much history has accumulated across the session","The hook lifecycle, where a PostToolUse hook automatically re-reads any file Claude has just edited"],"correct":0,"explain":"Re-reading the file and comparing it to the original request is the loop's verification step, checking the result against intent before deciding whether to stop or loop back. Approval gating, context summarization, and an automatic PostToolUse re-read are each a different mechanism entirely, none of which describe comparing the outcome to the original goal."},{"id":"q_agentic_loop_cc_1b","q":"Two developers argue about whether Claude Code should be described as 'an LLM that answers questions' or 'an agent.' Which fact makes 'agent' the accurate description here?","options":["It uses a larger context window than earlier chat-only models, letting it read more of the codebase per turn","It runs in a loop that gathers context, takes real actions like edits and commands, and verifies its own results against the original goal","It has been fine-tuned specifically on software engineering tasks rather than general conversation","It runs inside a terminal application instead of a web browser, which changes the interface but not the underlying request-response mechanics involved"],"correct":1,"explain":"What makes it an agent is the loop itself: gathering context, taking real actions on the environment, and verifying the outcome against the goal before stopping or continuing. Context window size, task-specific tuning, and the terminal interface are all incidental details that don't define the agentic loop."}]}],"reviewQuestions":[{"id":"r_agentic_loop_cc_1","q":"A new team member asks why Claude Code doesn't just 'read the whole codebase' before starting every task instead of exploring strategically. What in the loop's design explains this?","options":["Permission mode restricts which directories Claude is allowed to read from during exploration","Hooks intercept read calls once a certain file count is reached and automatically block any further reads for the rest of that turn","The context window is finite, so Claude searches for relevant information deliberately rather than loading everything up front","The verification step only checks the final result, so exploration depth doesn't affect correctness anyway"],"correct":2,"explain":"The context window is finite working memory, which is exactly why the loop searches strategically for relevant information instead of loading an entire codebase up front. Permission modes govern write actions rather than reads, hooks aren't a file-count limiter, and exploration depth does affect whether the loop has what it needs to verify correctly."}]},{"id":"mt_epcc_workflow","type":"PROCEDURAL","cluster":"cl_foundations","name":"Explore → Plan → Code → Commit","description":"The recommended default shape for a Claude Code task: Explore the codebase before acting, Plan because a plan is far cheaper to correct than written code, Code against explicit success criteria with a way to self-verify, then Commit behind a fresh review.","core":true,"estMinutes":6,"evidence":["Name all four phases in order and what each one buys","Explain why correcting a plan is cheaper than correcting code that already exists"],"assessmentPrompt":"Why does the workflow put Plan before Code as a separate phase rather than letting Claude plan and implement in the same pass?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Explore → Plan → Code → Commit","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.075,"layer":1,"x":330,"y":46,"knowledgePoints":[{"id":"kp_epcc_workflow_1","name":"Four phases, cheapest correction first","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>Explore, then Plan, then Code, then Commit — in that order, because a plan is far cheaper to correct than code that's already been written.</p><p><strong>Explore</strong> — let Claude investigate the codebase before acting, rather than assuming. <strong>Plan</strong> — the best place to course-correct, precisely because it happens before any code exists; revising a plan costs a conversation, revising committed code costs an undo. <strong>Code</strong> — define explicit success criteria up front, give Claude a way to verify its own work (a test suite, a tool like Claude in Chrome for UI changes), and keep validating continuously rather than only at the end. <strong>Commit</strong> — behind a fresh subagent review, with <code>/commit-push-pr</code> handling commit, push, and PR creation in one step.</p>","questions":[{"id":"q_epcc_workflow_1a","q":"A team lead insists Claude should skip straight from a one-line prompt to writing code on a multi-file refactor, since 'planning just adds a step.' What does the workflow say is lost by skipping the Plan phase specifically?","options":["The only opportunity to run the test suite, since tests can't be executed once the Code phase has already started","The chance to assign the task to a subagent, since subagent delegation is only available during the Plan phase","The step where Claude Code chooses which permission mode to run in for the rest of the session, a decision that otherwise defaults to whatever mode the terminal last used","The cheapest point to catch a wrong approach, since reviewing and revising a plan costs far less than reviewing and undoing code that's already been written"],"correct":3,"explain":"Plan is deliberately the cheapest point to course-correct: revising a proposed approach is far less costly than reviewing and undoing code that already exists. Tests remain runnable in the Code phase, subagent delegation isn't restricted to Plan, and permission-mode selection isn't what the phase is for."},{"id":"q_epcc_workflow_1b","q":"During the Code phase of a task, a developer wants to reduce how often they have to manually check whether Claude's changes actually work. Which practice does the workflow recommend for this?","options":["Defining explicit success criteria up front and giving Claude a way to verify its own work, such as a reliable test suite","Switching to Accept-edits permission mode so file changes apply without any review at all","Asking Claude to summarize its own confidence level in the change before moving on, instead of checking the result against anything concrete","Delaying all verification until the Commit phase, when the full diff can be reviewed at once"],"correct":0,"explain":"Setting explicit success criteria and giving Claude a way to check its own work — like a reliable test suite — is what reduces reliance on manual checking during Code. A permission-mode switch changes who approves edits, not whether the work is verified; a self-reported confidence level isn't evidence; and deferring all verification to Commit throws away the whole point of continuous validation during Code."},{"id":"q_epcc_workflow_1c","q":"Before making a single change, Claude Code spends several tool calls reading related files and searching for existing patterns similar to what it's about to build. Which phase of the workflow is this, and what is it meant to prevent?","options":["Plan — meant to prevent Claude from proposing an approach without first stating success criteria","Explore — meant to prevent Claude from acting on assumptions about a codebase it hasn't actually investigated","Code — meant to prevent Claude from writing tests before the implementation itself exists","Commit — meant to prevent Claude from pushing a change to the remote before a fresh subagent has had a chance to review it"],"correct":1,"explain":"Reading related files and searching for existing patterns before acting is the Explore phase, meant to prevent Claude from acting on assumptions about a codebase it hasn't actually investigated. Stating success criteria belongs to Plan, test-writing order is a Code-phase concern, and subagent review before pushing belongs to Commit."}]}],"reviewQuestions":[{"id":"r_epcc_workflow_1","q":"A developer wants Claude to verify UI changes visually rather than just by reading the code. What kind of tool does the Code phase recommend adding for this?","options":["A tool that automatically writes the commit message once the diff is finalized, summarizing the change for the pull request description","A tool that restricts which files Claude is allowed to open during the Code phase","A tool that lets Claude verify its own work directly, such as a browser-automation tool for checking UI changes","A tool that summarizes the conversation once the Code phase exceeds a certain length"],"correct":2,"explain":"The Code phase recommends giving Claude a way to verify its own work directly — for UI changes, that means a browser-automation tool that can check the result, not just read the source. A commit-message writer, a file-access restrictor, and a conversation summarizer are unrelated to self-verification of the work itself."}]},{"id":"mt_commit_review","type":"PROCEDURAL","cluster":"cl_foundations","name":"A cold review before commit","description":"Before committing, run a subagent code review in a fresh context rather than trusting the same session that just wrote the code — a fresh reviewer has no same-session bias toward believing its own work is correct. claude --from-pr <PR_NUMBER> resumes a session already linked to a specific pull request.","core":false,"estMinutes":5,"evidence":["Explain why the review subagent needs a context the implementing session did not have","Recall what --from-pr resumes"],"assessmentPrompt":"Why run the pre-commit code review in a fresh subagent rather than asking the same session that wrote the code to review it?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Explore → Plan → Code → Commit","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.05,"layer":2,"x":540,"y":46,"knowledgePoints":[{"id":"kp_commit_review_1","name":"A reviewer with no stake in the outcome","keyPrerequisites":["mt_epcc_workflow"],"teach":"<p class='kp-key'>Review with a fresh subagent, not the session that just wrote the code — it has no stake in believing its own work is already correct.</p><p>The session that just implemented a change has been reasoning its way toward that implementation for a while; asking it to review its own diff inherits all of that same-session bias. A subagent spun up specifically for review starts with the diff and nothing else, and catches what the implementing session would talk itself past. <code>/commit-push-pr</code> runs commit, push, and PR creation as one step, and can post the PR link to a configured Slack MCP server automatically. <code>claude --from-pr &lt;PR_NUMBER&gt;</code> resumes a session already linked to a specific pull request, picking back up with its full context intact.</p>","questions":[{"id":"q_commit_review_1a","q":"A developer asks the same session that just implemented a feature to also review its own diff before committing, reasoning that it has the most complete context on the change. What's the problem with this approach?","options":["The same session cannot technically read its own prior tool calls once the turn has ended, so it would effectively be reviewing the diff blind without that history","Reviewing a diff requires a different permission mode than editing files, which the same session can't switch into","Reviewing a diff requires a different permission mode than editing files, and the same session can't switch into that mode without ending its current turn first","The session has already reasoned its way toward this implementation, so it inherits the same bias toward believing the result is correct that a fresh reviewer wouldn't have"],"correct":3,"explain":"Having just reasoned its way toward the implementation, the same session carries a bias toward believing its own result is already correct — exactly what a fresh reviewer doesn't inherit. It can read its own prior tool calls, review doesn't require a different permission mode, and context cost isn't the actual problem being described."},{"id":"q_commit_review_1b","q":"A developer wants to resume work on a change that already has an open pull request, picking back up with the full context of that PR rather than starting a fresh session. What lets them do that directly?","options":["claude --from-pr <PR_NUMBER>, which resumes a session already linked to that specific pull request","claude --resume <PR_NUMBER>, which reopens the most recent session that touched that PR's branch","/commit-push-pr <PR_NUMBER>, which re-runs the commit-push-PR sequence against the existing PR","claude --checkout <PR_NUMBER>, which switches the working session's local files onto that pull request's branch"],"correct":0,"explain":"claude --from-pr <PR_NUMBER> is the command that resumes a session already linked to that specific pull request, with its context intact. --resume takes a session name rather than a PR number, /commit-push-pr is for creating a PR in the first place, and there's no --checkout flag serving this purpose."}]}],"reviewQuestions":[{"id":"r_commit_review_1","q":"A team has an MCP server for Slack configured in their CLAUDE.md. What can /commit-push-pr do as a result, beyond committing, pushing, and opening the PR?","options":["Automatically assign the PR to a reviewer based on the Slack channel's on-call rotation","Automatically post the PR link to Slack using the configured MCP server","Automatically summarize the Slack channel's recent discussion into the PR description","Automatically block the commit if the Slack channel reports an ongoing incident"],"correct":1,"explain":"With a Slack MCP server configured, /commit-push-pr can automatically post the PR link to Slack. Assigning reviewers by on-call rotation, pulling channel discussion into the PR description, and blocking commits based on incident status are not part of what this command does."}]},{"id":"mt_permission_modes_six","type":"FACTUAL","cluster":"cl_control_surface","name":"The six permission modes","description":"The six permission modes form a risk spectrum from most to least supervised: Manual (reads only), Accept edits (reads and edits run unasked), Plan (read-only, proposes without editing), Auto (accepts everything but a classifier reviews intent first), Don't ask (only pre-approved tools run, everything else auto-denied), and Bypass (skips all checks, container-only).","core":true,"estMinutes":8,"evidence":["Order all six modes by how much runs without asking","Say which mode is appropriate for an unattended CI job versus a container-isolated experiment"],"assessmentPrompt":"Put the six permission modes in order from most to least supervised, and say which one an unattended CI pipeline should run under.","standards":["ccaf:d4"],"sources":[{"label":"Notes · Permission modes — the full six","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.1,"layer":1,"x":330,"y":108,"knowledgePoints":[{"id":"kp_permission_modes_six_1","name":"Six modes, one risk spectrum","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>Manual, Accept edits, Plan, Auto, Don't ask, Bypass — ordered from most to least supervised.</p><ul><li><strong>Manual</strong> — reads only without prompting; everything else asks first.</li><li><strong>Accept edits</strong> — reads, file edits, and common filesystem bash commands run without asking; good for iterating on code you review after the fact.</li><li><strong>Plan</strong> — read-only; researches and proposes without editing.</li><li><strong>Auto</strong> — accepts everything, but a separate classifier model reviews each action's intent before it runs.</li><li><strong>Don't ask</strong> — only pre-approved tools run; everything else is auto-denied with no prompt. Built for unattended pipelines like CI or scheduled jobs.</li><li><strong>Bypass</strong> — skips all checks (<code>--dangerously-skip-permissions</code>). Only ever run inside an isolated container or VM.</li></ul><p>Shift+Tab cycles the four everyday ones: Manual, Accept edits, Plan, Auto.</p>","questions":[{"id":"q_permission_modes_six_1a","q":"A developer wants a mode where Claude can read and edit files and run everyday filesystem commands without asking each time, but the developer plans to review every change afterward before committing anything. Which mode fits?","options":["Don't ask, since it lets a fixed allowlist of pre-approved tools run without prompting for approval on each one","Accept edits, since reads, file edits, and common filesystem commands run without asking while the developer reviews afterward","Auto, since it accepts everything but a classifier still checks each action's intent first","Plan, since it lets Claude propose changes that get applied automatically the moment a human approves the proposed plan"],"correct":1,"explain":"Accept edits is exactly this: reads, file edits, and common filesystem commands proceed without asking, suited to a developer who reviews after the fact. Don't ask is for unattended pipelines with a pre-approved allowlist, Auto still runs a classifier on every action, and Plan is read-only and never applies changes itself."},{"id":"q_permission_modes_six_1b","q":"A CI pipeline needs to run Claude Code completely unattended, and only a small set of pre-approved tools should ever execute — anything else should be silently denied rather than prompting, since nobody is present to answer a prompt. Which mode is built for exactly this?","options":["Manual, since it defaults to asking before every action other than reads, which still requires someone present to answer","Accept edits, since edits run automatically without needing a person present","Don't ask, since only pre-approved tools run and everything else is auto-denied with no prompt","Bypass, since it skips every permission check entirely, unattended or not"],"correct":2,"explain":"Don't ask is built for unattended pipelines: only pre-approved tools run, and anything outside that allowlist is auto-denied instead of prompting. Manual still prompts for non-reads, Accept edits still allows anything beyond its default scope to prompt, and Bypass removes the allowlist protection entirely rather than enforcing it."}]},{"id":"kp_permission_modes_six_2","name":"Bypass is a container decision, not a convenience one","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'><code>--dangerously-skip-permissions</code> (Bypass) skips every check — which is exactly why it belongs only inside an isolated container or VM, never on a machine holding anything you'd mind losing.</p><p>Bypass and Don't ask look similar from the outside — both let a session run unattended — but they solve different problems. Don't ask still enforces an allowlist: only pre-approved tools run, everything else is silently denied. Bypass enforces nothing at all. Reaching for Bypass because a CI job is 'annoying to keep approving' is a category error — the fix for that friction is Don't ask with the right tools pre-approved, or Auto with its classifier still watching intent. Bypass is for a disposable, isolated environment where nothing sensitive is reachable even if every check is off.</p>","questions":[{"id":"q_permission_modes_six_2a","q":"A developer is frustrated that their CI pipeline keeps needing new tools pre-approved every time the job changes, and considers switching the pipeline to Bypass mode to stop the friction. What's the problem with that fix?","options":["Bypass mode is functionally identical to Don't ask, so it wouldn't actually change anything about the friction the developer is experiencing","Bypass skips every permission check entirely, and is only appropriate inside an isolated container or VM — not as a fix for allowlist friction on a shared or sensitive machine","Bypass mode requires an interactive terminal session to acknowledge its warning banner, so it can't realistically be used inside an unattended CI pipeline in the first place","Bypass mode disables the Auto-mode classifier specifically, while leaving every other permission check in the six-mode spectrum still fully active"],"correct":1,"explain":"Bypass removes every permission check, so it's appropriate only inside an isolated container or VM — not as a general fix for allowlist friction on a machine that holds anything sensitive. It is not identical to Don't ask (which still enforces an allowlist), it doesn't require an interactive terminal, and it doesn't leave other checks active — it removes all of them."},{"id":"q_permission_modes_six_2b","q":"What distinguishes Don't ask from Bypass, given that both are meant to run without a person present to approve actions?","options":["Don't ask still enforces an allowlist of pre-approved tools and denies everything else; Bypass skips every check with nothing left enforced","Don't ask is for CI pipelines specifically, while Bypass is for interactive sessions run by a single trusted developer","Don't ask requires a dedicated container or VM to run safely, while Bypass can run on any machine at all since it skips every check instantly","Don't ask only applies to file edits, while Bypass extends that same allowlist logic to cover Bash commands and MCP tool calls as well"],"correct":0,"explain":"Don't ask still enforces an allowlist — pre-approved tools run, everything else is denied — while Bypass enforces nothing at all. The container/VM requirement belongs to Bypass, not Don't ask; both modes can apply beyond just CI or just file edits, so those distinctions are backwards or incomplete."}]}],"reviewQuestions":[{"id":"r_permission_modes_six_1","q":"Which mode is read-only and produces a proposal rather than any file changes?","options":["Manual, which asks before every action beyond a read but does not stay confined to proposing without editing","Plan, which researches and proposes without editing","Accept edits, which applies file edits automatically instead of stopping at a proposal","Auto, which accepts every action once a classifier has checked its intent"],"correct":1,"explain":"Plan is the read-only mode: it researches the codebase and proposes an approach without editing anything. Manual still allows edits after asking, Accept edits applies edits automatically, and Auto accepts everything subject to a classifier check."},{"id":"r_permission_modes_six_2","q":"Why is Bypass mode specifically recommended for use only inside a container or VM?","options":["Because containers run Claude Code with a smaller default context window, reducing the blast radius of a mistake","Because Bypass mode is only technically compatible with containerized installations of Claude Code, and fails to start anywhere else","Because a container automatically reverts any file changes Claude Code makes once the session ends","Because it skips all permission checks, so any damage it could cause should be contained to a disposable environment"],"correct":3,"explain":"Bypass skips every permission check, so confining it to a disposable, isolated environment limits what any resulting mistake can actually reach. It isn't about a smaller context window, technical compatibility, or automatic reverting on session end — none of those are how the isolation actually protects anything."}]},{"id":"mt_auto_classifier","type":"CONCEPTUAL","cluster":"cl_control_surface","name":"Auto mode's classifier checks intent, not correctness","description":"In Auto mode, a separate classifier model reviews each action before it runs, checking whether it matches your intent — blocking things like production deploys, force-pushes, or piping downloaded code into a shell. It does not check correctness, so broken-but-safe code still runs; pairing Auto with a Stop hook that runs tests covers what the classifier can't.","core":true,"estMinutes":6,"evidence":["State what the Auto-mode classifier checks and what it explicitly does not check","Name the mechanism that has to cover the gap the classifier leaves"],"assessmentPrompt":"A teammate says Auto mode is safe to leave running overnight because 'the classifier catches anything dangerous.' What is the classifier actually checking, and what does that claim miss?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Permission modes — the full six","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":2,"x":540,"y":108,"knowledgePoints":[{"id":"kp_auto_classifier_1","name":"Checks intent, not correctness","keyPrerequisites":["mt_permission_modes_six"],"teach":"<p class='kp-key'>Auto's classifier checks whether an action matches your intent — not whether the resulting code is correct.</p><p>In Auto mode, every action still passes through a separate classifier model before it runs, watching for things like a production deploy, a force-push, piping downloaded code into a shell, sending sensitive data externally, or destroying session files. That's real protection against a certain class of mistake. But the classifier has no opinion on whether the code it approved actually works — broken-but-safe code sails straight through. Pairing Auto with a <strong>Stop hook that runs the tests</strong> is what covers the correctness gap the classifier was never designed to check.</p>","questions":[{"id":"q_auto_classifier_1a","q":"In Auto mode, what does the classifier model actually check before letting an action proceed?","options":["It verifies the code that was written is correct before allowing the action, in addition to checking intent","It only activates for Bash commands, leaving file edits and other tool calls to run through without any classifier review at all","It checks whether the action matches your intent — like blocking a force-push or a production deploy — but doesn't verify the code produced is actually correct","It replaces the need for a Stop hook entirely, since it already covers both the intent of an action and the correctness of the code it produces"],"correct":2,"explain":"The classifier checks whether an action matches your intent — catching things like a force-push or a production deploy — without verifying that the underlying code is correct. It doesn't additionally check correctness, it isn't limited to Bash commands, and it doesn't make a Stop hook redundant since correctness is exactly what it leaves uncovered."},{"id":"q_auto_classifier_1b","q":"A teammate says Auto mode is safe to leave running overnight because 'the classifier catches anything dangerous.' What does that claim miss?","options":["That the classifier only runs during business hours in its default configuration, so overnight actions bypass it entirely","That the classifier requires a human in the loop to confirm its judgment before any action proceeds","That the classifier's checks apply only to MCP tool calls, not to Claude Code's built-in tools","That the classifier checks intent, not correctness, so broken-but-safe code can still pass through unnoticed"],"correct":3,"explain":"The claim misses that the classifier checks intent, not correctness — code that's safe but simply wrong still passes through undetected. The classifier isn't restricted to business hours, doesn't require a human confirmation on each action, and isn't limited to MCP tool calls."}]}],"reviewQuestions":[{"id":"r_auto_classifier_1","q":"What mechanism is recommended alongside Auto mode specifically to catch broken-but-safe code the classifier lets through?","options":["A Stop hook that runs the test suite and can refuse to let the turn end on failure","A stricter CLAUDE.md instruction telling Claude to double-check its own work before finishing","Switching briefly to Manual mode at the end of each task to review the diff by eye","A PostToolUse hook that re-runs the classifier a second time after each action completes"],"correct":0,"explain":"A Stop hook that runs the tests and can refuse to end the turn on failure is what covers the correctness gap the classifier leaves. A stronger CLAUDE.md instruction is still just guidance, manually switching to Manual mode isn't a deterministic check, and re-running the same intent classifier wouldn't add a correctness check it was never designed to perform."}]},{"id":"mt_plan_vs_direct","type":"PROCEDURAL","cluster":"cl_control_surface","name":"Plan mode versus direct execution","description":"Plan mode (read-only, researches and proposes before any edit) earns its cost on large-scale changes, unfamiliar codebases, or genuine architectural decisions with multiple valid approaches. Direct execution suits a well-scoped, single-location change where the fix is already clear — entering plan mode there just adds a review step nothing needed.","core":true,"estMinutes":6,"evidence":["State the two or three conditions that make plan mode worth the extra step","Recognize a well-scoped single-file change as a direct-execution case"],"assessmentPrompt":"A developer is asked to add one null-check to a function whose signature and fix are already clear. Should that start in plan mode or direct execution, and why?","standards":["ccaf:d4","ccaf-glossary:t38","ccaf-glossary:t39"],"sources":[{"label":"Notes · Basic permission modes","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.05,"layer":2,"x":540,"y":170,"knowledgePoints":[{"id":"kp_plan_vs_direct_1","name":"Architectural decisions versus a clear, scoped fix","keyPrerequisites":["mt_permission_modes_six"],"teach":"<p class='kp-key'>Plan mode earns its cost on large-scale, unfamiliar, or genuinely multi-approach work; direct execution suits a change that's already well-scoped.</p><p>Restructuring a monolith into microservices — dozens of files, real decisions about service boundaries — is exactly what plan mode is for: explore, understand the dependencies, and design an approach before touching anything. Adding a single null-check to one function whose signature and fix are already clear doesn't need that step; direct execution goes straight to the well-understood change. The question to ask isn't 'could this change have side effects' — almost any change could — it's whether the task involves genuine architectural decisions or multiple valid approaches worth comparing first.</p>","questions":[{"id":"q_plan_vs_direct_1a","q":"A developer is asked to restructure a monolithic application into microservices, a change spanning dozens of files with real decisions about service boundaries and dependencies. Which approach fits, and why?","options":["Direct execution, because incremental changes will reveal the natural service boundaries as implementation proceeds","Plan mode, because the task involves large-scale changes and architectural decisions that are worth exploring and comparing before anything is written","Direct execution, but only if paired with comprehensive upfront instructions specifying exactly how each individual service should be structured","Plan mode, but only after starting in direct execution and running into unexpected complexity partway through the refactor"],"correct":1,"explain":"This is large-scale, architecturally significant work with multiple valid approaches, which is exactly what plan mode is designed to explore before any code is written. Letting boundaries emerge incrementally, or writing comprehensive instructions for direct execution, skips the exploration step; and waiting to switch to plan mode only after hitting complexity gives up the chance to catch a wrong approach early."},{"id":"q_plan_vs_direct_1b","q":"A developer asks Claude Code to add a single null-check to one function whose signature, expected behavior, and fix are all already clear. Should this start in plan mode or direct execution?","options":["Plan mode, because any code change could have unexpected side effects that need exploring first, regardless of how small the change looks","Plan mode, because exploring the codebase first prevents Claude from making assumptions about dependencies","Direct execution, because the task is well-scoped with a clear fix that doesn't need architectural decisions or multi-file analysis","Direct execution first, followed by a quick plan-mode scan afterward to catch any side effects before committing"],"correct":2,"explain":"A well-scoped, single-location change with a clear fix doesn't involve architectural decisions or multi-file analysis, so direct execution is appropriate. 'Could have side effects' is true of almost any change and isn't the actual criterion; exploring dependencies first adds a step nothing here needs; and bolting a plan-mode scan on afterward isn't how the distinction is meant to be applied."}]}],"reviewQuestions":[{"id":"r_plan_vs_direct_1","q":"What's the actual criterion for choosing plan mode over direct execution, according to the workflow's own reasoning?","options":["Whether the change could theoretically have any side effects at all, since that alone justifies exploring first","Whether the developer personally feels uncertain about the codebase in question, regardless of what the change's actual scope turns out to be","Whether the change touches more than one file, regardless of how well-understood each individual file's change already is","Whether the task involves genuine architectural decisions or multiple valid approaches worth comparing before writing code"],"correct":3,"explain":"The criterion is whether the task involves real architectural decisions or multiple valid approaches worth comparing — not whether a change could theoretically have side effects, how confident the developer personally feels, or a simple file count. Almost any change clears that first bar, which is exactly why it isn't the actual test."}]},{"id":"mt_interview_pattern","type":"PROCEDURAL","cluster":"cl_control_surface","name":"The interview pattern","description":"The interview pattern asks Claude to question you about requirements, constraints, and assumptions before proposing any design — useful specifically when you suspect there are design considerations, edge cases, or tradeoffs you haven't thought to specify, such as security-sensitive or unfamiliar-architecture work.","core":false,"estMinutes":5,"evidence":["Explain what problem the interview pattern solves that a detailed upfront spec does not","Recognize a scenario (unfamiliar codebase, high-stakes design) that calls for it"],"assessmentPrompt":"Before implementing a new authentication module, a team lead wants Claude to surface security considerations and edge cases nobody on the team has thought to ask about yet. Which technique fits, and why does writing a more detailed spec not solve the same problem?","standards":["ccaf:d3","ccaf:d4","ccaf-glossary:t41"],"sources":[{"label":"Notes · Course-derived operating checklist","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":108,"knowledgePoints":[{"id":"kp_interview_pattern_1","name":"Questions surface what a spec can't","keyPrerequisites":["mt_plan_vs_direct"],"teach":"<p class='kp-key'>The interview pattern has Claude question you about requirements, constraints, and assumptions before proposing any design — for exactly the considerations you didn't think to specify.</p><p>A detailed spec only covers what you already know to write down; it can't surface a tradeoff you never thought of. The interview pattern flips the direction: instead of Claude waiting for a complete brief, Claude asks the questions, drawing out edge cases, security considerations, and constraints the developer may not have anticipated before any implementation begins. It's most valuable exactly where the stakes of missing something are highest — a new authentication module, an unfamiliar service whose original authors are long gone — not for routine, well-understood changes.</p>","questions":[{"id":"q_interview_pattern_1a","q":"Before implementing a new authentication module, a team lead wants Claude to surface security considerations and edge cases the team hasn't thought to ask about. Why would writing a more detailed specification first not solve the same problem?","options":["A spec only documents what the team already knows to write down; it can't surface a consideration nobody on the team has thought of yet","A spec takes longer to write than running the interview pattern would, so it's a worse use of the team's time regardless of what it covers","A spec is only usable for direct execution tasks, and authentication work always requires plan mode instead","A spec would need to be reviewed by Claude before implementation anyway, adding an identical step to the interview pattern"],"correct":0,"explain":"A spec can only capture what the team already knows to write down, so it can't surface a consideration nobody has thought of — which is exactly the gap the interview pattern is designed to close by having Claude ask the questions instead. The time-cost comparison, the direct-execution restriction, and the claim that reviewing a spec is identical to the interview pattern are all beside the actual point."},{"id":"q_interview_pattern_1b","q":"A developer is adding a caching layer to a service written by engineers no longer on the team, whose lifecycle and dependency-injection patterns are unfamiliar. Which technique surfaces the design considerations the developer doesn't know to ask about, before any code is written?","options":["Implementing a generic caching layer using standard framework patterns, then reviewing the output for compatibility issues","The interview pattern: asking Claude to question the developer about the service's architecture and constraints before proposing a design","Providing the entry-point files and asking Claude to generate a design document unprompted for the developer to review afterward","Starting with a minimal proof-of-concept for one endpoint and iterating based on whatever failures show up during testing"],"correct":1,"explain":"The interview pattern is designed for exactly this: Claude questions the developer about the service's architecture and constraints before any design is proposed, surfacing what the developer doesn't know to specify. Implementing generically and reviewing afterward, having Claude generate a design document unprompted, and iterating from a proof-of-concept all still depend on considerations the developer would have to have already thought to ask about."}]}],"reviewQuestions":[{"id":"r_interview_pattern_1","q":"The interview pattern is described as most valuable in high-stakes or unfamiliar situations. Why isn't it the default technique for every task, including routine ones?","options":["It only functions correctly when paired with Plan mode, and most routine tasks run in direct execution instead","It requires a dedicated skill to be installed and configured first, unlike plan mode, which is available by default in every session","Routine, well-understood changes don't have hidden considerations worth surfacing, so the extra question-asking step adds cost without benefit","It can only be invoked through the /agents interface, which routine single-file changes don't typically need to go through at all"],"correct":2,"explain":"Routine, well-understood changes generally don't carry hidden considerations worth surfacing, so the interview pattern's extra step adds cost without much benefit there. It isn't dependent on plan mode, doesn't require a separate skill install, and isn't restricted to the /agents interface."}]},{"id":"mt_explore_subagent_cc","type":"PROCEDURAL","cluster":"cl_control_surface","name":"The Explore subagent isolates discovery","description":"Verbose plan-mode discovery — full file reads, search output, dependency traces — can fill the main session's context before a plan is even finished. The Explore subagent isolates that discovery in its own context window and returns a structured summary, keeping the raw output out of the main conversation entirely.","core":true,"estMinutes":5,"evidence":["Explain why Explore fixes the problem rather than just delaying it, unlike running /compact mid-exploration","State what crosses back into the main session versus what stays isolated"],"assessmentPrompt":"Plan-mode exploration of a large codebase is filling up the main session's context before the plan is finished. What technique isolates that discovery, and what does it hand back to the main session?","standards":["ccaf:d4","ccaf:d5","ccaf-glossary:t40"],"sources":[{"label":"Notes · Course-derived operating checklist","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":46,"knowledgePoints":[{"id":"kp_explore_subagent_cc_1","name":"Discovery in isolation, a summary back","keyPrerequisites":["mt_plan_vs_direct"],"teach":"<p class='kp-key'>The Explore subagent runs plan-mode discovery in its own context window and hands back a structured summary — the raw file reads and search output never touch the main session.</p><p>Verbose discovery — full file reads, search results, dependency traces — can fill a main session's context before a plan is even finished. Running <code>/compact</code> mid-exploration only delays the same problem, since the raw output already landed in context before it gets summarized. Explore sidesteps that: it performs the discovery in isolation and returns only what the main session actually needs, keeping the exploration trail out of the budget the rest of the task has to work with.</p>","questions":[{"id":"q_explore_subagent_cc_1a","q":"Plan-mode exploration of a large, unfamiliar codebase is filling the main session's context with file reads and search output before the plan is even finished. What technique isolates that discovery, and what crosses back into the main session?","options":["Running /compact partway through exploration, which discards the raw output already accumulated in context","Restricting Claude to reading only entry-point files, which limits how much the exploration can turn up","Switching to direct execution partway through, which reduces how many tool calls the exploration uses but abandons the discovery goal entirely","The Explore subagent, which performs the discovery in an isolated context window and returns a structured summary rather than the raw output"],"correct":3,"explain":"The Explore subagent performs the discovery inside its own isolated context window and hands the main session only a structured summary, keeping raw file reads and search results out of it entirely. Compacting mid-exploration acts on output that's already landed in context, restricting files to entry points just limits what exploration can find, and switching to direct execution abandons the exploration goal rather than isolating it."},{"id":"q_explore_subagent_cc_1b","q":"Why doesn't running /compact mid-exploration solve the same problem the Explore subagent solves?","options":["The raw discovery output already landed in the main session's context before /compact runs, so /compact only delays the problem rather than preventing it","/compact is disabled by default during plan mode sessions, so it isn't actually available as an option to run mid-exploration","/compact only summarizes conversation turns, not tool call results, so it wouldn't touch the discovery output at all","/compact requires explicit steering text to work well, and mid-exploration there usually isn't a natural direction yet to actually give it"],"correct":0,"explain":"By the time /compact runs, the raw discovery output has already entered the main session's context — compacting summarizes it after the fact rather than preventing it from accumulating there in the first place. /compact isn't disabled during plan mode, it does cover tool results as part of the conversation, and steering text isn't a hard requirement for it to run."}]}],"reviewQuestions":[{"id":"r_explore_subagent_cc_1","q":"What does the main session actually receive back from an Explore subagent's investigation?","options":["The complete transcript of every file read and search performed during the exploration, appended directly into the main session","A structured summary, with the raw tool results and reasoning trail kept out of the main session's context entirely","A list of file paths only, leaving the main session to re-read each one to see what changed","Nothing automatically — the developer must manually request the findings after the subagent completes"],"correct":1,"explain":"The main session receives a structured summary; the raw tool results and reasoning trail stay isolated in the subagent's own context. It doesn't hand back the full transcript, a bare list of file paths requiring re-reads, or nothing at all — the summary is returned automatically as part of the delegation."}]},{"id":"mt_claudemd_basic","type":"FACTUAL","cluster":"cl_claudemd","name":"What CLAUDE.md solves","description":"CLAUDE.md is read automatically at the start of every session and its contents are appended to the prompt — without it, Claude re-explores your codebase and re-derives assumptions from scratch each time. /init generates a starting file; a good habit is starting lean and adding a rule only once you've actually had to correct Claude on it.","core":true,"estMinutes":5,"evidence":["State what problem CLAUDE.md solves and when it gets loaded","Explain why starting with an empty or minimal file is recommended over front-loading it"],"assessmentPrompt":"What breaks, session to session, if a project has no CLAUDE.md at all — and why is 'start without one' still reasonable advice?","standards":["ccaf:d4"],"sources":[{"label":"Notes · CLAUDE.md — the basic model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.425,"layer":1,"x":330,"y":170,"knowledgePoints":[{"id":"kp_cmdbasic_1","name":"What breaks without it, and how to start","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>CLAUDE.md is read automatically every session and appended to the prompt — without it, Claude re-explores and re-derives assumptions from scratch each time.</p><p><code>/init</code> generates a starting file. The recommended habit is to start lean — empty or minimal — and add a rule only once you've actually had to correct Claude on that point, rather than guessing everything a project might need up front.</p>","questions":[{"id":"q_cmdbasic_1a","q":"A developer asks why a project bothers maintaining CLAUDE.md instead of just re-explaining conventions inside each individual prompt. What actually breaks without it?","options":["Nothing is persisted between sessions, so Claude re-explores the codebase and re-derives its assumptions about conventions from scratch each time.","Claude retains a compressed memory of the previous session automatically, so the absence of CLAUDE.md only shows up after weeks of drift.","Only the CLI's shell history is lost between sessions, while Claude's understanding of the codebase's conventions carries over normally.","Nothing changes for a single-developer project, since re-deriving conventions matters only once a second developer joins the repo."],"correct":0,"explain":"Without CLAUDE.md, nothing about a project's conventions persists between sessions, so Claude re-explores the codebase and re-derives assumptions fresh every time — CLAUDE.md closes that gap by being read automatically and appended to the prompt. Claude does not carry a compressed memory across sessions on its own, the loss affects far more than shell history, and the problem exists for a solo developer just as much as for a team."},{"id":"q_cmdbasic_1b","q":"Before starting a new project, one engineer wants to write an exhaustive CLAUDE.md covering every convention imaginable; another argues for starting with none, or a minimal file from /init. Which approach is recommended, and why?","options":["Write the exhaustive CLAUDE.md up front, since Claude only reads the file once at the very start of the project.","Start with no CLAUDE.md (or a minimal one from /init), adding a rule only once Claude has actually needed correcting on it.","Skip CLAUDE.md entirely and rely on inline code comments throughout the codebase to communicate every convention.","Write the exhaustive file up front, then delete it once the team has been correcting Claude for a few weeks."],"correct":1,"explain":"Starting lean and adding a rule only once Claude has actually needed correcting on it keeps the file compact and tied to real friction rather than guesswork; /init generates a starting point for exactly this. CLAUDE.md is read every session, not just once, so front-loading isn't about read frequency; skipping it for inline comments abandons the mechanism that gets appended to every prompt automatically; and writing exhaustively before deleting later just does the expensive mistake in reverse."}]}],"reviewQuestions":[{"id":"r_cmdbasic_1","q":"A new engineer asks why the team bothers maintaining a CLAUDE.md file instead of just re-explaining conventions in each prompt. What's the most accurate answer?","options":["CLAUDE.md exists mainly to reduce the number of tokens billed per request, since prompt text and file text are priced differently.","CLAUDE.md matters only for very large codebases; on a small project, re-explaining conventions each prompt costs about the same.","CLAUDE.md is read automatically every session and appended to the prompt, so conventions persist without being retyped each time.","CLAUDE.md is required by Claude Code before any session can start, similar to how a missing package.json blocks an npm install."],"correct":2,"explain":"CLAUDE.md is loaded automatically at the start of every session and appended to the prompt, so conventions the team has already written down don't need to be retyped each time. It isn't a billing optimization — prompt text and file text aren't priced differently — its value doesn't disappear on a small codebase, and no CLAUDE.md file is actually required for a session to start."}]},{"id":"mt_claudemd_hierarchy","type":"FACTUAL","cluster":"cl_claudemd","name":"Four locations, one stack","description":"Four CLAUDE.md locations load together and stack at launch: managed policy (org-level, cannot be excluded), user (personal, follows you across projects, never committed), project (shared with the team, checked into the repo), and local (git-ignored, personal notes for this one repo). /memory shows which files actually loaded this session.","core":true,"estMinutes":7,"evidence":["Name all four locations and which ones are version-controlled versus personal","Diagnose a 'works for me but not my teammate' report as a user-level-file symptom"],"assessmentPrompt":"A senior engineer's detailed standards live only in ~/.claude/CLAUDE.md. A new teammate clones the repo and Claude doesn't follow those standards. What's the most likely cause, and where should the standards have gone instead?","standards":["ccaf:d4","ccaf-glossary:t28","ccaf-glossary:t31"],"sources":[{"label":"Notes · A CLAUDE.md that Claude actually follows","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.075,"layer":2,"x":540,"y":232,"knowledgePoints":[{"id":"kp_hierarchy_1","name":"Four locations, stacked","keyPrerequisites":["mt_claudemd_basic"],"teach":"<p class='kp-key'>Four CLAUDE.md locations load together and stack at launch — none overrides the others, they all just add up.</p><ul><li><strong>Managed policy</strong> — org-level, controlled by the platform team, cannot be excluded.</li><li><strong>User</strong> — personal, in <code>~/.claude/CLAUDE.md</code>, follows you across every project, never committed.</li><li><strong>Project</strong> — shared with the team, checked into the repo.</li><li><strong>Local</strong> — git-ignored, personal notes for this one repo only.</li></ul><p><code>/memory</code> shows exactly which files loaded for the current session — useful when something isn't behaving as expected.</p>","questions":[{"id":"q_hierarchy_1a","q":"Of the four CLAUDE.md locations, which pair is shared with the team through version control, and which pair stays personal to each developer?","options":["Managed policy and Local are both shared with the team, while User and Project stay personal to each developer.","All four locations are personal to each developer; none of them is ever checked into version control.","User and Local are both shared with the team on clone, while Managed policy and Project stay personal to each developer.","Project is shared with the team via version control, while User and Local are personal and never committed."],"correct":3,"explain":"Project-level CLAUDE.md is checked into the repository and shared with the team on clone, while User (personal, follows you across projects) and Local (git-ignored, notes for this one repo) both stay on the individual developer's machine. Managed policy is shared too, but at the org level rather than through the project's own version control, which the other pairings get backwards."},{"id":"q_hierarchy_1b","q":"A senior engineer adds detailed coding standards to their ~/.claude/CLAUDE.md file. A new teammate clones the repo and Claude Code doesn't follow those standards for them. What's the most likely cause?","options":["The standards live in the user-level file, which is personal and never committed, so a teammate cloning the repo never receives them.","Claude Code caches CLAUDE.md the first time it's read and needs the cache manually cleared before any teammate sees new rules.","The new teammate's Claude Code version is older and doesn't yet support reading a CLAUDE.md file placed anywhere but the root.","The standards were written in a format Claude only parses when placed inside a .claude/rules/ directory instead of CLAUDE.md."],"correct":0,"explain":"~/.claude/CLAUDE.md is the user-level location — personal to that one developer's machine and never checked into version control — so cloning the repository can't hand a teammate rules written only there; moving them into the project-level file is what actually shares them. There's no CLAUDE.md cache to clear, version support for file location isn't the issue, and plain prose rules don't need to live inside .claude/rules/ to be read at all."}]},{"id":"kp_hierarchy_2","name":"Stacking, not overriding","keyPrerequisites":["mt_claudemd_basic"],"teach":"<p class='kp-key'>The four locations don't override each other — they stack. A rule in the wrong one doesn't get replaced by a better rule elsewhere; both are simply present at once.</p><p>Because everything loaded is just appended together, a rule placed in the local, git-ignored file is invisible outside that one repo — and a user-level rule is invisible to teammates no matter how the project file is written. Run <code>/memory</code> when a session's behavior doesn't match expectations, to see exactly which files actually loaded.</p>","questions":[{"id":"q_hierarchy_2a","q":"A developer wants to check exactly which CLAUDE.md and memory files loaded for a confusing session, before assuming a typo in the project file. What command shows this?","options":["/rules, which lists every .claude/rules/ file currently active for the files touched so far in the session.","/memory, which shows exactly which CLAUDE.md and memory files actually loaded for the current session.","/context, which breaks down token usage by source but does not name which configuration files were read.","/init, which regenerates a fresh CLAUDE.md and reports which of the previous file's rules were kept."],"correct":1,"explain":"/memory is the command that shows exactly which CLAUDE.md and memory files actually loaded for the session, which is the direct way to confirm whether a suspected typo or missing file is the real cause. /rules isn't a way to audit loaded memory files, /context reports token consumption rather than which files were read, and /init generates a new file rather than reporting on what already loaded."},{"id":"q_hierarchy_2b","q":"A developer writes a rule only in a repo's git-ignored local CLAUDE.md — never committed — and expects it to also silently apply to every other project they work on, the way a user-level rule would. Is that correct?","options":["Yes — any git-ignored CLAUDE.md file, regardless of which location it lives in, behaves the same as a user-level file.","Yes — 'local' and 'user' are two names Claude Code uses interchangeably for the exact same configuration location.","No — the local file is scoped to that one repository, unlike the user location, which is the one that follows you everywhere.","No — a local CLAUDE.md file is disabled by default and must be explicitly enabled once per repository before it's read."],"correct":2,"explain":"The local file is scoped to just that one repository — git-ignored personal notes for that repo specifically — while the user-level file is the one that follows a developer across every project; being git-ignored isn't what makes something 'user-level.' Local and user are two distinct locations rather than interchangeable names, and there's no separate enable step a local CLAUDE.md file needs before it's read."}]}],"reviewQuestions":[{"id":"r_hierarchy_1","q":"Which of the four CLAUDE.md locations is controlled by the platform/org team and cannot be excluded from a session, regardless of project or personal settings?","options":["Project — because it's checked into the repository, every developer effectively has to accept whatever rules it contains.","Local — because it's git-ignored, it silently persists across every clone even after the repo is deleted and re-cloned.","User — because it lives in the developer's own config folder, no project can prevent it from being loaded at launch.","Managed policy — org-level, controlled by the platform team, and cannot be excluded from any session."],"correct":3,"explain":"Managed policy is the org-level location controlled by the platform team, and it's specifically the one that cannot be excluded from a session no matter what the project or personal settings say. Project rules can technically still be worked around by a developer's own choices, Local is scoped to one repo and doesn't survive a fresh clone the way the claim describes, and User is personal preference rather than an org-enforced floor."}]},{"id":"mt_claudemd_imports","type":"FACTUAL","cluster":"cl_claudemd","name":"@import: organization, not context savings","description":"@import (e.g. @.claude/conventions/code-style.md) pulls another file's content into CLAUDE.md, expanded inline at launch — so imports help organize a large file into pieces, but do not reduce how much of it lands in context. The pattern that scales across a monorepo: package-specific CLAUDE.md files plus a root file that imports the conventions shared by all of them.","core":false,"estMinutes":5,"evidence":["State precisely what @import does and does not save (organization, not context size)","Design an import layout for shared-plus-package-specific conventions"],"assessmentPrompt":"A monorepo has five packages, each with its own linting and testing conventions, plus conventions all five share. What's the most maintainable CLAUDE.md layout, and does splitting the file with @import actually shrink what loads into context?","standards":["ccaf:d4","ccaf-glossary:t29"],"sources":[{"label":"Notes · A CLAUDE.md that Claude actually follows","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":232,"knowledgePoints":[{"id":"kp_imports_1","name":"Organization, not a smaller context","keyPrerequisites":["mt_claudemd_hierarchy"],"teach":"<p class='kp-key'>@import expands inline at launch — it buys organization, not a smaller context.</p><p><code>@.claude/conventions/code-style.md</code> pulls another file's content into CLAUDE.md at the point of the import. Claude Code expands every import before the session starts, so whatever lands in context is identical in size to having pasted the imported file's contents directly into the root file — imports only change how the source is organized on disk.</p><p>The pattern that scales across a monorepo: give each package its own CLAUDE.md for what's specific to it, and have the root file <code>@import</code> the conventions every package shares.</p>","questions":[{"id":"q_imports_1a","q":"A monorepo has five packages, each with its own linting and testing conventions, plus conventions all five share. What's the most maintainable CLAUDE.md layout?","options":["Give each of the five packages its own CLAUDE.md for what's specific to it, and have the root CLAUDE.md @import the conventions all five share.","Keep a single root CLAUDE.md and use @import to pull in nothing, relying on Claude to infer which section applies to which package.","Delete the root CLAUDE.md file entirely and duplicate the shared conventions manually into each of the five package-level files.","Move every convention, shared and package-specific alike, into one .claude/rules/ file scoped with a paths pattern per package."],"correct":0,"explain":"This layout keeps each package's own conventions local to that package while letting the root file @import the standards genuinely shared across all five — the maintainable middle ground between one giant file and total duplication. Relying on Claude to infer which section applies is the exact failure mode a growing single file runs into, deleting the root file forces manual duplication of shared rules, and folding everything into one .claude/rules/ file abandons the per-package CLAUDE.md split."},{"id":"q_imports_1b","q":"Does splitting a large CLAUDE.md into several files pulled in with @import reduce how much of it lands in a session's context, compared to one unsplit file?","options":["Yes — Claude Code only loads an imported file's content on demand, the first time a rule from it is actually needed.","No — imports are expanded inline at launch, so what lands in context is the same size as if the text had been pasted directly in.","Yes — an imported file counts toward a separate, smaller context budget than text written directly in the root CLAUDE.md.","No — imports are blocked by default and only expand into context once a developer manually approves each one, once."],"correct":1,"explain":"Imports are expanded inline at launch, so whatever an @import pulls in lands in context exactly as if it had been pasted directly into the root file — the benefit is purely organizational, not a smaller context footprint. There's no on-demand loading of imported files, no separate smaller budget they count against, and no manual per-import approval step."}]}],"reviewQuestions":[{"id":"r_imports_1","q":"A developer splits a 500-line CLAUDE.md into six topic files using @import and expects the session's context usage to drop noticeably as a result. What should actually happen?","options":["Context usage drops by roughly the fraction the developer split off into imports, since each import loads lazily.","Context usage becomes unpredictable, since imports load in a randomized order chosen fresh for every session.","Context usage stays about the same, since every import is expanded inline at launch just like the original single file.","Context usage increases, since each imported file adds its own filename and a header on top of the original content."],"correct":2,"explain":"Because every @import is expanded inline before the session starts, the total content landing in context is the same either way — splitting the file changes how it's organized on disk, not how much of it Claude actually reads. There's no lazy loading to produce a proportional drop, no randomized import order, and no meaningful per-file overhead that would make the split cost more."}]},{"id":"mt_claude_rules","type":"PROCEDURAL","cluster":"cl_claudemd","name":".claude/rules/: path-scoped conventions","description":".claude/rules/ splits conventions into topic-focused files, each with YAML frontmatter naming glob patterns (e.g. paths: [\"infra/**/*\"]) that scope when the rule loads — so Terraform conventions only enter context in a session touching .tf files, instead of sitting in every session via one long root CLAUDE.md.","core":true,"estMinutes":6,"evidence":["Explain what problem path-scoped rules solve that a single growing CLAUDE.md doesn't","Write the shape of a frontmatter path scope for a directory-specific rule"],"assessmentPrompt":"A 400-line root CLAUDE.md mixes Python, TypeScript, infrastructure, and testing rules, and Claude keeps applying the wrong ones to the wrong files. What reorganizes this so each rule set only loads for its own files?","standards":["ccaf:d4","ccaf-glossary:t30"],"sources":[{"label":"Notes · A CLAUDE.md that Claude actually follows","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":170,"knowledgePoints":[{"id":"kp_rules_1","name":"Path-scoped conventions","keyPrerequisites":["mt_claudemd_hierarchy"],"teach":"<p class='kp-key'>.claude/rules/ splits conventions into topic files, each scoped by YAML frontmatter naming glob patterns for when it should load.</p><pre class='kp-code'>---\npaths: [\"infra/**/*\"]\n---\nUse modules from the shared registry; every resource needs a Name tag.</pre><p>A file like this only enters context in a session that's touching a matching path — a Terraform rule stays out of a session working on the Python backend entirely, instead of sitting in every session the way one long root CLAUDE.md would.</p><p>The glob applies wherever a matching file lives, so <code>paths: [\"**/*.test.tsx\"]</code> catches a test file regardless of which directory it's in — no need for a rule per directory.</p>","questions":[{"id":"q_rules_1a","q":"A 400-line root CLAUDE.md mixes Python, TypeScript, infrastructure, and testing rules, and Claude keeps applying the wrong ones to the wrong files. What reorganizes this so each rule set only loads for its own files?","options":["Add inline section headers to the single file and use /memory to tell Claude which section to prioritize for the current task.","Create a separate CLAUDE.md in each top-level package directory and delete the root file entirely.","Break the file into topic files and use @import directives in the root file to pull all of them in unconditionally.","Split the content into .claude/rules/ files, each scoped with YAML frontmatter path patterns so it only loads for matching files."],"correct":3,"explain":".claude/rules/ is built for exactly this: topic-focused files with YAML frontmatter path scoping so each rule set activates only when editing files it actually applies to, cutting both the wrong-convention problem and the cost of loading everything every time. Section headers plus /memory still leaves every section loaded in every session, deleting the root file loses genuinely shared conventions, and @import still pulls every file in unconditionally regardless of what's being edited."},{"id":"q_rules_1b","q":"A team uses Terraform only in the infra/ directory and wants those conventions to load only in sessions touching .tf files, without appearing in Python or TypeScript sessions. What achieves that?","options":["A .claude/rules/terraform.md file with frontmatter paths: [\"infra/**/*\"], listing the Terraform conventions.","A CLAUDE.md file placed directly inside the infra/ directory, containing the Terraform-specific conventions.","The Terraform rules added to the root CLAUDE.md under a clearly marked heading, with a system-prompt instruction to scope them.","A skill at .claude/skills/terraform.md with allowed-tools restricted to file operations under the infra/ directory."],"correct":0,"explain":".claude/rules/ with a paths glob is the mechanism designed to conditionally load conventions by file path, so a terraform.md rule scoped to infra/**/* loads only in sessions touching that directory tree and stays out of everything else. A directory-level CLAUDE.md isn't part of the location hierarchy, a heading in the root file plus a system-prompt instruction still loads in every session, and restricting a skill's allowed-tools controls what it may call, not when its content loads."}]}],"reviewQuestions":[{"id":"r_rules_1","q":"A glob pattern in a .claude/rules/ file's frontmatter reads paths: [\"**/*.test.tsx\"]. What determines whether that rule loads for a given file?","options":["Whether the file sits inside a directory literally named test, regardless of the file's own extension.","Whether the file's path matches the pattern anywhere in the tree, regardless of which directory it happens to live in.","Whether the file was the most recently modified .tsx file in the repository at the time the session started.","Whether the file is explicitly listed by name in a manifest alongside the .claude/rules/ file itself."],"correct":1,"explain":"A glob pattern like **/*.test.tsx matches any file with that name shape anywhere in the tree, regardless of which directory it's actually in — that's the point of path-scoped rules over one CLAUDE.md per directory. It doesn't require a literal test directory, it isn't based on recency of modification, and there's no separate manifest listing files by name."}]},{"id":"mt_claudemd_phrasing","type":"PROCEDURAL","cluster":"cl_claudemd","name":"Phrasing that sticks, and emphasis as a budget","description":"CLAUDE.md is guidance competing for attention, not enforced configuration — every added line makes every other line's compliance a little less reliable. Effective phrasing is specific and checkable, names the replacement instead of just banning something ('use named exports' beats 'don't use default exports'), and treats emphasis words like IMPORTANT and YOU MUST as a small budget, not decoration.","core":false,"estMinutes":5,"evidence":["Explain why a longer CLAUDE.md tends to have lower per-rule compliance","Rewrite a vague, ban-only instruction as a specific, checkable one"],"assessmentPrompt":"Why does adding a tenth 'IMPORTANT' rule to an already-long CLAUDE.md tend to make Claude follow all ten worse, not just the new one?","standards":["ccaf:d4"],"sources":[{"label":"Notes · A CLAUDE.md that Claude actually follows","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.3,"layer":2,"x":540,"y":294,"knowledgePoints":[{"id":"kp_phrasing_1","name":"Specific, checkable, and a budget for emphasis","keyPrerequisites":["mt_claudemd_basic"],"teach":"<p class='kp-key'>CLAUDE.md is guidance competing for attention, not enforced configuration — every added line makes every other line's compliance a little less reliable.</p><p>Effective phrasing is specific and checkable, and names the replacement rather than only banning something: <code>\"Use named exports\"</code> does more work than <code>\"Don't use default exports\"</code>, because it tells Claude what to do instead of only what not to do.</p><p>Emphasis words like <strong>IMPORTANT</strong> and <strong>YOU MUST</strong> are a budget, not decoration — spend them on the two or three rules that actually matter, or none of them will stand out anymore. Treat the file as living code: a wrong Claude action is a bug report against CLAUDE.md, and the fix is editing the file, not repeating the instruction louder.</p>","questions":[{"id":"q_phrasing_1a","q":"Why does a CLAUDE.md file that keeps growing tend to have lower per-rule compliance, rather than staying just as reliable with more rules added?","options":["A longer file takes measurably longer to load at session start, so Claude has less time left to act on any single rule.","A longer file is more likely to contain a rule that directly contradicts another rule already written earlier in the file.","Every line in CLAUDE.md competes with every other line for attention, so adding more lines lowers each one's reliability.","A longer file exceeds a fixed line-count limit past which Claude Code silently stops reading the rest of the file."],"correct":2,"explain":"CLAUDE.md is guidance rather than enforced configuration, and every line added competes with every other line for the model's attention — so a longer file spreads that attention thinner and each rule becomes a little less reliably followed. It isn't a load-time issue, a longer file isn't inherently more self-contradictory, and there's no fixed line-count cutoff past which the file stops being read."},{"id":"q_phrasing_1b","q":"Which rewrite of a CLAUDE.md rule best follows the 'name the replacement, don't just ban' phrasing habit?","options":["\"IMPORTANT: never write bad code\" — marking a rule IMPORTANT is what makes an instruction specific and checkable.","\"Don't use console.log\" — a ban is inherently more checkable than a positive instruction, since it's easy to search for.","\"Write clean, idiomatic code that a senior engineer would approve of\" — it states the general standard being asked for.","\"Use the project's logger module instead of console.log\" — it names the replacement, not just the thing being banned."],"correct":3,"explain":"Naming the replacement — use logger instead of console.log — gives Claude something specific and checkable to do, not just something to avoid, which is the phrasing habit that sticks. Marking a vague rule IMPORTANT doesn't make it specific, a ban alone still leaves Claude without a positive instruction, and 'clean, idiomatic code a senior engineer would approve of' is a standard with no checkable content."}]}],"reviewQuestions":[{"id":"r_phrasing_1","q":"A CLAUDE.md file has grown to include fifteen separate rules each prefixed with 'YOU MUST.' A teammate proposes trimming that to two or three genuinely critical rules marked that way, with the rest phrased normally. What's the reasoning?","options":["Emphasis words are a budget — spreading them across fifteen rules means none of them stand out anymore.","Claude Code enforces a hard limit on how many 'YOU MUST' rules a single CLAUDE.md file is allowed to contain.","Rules marked 'YOU MUST' are checked against a separate hook before every session, so excess ones slow down startup.","Only the first three 'YOU MUST' rules in the file are ever actually read; the rest are silently truncated."],"correct":0,"explain":"Treating emphasis as a limited budget is the actual reasoning: spending it on fifteen rules dilutes all of them, while reserving it for the two or three that truly matter keeps them standing out. There's no enforced count limit on 'YOU MUST' rules, no separate hook checking them at startup, and no truncation past a fixed number — the file is guidance the model reads and weighs like anything else in it."}]},{"id":"mt_rule_placement","type":"CONCEPTUAL","cluster":"cl_mechanism","name":"The right mechanism for the rule","description":"Every rule belongs to exactly one of three mechanisms depending on how firm it needs to be: always-true project context and conventions go in CLAUDE.md (guidance), a reusable task-specific procedure becomes a Skill (loaded when relevant), and anything that must never be skippable — a hard constraint, not a preference — becomes a hook (deterministic enforcement).","core":true,"estMinutes":7,"evidence":["Sort a given rule into CLAUDE.md, Skill, or hook based on whether it can ever be skipped","Explain why a 'must never happen' rule in CLAUDE.md is a category error, not just weak phrasing"],"assessmentPrompt":"A team wants Claude to never commit directly to main. Is that a CLAUDE.md instruction, a Skill, or a hook — and what makes the other two the wrong mechanism for it?","standards":["ccaf:d4","ccaf-glossary:t9"],"sources":[{"label":"Notes · Course-derived operating checklist","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.275,"layer":3,"x":750,"y":356,"knowledgePoints":[{"id":"kp_placement_1","name":"Three mechanisms, one test","keyPrerequisites":["mt_claudemd_phrasing"],"teach":"<p class='kp-key'>Every rule belongs to exactly one of three mechanisms: CLAUDE.md for always-true context, a Skill for a reusable procedure, a hook for anything that must never be skippable.</p><p>The test is whether the rule can ever be allowed to slip: if occasionally missing it is acceptable (a style preference, background context), it belongs in CLAUDE.md, which is guidance the model usually follows. If it's a repeatable multi-step procedure worth reusing across sessions, it becomes a Skill. If it is a hard constraint — the kind where \"usually\" is not good enough — it has to be a hook, because only a hook is code that runs deterministically rather than a request the model may or may not follow.</p><p>Writing a \"must never happen\" rule into CLAUDE.md is a category error, not a phrasing problem: no amount of emphasis turns guidance into enforcement.</p>","questions":[{"id":"q_placement_1a","q":"A team wants Claude to never commit directly to main, under any circumstances. Which mechanism actually holds that rule reliably?","options":["A CLAUDE.md rule stated with strong emphasis, since 'IMPORTANT: never commit to main' is specific and checkable enough to hold reliably.","A hook, because the rule must hold on every single commit with no exceptions, which only deterministic enforcement can guarantee.","A Skill, since a reusable procedure can be written to check the current branch before every commit and refuse if it's main.","Whichever mechanism the team already has the most experience configuring, since all three enforce a rule with roughly equal reliability."],"correct":1,"explain":"'Never commit to main' has to hold on every single occurrence with zero tolerance for a skipped case, and only a hook is code that runs deterministically rather than guidance the model may or may not follow — that's what a genuine 'must never happen' rule requires. No amount of emphasis turns a CLAUDE.md instruction into enforcement, a Skill only runs when Claude decides to invoke it rather than gating every commit automatically, and the three mechanisms are not interchangeable in reliability."},{"id":"q_placement_1b","q":"A team wants Claude to prefer named exports over default exports across the codebase, but agrees an occasional miss isn't the end of the world. Where does that rule belong?","options":["As a hook, since any project convention is worth enforcing with code the moment the team agrees it should apply consistently.","As a Skill, since preferring named exports is a multi-step procedure that benefits from being triggered automatically when relevant.","As a CLAUDE.md rule, since it's a project convention that's fine to phrase as guidance rather than something that must never be violated.","As neither, since stylistic preferences like export style aren't the kind of thing any of the three mechanisms is meant to hold."],"correct":2,"explain":"'Prefer named exports over default exports' is an always-true convention where an occasional miss is tolerable, not a hard invariant — exactly the case CLAUDE.md's guidance role fits, rather than spending a hook's deterministic enforcement on something that doesn't need it. It isn't a multi-step reusable procedure that calls for a Skill, escalating it to a hook is more machinery than the rule needs, and it does belong in one of the three mechanisms — CLAUDE.md is the right one."}]}],"reviewQuestions":[{"id":"r_placement_1","q":"A team has a CLAUDE.md rule reading 'YOU MUST always run the linter before finishing a task,' phrased with the strongest emphasis the file uses, but production logs still show sessions ending without the linter having run. What does this reveal about the rule's placement?","options":["The rule needs even stronger emphasis than 'YOU MUST,' since emphasis strength is the only lever CLAUDE.md phrasing has.","The rule was placed correctly; occasional misses are an acceptable and expected part of how any CLAUDE.md rule behaves.","The rule should move into a Skill instead, since a Skill is guaranteed to run to completion once Claude decides to invoke it.","The rule is a hard invariant, not guidance, and belongs in a Stop hook that can actually refuse to end the turn without it."],"correct":3,"explain":"A rule that must hold every time — no exceptions — is exactly the category CLAUDE.md can't guarantee no matter how it's phrased, since CLAUDE.md is guidance the model usually follows rather than code that runs; the fix is a hook (a Stop hook, specifically) that can refuse to end the turn until the linter has run. There's no stronger emphasis word that closes the gap, occasional misses aren't 'acceptable' when the requirement is 'always,' and a Skill still only runs when Claude chooses to invoke it, which doesn't guarantee it either."}]},{"id":"mt_slash_commands","type":"FACTUAL","cluster":"cl_mechanism","name":"Shared versus personal slash commands","description":"Custom slash commands split the same way CLAUDE.md's locations do: .claude/commands/ in the repo is version-controlled and reaches every developer on clone or pull, while ~/.claude/commands/ is personal and never shared. A /review command meant for the whole team belongs in the project location, not a personal one.","core":true,"estMinutes":4,"evidence":["Place a shared team command and a personal command in their correct directories","Explain why a personal command directory can't satisfy a 'every developer gets this' requirement"],"assessmentPrompt":"You want a /review slash command that every developer gets automatically when they clone the repo. Which directory does it go in, and why wouldn't the personal-commands directory work?","standards":["ccaf:d4","ccaf-glossary:t32","ccaf-glossary:t33"],"sources":[{"label":"Notes · Course-derived operating checklist","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":294,"knowledgePoints":[{"id":"kp_commands_1","name":"Shared versus personal","keyPrerequisites":["mt_claudemd_hierarchy"],"teach":"<p class='kp-key'>.claude/commands/ is project-scoped and version-controlled; ~/.claude/commands/ is personal and never shared.</p><p>A command meant for the whole team — a <code>/review</code> that runs the team's standard checklist, say — goes in <code>.claude/commands/</code> inside the repository, so every developer gets it automatically on clone or pull, the same way a project-level CLAUDE.md is shared. A command that's just for one developer's own workflow goes in <code>~/.claude/commands/</code> instead, and stays invisible to everyone else.</p><p>CLAUDE.md itself is not where command definitions live — it's for context and instructions, not for defining new slash commands.</p>","questions":[{"id":"q_commands_1a","q":"You want to create a custom /review slash command that runs your team's standard code review checklist and should be available to every developer when they clone or pull the repository. Where should you create this command file?","options":["In the .claude/commands/ directory within the project repository, so it's version-controlled and available to every developer automatically.","In ~/.claude/commands/ in each developer's home directory, so every developer installs the same command manually themselves.","In the CLAUDE.md file at the project root, defined as a named section that Claude Code treats as a callable command.","In a .claude/config.json file, listed under a commands array alongside the other project-level configuration."],"correct":0,"explain":".claude/commands/ inside the repository is project-scoped and version-controlled, so every developer gets the /review command automatically the moment they clone or pull — no manual install step needed. ~/.claude/commands/ is personal and wouldn't reach anyone but the one developer who created it there, CLAUDE.md holds context and instructions rather than command definitions, and there's no .claude/config.json commands array in the actual mechanism."},{"id":"q_commands_1b","q":"A developer wants a personal /scratch command for their own workflow, one they don't want teammates to see or use. Where should that command file go?","options":["In .claude/commands/, since committing a personal command to the repo is the only way Claude Code will recognize it at all.","In ~/.claude/commands/, since a personal, non-shared command belongs in the developer's own home directory, not the repo.","In the project's CLAUDE.md, under a section marked personal so teammates know not to rely on that command themselves.","In .claude/rules/, scoped with frontmatter naming only that one developer's username as the intended audience."],"correct":1,"explain":"A personal command that isn't meant to be shared belongs in ~/.claude/commands/, the developer's own home-directory location, rather than anywhere inside the repository. Committing it to .claude/commands/ would share it with the whole team by definition, CLAUDE.md doesn't hold command definitions, and .claude/rules/ scopes CLAUDE.md-style conventions by file path, not commands by username."}]}],"reviewQuestions":[{"id":"r_commands_1","q":"A team lead wants to confirm a /scaffold command they just added will actually reach every teammate the next time they pull the repository, without anyone running a manual install step. Where must the command file live for that to hold?","options":["Anywhere under .claude/, since Claude Code treats the entire .claude/ directory as automatically shared on every pull.","In ~/.claude/commands/, since personal commands are the ones that sync automatically across a team's machines.","In .claude/commands/ inside the repository, since that specific location is version-controlled and reaches every clone.","In the root CLAUDE.md, since any file at the project root is treated as shared regardless of its actual contents."],"correct":2,"explain":".claude/commands/ inside the repository is the specific, version-controlled location that reaches every developer automatically on clone or pull — not the whole .claude/ directory generically, and not any file simply for sitting at the project root. ~/.claude/commands/ is exactly the opposite of shared: it's personal and tied to one developer's own machine."}]},{"id":"mt_skill_frontmatter","type":"FACTUAL","cluster":"cl_mechanism","name":"SKILL.md frontmatter","description":"A Skill's SKILL.md frontmatter configures how it runs: context: fork runs the skill in an isolated sub-agent context so its exploratory tool calls never accumulate in the main session, allowed-tools restricts which tools it may call, and argument-hint prompts the caller for the parameters the skill expects. A skill folder can also hold a reference.md and executable scripts Claude runs rather than reads into context.","core":true,"estMinutes":6,"evidence":["Name what context: fork, allowed-tools, and argument-hint each control","Pick the frontmatter setting that stops a skill's exploratory output from polluting the main conversation"],"assessmentPrompt":"A /scaffold skill runs hundreds of lines of exploratory reads and Bash commands before generating output, and teammates complain it pollutes their main conversation. Which frontmatter setting fixes this, and what does it actually change about where the skill runs?","standards":["ccaf:d4","ccaf-glossary:t34","ccaf-glossary:t35","ccaf-glossary:t36","ccaf-glossary:t37"],"sources":[{"label":"Notes · Course-derived operating checklist","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.05,"layer":4,"x":960,"y":46,"knowledgePoints":[{"id":"kp_frontmatter_1","name":"Three fields, three jobs","keyPrerequisites":["mt_rule_placement"],"teach":"<p class='kp-key'>SKILL.md frontmatter configures how a skill runs: context: fork isolates it, allowed-tools restricts it, argument-hint prompts for its parameters.</p><pre class='kp-code'>---\nname: scaffold\ndescription: Generate boilerplate for a new microservice\ncontext: fork\nallowed-tools: [Read, Grep, Bash]\nargument-hint: service-name\n---</pre><ul><li><code>context: fork</code> — runs the skill in an isolated sub-agent context, so its own tool calls and reasoning don't accumulate in the main session.</li><li><code>allowed-tools</code> — restricts which tools the skill may call; it controls capability, not where the skill's context lives.</li><li><code>argument-hint</code> — prompts the caller for the parameters the skill expects; it documents inputs, it doesn't isolate anything.</li></ul><p>A skill folder can also hold a <code>reference.md</code> for detail only read when needed, and executable scripts Claude runs rather than reads into context.</p>","questions":[{"id":"q_frontmatter_1a","q":"A /scaffold skill runs many exploratory file reads and Bash commands to understand existing patterns before generating output, producing hundreds of lines of intermediate output. Teammates complain this pollutes their main conversation context. Which frontmatter setting resolves this?","options":["Set allowed-tools: [] in the skill's frontmatter, which prevents the skill from calling any tool during execution.","Move the skill file from .claude/skills/ to .claude/commands/ so it runs as a command instead of a skill.","Add argument-hint: \"service-name\" so the skill receives a clean input without inheriting the main session's context.","Set context: fork in the skill's frontmatter, running it in an isolated sub-agent context that doesn't touch the main session."],"correct":3,"explain":"context: fork is the setting that runs a skill's execution in an isolated sub-agent context, so every exploratory read and Bash call it makes stays out of the main conversation entirely. Setting allowed-tools: [] would stop the skill from doing its exploratory work at all rather than isolating it, moving the file to .claude/commands/ doesn't change where its context lands, and argument-hint only shapes what parameters the caller is prompted for, not where execution happens."},{"id":"q_frontmatter_1b","q":"How does allowed-tools in a skill's frontmatter relate to whether that skill's execution is isolated from the main session?","options":["allowed-tools restricts which tools a skill may call; it has no effect on whether the skill's execution is isolated from the main session.","allowed-tools and context: fork are two different names for configuring the exact same behavior in a skill's frontmatter.","allowed-tools isolates a skill's execution into its own sub-agent context, the same effect context: fork produces on its own.","allowed-tools only takes effect once context: fork is also set; on its own it has no effect on the skill at all."],"correct":0,"explain":"allowed-tools controls which tools a skill is permitted to call — a capability restriction — and is independent of whether the skill's execution runs isolated from the main session, which is specifically what context: fork controls. They aren't two names for the same setting, allowed-tools doesn't isolate context on its own, and allowed-tools works regardless of whether context: fork is also set."}]},{"id":"kp_frontmatter_2","name":"The trap: isolation is context: fork, and nothing else","keyPrerequisites":["mt_rule_placement"],"teach":"<p class='kp-key'>When a skill's exploratory output is the complaint, the fix is always context: fork — allowed-tools and argument-hint are common wrong answers to the same question.</p><p>Both are real, useful settings, but neither touches where a skill's execution happens. A skill restricted with <code>allowed-tools</code> can still fill the main session with the reasoning and results from every call it's still permitted to make. A skill with a well-written <code>argument-hint</code> still runs its exploration inline in the main conversation unless <code>context: fork</code> is also set. The question \"which setting keeps this skill's noise out of the main session\" has exactly one answer.</p>","questions":[{"id":"q_frontmatter_2a","q":"A skill's allowed-tools is already narrowed to just Read and Grep, but teammates still complain its exploratory output fills the main session's context. What actually fixes this?","options":["Narrow allowed-tools further, down to a single tool, since fewer permitted tools means less output accumulating in context.","Add context: fork, since allowed-tools restricts capability but does nothing about where the skill's execution runs.","Remove argument-hint entirely, since a skill without a defined argument hint runs its exploration in an isolated context by default.","Rename the skill's folder, since Claude Code isolates any skill whose name matches a reserved isolation-related keyword."],"correct":1,"explain":"allowed-tools only restricts which tools a skill may call — it doesn't touch where the skill's execution and reasoning land, so even a tightly restricted skill still runs inline in the main session unless context: fork is set. Narrowing allowed-tools further doesn't change that, removing argument-hint has no effect on isolation, and there's no naming convention that triggers automatic isolation."},{"id":"q_frontmatter_2b","q":"A developer adds argument-hint: \"service-name\" to a skill hoping it will stop the skill's exploration from polluting the main session. Does this fix the problem?","options":["It should now succeed, since argument-hint's purpose is specifically to isolate a skill's exploratory reasoning from the main session.","It depends on which tools are listed in allowed-tools alongside the new argument-hint setting.","It should still fail, since argument-hint only documents the parameters a caller should provide and doesn't isolate execution.","It should succeed only the first time the skill runs after argument-hint is added, then revert to polluting context again."],"correct":2,"explain":"argument-hint's job is prompting the caller for the parameters a skill expects — it says nothing about where the skill's own tool calls and reasoning happen, so adding it doesn't touch the pollution problem at all; only context: fork does. The outcome doesn't depend on which tools allowed-tools happens to list, and there's no first-run exception where isolation briefly works before reverting."}]}],"reviewQuestions":[{"id":"r_frontmatter_1","q":"A skill folder contains SKILL.md plus a reference.md and an executable validate.sh script. How does Claude typically make use of the latter two, compared to SKILL.md itself?","options":["Both reference.md and validate.sh load into context immediately whenever the skill is invoked, exactly like SKILL.md does.","reference.md is executed as a script, while validate.sh is only read into context when its detail is actually needed.","Neither file is ever used automatically; both require the caller to reference them explicitly in the prompt each time.","reference.md is read into context only when its detail is actually needed, while validate.sh is run rather than read."],"correct":3,"explain":"A reference.md is meant for detail that's only read into context when it's actually needed, while an executable script like validate.sh is run by Claude rather than loaded as text — keeping both out of context by default is what makes a skill folder able to hold more than just SKILL.md without bloating every invocation. Loading everything immediately defeats that purpose, the roles in the second option are swapped, and both files are available for Claude to use automatically without the caller having to reference them by name."}]},{"id":"mt_verification_skill","type":"PROCEDURAL","cluster":"cl_mechanism","name":"Build a verification skill first","description":"A verification skill — one that runs the test suite, reads the diff, checks tests weren't weakened just to pass, and reports pass/fail with evidence — is the highest-leverage skill to build first, because it replaces a check you'd otherwise have to remember to ask for by hand every single time.","core":true,"estMinutes":6,"evidence":["Explain why a verification skill outranks other automation candidates for being built first","State the shape a verification skill's check should take beyond 'ran the tests'"],"assessmentPrompt":"Why does 'the code looks right' from a diff read not count as verification, and what does a verification skill add that a manual glance doesn't?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Verification skills","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":5,"x":1170,"y":46,"knowledgePoints":[{"id":"kp_verifyskill_1","name":"The highest-leverage skill to build first","keyPrerequisites":["mt_skill_frontmatter"],"teach":"<p class='kp-key'>A verification skill — run the tests, read the diff, check tests weren't weakened, report evidence — is the highest-leverage skill to build first.</p><p>Manual checking depends on remembering to ask for it every single time; a skill triggered automatically once its description matches the situation removes that dependency entirely. \"Done\" means gates ran and their results are stated explicitly — not \"the code looks right\" from a diff read alone, which is a judgment call, not evidence.</p>","questions":[{"id":"q_verifyskill_1a","q":"Why does a verification skill outrank other automation candidates for being the first skill a project builds?","options":["Because it replaces a check that otherwise depends on someone remembering to ask for it by hand, every single time.","Because it's the only kind of skill whose frontmatter is permitted to include the context: fork setting.","Because verification skills run faster than any other type of skill, regardless of what task they're built for.","Because it's a required skill; a project without one is blocked from running any other skill at all."],"correct":0,"explain":"The reason a verification skill earns first priority is that it replaces something that otherwise depends entirely on a human remembering to ask for it every time — automating that removes the weakest link in the loop. context: fork isn't restricted to verification skills specifically, there's no inherent speed advantage tied to what a skill verifies, and no skill is a hard prerequisite for others to run."},{"id":"q_verifyskill_1b","q":"A session reads its own diff and reports 'the code looks right' before finishing. Does that count as verification in the sense a verification skill is meant to provide?","options":["Yes — 'done' is reached once a diff read shows no obviously wrong-looking lines, since that's the fastest check available.","Yes — 'done' is whatever the implementing session itself reports, since it has the fullest context on what changed and why.","No — 'done' means the relevant gates actually ran and their results are stated explicitly, not inferred from how the code looks.","No — 'done' is reached the moment a pull request is opened, since opening it implies the author already checked their own work."],"correct":2,"explain":"The bar is gates run and observed with results stated explicitly — 'the code looks right' from reading the diff is a judgment call, not evidence that anything was actually verified. A clean-looking diff isn't the same as a passing test suite, trusting the implementing session's own report is exactly the same-session bias a fresh review exists to avoid, and opening a PR says nothing about whether checks were run at all."}]}],"reviewQuestions":[{"id":"r_verifyskill_1","q":"A team already has a habit of manually running tests and reading the diff before every commit, and someone questions whether building a dedicated verification skill adds anything beyond what the team already does by hand. What's the actual gain?","options":["None — a skill can only perform checks a human is already capable of running manually, so it adds no new capability.","A skill runs the checks measurably faster than a human would, which is the entire justification for building one.","The skill removes the dependency on someone remembering to run the check every time, and reports evidence explicitly instead of a glance.","The skill's main advantage is that it can approve and merge the pull request automatically once its own checks pass."],"correct":2,"explain":"The gain isn't new capability the skill alone can perform — it's removing the dependency on a human remembering to run the same check every single time, plus reporting explicit pass/fail evidence instead of relying on a glance at the diff. Raw speed isn't the stated justification, and a verification skill reporting evidence is a different thing from it having authority to approve or merge anything."}]},{"id":"mt_compact_direction","type":"PROCEDURAL","cluster":"cl_sessions","name":"/compact with a direction","description":"/compact <instructions> summarizes the conversation so far, and the text after the command shapes what the summary keeps — /compact Focus on the --version flag implementation, for instance. Compacting without any steering risks the summary dropping details that later turns still need.","core":true,"estMinutes":5,"evidence":["Explain what steering text after /compact does to the resulting summary","State the risk of compacting without any direction"],"assessmentPrompt":"A long session is about to run out of context, and only the work on one specific feature still matters going forward. What command keeps that feature's details out of the summarization's blind spot?","standards":["ccaf:d5","ccaf-glossary:t60"],"sources":[{"label":"Notes · Steering long sessions","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.075,"layer":1,"x":330,"y":232,"knowledgePoints":[{"id":"kp_compact_1","name":"Steering the summary with a direction","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>The text after <code>/compact</code> is not a comment — it steers what the summary keeps.</p><p><code>/compact</code> on its own summarizes the whole conversation and hopes the result still has what later turns will need. <code>/compact Focus on the --version flag implementation</code> tells the summarizer which thread of the conversation matters, so unrelated exploration gets compressed harder while the thing you're about to keep working on survives intact.</p><p>Compacting with no direction is not neutral — it's a bet that the automatic summary happens to keep the right details. Steering removes the bet.</p>","questions":[{"id":"q_compact_1a","q":"A session has spent an hour debugging three unrelated flaky tests and is now about to run out of context, but only the fix for the --version flag test still matters for the rest of the work. What should you run to keep that fix's details out of the summarizer's blind spot?","options":["/context, to inspect what is consuming the window before deciding whether to summarize at all.","/goal Fix the --version flag test, so Claude keeps working across turns until that is resolved.","/clear, so the session starts over with no memory of any of the three tests that were debugged.","/compact Focus on the --version flag implementation, so the summary keeps that thread intact."],"correct":3,"explain":"Text after /compact steers what the summary keeps, so naming the --version flag thread specifically is what prevents the summarizer from compressing away the one detail that still matters. /context only inspects usage without freeing any of it, /clear discards everything including the useful fix, and /goal sets a completion condition rather than shaping a summary."},{"id":"q_compact_1b","q":"A teammate runs /compact with no instructions at all midway through a multi-part feature, assuming the summary will naturally preserve whatever still matters. What is the actual risk of compacting without any direction?","options":["None — /compact without instructions produces the identical summary as /compact with a direction attached.","The command silently fails and the conversation continues unsummarized until direction is supplied.","The summary can drop details that later turns still need, since nothing told it what to prioritize.","Context usage increases instead of decreasing, since the summarizer itself consumes additional tokens."],"correct":2,"explain":"Without steering, the summarizer has no signal for which thread of the conversation still matters, so it can compress away details a later turn needed. Direction changes what survives, so the two invocations don't produce the same result, /compact doesn't simply fail without arguments, and the point of compaction is still to shrink the context, not to grow it."}]}],"reviewQuestions":[{"id":"r_compact_1","q":"A session has been working across several files on a single feature and needs to keep going well past the point where the raw conversation would exceed the context window. Which habit keeps the summary useful rather than just shorter?","options":["Running /clear partway through so the summarizer starts from a smaller conversation each time.","Switching to a smaller model mid-session, since smaller models summarize conversations more efficiently.","Waiting until the window is completely full before summarizing, to minimize how often it happens.","Running /compact with a direction naming the thread of work that still needs to survive the summary."],"correct":3,"explain":"Naming the thread that still matters is what makes a /compact summary useful rather than just shorter — the steering text is what the summarizer prioritizes. /clear discards the conversation instead of summarizing it, waiting until the window is full just delays the same unsteered risk, and model size isn't the lever that determines what a summary keeps."}]},{"id":"mt_context_clear","type":"CONCEPTUAL","cluster":"cl_sessions","name":"/clear versus /compact, and /context","description":"/clear wipes the conversation entirely and starts fresh, appropriate when starting a new feature so old-conversation bias doesn't bleed in; /compact keeps going mid-feature by summarizing instead of erasing. /context shows a breakdown of what's actually consuming the context window, and being specific in a prompt saves more context than it costs, since a vague prompt forces more exploration and reasoning.","core":true,"estMinutes":5,"evidence":["Choose /clear versus /compact for a given situation based on whether the task itself has changed","Explain why a more detailed prompt can cost less context than a vague one"],"assessmentPrompt":"You're starting a brand-new feature in a session that's been deep in unrelated debugging for an hour. Should you /compact or /clear, and why would carrying the debugging summary forward be worse than starting over?","standards":["ccaf:d5"],"sources":[{"label":"Notes · Steering long sessions","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":2,"x":540,"y":356,"knowledgePoints":[{"id":"kp_context_1","name":"/clear's clean slate versus /compact's summary, and /context's breakdown","keyPrerequisites":["mt_compact_direction"],"teach":"<p><code>/clear</code> and <code>/compact</code> solve different problems. <code>/compact</code> keeps a summarized memory of the conversation so far — right for continuing the same feature. <code>/clear</code> wipes it entirely — right for starting a new one, so old-conversation bias (assumptions, half-abandoned approaches, unrelated context) doesn't bleed into work that has nothing to do with it.</p><p><code>/context</code> shows a breakdown of what's actually consuming the window right now, which is the diagnostic step before deciding whether either command is even needed.</p><p class='kp-key'>Being specific in a prompt saves context, counterintuitively — a vague prompt forces more exploration and reasoning to fill in what you left out, and that costs more than the detail would have.</p>","questions":[{"id":"q_context_1a","q":"A session that has spent the last hour deep in unrelated debugging is about to start a brand-new feature with no connection to that work. Should it /compact or /clear, and why would carrying the debugging summary forward be worse than starting over?","options":["/compact, since a summary of the debugging at least keeps some record of what was tried, in case it's relevant later.","/compact with a direction naming the new feature, so the debugging summary gets steered toward the new work instead.","Neither — running /context first is sufficient on its own to prevent old-conversation bias from affecting new work.","/clear, since a new feature has nothing to gain from the debugging thread and old-conversation bias would only get in the way."],"correct":3,"explain":"Since the new feature shares nothing with the debugging session, a clean slate avoids carrying forward assumptions and half-abandoned approaches that have no bearing on the new work — that's exactly what /clear is for. A summary, even a well-directed one, still carries forward some trace of the unrelated debugging, and /context only inspects usage, it doesn't reset anything."},{"id":"q_context_1b","q":"A developer writes a deliberately vague prompt, reasoning that a shorter prompt will leave more of the context window free for Claude's actual work. What actually happens to context usage as a result?","options":["Usage drops as expected, since a shorter prompt is a smaller addition to the conversation than a detailed one.","Usage stays essentially flat, since prompt length is a small fraction of what a typical session consumes overall.","Usage rises, since the vague prompt forces more exploration and reasoning to fill in what was left unspecified.","Usage becomes unpredictable, since vague prompts route to a different, more exploratory reasoning mode entirely."],"correct":2,"explain":"A vague prompt leaves Claude to fill in the gaps through extra exploration and reasoning, and that extra work costs more context than the missing detail would have taken to state up front — being specific saves context rather than costing it. The prompt's own length isn't the variable driving usage here, and there's no separate reasoning mode that vague prompts trigger."}]}],"reviewQuestions":[{"id":"r_context_1","q":"You want to know exactly what is consuming a session's context window before deciding whether to /compact, /clear, or just keep going. Which command gives you that breakdown?","options":["/context, which shows a breakdown of what is currently consuming the context window.","/compact, run without a direction, which reports usage before producing its summary.","/goal, which reports remaining context budget alongside the completion condition it tracks.","/loop, which polls context usage on an interval and reports any significant change."],"correct":0,"explain":"/context is the command that shows the breakdown of what's consuming the window, which is the diagnostic step before choosing between /compact and /clear. /compact's job is to summarize, not report usage as its primary function, and neither /goal nor /loop are about inspecting context consumption at all."}]},{"id":"mt_rewind_menu","type":"PROCEDURAL","cluster":"cl_sessions","name":"The rewind menu","description":"Double-tapping Escape on an empty prompt opens the rewind menu; every user prompt created a checkpoint you can return to. Its five options split code and conversation independently: restore both, restore conversation only, restore code only, summarize from here, or summarize up to here (compressing everything before while keeping the recent implementation intact).","core":false,"estMinutes":5,"evidence":["Recall how a checkpoint is created and how to open the menu","Pick the correct restore option for 'keep the code, discard the last few messages of conversation'"],"assessmentPrompt":"An exploratory edit turned out to be a dead end, but the conversation that led up to it is worth keeping. Which rewind option undoes just the code, and how is a checkpoint created in the first place?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Steering long sessions","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":1,"x":330,"y":294,"knowledgePoints":[{"id":"kp_rewind_1","name":"Five ways to undo, split by code and conversation","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p>Double-tap Escape on an empty prompt to open the rewind menu. Every user prompt you sent created a checkpoint, so the menu is really a list of \"go back to right before this message.\"</p><p>The five options split code and conversation independently, which is the part worth remembering exactly:</p><ul><li><strong>Restore code and conversation</strong> — full rewind, both together.</li><li><strong>Restore conversation only</strong> — undo what was said, keep the files as they are now.</li><li><strong>Restore code only</strong> — revert the files to the checkpoint's state, keep the conversation that followed.</li><li><strong>Summarize from here</strong> — compress everything after the checkpoint.</li><li><strong>Summarize up to here</strong> — compress everything before the checkpoint, keeping the recent implementation intact.</li></ul>","questions":[{"id":"q_rewind_1a","q":"An exploratory edit a few messages back turned out to be a dead end and should be undone, but the conversation that led up to and past it is still useful context worth keeping. Which rewind option undoes just the code?","options":["Restore conversation only, since discarding the conversation while keeping the dead-end edit isn't what's needed here.","Summarize up to here, which compresses the earlier conversation but leaves the current code exactly as it is.","Restore code only, which reverts the files to that earlier checkpoint's state while leaving the conversation untouched.","Restore code and conversation, which is the safest choice whenever you're unsure exactly what changed."],"correct":2,"explain":"Restore code only is the option built for exactly this split: it reverts the files to the checkpoint's state while leaving the conversation that followed completely intact. Restoring the conversation instead would discard the useful discussion rather than the bad edit, summarizing up to here doesn't touch the code at all, and restoring both would throw away the conversation right along with the dead-end edit."},{"id":"q_rewind_1b","q":"A developer wants to open the rewind menu to see the checkpoints available in the current session, but isn't sure what actually creates a checkpoint in the first place or how to bring the menu up. What are the two facts they need?","options":["Every user prompt creates a checkpoint, and double-tapping Escape on an empty prompt opens the menu.","Only a successful /compact creates a checkpoint, and the menu opens automatically right after it runs.","Every tool call creates a checkpoint, and a single Escape press on an empty prompt opens the menu.","Only an explicit /checkpoint command creates one, and double-tapping Escape then opens the menu."],"correct":0,"explain":"Every user prompt you send creates a checkpoint automatically, and double-tapping Escape on an empty prompt is what opens the rewind menu to browse them. Neither compaction nor an explicit checkpoint command is what creates them, and a single Escape press isn't the gesture that opens the menu."}]}],"reviewQuestions":[{"id":"r_rewind_1","q":"You want to keep the code exactly as it stands right now, but discard the last several messages of conversation because they've gone down an unproductive tangent. Which rewind option fits?","options":["Restore code only, which would also revert the current, useful code back to an earlier checkpoint.","Restore conversation only, which reverts the conversation to a checkpoint while leaving the current code untouched.","Summarize from here, which compresses everything going forward rather than discarding what already happened.","Restore code and conversation, which would discard the current code along with the unproductive messages."],"correct":1,"explain":"Restore conversation only reverts just the conversation to an earlier checkpoint while leaving the current code exactly as it is — the split that's needed here. Restoring code only would touch the wrong half, restoring both would take the useful code down with the tangent, and summarizing from here compresses future turns rather than removing past ones."}]},{"id":"mt_goal_loop","type":"FACTUAL","cluster":"cl_sessions","name":"/goal and /loop","description":"/goal sets a completion condition Claude works toward across turns until a fast evaluator confirms it's met — but the evaluator only reads the transcript, so the condition must be checkable from Claude's own output (like test results), not from state outside the conversation. /loop runs a prompt on an interval, useful for polling external state like a CI run or a deploy and acting on change.","core":false,"estMinutes":5,"evidence":["State the one constraint that limits what a /goal condition can check","Distinguish when /goal fits versus when /loop does"],"assessmentPrompt":"Why can't a /goal condition be 'wait until the production deploy finishes,' even though /goal is designed to work across turns until a condition is met?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Steering long sessions","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":2,"x":540,"y":418,"knowledgePoints":[{"id":"kp_goal_1","name":"/goal's transcript-only evaluator, and /loop for polling","keyPrerequisites":["mt_compact_direction"],"teach":"<p><code>/goal</code> sets a completion condition and Claude keeps working across turns until a fast evaluator confirms it's met. <code>/goal clear</code> cancels it.</p><p class='kp-key'>The evaluator only reads the transcript — so the condition has to be something visible in Claude's own output, like test results appearing in the conversation, never state that lives outside it.</p><p><code>/loop</code> is the other half of the pair: it runs a prompt on an interval between turns, which fits polling external state — a CI run, a deploy — and acting once it changes. Stop it with Escape.</p><p>Rule of thumb: if the condition is 'is this true in what I can already see,' reach for <code>/goal</code>. If it's 'check back until something out there changes,' reach for <code>/loop</code>.</p>","questions":[{"id":"q_goal_1a","q":"A developer sets /goal Wait until the production deploy finishes, expecting Claude to keep working across turns until that becomes true. Why doesn't this work the way /goal is designed to?","options":["It does work as intended — /goal's evaluator can poll any external system, including a deploy's status, directly.","The deploy condition is fine, but /goal caps how many turns it will run before giving up regardless of outcome.","/goal only accepts conditions phrased as a question, not an instruction like 'wait until' something happens.","The evaluator only reads the transcript, and a deploy finishing isn't something visible in Claude's own output."],"correct":3,"explain":"The fast evaluator behind /goal only reads the transcript, so it can confirm something visible in Claude's own output — like test results appearing in the conversation — but has no way to observe a deploy's status happening outside it. /goal's evaluator can't poll external systems directly, the phrasing of the condition isn't the issue, and there's no turn cap that explains the mismatch here."},{"id":"q_goal_1b","q":"A team wants Claude to check on a CI run at intervals and act as soon as it finishes, rather than working toward a single condition checkable purely from the conversation itself. Which command fits that job better than the other?","options":["/goal, since a CI run finishing is exactly the kind of condition its transcript-reading evaluator is built to confirm.","/loop, since it runs a prompt on an interval and fits polling external state and acting once it changes.","/compact, since summarizing the conversation periodically is the mechanism that checks on external state.","/context, since inspecting the context window on an interval is how external state gets tracked over time."],"correct":1,"explain":"/loop running a prompt on an interval is exactly built for polling something external like a CI run and acting once it changes state. /goal's evaluator can't see a CI run's status since it only reads the transcript, and neither /compact nor /context has anything to do with checking on external systems."}]}],"reviewQuestions":[{"id":"r_goal_1","q":"A condition needs to be checkable purely from what already appears in the conversation, such as a specific test suite reporting all green in the transcript. Which command is designed for working toward that kind of condition across turns?","options":["/loop, since it repeats a prompt on an interval rather than tracking a single completion condition.","/goal, since its evaluator reads the transcript and can confirm a condition visible there, like test output.","/compact, since it summarizes the conversation but does not evaluate whether any condition has been met.","/clear, since starting fresh has no way to track progress toward a condition across multiple turns."],"correct":1,"explain":"/goal is built for exactly this: a completion condition its transcript-reading evaluator can confirm from Claude's own output, like test results appearing in the conversation. /loop repeats on an interval rather than tracking one condition, and neither /compact nor /clear evaluates anything at all."}]},{"id":"mt_worktrees","type":"FACTUAL","cluster":"cl_sessions","name":"Worktrees for parallel sessions","description":"A worktree gives each parallel Claude Code session its own independent file tree, so two sessions working the same repo at once don't clobber each other's changes; worktrees are auto-removed when their session exits. .worktreeinclude lists git-ignored files (env vars, local config) that should still be copied into every new worktree.","core":false,"estMinutes":4,"evidence":["Explain the collision worktrees prevent when running parallel sessions","State what .worktreeinclude is for"],"assessmentPrompt":"Two Claude Code sessions are working on unrelated features in the same repository at the same time. What isolates their file changes from each other, and what cleans it up afterward?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Steering long sessions","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":1,"x":330,"y":356,"knowledgePoints":[{"id":"kp_worktrees_1","name":"Isolating parallel sessions' file trees","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p>Two Claude Code sessions working the same repository at the same time will edit the same files unless something separates them — that's what a <strong>worktree</strong> does: each parallel session gets its own independent file tree, so simultaneous changes don't clobber each other.</p><p>Worktrees are auto-removed when their session exits, so they don't accumulate as clutter. Because they're separate checkouts, git-ignored files like environment variables and local config wouldn't normally be there — <code>.worktreeinclude</code> lists the ones that should still be copied into every new worktree.</p>","questions":[{"id":"q_worktrees_1a","q":"Two Claude Code sessions are working on unrelated features in the same repository at the same time, and the team wants to avoid one session's file edits colliding with the other's. What isolates their changes, and what happens to that isolation once a session ends?","options":["A worktree per session isolates the file trees, and each is auto-removed when its session exits.","A worktree per session isolates the file trees, and they persist indefinitely until manually deleted.","Running one session in Manual mode and the other in Auto mode keeps their edits from colliding.","Nothing needs to isolate them, since Claude Code serializes concurrent sessions on the same repo automatically."],"correct":0,"explain":"A separate worktree per session gives each one its own independent file tree so simultaneous edits don't collide, and worktrees are auto-removed once their session exits rather than accumulating. Permission mode has nothing to do with file-tree isolation, and Claude Code does not automatically serialize concurrent sessions on its own."},{"id":"q_worktrees_1b","q":"A new worktree is created for a parallel session, but the local .env file and other git-ignored local config the project relies on aren't present in it by default, since worktrees are separate checkouts. What solves this?","options":[".worktreeinclude, which lists git-ignored files like env vars and local config to copy into every new worktree.","A CLAUDE.md rule instructing Claude to manually recreate the missing .env file inside each new worktree.","Committing the .env file to version control so every worktree checkout includes it automatically.","Nothing — git-ignored files are unavailable in a worktree by design, with no supported way to include them."],"correct":0,"explain":".worktreeinclude exists specifically to list git-ignored files like environment variables and local config that should still be copied into every new worktree despite not being tracked. Instructing Claude to recreate them manually isn't the supported mechanism, committing a .env file defeats the reason it was git-ignored in the first place, and there is in fact a supported way to include them."}]}],"reviewQuestions":[{"id":"r_worktrees_1","q":"A project's .worktreeinclude file lists a handful of paths including a local .env file. What is the purpose of listing them there specifically?","options":["It tells git which paths to ignore when computing diffs shown to a reviewer during a pull request.","It lists git-ignored files that should still be copied into every new worktree despite not being tracked.","It restricts which files a Claude Code session running inside any worktree is permitted to read.","It marks those paths as read-only inside every worktree so a session can't accidentally modify them."],"correct":1,"explain":".worktreeinclude's job is to name git-ignored files — like environment variables and local config — that should still get copied into every new worktree, even though they aren't tracked by git. It isn't about diff visibility in a PR, read permissions, or marking files read-only."}]},{"id":"mt_hook_events_cc","type":"FACTUAL","cluster":"cl_hooks","name":"The hook event lifecycle","description":"Roughly 30 hook events fire across a Claude Code session; the most-used are PreToolUse (before a call, can block it), PostToolUse (after a call succeeds, e.g. auto-formatting), Stop (Claude wants to end its turn, can refuse — paired with SubagentStop), PreCompact/PostCompact (around compaction), and SessionStart (primes the environment at launch, or right after compaction via its compact matcher). Project-level hooks live in .claude/settings.json, checked into the repo.","core":true,"estMinutes":6,"evidence":["Name which events fire before versus after a tool call, and what that timing lets each one do","State where project-level hooks are configured"],"assessmentPrompt":"Which hook event fires early enough to block a tool call before it runs, and which one fires too late to stop anything but can still react to what just happened?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Hooks — the deeper model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.15,"layer":4,"x":960,"y":108,"knowledgePoints":[{"id":"kp_events_1","name":"PreToolUse guards outgoing calls; PostToolUse reacts after","keyPrerequisites":["mt_rule_placement"],"teach":"<p class='kp-key'>Direction is the first thing to check about any hook event: does it fire before the tool call, or after it?</p><p><strong>PreToolUse</strong> fires before a call — it can still block it. <strong>PostToolUse</strong> fires after a call has already succeeded — useful for reacting to the result (auto-formatting a file that was just written), but too late to prevent the call itself.</p><p>Project-level hooks are configured in <code>.claude/settings.json</code>, checked into the repo so the whole team gets the same enforcement automatically.</p>","questions":[{"id":"q_events_1a","q":"A team wants a hook that can refuse to let a Bash command run at all when it matches a dangerous pattern, versus a separate hook that reformats a file right after Claude finishes writing it. Which events fire early enough to block, and which reacts only after the fact?","options":["PostToolUse can still block the Bash call; PreToolUse only reacts to the file after it's written.","Both events can block equally well, since blocking depends on the hook's exit code, not which event fired.","PreToolUse can block the Bash call before it runs; PostToolUse only reacts after the file is already written.","Neither event can block anything directly; blocking always requires a separate permission-mode setting instead."],"correct":2,"explain":"PreToolUse fires before the tool call runs, so it's the one positioned to block the dangerous Bash command outright, while PostToolUse only fires once the write has already succeeded, which is why it's suited to reacting — like reformatting — rather than preventing. The two events aren't interchangeable through exit code alone, and blocking doesn't require a separate permission-mode setting to work."},{"id":"q_events_1b","q":"A team wants their PreToolUse and PostToolUse hooks checked into version control so every developer's clone enforces the exact same rules automatically. Where should project-level hooks be configured to achieve that?","options":["In ~/.claude/settings.json, since user-level configuration is what gets shared across a team by default.","Nowhere in the repo — hooks must be configured interactively by each developer after cloning the project.","In a CLAUDE.md file at the project root, since hook configuration and project guidance share one file.","In .claude/settings.json, checked into the repository so every clone gets the identical configuration."],"correct":3,"explain":"Project-level hooks belong in .claude/settings.json, checked into the repo, so every developer's clone enforces the identical rules without any manual setup. The user-level file is personal and not shared, CLAUDE.md carries guidance rather than hook configuration, and there's no requirement to configure hooks by hand after cloning."}]},{"id":"kp_events_2","name":"Stop, SubagentStop, and the wider event lifecycle","keyPrerequisites":["mt_rule_placement"],"teach":"<p>Beyond the tool-call pair, roughly 30 hook events fire across a session, most of them turn- or session-level rather than per-call. <strong>Stop</strong> fires when Claude wants to end its turn — and, notably, it can refuse, holding the turn open until some condition is met. Its counterpart for delegated work is <strong>SubagentStop</strong>, which fires when a subagent wants to end its own turn.</p><p><strong>PreCompact</strong> and <strong>PostCompact</strong> fire around compaction. <strong>SessionStart</strong> primes the environment at launch.</p><p>The pattern worth internalizing: per-call events (PreToolUse/PostToolUse) can gate a single action; per-turn events (Stop/SubagentStop) can gate whether a turn is allowed to end at all.</p>","questions":[{"id":"q_events_2a","q":"A compliance rule requires that the agent has cited at least one source before it's allowed to finish responding to the user, and the check needs to run every single time the agent attempts to end its turn. Which hook event should this check subscribe to?","options":["PreCompact, since it fires early enough in the session lifecycle to check for a citation before anything else happens.","PostToolUse, since it fires immediately after each individual tool result is returned to the model.","Stop, since it fires when the agent attempts to end its turn and can refuse to let that happen without a citation.","SessionStart, since it primes the environment and can hold the session open until a citation exists."],"correct":2,"explain":"Stop fires exactly when the agent attempts to end its turn, and it can refuse — which is what lets it require a citation before that's allowed to happen. PreCompact and SessionStart fire at points unrelated to turn completion, and PostToolUse fires per tool call rather than per turn."},{"id":"q_events_2b","q":"The same 'must cite a source before finishing' rule needs to apply not just to the main agent's turns but to a subagent's turns as well, whenever one is delegated a research task. Which event is the subagent-scoped counterpart to Stop?","options":["SubagentStop, which fires when a subagent wants to end its own turn, mirroring what Stop does for the main agent.","PostToolUse, since a subagent's final action is itself a tool call the hook can intercept afterward.","PreCompact, since a subagent's context is compacted separately from the main agent's before it returns.","Notification, since subagent completion is surfaced to the main session as a notification event."],"correct":0,"explain":"SubagentStop is the direct counterpart to Stop for delegated work — it fires when a subagent wants to end its own turn, so the same citation requirement can be enforced there. PostToolUse concerns individual tool calls rather than turn completion, and neither PreCompact nor Notification is the event tied to a subagent finishing its work."}]}],"reviewQuestions":[{"id":"r_events_1","q":"A new team member is deciding, as a general rule rather than case by case, whether a given requirement belongs on PreToolUse or PostToolUse. Which rule correctly captures the distinction?","options":["PreToolUse guards what goes out before a call runs; PostToolUse handles what comes back after it succeeds.","PreToolUse is for cheap, fast checks only; PostToolUse is reserved for checks that are expensive to compute.","PreToolUse applies only to Bash calls; PostToolUse applies only to file-editing tools like Write and Edit.","PreToolUse can only allow or deny; PostToolUse is the only event that can rewrite a call's arguments."],"correct":0,"explain":"The direction — before the call runs versus after it comes back — is the real distinction, not cost or which specific tool is involved. PreToolUse isn't restricted to Bash, PostToolUse isn't restricted to file edits, and it's PreToolUse, not PostToolUse, that can rewrite a call's arguments via updatedInput."}]},{"id":"mt_hook_exit_codes","type":"FACTUAL","cluster":"cl_hooks","name":"Exit codes, and the exit-1 trap","description":"A hook's exit code is how it reports back: 0 means success, 2 is a blocking error whose stderr gets fed back to Claude as context (works almost everywhere, including blocking Stop), and any other code — including 1 — is non-blocking. Exit 1 not blocking is the single most common mistake in hook scripts. PostToolUse fires after the tool already ran, so no exit code there can prevent that call. Notification and SessionStart ignore blocking entirely.","core":true,"estMinutes":6,"evidence":["State what each of exit 0, exit 2, and exit 1 actually does","Explain why PostToolUse can never block the call it fires after"],"assessmentPrompt":"A hook script exits with status 1 when it detects a problem, expecting Claude Code to block the action — but the action goes through anyway. What exit code should it have used, and why did 1 fail silently instead of erroring loudly?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Hooks — the deeper model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.1,"layer":5,"x":1170,"y":108,"knowledgePoints":[{"id":"kp_exitcodes_1","name":"0, 2, and everything else","keyPrerequisites":["mt_hook_events_cc"],"teach":"<p>A hook communicates its decision through its exit code:</p><ul><li><code>0</code> — success, proceed.</li><li><code>2</code> — a blocking error; stderr is fed back to Claude as context. This works almost everywhere, including blocking a Stop event from ending the turn.</li></ul><p>Because <strong>PostToolUse</strong> fires after the tool has already run, no exit code returned there can prevent that call — the moment for blocking has already passed. <strong>Notification</strong> and <strong>SessionStart</strong> ignore blocking entirely, no matter what exit code they return.</p>","questions":[{"id":"q_exitcodes_1a","q":"A PreToolUse hook detects a problem with an outgoing call and wants both to stop the call from running and to have its explanation reach Claude so it can adjust its next attempt. What should the hook's exit code be?","options":["Exit code 1, since any nonzero code is treated identically as a blocking signal by the framework.","Exit code 0, paired with a detailed message printed to stdout instead of stderr for visibility.","Exit code 2, which blocks the call and feeds the hook's stderr back to Claude as context.","No exit code convention applies here — blocking is controlled entirely by a separate settings.json flag."],"correct":2,"explain":"Exit code 2 is specifically the blocking signal, and it's what feeds the hook's stderr back to Claude as context so the model can adjust its next attempt. Nonzero codes aren't treated identically, exit 0 signals success rather than a block, and there's no separate settings.json flag standing in for the exit code itself."},{"id":"q_exitcodes_1b","q":"A PostToolUse hook inspects a file that was just written and wants to prevent Claude from ever seeing the result if it looks wrong, returning exit code 2 to try to block it. Why doesn't this stop the result from reaching Claude?","options":["Exit code 2 only blocks Bash calls specifically; file-write tools are exempt from PostToolUse blocking.","PostToolUse hooks can block results, but only when the matcher targets exactly one specific tool name.","The hook needed to return exit code 3 instead, which is the code reserved specifically for PostToolUse.","PostToolUse fires after the tool already ran, so no exit code there can prevent that call from having happened."],"correct":3,"explain":"By the time PostToolUse fires, the tool call has already completed successfully, so no exit code returned there — including 2 — can undo or prevent something that already happened. There's no tool-type exemption, no separate exit code 3 reserved for this event, and matcher scope doesn't change the fundamental timing problem."}]},{"id":"kp_exitcodes_2","name":"The exit-1 trap","keyPrerequisites":["mt_hook_events_cc"],"teach":"<p class='kp-key'>Exit code 1 does not block. This is the single most common mistake in hook scripts.</p><p>Only exit code <code>2</code> blocks. Any other nonzero code — including the very natural-feeling <code>1</code> that most shell scripts use for 'something went wrong' — is treated as non-blocking. The action proceeds anyway, silently, which is worse than an obvious failure: the script author believes the guardrail is in place.</p><p>And two events opt out of the whole mechanism regardless of exit code: <strong>Notification</strong> and <strong>SessionStart</strong> ignore blocking entirely.</p>","questions":[{"id":"q_exitcodes_2a","q":"A hook script exits with status 1 whenever it detects a problem, following the common shell convention that nonzero means failure, expecting Claude Code to block the action as a result. In production, the action goes through anyway. What exit code should the script have used instead, and why did 1 fail silently rather than erroring loudly?","options":["It should have used exit code 2 — only that specific code blocks, and any other nonzero code, including 1, is non-blocking.","It should have used exit code 0 — a successful exit is what actually signals the framework to block the call.","The script was correct — exit code 1 does block, so something else must explain the action going through anyway.","It should have printed to stderr instead of returning a nonzero code, since stderr content alone determines blocking."],"correct":0,"explain":"Only exit code 2 blocks; exit 1 — despite feeling like the natural 'something failed' signal in shell scripting — is treated as non-blocking along with every other nonzero code except 2, which is exactly why the action went through silently. Exit 0 signals success rather than a block, exit 1 genuinely does not block despite the intuition, and stderr content alone doesn't determine whether a call is blocked without the right exit code alongside it."},{"id":"q_exitcodes_2b","q":"A hook registered on Notification returns exit code 2, expecting that to block whatever follows, the same way it would on a PreToolUse or Stop hook. What actually happens?","options":["Nothing is blocked — Notification ignores blocking entirely, regardless of what exit code the hook returns.","The block succeeds exactly as it would on PreToolUse, since exit code 2 blocks uniformly across every event.","The block succeeds, but only the current turn is affected rather than the specific notification itself.","The framework raises a configuration error, since Notification hooks are not permitted to return exit code 2 at all."],"correct":0,"explain":"Notification is one of the events that ignores blocking entirely, no matter what exit code its hook returns, unlike PreToolUse or Stop where exit code 2 is meaningful. Exit code 2 doesn't block uniformly across every event, and returning it from a Notification hook doesn't raise a configuration error — it's simply not honored for blocking there."}]}],"reviewQuestions":[{"id":"r_exitcodes_1","q":"A hook author wants a mental model simple enough to never mix up again: which exit codes actually block an action, across the events where blocking is honored at all?","options":["Exit code 2 blocks; every other code, including 0 and 1, is treated as non-blocking.","Exit codes 1 and 2 both block; only exit code 0 is treated as non-blocking.","Any nonzero exit code blocks; only exit code 0 is treated as a success that proceeds.","Exit code 0 blocks by signaling the hook succeeded; nonzero codes all proceed without incident."],"correct":0,"explain":"Only exit code 2 blocks; every other code, including the intuitive-feeling 1, is non-blocking — that specificity is exactly what makes exit 1 such a common trap. Treating any nonzero code as blocking, or treating 0 as the blocking code, both get the actual contract backwards."}]},{"id":"mt_pretooluse_decision","type":"FACTUAL","cluster":"cl_hooks","name":"permissionDecision","description":"A PreToolUse hook's JSON response carries a permissionDecision field with three values available interactively: allow, deny, or ask; a fourth value, defer, only applies to non-interactive -p runs. This sits above the general exit-code contract as the PreToolUse-specific way to communicate a finer-grained decision than block-or-not.","core":true,"estMinutes":6,"evidence":["Name the three permissionDecision values available in an interactive session","State which fourth value exists and the one context it applies to"],"assessmentPrompt":"What field does a PreToolUse hook's JSON response use to communicate its decision, and why is one of that field's possible values only meaningful outside an interactive session?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Hooks — the deeper model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":6,"x":1380,"y":46,"knowledgePoints":[{"id":"kp_pretooluse_1","name":"allow / deny / ask, and defer for -p only","keyPrerequisites":["mt_hook_exit_codes"],"teach":"<p>A PreToolUse hook's JSON response carries a <code>permissionDecision</code> field, layered on top of the general exit-code contract as a finer-grained way to communicate a decision than simple block-or-not.</p><p>Three values apply in an interactive session: <code>allow</code>, <code>deny</code>, <code>ask</code>. A fourth, <code>defer</code>, exists but <strong>only applies to non-interactive <code>-p</code> runs</strong> — there's no one present in an interactive session for a deferred decision to wait on, so it's meaningless there.</p>","questions":[{"id":"q_pretooluse_1a","q":"A PreToolUse hook needs to communicate a finer-grained decision than a plain block-or-proceed, in an ordinary interactive Claude Code session. Which field does its JSON response use, and which three values are actually usable there?","options":["The decision field, using block, allow, and defer as the three values usable in an interactive session.","The permissionDecision field, using allow, deny, and ask as the three values usable in an interactive session.","The permissionDecisionReason field, using allow, deny, and defer as the three values usable interactively.","The exitCode field, using 0, 2, and defer as the three values usable in an interactive session."],"correct":1,"explain":"permissionDecision is the field, and allow, deny, and ask are the three values that apply in an ordinary interactive session. decision and exitCode name different mechanisms entirely, and permissionDecisionReason carries explanatory text rather than the decision itself."},{"id":"q_pretooluse_1b","q":"A hook author sets permissionDecision to defer inside an interactive Claude Code session, expecting the call to pause until something resolves it. Why is this value meaningless in that context?","options":["defer is a typo for ask, and Claude Code silently treats it as ask regardless of which session type is running.","defer only applies to non-interactive -p runs, and an interactive session has no one to wait for it to resolve.","defer requires a paired PostToolUse hook to be registered, or the PreToolUse decision is discarded outright.","defer is deprecated in the current version and has already been fully replaced by the ask value everywhere."],"correct":1,"explain":"defer is specifically scoped to non-interactive -p runs; an interactive session has no external process waiting to resolve a deferred decision, which is exactly why it's meaningless there. It isn't silently aliased to ask, it doesn't require a paired PostToolUse hook, and it isn't a deprecated value being phased out."}]}],"reviewQuestions":[{"id":"r_pretooluse_1","q":"Which permissionDecision value hands the decision back to the normal interactive permission prompt, for a PreToolUse hook that has no strong opinion of its own on a given call?","options":["allow, since it lets the call proceed immediately without engaging the normal permission flow at all.","deny, since refusing outright is the safest default when a hook has no opinion on a call.","ask, since it explicitly falls through to the normal interactive permission prompt instead of deciding.","defer, since parking the call for later resolution is equivalent to having no opinion on it."],"correct":2,"explain":"ask is exactly the value that hands the decision back to the normal permission prompt rather than the hook deciding on its own. allow and deny both make an active decision instead of deferring to the prompt, and defer's meaning is scoped to non-interactive -p runs, not to expressing indecision inside an interactive one."}]},{"id":"mt_updated_input","type":"PROCEDURAL","cluster":"cl_hooks","name":"updatedInput: rewrite instead of block","description":"A PreToolUse hook can return updatedInput to rewrite a tool call instead of blocking it outright — for example, detecting a live secret pattern in a Bash command and swapping it for a placeholder so the command still runs, but the secret never makes it through. updatedInput replaces the whole input object, so a hook that only wants to change one field must still echo back every other field or lose them.","core":true,"estMinutes":6,"evidence":["Explain the redact-instead-of-block pattern and what it preserves versus a hard deny","State the one thing a hook using updatedInput must remember to do, and what happens if it doesn't"],"assessmentPrompt":"A PreToolUse hook wants to strip a live secret out of a Bash command while still letting the (now-redacted) command run. Which field does it return, and what mistake loses the command's other parameters if the hook isn't careful?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Hooks — the deeper model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":7,"x":1590,"y":46,"knowledgePoints":[{"id":"kp_updatedinput_1","name":"Redact instead of block, and echo back every field","keyPrerequisites":["mt_pretooluse_decision"],"teach":"<p>A PreToolUse hook can return <code>updatedInput</code> to rewrite the tool call instead of blocking it outright. Canonical case: a Bash guardrail detects a live secret pattern like <code>sk_live_</code> and swaps it for a placeholder — the command still runs, but the secret never makes it through.</p><p class='kp-key'><code>updatedInput</code> replaces the whole input object.</p><p>That means a hook that only wants to change one field has to echo back every other field unchanged, or those other fields are silently lost — the tool call runs with only the field the hook bothered to set, and everything else vanishes rather than raising an error.</p>","questions":[{"id":"q_updatedinput_1a","q":"A PreToolUse Bash guardrail detects a live secret pattern like sk_live_ inside an otherwise legitimate command and wants the command to still run, just with the secret swapped for a placeholder. Which field does it return to accomplish this, rather than denying the call outright?","options":["permissionDecisionReason, since supplying an explanatory reason string is what triggers the redaction to occur.","A deny decision combined with a retry hint, so Claude regenerates the command without the secret itself.","additionalContext, since injecting a note about the redaction is sufficient to remove it from the command.","updatedInput, which rewrites the tool call — swapping the secret for a placeholder while letting it still run."],"correct":3,"explain":"updatedInput is exactly the mechanism for rewriting a tool call — swapping the live secret for a placeholder so the command still runs without the secret ever making it through. permissionDecisionReason only explains a decision, additionalContext doesn't remove anything from the outgoing call, and denying the call would stop it from running at all rather than letting a redacted version through."},{"id":"q_updatedinput_1b","q":"A hook only wants to change one field of an outgoing tool call's arguments and returns updatedInput containing just that single field, assuming the rest of the arguments stay as they were. What actually happens to the other fields?","options":["They stay unchanged automatically, since updatedInput only overrides the fields it explicitly names.","They're silently lost — updatedInput replaces the whole input object, so unset fields don't survive.","The tool call is rejected outright, since updatedInput requires every original field to be present.","They're merged with a warning logged, alerting the hook author that fields were left out this time."],"correct":1,"explain":"updatedInput replaces the entire input object rather than patching it, so any field the hook didn't explicitly echo back is silently lost, not merged, warned about, or preserved automatically. A hook that wants to change one field still has to include every other field unchanged in the same response."}]}],"reviewQuestions":[{"id":"r_updatedinput_1","q":"A hook rewrites an outgoing tool call by returning updatedInput with only the one field it cares about changing, omitting the rest of the original arguments. What mistake does this make?","options":["None — updatedInput always preserves every field the hook didn't explicitly mention in its response.","It forgets that updatedInput replaces the whole input object, so the omitted fields are silently lost.","It should have used additionalContext instead, since that field is the one meant for partial rewrites.","It should have returned exit code 2 alongside updatedInput, or the rewrite is ignored entirely."],"correct":1,"explain":"updatedInput replaces the whole input object rather than patching individual fields, so leaving out the rest of the original arguments silently loses them — this is the exact mistake to avoid. There's no automatic preservation of omitted fields, additionalContext solves a different problem, and no paired exit code is required to make the rewrite take effect."}]},{"id":"mt_sessionstart_compact","type":"FACTUAL","cluster":"cl_hooks","name":"SessionStart's compact matcher versus PostCompact","description":"SessionStart primes the environment when a session launches, and its compact matcher specifically re-injects concise context right after a compaction finishes. PostCompact fires around the same moment but does not feed its output back into the conversation — so re-priming context after a compact has to go through SessionStart's compact matcher, not PostCompact.","core":false,"estMinutes":5,"evidence":["Name which hook actually feeds output back into the conversation after a compaction","Explain why PostCompact looks like the obvious choice but isn't"],"assessmentPrompt":"You want to re-inject a short reminder of project context immediately after every /compact. Which hook (and matcher) actually does that, and why doesn't PostCompact work for this even though it also fires around compaction?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Notes · Hooks — the deeper model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":5,"x":1170,"y":170,"knowledgePoints":[{"id":"kp_sessionstart_1","name":"SessionStart's compact matcher versus PostCompact's silence","keyPrerequisites":["mt_hook_events_cc","mt_compact_direction"],"teach":"<p><strong>SessionStart</strong> primes the environment when a session launches. Its <code>compact</code> matcher is the specific trigger for re-injecting concise context right after a compaction finishes — and its output <em>does</em> feed back into the conversation.</p><p class='kp-key'><code>PostCompact</code> looks like the obvious choice for the same job and isn't.</p><p>PostCompact fires around the same moment as a compaction, but it does not feed its output back into the conversation — so anything it returns has nowhere to go. Re-priming context after a <code>/compact</code> has to go through SessionStart's <code>compact</code> matcher, never PostCompact.</p>","questions":[{"id":"q_sessionstart_1a","q":"A team wants to re-inject a short reminder of project context immediately after every /compact, so the summarized conversation doesn't lose track of a few essential facts. Which hook — and matcher — actually accomplishes this?","options":["PostCompact, since it fires at the precise moment compaction finishes, right when the reminder should appear.","PreCompact, configured with extra instructions that get carried forward into the post-compaction conversation.","A PostToolUse hook matched against the /compact command itself, treating compaction like any other tool call.","SessionStart, using its compact matcher, since its output is what actually feeds back into the conversation."],"correct":3,"explain":"SessionStart's compact matcher is specifically built for this — it fires right after a compaction, and unlike PostCompact, its output does feed back into the conversation, which is what makes the reminder actually appear. PostCompact fires around the same moment but its output goes nowhere, PreCompact runs before compaction rather than after, and /compact isn't a tool call that PostToolUse would match against."},{"id":"q_sessionstart_1b","q":"A hook author configures a PostCompact hook to return a short context reminder, reasoning that since it fires right after compaction, its output will naturally appear in the conversation that follows. What actually happens?","options":["The reminder appears exactly as intended, since PostCompact is designed to feed its output back to Claude.","The reminder is delayed until the next tool call, appearing then rather than immediately after compaction.","The reminder is silently redirected to SessionStart's compact matcher, which surfaces it automatically instead.","PostCompact does not feed its output back into the conversation, so the reminder has nowhere to go."],"correct":3,"explain":"PostCompact fires around the same moment as SessionStart's compact matcher, but it does not feed its output back into the conversation, so a reminder returned there simply has nowhere to go. It isn't delayed until the next tool call, and there's no automatic redirection to SessionStart on the hook's behalf — that requires actually configuring the compact matcher there instead."}]}],"reviewQuestions":[{"id":"r_sessionstart_1","q":"Two hooks both fire around the moment a compaction finishes: SessionStart configured with its compact matcher, and PostCompact. What is the key functional difference between them?","options":["There is no real difference — either one reliably re-injects context back into the conversation after a compact.","PostCompact fires only for automatic compactions, while SessionStart's compact matcher fires only for manual ones.","SessionStart's compact matcher feeds its output back into the conversation; PostCompact's output does not.","SessionStart's compact matcher can block the compaction from completing; PostCompact cannot block anything."],"correct":2,"explain":"The functional difference is exactly that SessionStart's compact matcher feeds its output back into the conversation while PostCompact's does not, which is why only the former can actually re-inject a reminder. They aren't split by manual versus automatic compaction, and neither one is about blocking the compaction itself from completing."}]},{"id":"mt_mcp_scoping_cc","type":"FACTUAL","cluster":"cl_mcp_tools","name":"MCP scoping and .mcp.json","description":"claude mcp add wires up an MCP server over one of two transports, HTTP (remote/hosted) or Stdio (local process); /mcp manages servers in-session. Scoping controls who gets it: Local (just you, this project), User (all your projects), or Project (.mcp.json, committed to the repo, so the whole team gets identical servers automatically) — with environment variables like ${GITHUB_TOKEN} expanded from the caller's environment rather than stored in the file.","core":true,"estMinutes":6,"evidence":["Match each scope (Local/User/Project) to who receives the server","Explain why ${GITHUB_TOKEN}-style expansion belongs in .mcp.json instead of a literal token"],"assessmentPrompt":"A team wants every developer to get the same MCP server automatically, without anyone committing a personal API token to the repo. Which scope and which config mechanism deliver both at once?","standards":["ccaf:d2","ccaf-glossary:t21","ccaf-glossary:t22","ccaf-glossary:t23"],"sources":[{"label":"Notes · MCP and Hooks — the basic model","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.05,"layer":1,"x":330,"y":418,"knowledgePoints":[{"id":"kp_mcpscope_1","name":"Three scopes, one config file","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>Where an MCP server is configured decides who gets it: Local (you, this project), User (you, every project), or Project (the whole team, via .mcp.json).</p><p><code>claude mcp add</code> wires up a server over one of two transports — <strong>HTTP</strong> for something remote or hosted, <strong>Stdio</strong> for a local process — and <code>/mcp</code> manages what's connected from inside a session.</p><p>Project scope writes to <code>.mcp.json</code> at the repo root and gets committed, so every developer who clones the repo gets the identical server automatically — no one has to run <code>claude mcp add</code> themselves. A personal API token has no business living in a committed file, so <code>.mcp.json</code> expands environment variables instead of storing secrets literally:</p><pre class='kp-code'>{\n  \"mcpServers\": {\n    \"github\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-github\"],\n      \"env\": { \"GITHUB_TOKEN\": \"${GITHUB_TOKEN}\" }\n    }\n  }\n}</pre><p>Each developer's own <code>GITHUB_TOKEN</code> gets substituted in from their local environment at launch — the file itself never carries a real credential.</p>","questions":[{"id":"q_mcpscope_1a","q":"A team wants every developer to get an internal GitHub MCP server automatically the moment they clone the repo, without any of them committing a personal access token to version control. Which combination delivers both requirements at once?","options":["User scope, so each developer configures the server once in their own home directory and it silently follows them into every other project too.","Local scope, with each developer pasting their own token directly into their private, uncommitted copy of the server configuration.","Project scope via .mcp.json, with the token hardcoded directly into the committed file so the server works identically for absolutely everyone the moment they clone it.","Project scope via .mcp.json committed to the repo, with the token referenced as an environment variable expansion like ${GITHUB_TOKEN} rather than a literal value."],"correct":3,"explain":"Project scope's .mcp.json is the mechanism that reaches every developer automatically on clone, and expanding ${GITHUB_TOKEN} from each developer's own environment is what keeps a real credential out of the committed file. User scope follows one person across unrelated projects rather than reaching the whole team on this one, Local scope has to be set up by hand per developer rather than arriving automatically, and hardcoding a literal token into a committed file is exactly the leak the team is trying to avoid."},{"id":"q_mcpscope_1b","q":"A developer wants to experiment with an MCP server on their current branch only, without it appearing in any teammate's session or in any other project they personally work on. Which scope fits, and what happens to the configuration if they later run claude mcp add for a different, unrelated project?","options":["User scope, since 'this project only' is enforced by Claude Code checking the current working directory against a per-project allowlist you maintain.","Project scope, since .mcp.json is the only file Claude Code reads for any MCP configuration regardless of who set it up or why.","Local scope, but the server then follows that developer into every other project automatically until they explicitly disable it via /mcp.","Local scope, tied to just this one project on this one machine; setting it up again for a different project creates a separate, independent entry."],"correct":3,"explain":"Local scope is exactly the 'just me, just this project' tier, and it doesn't generalize across projects — configuring a server for one project under Local scope leaves an unrelated project untouched, requiring its own separate setup. User scope is the one that follows a developer across every project, which is the opposite of what's wanted here; Project scope is for the whole team via a committed file, not a personal experiment; and Local scope doesn't leak into other projects the way the last option describes."}]}],"reviewQuestions":[{"id":"r_mcpscope_1","q":"An MCP server calls out to a hosted API over the network rather than running as a process on the developer's own machine. Which transport does claude mcp add wire it up with, and which transport would a local script-based server use instead?","options":["HTTP for the hosted/remote server; Stdio for a server that runs as a local process on the developer's machine.","Stdio for the hosted/remote server, since Stdio is the default transport claude mcp add falls back to when none is specified.","Both transports work identically for either case, and the choice only affects which port the server listens on locally.","HTTP for both cases, since Stdio was deprecated in favor of HTTP for all MCP servers regardless of where they run."],"correct":0,"explain":"HTTP is the transport for something remote or hosted, and Stdio is for a local process — that's the whole basis for the split, so a hosted API server uses HTTP and a local script-based server uses Stdio. The transports aren't interchangeable defaults, and Stdio was not deprecated; both remain live options depending on where the server actually runs."}]},{"id":"mt_mcp_context_cost","type":"CONCEPTUAL","cluster":"cl_mcp_tools","name":"MCP's context tax, and tool search mode","description":"Every configured MCP server's tool definitions load into context regardless of whether that session ever calls them. Once MCP tool definitions exceed roughly 10% of the context window, Claude Code auto-switches to 'tool search mode,' discovering tools on demand rather than holding them all loaded — a tradeoff of reliability for context space.","core":true,"estMinutes":6,"evidence":["State what triggers the switch to tool search mode and what changes when it does","Explain why disabling an unused server helps even if that session never called its tools"],"assessmentPrompt":"A project has six MCP servers connected but a given session only ever uses tools from two of them. What is the unused four still costing, and what threshold makes Claude Code start discovering tools on demand instead of holding them all in context?","standards":["ccaf:d2","ccaf:d5"],"sources":[{"label":"Notes · MCP inside Claude Code","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":2,"x":540,"y":480,"knowledgePoints":[{"id":"kp_mcpcost_1","name":"Paying for tools you never call","keyPrerequisites":["mt_mcp_scoping_cc"],"teach":"<p class='kp-key'>Every configured MCP server's tool definitions load into context whether that session ever calls them or not — connected is a cost, used is not required.</p><p>This is a straightforward tax: six servers connected means six servers' worth of tool descriptions sitting in context from the first turn, even if a given session only ever touches two of them. Disabling a server you're not using in this session (via <code>/mcp</code>) removes that tax immediately, even though the tools were never called.</p><p>Once MCP tool definitions cross roughly <strong>10% of the context window</strong>, Claude Code auto-switches to <strong>tool search mode</strong> — instead of holding every tool definition loaded, it discovers tools on demand as they become relevant. That trades some reliability (a tool found by search is a little less certain to be the right one than a full definition sitting in context) for a lot of context space back.</p><p>Two mitigations beyond disabling unused servers: prefer a CLI equivalent (<code>gh</code>, <code>aws</code>) when one already exists, since it carries no persistent tool-definition cost at all; or reach for a Skill instead of an MCP server, since a Skill's content only loads in full once it's actually relevant.</p>","questions":[{"id":"q_mcpcost_1a","q":"A project has six MCP servers connected, but a particular debugging session only ever calls tools from two of them. What is the context cost of the other four servers during that session, and what's the most direct fix?","options":["Zero cost, since Claude Code only loads a server's tool definitions into context the first time one of its tools is actually called.","A small one-time cost paid once at session start, after which the unused servers' tool definitions are automatically evicted from context entirely.","No cost to context, but each unused server still consumes a background API quota that counts against the project's usage limits.","Their tool definitions are already loaded into context regardless of use; disabling the four unused servers for the session removes that cost."],"correct":3,"explain":"Configured servers load their tool definitions into context whether or not their tools get called, so all four unused servers are already costing context space for the entire session — disabling them is what actually removes that cost. Definitions aren't loaded lazily on first call, they aren't auto-evicted after some initial window, and the cost in question is context space, not a separate API quota."},{"id":"q_mcpcost_1b","q":"As a project accumulates more and more connected MCP servers, at what point does Claude Code change how it handles tool discovery, and what does that change actually trade away?","options":["Once server count exceeds ten, it stops accepting new MCP connections entirely until an existing one is removed via /mcp.","There's no such threshold at all — Claude Code always holds every connected server's full tool definitions in context no matter how many are ever configured.","Once MCP tool definitions exceed roughly 10% of the context window, it switches to discovering tools on demand, trading some reliability for context space.","Once total token usage for the session crosses 50%, it pauses tool calls entirely until the developer manually runs /compact."],"correct":2,"explain":"The switch is triggered by MCP tool definitions crossing roughly 10% of the context window, and what changes is discovery becoming on-demand ('tool search mode') instead of everything staying fully loaded — a real but modest reliability cost in exchange for real context space back. There's no hard connection-count cap, no unconditional 'always fully loaded' behavior regardless of scale, and no session-wide pause tied to overall token usage."}]}],"reviewQuestions":[{"id":"r_mcpcost_1","q":"A project relies on the gh CLI being available in every session's environment, and a teammate proposes also connecting a GitHub MCP server so 'the agent has a proper tool interface' for the same operations gh already covers. What's the most direct objection to adding it?","options":["MCP servers can't actually be scoped to a project the way .mcp.json claims, so the new server would silently become available in every unrelated project too.","It would duplicate functionality gh already provides while adding a persistent tool-definition cost to context that the CLI equivalent doesn't carry.","GitHub specifically blocks MCP servers from being connected alongside CLI tools that hit the same API, so the connection would simply fail.","MCP servers can only be added at User scope, so the new server would be tied to one developer's account instead of the whole team."],"correct":1,"explain":"Preferring a CLI equivalent when one already exists is exactly the point — gh already does the job without paying a persistent tool-definition tax, so adding a redundant MCP server is pure cost for no new capability. Project scoping does work as described elsewhere in this section, GitHub imposes no such technical block, and MCP servers aren't restricted to User scope only."}]},{"id":"mt_tool_design_principle","type":"CONCEPTUAL","cluster":"cl_mcp_tools","name":"Abstract, combinable tools","description":"Claude Code's own toolset — bash, read, write, edit, glob, grep — stays deliberately generic rather than hyper-specialized, so Claude can combine a handful of primitives creatively for tasks nobody anticipated in advance. Preferring a CLI equivalent (gh, aws) over a dedicated MCP server, when one exists, follows the same instinct: fewer, more general tools over many narrow ones.","core":false,"estMinutes":5,"evidence":["Explain why generic primitives beat a large set of narrow, task-specific tools for an open-ended agent","Recognize the CLI-equivalent preference as an instance of the same principle"],"assessmentPrompt":"Why does Claude Code ship with a small set of generic tools like bash and grep instead of many narrow, task-specific ones — and what does that same reasoning say about adding an MCP server when a CLI tool already does the job?","standards":["ccaf:d2","ccaf-glossary:t25","ccaf-glossary:t26"],"sources":[{"label":"Notes · MCP inside Claude Code","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":418,"knowledgePoints":[{"id":"kp_tooldesign_1","name":"Fewer, more general tools","keyPrerequisites":["mt_mcp_context_cost"],"teach":"<p class='kp-key'>Claude Code's own toolset stays generic on purpose, so a handful of primitives can be combined creatively instead of needing a purpose-built tool for every task in advance.</p><p><code>bash</code>, <code>read</code>, <code>write</code>, <code>edit</code>, <code>glob</code>, and <code>grep</code> are not specialized to any one workflow — <code>bash</code> alone can do almost anything a narrow, task-specific tool could do, just composed rather than pre-packaged. That generality is what lets Claude handle a request nobody designed a tool for ahead of time.</p><p>The same instinct explains preferring a CLI equivalent (<code>gh</code>, <code>aws</code>) over a dedicated MCP server when one already exists: it's not really about avoiding MCP, it's the same bet on a small set of general tools over an ever-growing set of narrow ones, each with its own definition sitting in context.</p>","questions":[{"id":"q_tooldesign_1a","q":"A team is designing custom tools for a coding agent and is debating between one broad bash tool versus a large library of narrow, purpose-built tools (create_branch, run_linter, search_imports, and so on) covering the same ground. What's the strongest argument for the broad, generic approach, in the spirit of how Claude Code's own toolset is built?","options":["Narrow tools always execute faster than an equivalent bash invocation, since they skip the shell's parsing and process-spawn overhead entirely.","Broad tools require no tool description at all whatsoever, since the model can always infer their full behavior directly from the tool's name without any further explanation.","Narrow tools cannot be listed in allowedTools alongside a bash tool, so a team is forced to pick one approach or the other for the whole agent.","A small set of generic, combinable primitives lets the agent handle tasks nobody anticipated in advance, instead of being limited to whatever narrow tools were pre-built."],"correct":3,"explain":"The core argument for generic, combinable primitives is flexibility for the unanticipated case — a library of narrow tools only covers what someone thought to pre-build, while bash and friends can be composed for whatever comes up. Execution speed isn't the reasoning behind this design choice, every tool still needs a description regardless of how broad or narrow it is, and narrow and generic tools can coexist in the same allowedTools list."},{"id":"q_tooldesign_1b","q":"A team already has the aws CLI available and reliable in their environment, and someone proposes connecting a dedicated AWS MCP server 'to give the agent first-class AWS tools.' Applying the same tool-design instinct Claude Code's own toolset follows, what's the most likely recommendation?","options":["Connect the MCP server anyway, since MCP tools are always inherently more reliable for the model to call correctly than an equivalent CLI command run through bash.","Skip the MCP server; the CLI equivalent already does the job without adding a persistent tool-definition cost to context for capability that already exists.","Connect the MCP server, but only at Local scope, since AWS-specific tools should never be shared with the rest of the team via .mcp.json.","Do neither — remove aws CLI access entirely and require every AWS operation to go through a narrow, purpose-built tool created for it."],"correct":1,"explain":"This is the CLI-equivalent case directly: when a reliable CLI already covers the need, adding a dedicated MCP server duplicates that capability while adding a real, persistent context cost the CLI doesn't carry. MCP tools aren't inherently more reliable to call than an equivalent bash command, scoping AWS tools to Local specifically isn't the actual objection here, and the recommendation is to avoid redundant tooling, not to remove the CLI access that already works."}]}],"reviewQuestions":[{"id":"r_tooldesign_1","q":"Why does bash, on its own, cover so much of what a purpose-built tool library would otherwise need dozens of narrow tools to do?","options":["Because Claude Code silently expands every bash call into the equivalent of several narrow tools behind the scenes before executing it.","Because bash calls skip the permission system entirely, so it can perform operations narrow tools are specifically blocked from doing.","Because bash is the only tool Claude Code ships with by default, so every other capability always has to be reachable through it in one way or another.","Because bash is a generic primitive that can be composed in whatever shape a given task requires, rather than being scoped to one predefined operation."],"correct":3,"explain":"bash's coverage comes from being a general, composable primitive rather than being pre-scoped to one operation the way a narrow tool is — that's the whole point of preferring generic tools. Claude Code does not secretly expand bash calls into narrow-tool equivalents, bash is still subject to the same permission system as any other tool, and it ships alongside several other default tools (read, write, edit, glob, grep), not as the sole one."}]},{"id":"mt_subagent_isolation_cc","type":"CONCEPTUAL","cluster":"cl_mcp_tools","name":"Subagents in Claude Code: isolation and creation","description":"A subagent runs a delegated task in its own isolated context window in parallel with the main session, returning only a summary — so the exploration trail (every file read, every search) never lands in the main conversation. Create one via /agents → 'Create new agent'; Claude auto-generates the name, description, and prompt, and that description is also what later decides whether Claude invokes it for a future prompt.","core":true,"estMinutes":6,"evidence":["State what crosses back to the main session from a subagent and what stays isolated","Explain how a subagent's description does double duty (documentation and invocation trigger)"],"assessmentPrompt":"You only need the answer to 'where in this codebase is retry logic implemented,' not the search process that finds it. Why does delegating that to a subagent keep the main session cleaner than doing the search directly?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Subagents","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":1,"x":330,"y":480,"knowledgePoints":[{"id":"kp_subagentiso_1","name":"Delegate the search, keep the answer","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>A subagent runs in its own isolated context window in parallel with the main session and hands back only a summary — the exploration trail never lands in the main conversation.</p><p>If a question only needs an answer — 'where is retry logic implemented' — and not the process that found it, delegating the search to a subagent keeps every file read and every grep result out of the main session entirely. The main session gets the summary; the search trail stays isolated and gets discarded with the subagent.</p><p>Create one via <code>/agents</code> → 'Create new agent.' Claude auto-generates the name, description, and prompt from what you describe — and that generated <strong>description does double duty</strong>: it documents the subagent for a human reading the config, and it's also what Claude checks against a future prompt to decide whether to invoke that subagent automatically, without being asked to by name.</p>","questions":[{"id":"q_subagentiso_1a","q":"You need to know which file in a large codebase implements retry logic, but you don't need or want to see every grep result and file read that goes into finding it. Why does delegating this to a subagent keep the main session cleaner than running the same searches directly in it?","options":["Because a subagent automatically deletes its own tool-call history from disk once it finishes, whereas the main session's full history is kept permanently.","Because subagents are billed separately from the main session, so running the search there avoids charging the exploration against your usage.","Because a subagent can only return a single line of output, forcing it to compress the answer down regardless of how it was found.","Because the subagent runs its exploration in an isolated context window and returns only a summary, so the search trail never enters the main conversation."],"correct":3,"explain":"The isolation is the mechanism: everything the subagent does — reads, greps, dead ends — stays inside its own context window, and only the summary crosses back to the main session. It's not about deleting history from disk, billing differences, or an output-length limit forcing brevity; the main session simply never sees the trail in the first place."},{"id":"q_subagentiso_1b","q":"After creating a subagent through /agents → 'Create new agent,' Claude auto-generates its name, description, and prompt from what you described. What role does that generated description play beyond documenting the subagent for a human reading the config later?","options":["It sets the subagent's maximum context window size, scaling proportionally with how long the generated description text is.","It has no functional role at all — it exists purely for the config file's own readability and plays no part in how or when the subagent gets invoked.","It determines whether the subagent is automatically invoked for a future prompt, since Claude checks new prompts against it without being asked by name.","It controls which permission mode the subagent inherits, mapping longer, more detailed descriptions to more permissive modes automatically."],"correct":2,"explain":"The description does double duty: besides documenting the subagent, it's exactly what Claude checks a later prompt against to decide whether to invoke that subagent automatically. It does not size the context window, it is not purely cosmetic, and it has no bearing on which permission mode the subagent runs under."}]}],"reviewQuestions":[{"id":"r_subagentiso_1","q":"Two teammates disagree about what crosses back into the main session after a subagent finishes a delegated research task. One says 'everything it read, so I can double-check its reasoning'; the other says 'just the summary it decided to report.' Which is correct?","options":["Neither — subagents cannot return anything to the main session at all; their output is only visible in a separate, dedicated log file.","Just the summary; the full exploration trail (files read, searches run) stays inside the subagent's isolated context and is not carried back.","Everything it read; the isolated context window only limits what the subagent itself can see, not what it ultimately hands back to the main session.","It depends on the permission mode the main session is running under, with Manual mode being the only one that returns the full trail."],"correct":1,"explain":"The summary is what crosses back — that's the entire point of isolating the subagent's context in the first place, keeping the exploration trail out of the main conversation. Subagents do return something to the main session (a summary, not nothing), the full trail specifically does not carry back regardless of how thorough the exploration was, and this isn't something permission mode changes."}]},{"id":"mt_subagent_skill_preload","type":"FACTUAL","cluster":"cl_mcp_tools","name":"Preloading a skill into a subagent","description":"Preloading a skill into a subagent via the skill key loads that skill's entire content immediately — unlike the main conversation, where a skill loads progressively, starting with just its name and description until Claude decides it's relevant.","core":false,"estMinutes":4,"evidence":["State the difference between how a skill loads in the main conversation versus preloaded into a subagent","Explain why immediate full loading is acceptable for a subagent but would be wasteful by default in the main session"],"assessmentPrompt":"A subagent is configured with a preloaded skill via the skill key. How does that skill's content enter the subagent's context differently than it would enter the main conversation on its own?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Subagents","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":5,"x":1170,"y":232,"knowledgePoints":[{"id":"kp_skillpreload_1","name":"Preloaded means immediate, not progressive","keyPrerequisites":["mt_subagent_isolation_cc","mt_skill_frontmatter"],"teach":"<p class='kp-key'>In the main conversation, a skill loads progressively — name and description first, full content only once it's relevant. Preloading one into a subagent via the skill key skips that: the entire skill loads immediately.</p><p>This difference is specifically about the loading path, not about whether the skill's content is available at all — both paths eventually give access to the full skill. In the main session, progressive loading avoids spending context on skills that never end up mattering for the conversation. A subagent configured with a specific skill preloaded doesn't need that caution: the subagent already exists to do that one job, so loading the whole skill upfront isn't wasted the way it would be for a general-purpose main session juggling many possible skills.</p>","questions":[{"id":"q_skillpreload_1a","q":"A skill is preloaded into a subagent via the skill key, versus that same skill being available to the main conversation the ordinary way. How does the skill's content actually enter each context differently?","options":["Identically in both cases — the skill key only changes which subagent is allowed to call the skill, not when its content loads.","In the main conversation, only a shortened summary of the skill ever loads; a subagent's skill key is what loads the true full content.","In the subagent, the full skill content loads immediately and completely; in the main conversation, only the name and description load until the skill becomes relevant.","In the subagent, only the skill's name loads at first; the main conversation is actually the one that always loads its own full content immediately on every single turn."],"correct":2,"explain":"Preloading via the skill key loads the entire skill into the subagent right away, while the main conversation's ordinary path is progressive — name and description first, full content only once it's actually relevant. The two paths aren't identical, the main conversation doesn't receive a permanently shortened version, and the order in the fourth option has the two paths reversed."},{"id":"q_skillpreload_1b","q":"Why is loading an entire skill's content immediately reasonable for a subagent configured with that skill preloaded, when the same immediate full-load approach would be wasteful as the default for the main conversation?","options":["Because a subagent's context window is inherently much larger than the main session's own, so the same amount of upfront skill content costs proportionally less to load there.","Because subagent context, unlike main-session context, is never counted against any context-window budget at all.","Because a preloaded subagent already exists to do that one job, so there's no uncertainty about relevance the way there is across a main session juggling many possible skills.","Because skill content preloaded into a subagent is automatically compressed to a fraction of its original size before loading."],"correct":2,"explain":"The subagent was configured specifically to do one job, so there's no uncertainty about whether the preloaded skill will end up relevant — unlike the main session, where progressive loading exists precisely because many skills might never end up mattering to a given conversation. Subagent context windows aren't inherently larger, subagent context still counts against a budget just like any other context, and preloaded content isn't automatically compressed."}]}],"reviewQuestions":[{"id":"r_skillpreload_1","q":"A subagent is created with a skill preloaded via the skill key specifically for a repeated code-scaffolding task. A teammate assumes this must mean the subagent's context window skips the progressive name-and-description stage the main conversation uses for skills. Is that assumption correct?","options":["Yes — preloading via the skill key is exactly what skips the progressive stage, loading the skill's full content immediately instead.","No — every skill, preloaded or not, always starts with only its name and description loaded, regardless of where it's used.","No — preloading only affects which tools the skill is allowed to call, not how much of its content is loaded at any point.","Yes, but only for subagents created through /agents; a subagent created any other way still loads progressively like the main session."],"correct":0,"explain":"The assumption is correct: preloading a skill into a subagent via the skill key is exactly what bypasses the main conversation's progressive name-and-description stage, loading the full skill content immediately instead. It's not true that every skill always starts progressively regardless of context, preloading is about content loading rather than tool permissions, and this isn't limited to subagents created through one particular path."}]},{"id":"mt_automation_spectrum","type":"CONCEPTUAL","cluster":"cl_automation","name":"Routines, headless, and the Agent SDK: a spectrum","description":"Three ways to run Claude Code without a person watching each turn, ordered by how much control they trade for how little setup: Routines (a saved prompt on Anthropic's managed infrastructure, no server or workflow file of your own), Headless mode (-p, one-shot and pipeable inside your own scripting), and the Agent SDK (a library embedding Claude Code inside your own application, owning the loop yourself).","core":true,"estMinutes":7,"evidence":["Order the three approaches by how much of the infrastructure you own","Pick the right one for 'repeat work,' 'needs my own pipeline,' and 'belongs inside my product'"],"assessmentPrompt":"A team needs to run the same review prompt on every new pull request with zero infrastructure of their own to maintain. Which point on the automation spectrum defaults to that job, and what would push them toward one of the other two instead?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Automating repeat work","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.15,"layer":1,"x":330,"y":542,"knowledgePoints":[{"id":"kp_autospectrum_1","name":"Three ways to skip the watching","keyPrerequisites":["mt_agentic_loop_cc"],"teach":"<p class='kp-key'>Routines, headless mode, and the Agent SDK are the same agentic loop run without someone watching each turn — they differ in how much infrastructure that costs you.</p><p><strong>Routines</strong> sit on Anthropic's managed cloud infrastructure: a saved prompt plus a repo and optional connectors, with no server or workflow file of your own to run or maintain. <strong>Headless mode</strong> (<code>-p</code>) runs one-shot and pipeable, but the pipeline around it — scripting, scheduling, result-handling — is yours to build. The <strong>Agent SDK</strong> is a library that embeds Claude Code inside your own application, so you own the loop directly rather than invoking a CLI.</p><p>The three form a spectrum of ownership: Routines default for plain repeat work with zero infrastructure, Headless fits when the job needs to live inside a pipeline or script you already control, and the Agent SDK fits when the work belongs inside your own product rather than alongside it.</p>","questions":[{"id":"q_autospectrum_1a","q":"A team wants to run the same review prompt against every new pull request, with no server, workflow file, or scheduling infrastructure of their own to build or maintain. Which point on the automation spectrum is designed for exactly that, and what would push the team toward one of the other two instead?","options":["Headless mode by default; they'd move to a Routine only once the pipeline needed custom scripting beyond what -p alone provides on its own.","The Agent SDK by default, since it's the most capable option; they'd move to a Routine only once the SDK's setup cost became too high to justify maintaining themselves.","A Routine by default, but only after first building a minimal Agent SDK integration to confirm the review logic works correctly beforehand.","A Routine by default, since it needs no server of their own; they'd move to headless mode once the job needed to live inside their own pipeline."],"correct":3,"explain":"A Routine is exactly the 'zero infrastructure of your own' point on the spectrum, and the reason to move off it is needing the job to live inside your own pipeline or scripting — which is what pushes toward headless mode instead. Starting from headless or the Agent SDK reverses the actual default-first ordering, and validating logic through an SDK integration first isn't a step this spectrum calls for."},{"id":"q_autospectrum_1b","q":"A product team wants Claude Code's agentic loop running as a first-class feature inside their own application, not alongside it as a separate scheduled job or CLI invocation. Which point on the spectrum fits, and why don't the other two?","options":["Headless mode, because -p is designed to be embedded directly inside another application's own runtime rather than invoked as a standalone CLI process.","A Routine, because Routines can be configured to run inside any application's own infrastructure rather than Anthropic's managed cloud.","The Agent SDK, because it's a library that embeds the loop inside your own application, while a Routine and headless mode both run the loop from outside it.","Either a Routine or headless mode work equally well here, since both can be triggered directly from inside application code with identical results."],"correct":2,"explain":"Owning the loop from inside your own product is specifically what the Agent SDK is for — it's a library, not a scheduled job or a CLI invocation from outside. Headless mode is a one-shot CLI call, not something meant to be embedded as a runtime component; Routines run on Anthropic's managed infrastructure rather than inside your own application; and treating a Routine or headless invocation as equivalent to embedding misses that both still run as something separate from the application itself."}]}],"reviewQuestions":[{"id":"r_autospectrum_1","q":"Ordering Routines, headless mode, and the Agent SDK by how much of the surrounding infrastructure you're responsible for, from least to most, which ordering is correct?","options":["Routines (managed infrastructure) → headless mode (your own pipeline) → Agent SDK (your own application owning the loop).","The Agent SDK (managed infrastructure) → Routines (your own pipeline) → headless mode (your own application owning the loop).","Headless mode (managed infrastructure) → Agent SDK (your own pipeline) → Routines (your own application owning the loop).","All three carry an identical amount of infrastructure responsibility; the difference between them is purely about triggering mechanism, not ownership."],"correct":0,"explain":"Routines run on Anthropic's managed infrastructure with nothing of your own to maintain, headless mode's pipeline is yours to build around a one-shot CLI call, and the Agent SDK goes furthest by embedding the loop directly inside an application you own — that ordering, least to most infrastructure responsibility, is exactly right. The other two reorderings assign 'managed infrastructure' to the wrong end of the spectrum, and the three are not equivalent in ownership."}]},{"id":"mt_routines","type":"FACTUAL","cluster":"cl_automation","name":"Routines: triggers and constraints","description":"A Routine is a saved prompt plus a repo and optional connectors, run on Anthropic's managed cloud infrastructure — created via claude.ai/code/routines or /schedule <description>, triggered by a cron schedule, an HTTP POST, or a GitHub event. As a research preview it carries real constraints: a recurring schedule runs at most hourly, each run starts from a fresh clone of the default branch, and it can only push to claude/-prefixed branches by default.","core":false,"estMinutes":6,"evidence":["Name the three trigger types a Routine supports","State the schedule-frequency and branch-push constraints"],"assessmentPrompt":"A Routine is scheduled to run every 15 minutes. What actually happens, given the constraints on how often a recurring Routine can fire — and where does each run start from?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Automating repeat work","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":2,"x":540,"y":604,"knowledgePoints":[{"id":"kp_routines_1","name":"Managed, but not unconstrained","keyPrerequisites":["mt_automation_spectrum"],"teach":"<p class='kp-key'>A Routine is a saved prompt plus a repo and optional connectors, created via claude.ai/code/routines or /schedule &lt;description&gt;, and triggered by a cron schedule, an HTTP POST, or a GitHub event.</p><p>As a research preview, Routines carry real limits worth knowing precisely: a recurring schedule runs <strong>at most hourly</strong> — no sub-hourly polling — each run starts from a <strong>fresh clone of the default branch</strong> rather than a persistent long-lived workspace, and by default a Routine can only push to <strong>claude/-prefixed branches</strong>, not directly to whatever branch you point it at.</p><p>These constraints shape what a Routine is good for: repeat work that tolerates an hourly cadence and doesn't need to accumulate state across runs, with any code changes landing on a clearly-labeled branch rather than silently touching something like main.</p>","questions":[{"id":"q_routines_1a","q":"A developer configures a Routine's recurring schedule to fire every 15 minutes, expecting near-real-time polling of an external system. What actually happens, given the constraint on how often a recurring Routine schedule can fire?","options":["The Routine runs exactly as configured every 15 minutes, since the hourly limit only applies to Routines triggered by an HTTP POST, not a cron schedule.","The Routine is capped at running at most hourly, so a 15-minute schedule does not fire at the requested cadence.","The configuration is rejected outright at creation time, and the developer must choose a daily or weekly cadence instead of anything sub-hourly.","The Routine runs every 15 minutes for the first hour only, then automatically falls back to an hourly cadence for all runs after that."],"correct":1,"explain":"A recurring schedule is capped at running at most hourly, full stop — a 15-minute cadence simply doesn't fire at the requested rate, regardless of trigger type. The hourly cap isn't scoped only to HTTP-triggered Routines, the configuration isn't rejected outright at creation (it's the runtime cadence that's capped), and there's no described 'first hour at full speed, then throttle' behavior."},{"id":"q_routines_1b","q":"A Routine runs nightly and is expected to push a small automated fix directly to main once it completes. What actually happens by default, and which two facts about how a Routine executes explain it?","options":["It pushes to main as expected, because Routines inherit whatever branch permissions the connected repo grants to the triggering account.","It fails outright, because Routines cannot push any commits under any circumstances — they are strictly read-only against the connected repo.","It cannot push to main by default; each run starts fresh from the default branch and can only push to claude/-prefixed branches.","It pushes to main only on the first run of the day, since Routines default to write access on the initial invocation and read-only afterward."],"correct":2,"explain":"Two constraints explain this together: each run starts fresh from the default branch rather than carrying state forward, and by default a Routine can only push to claude/-prefixed branches, not main. Routine permissions aren't simply inherited from the triggering account's own repo access, Routines aren't strictly read-only (they can push, just not to arbitrary branches by default), and there's no 'write on the first run only' behavior."}]}],"reviewQuestions":[{"id":"r_routines_1","q":"A Routine has been running daily for two weeks, and a developer assumes each run continues from where the previous run's workspace left off — including any uncommitted local state. Is that assumption correct?","options":["Yes — Routines preserve the full workspace between runs by default, including any uncommitted changes from the prior run.","No — each run starts from a fresh clone of the default branch, so nothing from the prior run's workspace carries forward.","Only partially — committed changes carry forward automatically, but uncommitted local state is discarded between runs.","It depends entirely on the trigger type — cron-triggered runs preserve workspace state, while HTTP- and GitHub-event-triggered runs never do."],"correct":1,"explain":"Each run genuinely starts from a fresh clone of the default branch, so the assumption is wrong regardless of what the prior run left behind — nothing from a previous workspace, committed or not, carries forward automatically. The 'committed changes persist, uncommitted don't' framing overstates what actually carries forward, and this behavior doesn't vary by trigger type."}]},{"id":"mt_headless_flags","type":"PROCEDURAL","cluster":"cl_automation","name":"Headless mode: -p, JSON output, --bare","description":"Headless mode (-p / --print) runs one-shot with no interactive UI, but it also skips auto-discovery of hooks, skills, plugins, MCP servers, and CLAUDE.md — only explicitly allowed tools are available, trading breadth for a faster startup. --output-format json plus --json-schema '<schema>' puts the result in .structured_output for piping through jq; a multi-step run captures session_id and resumes with claude --resume; --bare gives deterministic, repeatable output for CI.","core":true,"estMinutes":7,"evidence":["State what headless mode skips by default and why that's the tradeoff, not a bug","Match --output-format json/--json-schema, --resume, and --bare to what each solves"],"assessmentPrompt":"A CI pipeline needs Claude Code's findings to be machine-parseable with file, line, and comment fields it can post as inline PR comments, instead of unstructured text a regex has to scrape. What combination of flags produces that directly, and why is that more robust than a stricter prompt plus a validator?","standards":["ccaf:d4","ccaf-glossary:t42","ccaf-glossary:t43"],"sources":[{"label":"Notes · Automating repeat work","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":2,"x":540,"y":542,"knowledgePoints":[{"id":"kp_headless_1","name":"-p trades discovery for speed","keyPrerequisites":["mt_automation_spectrum"],"teach":"<p class='kp-key'>-p / --print runs Claude Code one-shot with no interactive UI — but it also skips auto-discovery of hooks, skills, plugins, MCP servers, and CLAUDE.md, leaving only explicitly allowed tools available.</p><p>That's a genuine tradeoff, not an oversight: skipping discovery is exactly what makes headless startup fast. It means a headless invocation has to name what it needs rather than relying on whatever the project would normally auto-load in an interactive session — a hook that would normally block a risky command, or a CLAUDE.md rule that would normally shape behavior, simply isn't in play unless the invocation supplies an equivalent explicitly.</p><pre class='kp-code'>claude -p \"Analyze this pull request for security issues\"</pre><p>Without <code>-p</code>, that same invocation in a pipeline hangs waiting for interactive input it will never receive — the flag is what tells Claude Code not to expect a human at the keyboard.</p>","questions":[{"id":"q_headless_1a","q":"A CI script runs claude \"Analyze this pull request for security issues\" without any additional flags, and the job hangs indefinitely with logs showing Claude Code waiting for interactive input. What's the correct fix?","options":["Set an environment variable like CLAUDE_HEADLESS=true before the command, since Claude Code checks for this variable to decide whether to expect a terminal.","Redirect stdin from /dev/null with claude \"...\" < /dev/null, which signals to Claude Code that no interactive input will ever arrive.","Add a --batch flag to the invocation, which switches Claude Code into the same non-interactive pipeline mode -p provides.","Add the -p (or --print) flag, which runs Claude Code non-interactively so it processes the prompt and exits instead of waiting for input."],"correct":3,"explain":"-p / --print is the documented, correct way to run Claude Code non-interactively — it processes the prompt and exits rather than waiting on a terminal. There is no CLAUDE_HEADLESS environment variable Claude Code checks, redirecting stdin from /dev/null doesn't address how Claude Code decides to run interactively, and --batch isn't a real flag."},{"id":"q_headless_1b","q":"A team switches a script from an interactive session to claude -p for CI and is surprised when a PreToolUse hook that normally blocks a risky command no longer fires. What explains this, and what does the team need to do differently in the headless invocation?","options":["Headless mode skips auto-discovery of hooks, skills, plugins, MCP servers, and CLAUDE.md, so the invocation must supply an equivalent safeguard explicitly.","PreToolUse hooks are disabled specifically during headless runs as a deliberate safety measure, and there is no documented way to restore equivalent enforcement in -p mode.","The hook still fires normally in headless mode; the team's script must have a bug elsewhere unrelated to the switch from interactive to -p.","Headless mode runs hooks but silently downgrades every blocking exit code to non-blocking, so the hook fires but no longer prevents the action."],"correct":0,"explain":"Headless mode's real tradeoff is skipping auto-discovery entirely — hooks, skills, plugins, MCP servers, and CLAUDE.md — for faster startup, so a hook that would normally auto-load interactively simply isn't in play unless the invocation names an equivalent explicitly. It's not that hooks are specifically disabled as a safety measure with no workaround, the observed behavior is a direct and predictable consequence of the switch rather than an unrelated bug, and headless mode doesn't selectively downgrade exit-code semantics."}]},{"id":"kp_headless_2","name":"Making the output something else can trust","keyPrerequisites":["mt_automation_spectrum"],"teach":"<p class='kp-key'>--output-format json plus --json-schema turns Claude Code's answer into something machine-parseable by construction, instead of unstructured text a downstream script has to scrape.</p><p><code>--output-format json --json-schema '&lt;schema&gt;'</code> puts the result in <code>.structured_output</code>, ready to pipe through <code>jq</code> rather than parsed with a regex that breaks the moment Claude's phrasing drifts between runs. For multi-step work, capture the run's <code>session_id</code> and resume later with:</p><pre class='kp-code'>claude --resume \"$(jq -r .session_id result.json)\"</pre><p><code>--bare</code> is the deterministic-output mode for CI — the same input should produce comparably structured output run to run, which matters when a pipeline depends on the result being repeatable rather than just plausible.</p>","questions":[{"id":"q_headless_2a","q":"A CI pipeline currently parses Claude Code's plain-text output with a regex to extract a file path, a line number, and a comment, and the regex breaks every time Claude's phrasing drifts slightly between runs. What's the most robust fix?","options":["Add stricter formatting instructions to the prompt describing the exact text layout expected, and reject any response that doesn't match that layout precisely.","Use --output-format json with --json-schema defining file, line, and comment fields, making each finding machine-parseable by construction.","Add a second Claude call that reformats the first response into a consistent structure before the regex parser runs against it.","Switch to a single summary comment instead of per-line findings, which removes the need for line-level parsing entirely."],"correct":1,"explain":"--output-format json with --json-schema eliminates the format-drift problem at the source, by construction, rather than hoping stricter prose instructions hold or patching drift after the fact. A second normalization call still depends on that call's own output being consistent, and dropping to a single summary comment sidesteps the requirement (per-line, machine-parseable findings) instead of meeting it."},{"id":"q_headless_2b","q":"A multi-step headless workflow needs to resume the same Claude Code session across several separate invocations, and a separate CI job needs Claude Code's output to be identical in structure run over run given the same input. Which two mechanisms address these two needs respectively?","options":["Capturing session_id and resuming with --resume for the multi-step workflow; --bare for deterministic, repeatable output in the CI job.","--bare for the multi-step workflow, since it deterministically restores prior context; --resume for the CI job, since it guarantees identical output.","--output-format json for both needs, since structured output alone is sufficient to both resume a session and guarantee repeatable results.","Capturing session_id for the CI job's determinism; --bare for resuming the multi-step workflow across invocations."],"correct":0,"explain":"session_id plus --resume is specifically the multi-step resume mechanism, and --bare is specifically the deterministic-output mode for CI — matching each need to its own dedicated flag. The other options either swap the two mechanisms' actual purposes or claim --output-format json alone covers both needs, when it addresses structure, not session continuity or run-to-run determinism."}]}],"reviewQuestions":[{"id":"r_headless_1","q":"A CI job needs Claude Code's output to be both machine-parseable and repeatable run to run given the same input, but the pipeline only adds --output-format json and stops there. What's still missing?","options":["Nothing — --output-format json alone guarantees both structure and run-to-run repeatability, so the pipeline is already complete.","--json-schema, since without a schema --output-format json produces no output at all rather than unstructured JSON.","--bare, since --output-format json addresses structure but determinism/repeatability is specifically what --bare is for.","--resume, since without it every headless run is treated as a continuation of the previous one instead of a fresh invocation."],"correct":2,"explain":"--output-format json handles machine-parseable structure, but repeatability run to run is specifically what --bare addresses — the pipeline needs both flags for both properties. --json-schema refines the structure further but its absence doesn't mean no output at all, and --resume is about continuing a specific prior session, not about determinism or a default assumption of continuation."}]},{"id":"mt_github_review_action","type":"FACTUAL","cluster":"cl_automation","name":"Managed Code Review versus the GitHub Action","description":"Managed Code Review is Anthropic-hosted via the Claude GitHub app: it posts inline PR comments tagged by severity plus a summary, analyzed against the full codebase, triggered on PR open, on every push, or only on an @claude review comment — but it never approves or blocks a PR, and has no managed autofix. The GitHub Action (anthropics/claude-code-action@v1) is do-it-yourself, for anything that needs to act in CI rather than just comment, configured with trigger_phrase, prompt, and claude_args passed straight to the CLI.","core":false,"estMinutes":6,"evidence":["State the one thing managed Code Review never does, no matter the finding's severity","Choose managed Code Review versus the GitHub Action for a given CI requirement"],"assessmentPrompt":"A pipeline needs to automatically fix a lint violation Claude finds, not just leave a comment about it. Does managed Code Review or the GitHub Action fit that job, and why can't the other one do it?","standards":["ccaf:d4"],"sources":[{"label":"Notes · GitHub Actions and Code Review","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":3,"x":750,"y":480,"knowledgePoints":[{"id":"kp_ghreview_1","name":"Comments versus doing something about it","keyPrerequisites":["mt_headless_flags"],"teach":"<p class='kp-key'>Managed Code Review comments on a PR; the GitHub Action can act in CI. Picking between them comes down to whether the job needs to do something beyond leaving a finding.</p><p><strong>Managed Code Review</strong> is Anthropic-hosted via the Claude GitHub app: inline PR comments tagged by severity plus a summary, analyzed against the full codebase, triggered on PR open, on every push, or only on an <code>@claude review</code> comment. It <strong>never approves or blocks a PR</strong> — that judgment stays human — and it has <strong>no managed autofix</strong>; findings only, with <code>/code-review --fix</code> available locally if you want to apply them yourself.</p><p>The <strong>GitHub Action</strong> (<code>anthropics/claude-code-action@v1</code>) is do-it-yourself, for anything beyond leaving a comment. Setup runs through <code>/install-github-app</code> (needs repo admin), and key inputs are <code>trigger_phrase</code> (default <code>@claude</code>), <code>prompt</code>, and <code>claude_args</code> — raw CLI args passed straight through, e.g. <code>--max-turns 5</code>.</p>","questions":[{"id":"q_ghreview_1a","q":"A pipeline needs to automatically apply a small lint fix Claude identifies, not merely leave a comment describing it. Does managed Code Review or the GitHub Action fit this job, and why can't the other one do it?","options":["The GitHub Action, since it can act in CI beyond commenting; managed Code Review has no managed autofix and only ever leaves findings.","Managed Code Review, since its severity-tagged comments already include an auto-apply option a maintainer can toggle on per repository.","Either works identically here, since both are backed by the same underlying model and differ only in which UI surfaces the result.","The GitHub Action, but only for security findings specifically; lint fixes still require managed Code Review's comment-only workflow."],"correct":0,"explain":"The GitHub Action is the do-it-yourself surface for anything that needs to act in CI, while managed Code Review is comment-only with no managed autofix at all — so applying a fix requires the Action (or a local /code-review --fix), not Code Review. There's no toggle that adds auto-apply to managed Code Review, the two aren't interchangeable despite sharing an underlying model, and the Action isn't restricted to security findings only."},{"id":"q_ghreview_1b","q":"A repository wants a PR review that runs automatically on every push and can never itself approve or block the PR, keeping that judgment with a human reviewer. Which mechanism matches this out of the box, and what would change if the team instead wanted the automation to be able to act rather than just comment?","options":["Managed Code Review, which never approves or blocks a PR by design; acting instead of commenting would mean moving to the GitHub Action.","The GitHub Action, which is comment-only by default; acting instead of commenting would require managed Code Review's autofix setting.","Managed Code Review, which can be configured via claude_args to start blocking PRs once the team is ready for that behavior.","Neither — only a custom hook configured through .claude/settings.json can leave PR comments without being able to block a PR."],"correct":0,"explain":"Managed Code Review already matches 'reviews automatically, never approves or blocks' as its default behavior, and moving to something that can act rather than comment is exactly what pushes a team toward the GitHub Action instead. The GitHub Action isn't comment-only by default (it's built for CI actions beyond commenting), managed Code Review has no configurable blocking behavior via claude_args (that input belongs to the Action), and this isn't a job for a custom hook."}]}],"reviewQuestions":[{"id":"r_ghreview_1","q":"Setting up the GitHub Action requires running /install-github-app, which needs repo admin permissions. What are trigger_phrase and claude_args each used for once it's installed?","options":["trigger_phrase is the comment string (default @claude) that invokes the Action; claude_args passes raw CLI arguments through, e.g. --max-turns 5.","trigger_phrase sets the branch the Action watches for pushes; claude_args is a list of file globs the Action is restricted to reading only.","trigger_phrase and claude_args both configure the same setting — which comment string invokes the Action — with claude_args as a legacy alias for it.","trigger_phrase is the model identifier the Action runs with; claude_args configures which severity levels get surfaced as PR comments."],"correct":0,"explain":"trigger_phrase is the comment phrase (default @claude) that invokes the Action, and claude_args passes raw CLI arguments through to the underlying invocation, such as --max-turns 5 — two distinct, specific inputs. The Action isn't configured by watching a branch via trigger_phrase or restricted to file globs via claude_args, the two inputs aren't aliases of each other, and neither controls model selection or severity filtering."}]},{"id":"mt_plugins","type":"FACTUAL","cluster":"cl_automation","name":"Plugins: bundling, stacking, and trust","description":"A plugin bundles skills, subagents, hooks, and MCP server configs into one installable unit, solving the 'copy .claude files between machines' problem — installed via /plugin install org-name@plugin-name then /reload-plugins, with /plugin marketplace add centralizing team-wide discovery. Its components run alongside yours rather than replacing them: hooks stack, and skills/agents/commands are namespaced under the plugin name. Its own settings.json only honors two keys. Automated 'reviewed' status is not the same as trusted — a plugin runs its hooks on every matching tool call with your privileges, reviewed or not.","core":false,"estMinutes":6,"evidence":["Name what a plugin bundles and how its hooks interact with your own (stack, don't replace)","Explain why 'community-reviewed' isn't sufficient grounds to install a plugin"],"assessmentPrompt":"You install a community plugin that's marked as automated-community-reviewed. What does that label actually verify, and what does installing the plugin hand it access to regardless?","standards":["ccaf:d4"],"sources":[{"label":"Notes · Plugins","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":4,"x":960,"y":170,"knowledgePoints":[{"id":"kp_plugins_1","name":"Bundled, stacked, and only as trusted as their source","keyPrerequisites":["mt_rule_placement"],"teach":"<p class='kp-key'>A plugin bundles skills, subagents, hooks, and MCP server configs into one installable unit — and reviewed is not the same as trusted, because its hooks fire on every matching tool call whether or not you've read them.</p><p>Install with <code>/plugin install org-name@plugin-name</code> then <code>/reload-plugins</code>; a team centralizes discovery with <code>/plugin marketplace add your-org/claude-plugins</code>. A plugin's components run <strong>alongside</strong> yours, not in place of them: its hooks <em>stack</em> with your own local hooks rather than overriding them, and its skills, agents, and commands are namespaced under the plugin's name so they don't collide with yours. A plugin's own <code>settings.json</code> only honors two keys — the one that can promote a subagent to the main thread, and the subagent status-line key.</p><p>Automated 'reviewed' status (a community review pass) is not the same claim as 'trusted' — a plugin runs code on your machine with your privileges the moment it's installed, review or no review.</p>","questions":[{"id":"q_plugins_1a","q":"A developer is about to install a community plugin that's labeled as automated-community-reviewed and reasons this makes it safe to install without further scrutiny. What does that label actually verify, and what does installing the plugin hand it regardless?","options":["The label verifies the plugin passed a security scan blocking any dangerous hook, so installing it carries no meaningful risk beyond a normal skill.","The label reflects an automated review pass, not a trust guarantee; installing it still runs its hooks with your privileges either way.","The label means the plugin's hooks are sandboxed separately from your own local hooks, so they can't affect files outside the plugin's own directory.","The label is purely cosmetic marketing text with no defined meaning within the plugin marketplace's own review and approval process."],"correct":1,"explain":"Automated community review is a real but limited signal — it is explicitly not the same claim as 'trusted,' and installing the plugin still hands its hooks the ability to fire on your machine with your privileges regardless of whether you've personally read them. There's no described security scan that blocks dangerous hooks outright, plugin hooks aren't sandboxed away from affecting anything outside the plugin's own directory, and the label isn't meaningless — it just means less than full trust."},{"id":"q_plugins_1b","q":"A project already has a local PreToolUse hook enforcing a rule, and the team installs a plugin that also bundles a PreToolUse hook. What happens to the two hooks, and how are the plugin's skills and commands named to avoid colliding with the project's own?","options":["The plugin's hook silently replaces the local one entirely, and its skills/commands take on the exact same names as any matching local ones.","Both hooks stack and both run; the plugin's skills, agents, and commands are namespaced under the plugin's name rather than colliding with local ones.","Only the local hook runs, since a project's own settings.json always takes precedence over any hook bundled inside an installed plugin.","The two hooks run in a randomly chosen order determined at each session start, and naming collisions between plugin and local skills throw an error."],"correct":1,"explain":"Plugin components run alongside yours rather than replacing them — hooks stack, and skills/agents/commands are namespaced under the plugin's own name specifically so they don't collide with local ones. The local hook doesn't unconditionally win over the plugin's, hook execution order isn't randomized per session, and namespacing is what prevents collisions rather than the collision throwing an error."}]}],"reviewQuestions":[{"id":"r_plugins_1","q":"A plugin's own settings.json includes several keys a developer expects to control things like default permission mode and hook timeout. How many of those keys does Claude Code actually honor from a plugin's settings.json, and which ones?","options":["All of them — a plugin's settings.json is treated identically to a project's own settings.json, with every key fully honored.","None — plugin settings.json files are parsed for documentation purposes only and have no effect on runtime behavior whatsoever.","Only two — the key that can promote a subagent to the main thread, and the subagent status-line key; everything else is ignored.","Exactly one — the default permission mode key; every other key, including hook timeout, is silently ignored."],"correct":2,"explain":"A plugin's settings.json only honors two specific keys — the agent-promotion key and the subagent status-line key — everything else in that file is ignored regardless of what a developer expects it to control. It isn't treated identically to a project's own settings.json, it isn't purely inert documentation, and the two honored keys aren't 'permission mode' or 'hook timeout' as guessed in the other options."}]},{"id":"mt_verify_proportional","type":"CONCEPTUAL","cluster":"cl_verification","name":"Verify in proportion to how little you watched","description":"Verify in proportion to how little you watched: a quick glance suffices for a session you watched scroll by turn by turn, but an unattended or CI run needs a real, reconstructed check, not a summary read. Unattended runs should stay in Auto mode rather than Bypass — the intent classifier is still a safety net, even though it only checks intent, not correctness.","core":true,"estMinutes":5,"evidence":["State the rule that scales verification effort to supervision level","Explain why Auto, not Bypass, is the right mode for unattended-but-not-isolated work"],"assessmentPrompt":"How much verification does an overnight unattended run need compared to a session you watched scroll by in real time, and why does that run still belong in Auto mode rather than Bypass?","standards":["ccaf:d5"],"sources":[{"label":"Notes · Trust it: verifying unsupervised runs","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.05,"layer":2,"x":540,"y":666,"knowledgePoints":[{"id":"kp_verify_proportional_1","name":"Scale the check to the supervision","keyPrerequisites":["mt_automation_spectrum"],"teach":"<p class='kp-key'>Verify in proportion to how little you watched — a quick glance suffices for a session you saw scroll by, but an unattended run needs a real, reconstructed check.</p><p>A session run interactively, where every tool call scrolled past in real time, has already been watched — a final glance at the diff is enough. A session that ran overnight, or in CI, with nobody present, hasn't been watched at all, and deserves the same rigor as if you were reviewing a stranger's pull request. Keep unattended runs in <strong>Auto mode</strong> rather than Bypass: the intent classifier is still a safety net even though it only checks intent, not correctness — Bypass has no safety net left at all.</p>","questions":[{"id":"q_verify_proportional_1a","q":"An unattended overnight run and a session watched scroll by in real time both finish with clean-looking diffs. Does the overnight run need the same level of verification as the interactive one?","options":["No — an unattended run needs less verification, since nobody was present to introduce mistakes through mid-session steering","Yes, but only because overnight runs are more likely to touch unrelated files than interactive sessions are","No — the overnight run needs a real, reconstructed check, since a quick glance is only sufficient for a session that was actually watched as it happened","Yes, and both need identical verification regardless of how much of either session was actually observed while it was running, since risk doesn't depend on supervision"],"correct":2,"explain":"The overnight run needs a real, reconstructed check precisely because nobody watched it happen — a quick glance is only warranted for a session that was actually observed in real time. It doesn't need less verification for lack of steering, the file-touching claim isn't the actual reasoning given, and treating both runs identically ignores that supervision level is exactly what the rule scales against."},{"id":"q_verify_proportional_1b","q":"An unattended CI job needs to run without anyone approving each action. Why does the guidance recommend keeping it in Auto mode rather than switching to Bypass for convenience?","options":["Bypass mode isn't technically compatible with most CI environments at all, so Auto ends up being the only mode actually available there","Auto mode runs measurably faster than Bypass mode in practice, since it skips most of the permission-check overhead entirely during a session","Bypass mode requires an interactive terminal to acknowledge the --dangerously-skip-permissions flag first","Auto's intent classifier is still a safety net, even though it only checks intent and not correctness — Bypass leaves no safety net at all"],"correct":3,"explain":"Auto keeps the intent classifier active as a safety net, even though it only checks intent rather than correctness; Bypass removes every check, leaving nothing in place. Bypass is technically usable in CI, Auto isn't recommended for being faster, and Bypass doesn't require interactive acknowledgment to run."}]}],"reviewQuestions":[{"id":"r_verify_proportional_1","q":"What's the practical rule connecting how much a run was supervised to how much verification it needs afterward?","options":["Verification effort should scale with how little the run was watched — an unattended run needs a real, reconstructed check, not a summary glance","Verification effort should be identical for every single run, since the level of supervision doesn't actually change what could have gone wrong","Verification effort only matters once a run exceeds a fixed time threshold, regardless of who was actually watching it happen","Verification effort should decrease as a session runs longer, since a longer session implies more self-correction already happened"],"correct":0,"explain":"Verification effort should scale with how little a run was actually watched, with an unattended run needing a real, reconstructed check rather than a quick summary glance. Treating every run identically ignores supervision entirely, a fixed time threshold isn't the stated rule, and a longer session doesn't itself imply more reliable self-correction."}]},{"id":"mt_stop_hook_gate","type":"PROCEDURAL","cluster":"cl_verification","name":"A Stop hook turns tests into a gate","description":"Turning 'run the tests' into an actual gate — not a promise — means a Stop hook that runs the test suite and refuses to let the turn end on failure; exit code 2 feeds the failure straight back to Claude as context to self-fix, rather than the session ending and the failure going unnoticed.","core":true,"estMinutes":5,"evidence":["Explain the difference between asking Claude to run tests and gating on them via a Stop hook","State which exit code turns a Stop hook's failure into feedback Claude actually receives"],"assessmentPrompt":"A CLAUDE.md rule says 'always run the tests before finishing.' Why is a Stop hook a stronger version of that same intent, and what does it do differently when the tests fail?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Notes · Trust it: verifying unsupervised runs","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":6,"x":1380,"y":108,"knowledgePoints":[{"id":"kp_stop_hook_gate_1","name":"A promise versus a gate","keyPrerequisites":["mt_hook_exit_codes","mt_verify_proportional"],"teach":"<p class='kp-key'>A Stop hook that runs the tests and refuses to let the turn end on failure turns 'run the tests' from a promise into a gate.</p><p>A CLAUDE.md rule saying 'always run the tests before finishing' is still just guidance — Claude usually follows it, which isn't the same as always. A Stop hook wired to the same requirement is code that runs on every attempt to end the turn: it runs the suite, and on failure, exits with code <strong>2</strong>, which feeds the failure straight back to Claude as context to self-fix, rather than letting the turn end with the failure unnoticed.</p>","questions":[{"id":"q_stop_hook_gate_1a","q":"A CLAUDE.md file already instructs Claude to always run tests before finishing a task, but a Stop hook running the same tests is added anyway. What does the hook add that the instruction alone didn't already guarantee?","options":["Nothing meaningfully different — both mechanisms produce the same guarantee once the instruction is phrased clearly enough","A guarantee that holds on every attempt to end the turn, rather than a request Claude usually but not always follows","A faster test run, since hooks execute outside the normal request-response cycle that CLAUDE.md instructions go through","The ability to run a larger test suite than what CLAUDE.md instructions are capable of triggering"],"correct":1,"explain":"The hook guarantees the check runs on every attempt to end the turn, replacing a request Claude usually — but not always — follows with code that actually enforces it. No amount of rephrasing turns guidance into a guarantee, the hook isn't valued for test-run speed, and suite size isn't limited by which mechanism triggers it."},{"id":"q_stop_hook_gate_1b","q":"A Stop hook runs the test suite and finds a failure. What exit code does it need to return for that failure to actually reach Claude as feedback instead of the turn simply ending?","options":["Exit code 1, the conventional 'something went wrong' signal most command-line tools use","Exit code 0, since a successful hook execution is what triggers Claude Code to re-examine the results","Exit code 2, which blocks the turn from ending and feeds the failure's stderr back to Claude as context","Any nonzero exit code, since Claude Code treats every nonzero code as a request to retry the last action"],"correct":2,"explain":"Exit code 2 is what blocks the turn from ending and feeds the failure's stderr back to Claude as context to act on. Exit 1 is treated as non-blocking, exit 0 signals success rather than triggering re-examination, and nonzero codes other than 2 don't uniformly trigger a retry."}]}],"reviewQuestions":[{"id":"r_stop_hook_gate_1","q":"Why is a Stop hook described as a stronger version of a 'run the tests before finishing' instruction, rather than just a duplicate of it?","options":["Because a Stop hook can run a broader range of test frameworks and linters than a CLAUDE.md instruction is technically able to invoke on its own","Because a Stop hook only applies during unattended runs, while the CLAUDE.md instruction only applies during interactive ones","Because a Stop hook automatically rewrites failing tests until they pass, something no CLAUDE.md instruction is able to do on its own","Because the instruction is guidance Claude usually follows, while the hook is code that runs deterministically and can refuse to let the turn end"],"correct":3,"explain":"The instruction is guidance Claude usually follows; the hook is code that runs deterministically on every attempt to end the turn and can refuse to let it end on failure. Test-framework breadth isn't the distinction, neither mechanism is restricted to only unattended or only interactive runs, and a Stop hook doesn't rewrite tests to force a pass."}]},{"id":"mt_cold_second_opinion","type":"CONCEPTUAL","cluster":"cl_verification","name":"A cold second opinion","description":"A cold second opinion — a fresh subagent or session with no memory of how the code was built — catches what the original session rationalized past, because it has no investment in the choices already made. This generalizes the pre-commit review habit to any risky or heavily-iterated change, not just the moment right before a commit.","core":false,"estMinutes":5,"evidence":["Explain why a fresh reviewer catches things the original session wouldn't","Recognize when a change calls for a cold review beyond the routine pre-commit one"],"assessmentPrompt":"A session has been iterating on the same tricky bug for two hours and finally believes it's fixed. Why might a fresh subagent reviewing the diff catch something the original session wouldn't, even reading the exact same code?","standards":["ccaf:d5"],"sources":[{"label":"Notes · Trust it: verifying unsupervised runs","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0.025,"layer":3,"x":750,"y":542,"knowledgePoints":[{"id":"kp_cold_second_opinion_1","name":"No stake in the story already told","keyPrerequisites":["mt_commit_review"],"teach":"<p class='kp-key'>A fresh subagent or session with no memory of how the code was built catches what the original session rationalized past — it has no investment in the choices already made.</p><p>This is the same idea behind the pre-commit review habit, generalized past the moment of commit: any risky or heavily-iterated change benefits from a reviewer who wasn't in the room while the decisions were made. A session that spent two hours converging on a fix has, by then, a story it believes about why the fix is right — a cold reviewer reads the same diff without that story already installed.</p>","questions":[{"id":"q_cold_second_opinion_1a","q":"A session has spent two hours iterating on a tricky bug and is now confident the fix is correct. Why might a fresh subagent reviewing the exact same diff catch something the original session wouldn't?","options":["The original session has accumulated a story about why its fix is correct over two hours of iteration; a fresh reviewer reads the diff without that story already in place","The fresh subagent has access to a larger context window than the original session had left after two hours of iteration","The fresh subagent automatically re-runs the full test suite from scratch, which the original session may well have skipped by that point in its iteration","The original session's permission mode restricts it from re-reading its own prior edits once a turn ends, unlike a freshly spawned subagent"],"correct":0,"explain":"After two hours of iteration, the original session has a story about why its fix works; a fresh reviewer reads the same diff without that story already installed, which is what lets it catch what the original session rationalized past. Context-window size isn't the mechanism at play, automatic test re-running isn't guaranteed by fresh-review status alone, and permission mode doesn't restrict a session from re-reading its own edits."},{"id":"q_cold_second_opinion_1b","q":"A team's policy already includes a routine subagent review before every commit. When does the guidance suggest reaching for an additional cold second opinion beyond that routine review?","options":["Never — the routine pre-commit review by a fresh subagent is already considered sufficient for every kind of change, regardless of risk","Only for changes to test files specifically, since those are considered the most likely to be reviewed carelessly by the original implementing session","For any change that's especially risky or has accumulated a lot of iteration, where the original session is most likely to have rationalized past a problem","Only when the original session explicitly requests a second opinion itself, since it can best judge when one is needed"],"correct":2,"explain":"A cold second opinion is worth reaching for on changes that are especially risky or heavily iterated, where the original session is most likely to have talked itself past a problem. It's not limited to test-file changes, it isn't reserved only for cases where the original session itself asks, and treating routine review as always sufficient ignores exactly the risk-scaling this generalizes."}]}],"reviewQuestions":[{"id":"r_cold_second_opinion_1","q":"The pre-commit review habit and the cold second opinion are described as the same idea at different scopes. What's the relationship between them?","options":["They're unrelated — pre-commit review checks for bugs, while a cold second opinion checks only for style violations","The pre-commit review is the single-session version of the same idea; a cold second opinion generalizes it to any risky or heavily-iterated change, not just the moment before commit","A cold second opinion replaces the need for pre-commit review entirely once a team adopts it as their standard practice for every change","Pre-commit review is required by a hook that blocks the commit outright, while a cold second opinion is only ever offered as a suggestion somewhere in CLAUDE.md"],"correct":1,"explain":"Pre-commit review is the single-session version of the same idea; a cold second opinion generalizes it beyond the moment of commit to any risky or heavily-iterated change. The two aren't scoped to different concerns (bugs versus style), one doesn't replace the other, and neither is defined by being exclusively hook-enforced or exclusively a CLAUDE.md suggestion."}]},{"id":"mt_verification_checklist","type":"PROCEDURAL","cluster":"cl_verification","name":"The seven-step evidence checklist","description":"A reliable Claude Code workflow leaves evidence in seven steps: state the goal and success criteria, inspect and plan the smallest change, make it with least privilege, run the relevant build/test/lint/type checks, review the diff and confirm tests weren't weakened or deleted, report what was verified and what remains uncertain, and get a cold second opinion when the change is risky or context has run long.","core":true,"estMinutes":7,"evidence":["Recall all seven steps in order","Explain what 'report what remains uncertain' adds beyond a pass/fail statement"],"assessmentPrompt":"A change passes its tests and the diff looks clean. What two things does the seven-step checklist still require before calling the work done?","standards":["ccaf:d4","ccaf:d5"],"sources":[{"label":"Notes · Verification and trust","href":"../../07-claude-code-in-action/notes.md"}],"centrality":0,"layer":7,"x":1590,"y":108,"knowledgePoints":[{"id":"kp_verification_checklist_1","name":"Seven steps, in order","keyPrerequisites":["mt_stop_hook_gate","mt_cold_second_opinion","mt_rule_placement"],"teach":"<p class='kp-key'>Seven steps, in order: state the goal and success criteria; inspect and plan the smallest change; make it with least privilege; run the relevant checks; review the diff and confirm tests weren't weakened; report what was verified and what remains uncertain; get a cold second opinion when risk or context length calls for it.</p><p>Each step closes a specific gap the others don't: stating success criteria up front gives later steps something concrete to check against; least privilege limits what a mistake can reach; running the checks and reviewing the diff are separate steps because a passing test suite and an untouched-looking diff aren't the same evidence; and the report step exists because 'done' should mean stated evidence, not a feeling that the code looks right.</p>","questions":[{"id":"q_verification_checklist_1a","q":"Which step in the seven-step checklist comes immediately after making the change, and what does it check?","options":["State the goal and success criteria — it checks that the task itself is well-defined before any code is written","Get a cold second opinion — it checks that a fresh reviewer agrees with the change before it proceeds any further toward being committed","Report what was verified and what remains uncertain — it checks that the evidence gathered is communicated clearly","Run the relevant build, tests, lint, or type checks — it checks that the change actually behaves as intended once it exists"],"correct":3,"explain":"Running the relevant build, tests, lint, or type checks is the step right after the change is made, confirming it behaves as intended now that it exists. Stating success criteria happens before the change, a cold second opinion is invoked later when risk warrants it, and the report step comes after the checks and diff review, not immediately after coding."},{"id":"q_verification_checklist_1b","q":"Why does the checklist treat 'run the tests' and 'review the diff and confirm tests weren't weakened' as two separate steps rather than one?","options":["A passing test suite and a diff that hasn't quietly weakened or deleted a test are different kinds of evidence — one can be true while the other silently isn't","Running tests and reviewing diffs require different permission modes to execute, so the checklist can't combine them into a single step","The two steps are actually redundant in practice, and the checklist only separates them for readability rather than for any substantive underlying reason","Reviewing the diff is only necessary when the test suite doesn't cover the changed files, so it's a conditional follow-up to running tests"],"correct":0,"explain":"A passing test suite and a diff that hasn't quietly weakened or deleted a test are genuinely different kinds of evidence — a suite can pass while a test was silently gutted to make it pass. The two steps don't require different permission modes, they aren't redundant, and diff review isn't conditional only on test coverage gaps."}]},{"id":"kp_verification_checklist_2","name":"'Done' means stated evidence, not a feeling","keyPrerequisites":["mt_stop_hook_gate","mt_cold_second_opinion","mt_rule_placement"],"teach":"<p class='kp-key'>A change that passes its tests and looks clean in the diff still isn't done until what was verified — and what remains uncertain — has actually been stated.</p><p>'The code looks right' from reading a diff isn't verification; it's an impression. The checklist's report step asks for something stronger: which checks were actually run, and an honest account of what's still unconfirmed. That last part matters as much as the first — a report that only lists what passed, without naming what wasn't checked, quietly reintroduces the same gap the rest of the checklist exists to close.</p>","questions":[{"id":"q_verification_checklist_2a","q":"A change passes its tests and the diff looks clean. According to the checklist, what's still missing before the work can be called done?","options":["Nothing — passing tests and a clean-looking diff are themselves sufficient evidence that the checklist is satisfied","A second full test run of the entire suite, since a single passing run isn't considered reliable evidence of correctness on its own","An explicit report of what was actually verified and what remains uncertain, plus a cold second opinion if the change is risky or context has run long","A rewrite of the CLAUDE.md file to document the change in detail, since that's treated as the checklist's own definition of what counts as 'reported'"],"correct":2,"explain":"The checklist still calls for an explicit report of what was verified and what remains uncertain, plus a cold second opinion when risk or context length warrants it. Passing tests and a clean diff alone aren't treated as sufficient, a second identical test run isn't the missing piece, and 'reported' doesn't mean rewriting CLAUDE.md."},{"id":"q_verification_checklist_2b","q":"Why does the checklist specifically ask for 'what remains uncertain' in addition to 'what was verified'?","options":["Because listing uncertainty is a formality required for compliance audits, unrelated to the actual reliability of the change","Because it lets the reviewer skip reading the diff themselves entirely, relying only on the stated list of uncertainties to decide whether it's safe","Because 'what remains uncertain' is only relevant for changes made in Bypass mode, where no checks ran automatically in the first place","Because a report that only lists what passed can quietly hide the same gap the rest of the checklist exists to close — omitting what wasn't checked"],"correct":3,"explain":"A report that names only what passed can quietly hide exactly the kind of gap the checklist exists to catch, since it says nothing about what wasn't checked. It isn't a compliance formality, it doesn't substitute for a reviewer reading the diff, and it isn't specific to changes made under Bypass mode."}]}],"reviewQuestions":[{"id":"r_verification_checklist_1","q":"What is the first step in the seven-step checklist, before any inspection or code change happens?","options":["Make the change with the least privilege needed","State the goal and success criteria","Run the relevant build, tests, lint, or type checks","Ask a cold second opinion whether the plan looks reasonable"],"correct":1,"explain":"Stating the goal and success criteria is the checklist's first step, before any inspection, planning, or code change happens. Making the change and running checks both come later in the sequence, and a cold second opinion is reserved for risky or heavily-iterated work, not the opening step."},{"id":"r_verification_checklist_2","q":"Where does 'make the change with the least privilege needed' fall in the seven-step sequence, and what does it depend on already having been decided?","options":["It falls before any inspection happens, and depends only on the developer's general familiarity with the codebase","It falls after inspecting and planning the smallest change, and depends on already knowing which mechanism — CLAUDE.md, a skill, or a hook — should be enforcing what","It falls after the checks have run and the diff has been reviewed, and depends on the test suite having already passed at least once","It falls after the cold second opinion step has already concluded, and depends on that outside reviewer having approved the overall approach first"],"correct":1,"explain":"Least-privilege implementation comes after inspecting and planning the smallest change, and applying it well depends on already having sorted which mechanism should be enforcing what — the same distinction rule_placement draws. It isn't the first step, it doesn't come after the checks or the cold second opinion, and it isn't gated on a prior test pass."}]}],"dependencies":[{"topicId":"mt_epcc_workflow","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"EPCC is how the loop's plan/act/verify phases get named and separated into a recommended workflow"},{"topicId":"mt_commit_review","prerequisiteId":"mt_epcc_workflow","strength":"hard","reason":"Commit is the last EPCC phase; the pre-commit review habit only makes sense once code exists to review"},{"topicId":"mt_permission_modes_six","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"Permission modes govern how far each loop iteration's action can go before it needs to ask"},{"topicId":"mt_auto_classifier","prerequisiteId":"mt_permission_modes_six","strength":"hard","reason":"Auto is one of the six modes; its classifier behavior only makes sense against the full spectrum"},{"topicId":"mt_plan_vs_direct","prerequisiteId":"mt_permission_modes_six","strength":"hard","reason":"Plan mode is itself one of the six permission modes, so choosing it requires knowing the spectrum it sits in"},{"topicId":"mt_interview_pattern","prerequisiteId":"mt_plan_vs_direct","strength":"hard","reason":"The interview pattern is a plan-mode-adjacent technique for surfacing considerations before implementation begins"},{"topicId":"mt_explore_subagent_cc","prerequisiteId":"mt_plan_vs_direct","strength":"hard","reason":"The Explore subagent exists specifically to keep plan mode's discovery phase from filling main context"},{"topicId":"mt_claudemd_basic","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"CLAUDE.md is read every session and appended to the same prompt the loop starts from"},{"topicId":"mt_claudemd_hierarchy","prerequisiteId":"mt_claudemd_basic","strength":"hard","reason":"The four-location stack only matters once you know a single file is auto-loaded"},{"topicId":"mt_claudemd_imports","prerequisiteId":"mt_claudemd_hierarchy","strength":"hard","reason":"@import composes conventions across the same locations the hierarchy defines"},{"topicId":"mt_claude_rules","prerequisiteId":"mt_claudemd_hierarchy","strength":"hard","reason":".claude/rules/ is an alternative to a single growing project-level file within that same hierarchy"},{"topicId":"mt_claudemd_phrasing","prerequisiteId":"mt_claudemd_basic","strength":"hard","reason":"Phrasing advice applies once you're actually writing rules into a CLAUDE.md file"},{"topicId":"mt_rule_placement","prerequisiteId":"mt_claudemd_phrasing","strength":"hard","reason":"Deciding where a rule belongs presupposes knowing what CLAUDE.md is and how much weight its phrasing carries"},{"topicId":"mt_slash_commands","prerequisiteId":"mt_claudemd_hierarchy","strength":"hard","reason":"Commands share the same project-vs-personal location split CLAUDE.md's hierarchy already established"},{"topicId":"mt_skill_frontmatter","prerequisiteId":"mt_rule_placement","strength":"hard","reason":"Skills are one of the three mechanisms rule_placement distinguishes, so their config presupposes that distinction"},{"topicId":"mt_verification_skill","prerequisiteId":"mt_skill_frontmatter","strength":"hard","reason":"A verification skill is a skill; authoring one presupposes knowing skill frontmatter"},{"topicId":"mt_compact_direction","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"Compaction operates on the same context the loop accumulates turn by turn"},{"topicId":"mt_context_clear","prerequisiteId":"mt_compact_direction","strength":"hard","reason":"/clear and /context are the other two levers in the same session-steering toolkit as /compact"},{"topicId":"mt_rewind_menu","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"Rewind operates on the same per-prompt checkpoints the loop creates"},{"topicId":"mt_goal_loop","prerequisiteId":"mt_compact_direction","strength":"hard","reason":"/goal and /loop are session-steering commands introduced alongside /compact"},{"topicId":"mt_worktrees","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"A worktree isolates the file tree a given loop instance acts on"},{"topicId":"mt_hook_events_cc","prerequisiteId":"mt_rule_placement","strength":"hard","reason":"Hooks are the mechanism rule_placement identifies for rules that must never be skippable"},{"topicId":"mt_hook_exit_codes","prerequisiteId":"mt_hook_events_cc","strength":"hard","reason":"Exit codes are how a hook communicates its decision back for a given event"},{"topicId":"mt_pretooluse_decision","prerequisiteId":"mt_hook_exit_codes","strength":"hard","reason":"permissionDecision is the PreToolUse-specific payload layered on top of the general exit-code contract"},{"topicId":"mt_updated_input","prerequisiteId":"mt_pretooluse_decision","strength":"hard","reason":"updatedInput is a value the PreToolUse decision response can carry alongside permissionDecision"},{"topicId":"mt_sessionstart_compact","prerequisiteId":"mt_hook_events_cc","strength":"hard","reason":"The compact matcher is a SessionStart-specific trigger, so it presupposes knowing the event lifecycle"},{"topicId":"mt_sessionstart_compact","prerequisiteId":"mt_compact_direction","strength":"hard","reason":"The compact matcher only makes sense against knowing what /compact actually does to a session"},{"topicId":"mt_mcp_scoping_cc","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"MCP servers are configured before the loop starts, and their tools become available to every iteration"},{"topicId":"mt_mcp_context_cost","prerequisiteId":"mt_mcp_scoping_cc","strength":"hard","reason":"The context tax is a direct consequence of how many servers scoping wires in"},{"topicId":"mt_tool_design_principle","prerequisiteId":"mt_mcp_context_cost","strength":"hard","reason":"Preferring abstract, general tools is one response to the same context-budget pressure MCP servers create"},{"topicId":"mt_subagent_isolation_cc","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"A subagent runs its own instance of the same loop inside an isolated context window"},{"topicId":"mt_subagent_skill_preload","prerequisiteId":"mt_subagent_isolation_cc","strength":"hard","reason":"Preloading applies to a subagent's context, so it presupposes knowing how a subagent's context is isolated"},{"topicId":"mt_subagent_skill_preload","prerequisiteId":"mt_skill_frontmatter","strength":"hard","reason":"Preloading a skill into a subagent combines subagent isolation with how a skill's content loads in the first place"},{"topicId":"mt_automation_spectrum","prerequisiteId":"mt_agentic_loop_cc","strength":"hard","reason":"All three automation modes are ways of running the same loop without a human watching each turn"},{"topicId":"mt_routines","prerequisiteId":"mt_automation_spectrum","strength":"hard","reason":"Routines are one point on the spectrum this topic maps"},{"topicId":"mt_headless_flags","prerequisiteId":"mt_automation_spectrum","strength":"hard","reason":"Headless mode is the second point on the same spectrum"},{"topicId":"mt_github_review_action","prerequisiteId":"mt_headless_flags","strength":"hard","reason":"The GitHub Action is effectively a headless-mode deployment target inside CI"},{"topicId":"mt_plugins","prerequisiteId":"mt_rule_placement","strength":"hard","reason":"A plugin bundles the same skill/hook/MCP mechanisms rule_placement distinguishes into one installable unit"},{"topicId":"mt_verify_proportional","prerequisiteId":"mt_automation_spectrum","strength":"hard","reason":"How much verification a run needs scales with how far it sits from interactive supervision on that spectrum"},{"topicId":"mt_stop_hook_gate","prerequisiteId":"mt_hook_exit_codes","strength":"hard","reason":"A Stop hook gate is the hook mechanism applied specifically to enforce the verification a run needs"},{"topicId":"mt_stop_hook_gate","prerequisiteId":"mt_verify_proportional","strength":"hard","reason":"Gating with a hook is the concrete answer to how much verification an unsupervised run needs"},{"topicId":"mt_cold_second_opinion","prerequisiteId":"mt_commit_review","strength":"hard","reason":"Cold review before commit is the single-session version of what a cold second opinion generalizes further"},{"topicId":"mt_verification_checklist","prerequisiteId":"mt_stop_hook_gate","strength":"hard","reason":"The checklist synthesizes gated verification into its 'run the checks' step"},{"topicId":"mt_verification_checklist","prerequisiteId":"mt_cold_second_opinion","strength":"hard","reason":"The checklist's last step is exactly when to bring in a cold second opinion"},{"topicId":"mt_verification_checklist","prerequisiteId":"mt_rule_placement","strength":"hard","reason":"The checklist's 'least privilege' step presupposes knowing which mechanism should be enforcing what"}],"encompassings":[{"topicId":"mt_verification_checklist","encompassedId":"mt_stop_hook_gate","coverage":0.6,"reason":"The checklist's build/test/lint step is the Stop-hook gate written out as a manual sequence"},{"topicId":"mt_verification_checklist","encompassedId":"mt_verify_proportional","coverage":0.5,"reason":"Deciding when to add a cold second opinion is the checklist's version of scaling effort to supervision"},{"topicId":"mt_verification_checklist","encompassedId":"mt_cold_second_opinion","coverage":0.5,"reason":"The checklist names the cold second opinion as one of its own steps"},{"topicId":"mt_verification_checklist","encompassedId":"mt_rule_placement","coverage":0.4,"reason":"Applying least privilege in the checklist means having already sorted which mechanism enforces what"},{"topicId":"mt_plugins","encompassedId":"mt_mcp_scoping_cc","coverage":0.4,"reason":"A plugin's .mcp.json is the same scoping mechanism, just packaged for distribution"},{"topicId":"mt_plugins","encompassedId":"mt_hook_events_cc","coverage":0.3,"reason":"A plugin's bundled hooks fire through the same event lifecycle as any local hook"},{"topicId":"mt_plugins","encompassedId":"mt_skill_frontmatter","coverage":0.4,"reason":"A plugin's bundled skills use the same SKILL.md frontmatter as a locally-authored one"},{"topicId":"mt_updated_input","encompassedId":"mt_pretooluse_decision","coverage":0.8,"reason":"updatedInput is carried inside the same PreToolUse response permissionDecision belongs to"},{"topicId":"mt_pretooluse_decision","encompassedId":"mt_hook_exit_codes","coverage":0.7,"reason":"permissionDecision is a refinement layered on the same exit-code contract"},{"topicId":"mt_sessionstart_compact","encompassedId":"mt_compact_direction","coverage":0.5,"reason":"Re-priming after a compact only makes sense against knowing what /compact just did"},{"topicId":"mt_sessionstart_compact","encompassedId":"mt_hook_events_cc","coverage":0.6,"reason":"Naming SessionStart's compact matcher exercises the broader event lifecycle it belongs to"},{"topicId":"mt_headless_flags","encompassedId":"mt_automation_spectrum","coverage":0.5,"reason":"Choosing the right headless flag presupposes placing headless mode correctly on the spectrum"},{"topicId":"mt_routines","encompassedId":"mt_automation_spectrum","coverage":0.5,"reason":"Routine constraints only make sense in contrast to the other two points on the spectrum"},{"topicId":"mt_github_review_action","encompassedId":"mt_headless_flags","coverage":0.4,"reason":"The GitHub Action's claude_args pass-through exercises the same headless-flag knowledge"},{"topicId":"mt_subagent_skill_preload","encompassedId":"mt_subagent_isolation_cc","coverage":0.7,"reason":"Preloading is a detail of the same isolated-context mechanism subagent isolation establishes"},{"topicId":"mt_subagent_skill_preload","encompassedId":"mt_skill_frontmatter","coverage":0.4,"reason":"The skill key is a frontmatter-adjacent detail of how a skill's content actually loads"},{"topicId":"mt_commit_review","encompassedId":"mt_epcc_workflow","coverage":0.5,"reason":"Review-before-commit is the Commit phase of EPCC, worked in detail"},{"topicId":"mt_cold_second_opinion","encompassedId":"mt_commit_review","coverage":0.6,"reason":"A cold second opinion is the pre-commit review habit generalized past the moment of commit"},{"topicId":"mt_rule_placement","encompassedId":"mt_claudemd_phrasing","coverage":0.5,"reason":"Recognizing CLAUDE.md's guidance-not-enforcement nature is half of what motivates rule placement"},{"topicId":"mt_auto_classifier","encompassedId":"mt_permission_modes_six","coverage":0.7,"reason":"Explaining Auto in depth exercises its place in the six-mode spectrum"},{"topicId":"mt_explore_subagent_cc","encompassedId":"mt_plan_vs_direct","coverage":0.5,"reason":"Explore is a technique that only matters inside a plan-mode discovery phase"},{"topicId":"mt_verification_skill","encompassedId":"mt_skill_frontmatter","coverage":0.6,"reason":"Building a verification skill exercises the same frontmatter knowledge that defines any skill"},{"topicId":"mt_claude_rules","encompassedId":"mt_claudemd_hierarchy","coverage":0.4,"reason":".claude/rules/ is positioned as an alternative inside the same location hierarchy"},{"topicId":"mt_stop_hook_gate","encompassedId":"mt_hook_exit_codes","coverage":0.5,"reason":"A Stop hook gate is exit-code mechanics applied to one specific event"}],"layout":{"width":1710,"height":712}};
