/* GENERATED FILE — do not edit.
   Source of truth: learning-graph/data/*.json
   Rebuild with: node learning-graph/scripts/build-web-data.mjs */
window.CCAF_GRAPH = {"version":1,"builtAt":"2026-08-20","subject":"Claude Agent SDK","section":"Section 8 · The Claude Agent SDK","sectionShort":"Section 8","storageKey":"ccaf_learning_graph_v1","notesPath":"08-claude-agent-sdk/notes.md","clusters":[{"id":"cl_foundations","name":"Foundations & the SDK boundary","subject":"Claude Agent SDK","summary":"What the Agent SDK takes over from the hand-rolled agentic loop, the subprocess mental model that explains its whole surface area, and the four options that decide how loud or quiet an agent's defaults are.","color":"#7a4a6b","colorDark":"#a8779a","notesPart":"Part 1 — The SDK versus the hand-rolled loop"},{"id":"cl_subagents","name":"Subagents & orchestration","subject":"Claude Agent SDK","summary":"Defining subagents, the tool that spawns them, exactly what crosses the context boundary in each direction, and the three decomposition shapes the exam names.","color":"#2f6b6b","colorDark":"#4f9c9c","notesPart":"Part 2 — Subagents and multi-agent orchestration"},{"id":"cl_hooks","name":"Hooks & permissions","subject":"Claude Agent SDK","summary":"Programmatic enforcement versus prompt instruction, where hooks attach, which direction each one faces, and the permission surface that decides whether a hook is even reached.","color":"#8f3f3f","colorDark":"#c67878","notesPart":"Part 3 — Hooks: programmatic control of the loop"},{"id":"cl_tools","name":"Tools, MCP & structured errors","subject":"Claude Agent SDK","summary":"In-process MCP servers, the two shapes of tool(), description design as the primary selection mechanism, and the three error fields — only one of which is real.","color":"#5f6b3a","colorDark":"#8ba055","notesPart":"Part 4 — Tools and MCP inside the SDK"},{"id":"cl_sessions","name":"Sessions, context & reliability","subject":"Claude Agent SDK","summary":"Resuming versus forking, compaction and delegation as context levers, and the reliability posture that turns \"did you run the tests?\" into a condition the agent cannot skip.","color":"#3f6b86","colorDark":"#5f9cc0","notesPart":"Part 5 — Sessions, context, and reliability"}],"standards":[{"id":"ccaf:d1","family":"ccaf","code":"Domain 1","name":"Agentic Architecture & Orchestration","weight":0.27,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d2","family":"ccaf","code":"Domain 2","name":"Tool Design & MCP Integration","weight":0.18,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d3","family":"ccaf","code":"Domain 3","name":"Prompt Engineering & Structured Output","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d4","family":"ccaf","code":"Domain 4","name":"Claude Code Configuration & Workflows","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d5","family":"ccaf","code":"Domain 5","name":"Context Management & Reliability","weight":0.15,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf-glossary:t3","family":"ccaf-glossary","code":"t3","name":"Hub-and-spoke coordination","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t4","family":"ccaf-glossary","code":"t4","name":"Task tool in a coordinator's allowedTools","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t6","family":"ccaf-glossary","code":"t6","name":"How a subagent receives prior findings","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t7","family":"ccaf-glossary","code":"t7","name":"fork_session for comparing strategies","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t8","family":"ccaf-glossary","code":"t8","name":"--resume for a named prior conversation","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t9","family":"ccaf-glossary","code":"t9","name":"Deterministic compliance via programmatic enforcement","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t10","family":"ccaf-glossary","code":"t10","name":"PostToolUse normalization","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t11","family":"ccaf-glossary","code":"t11","name":"Tool call interception hook","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t12","family":"ccaf-glossary","code":"t12","name":"Prompt chaining","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t13","family":"ccaf-glossary","code":"t13","name":"Dynamic / adaptive decomposition","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t14","family":"ccaf-glossary","code":"t14","name":"Tool description as the selection mechanism","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t15","family":"ccaf-glossary","code":"t15","name":"isError","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t16","family":"ccaf-glossary","code":"t16","name":"errorCategory","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t17","family":"ccaf-glossary","code":"t17","name":"isRetryable","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t40","family":"ccaf-glossary","code":"t40","name":"Explore subagent as a context lever","source":"study-reference/ccaf_study_reference.md"},{"id":"ccaf-glossary:t60","family":"ccaf-glossary","code":"t60","name":"/compact","source":"study-reference/ccaf_study_reference.md"}],"topics":[{"id":"mt_subprocess_model","type":"CONCEPTUAL","cluster":"cl_foundations","name":"The subprocess mental model","description":"The SDK spawns the Claude Code CLI as a subprocess and talks to it over a control protocol. That one fact explains why a library has permission modes, hooks, settings files, CLAUDE.md loading, and subagents.","core":true,"estMinutes":6,"evidence":["Explain why a client library would have a `pathToClaudeCodeExecutable` option","Predict that a Claude Code feature (settings, CLAUDE.md, subagents) is reachable from the SDK before reading its docs"],"assessmentPrompt":"Why does the Agent SDK have permission modes and settings files at all, when the raw Messages API has neither?","standards":["ccaf:d1"],"sources":[{"label":"Lesson · The second engine","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene0"},{"label":"Notes · Part 1","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.698,"layer":1,"x":330,"y":170,"knowledgePoints":[{"id":"kp_subproc_1","name":"One fact explains the whole surface area","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>Here is the fact that makes the rest of the SDK stop feeling arbitrary:</p><p class='kp-key'>The SDK spawns the Claude Code CLI as a subprocess and talks to it over a control protocol.</p><p>It is a steering wheel bolted onto a car that already exists. Which is why a <em>library</em> — something you would expect to be a thin HTTP client — has permission modes, hooks, settings files, CLAUDE.md loading, and subagents. Those are Claude Code's features, exposed programmatically.</p><p>It also explains options that would be nonsense in an HTTP client: <code>pathToClaudeCodeExecutable</code>, <code>spawnClaudeCodeProcess</code>, and <code>settingSources</code>.</p>","questions":[{"id":"q_subproc_1a","q":"A developer new to the Agent SDK is confused that a client library — something they expected to be a thin wrapper making HTTP calls to the Messages API — ships with permission modes, hooks, settings-file loading, and CLAUDE.md support, none of which exist on the raw API. What single fact explains why the SDK has this much surface area?","options":["These features were added by the SDK team specifically to match capabilities offered by competing agent frameworks.","The SDK spawns the Claude Code CLI as a subprocess and drives it over a control protocol, inheriting its features.","The Messages API exposes all of these features already, gated behind a beta header the developer has not enabled.","The Model Context Protocol specification requires any compliant client to implement all of these capabilities."],"correct":1,"explain":"Because the SDK is a steering wheel bolted onto the Claude Code CLI, every feature the CLI already has — permission modes, hooks, settings files, CLAUDE.md loading — comes along for free as SDK options. None of this is exposed by the plain Messages API behind any header, and MCP itself doesn't mandate hooks or settings-file support. Competitive feature-matching isn't the explanation; the subprocess relationship is."},{"id":"q_subproc_1b","q":"While reading through the Agent SDK's options reference for the first time, a developer who has only ever used a plain HTTP client for the Messages API is trying to spot which option name could not possibly belong to a simple request-response client. Which one is that unmistakable tell?","options":["includePartialMessages, which controls whether streamed output arrives as incremental chunks or as one final block.","maxThinkingTokens, which caps how many tokens the model may spend on extended thinking before responding.","pathToClaudeCodeExecutable, since pointing at an executable only makes sense if a process is being spawned.","systemPromptPreset, which selects between a minimal system prompt and Claude Code's full default prompt."],"correct":2,"explain":"You only need to point a library at an executable path if that library is going to spawn and run that executable — no HTTP client needs one. includePartialMessages, maxThinkingTokens, and systemPromptPreset are all plausible fields for a plain request-response API and don't by themselves reveal a subprocess. pathToClaudeCodeExecutable is the option that only makes sense given the subprocess model."}]},{"id":"kp_subproc_2","name":"Using the model to predict features","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>The model earns its keep as a prediction tool. If a capability exists in the Claude Code CLI, the default assumption is that it is reachable from the SDK — usually under a camelCase version of the same name.</p><p>Skills, memory, plugins, MCP servers, hooks, permission modes, CLAUDE.md, subagents in <code>.claude/agents/</code>: all of these are Claude Code concepts you will meet again as SDK options.</p><p>The converse is also useful. If something is <em>not</em> a Claude Code feature — say, prompt caching controls or a beta header — expect it to belong to the API layer instead, and not to appear as an SDK option at all.</p>","questions":[{"id":"q_subproc_2a","q":"You are about to read the Agent SDK's subagents documentation for the first time, but you already know the SDK spawns the Claude Code CLI as a subprocess and that CLI features tend to surface as SDK options under similar names. Applying that model before opening the docs, what do you predict about where subagent definitions can live?","options":["Only inline in code, since a client library has no way to read arbitrary files from the local filesystem.","Only in a hosted registry that the SDK downloads subagent definitions from at the very start of every session.","Only in environment variables that are read once when the SDK process is first started up by the caller.","In code and on disk under .claude/agents/, mirroring how the underlying Claude Code CLI already defines them."],"correct":3,"explain":"Since subagents are a Claude Code CLI concept and the SDK drives that same CLI, the correct prediction is that both routes exist: programmatic definitions in code, and markdown files under .claude/agents/, the same place the CLI already looks. Filesystem access is exactly what the subprocess model grants, so ruling it out is the mistake the other three options make in different ways. This is the model paying off before you've read a single page of documentation."},{"id":"q_subproc_2b","q":"A study group is listing reasons the subprocess model is worth memorizing for the exam: it predicts which features the SDK is likely to have, it explains why settings files on disk affect an SDK-based application, and it answers the recurring \"why does the SDK look like this\" question. One member adds a fourth reason that turns out not to actually follow from the subprocess model at all. Which is it?","options":["It tells you which model id the SDK selects by default when none is explicitly passed in the options.","It predicts which Claude Code CLI features are likely to reappear as SDK options under similar names.","It explains why on-disk settings files influence the behavior of a program built entirely on the SDK.","It answers the recurring exam question of why the SDK's surface area looks the way that it does."],"correct":0,"explain":"Default model selection is an unrelated configuration detail — nothing about spawning a subprocess determines which model id gets used when one isn't specified. The other three are exactly the payoff of the subprocess model: it predicts SDK feature surface, explains disk-based settings inheritance, and is the underlying reason the SDK has the shape it does. That's why it's the odd one out in the group's list."}]}],"reviewQuestions":[{"id":"r_subproc_1","q":"A new hire asks why a client library — the Agent SDK — has permission modes, hooks, settings-file loading, CLAUDE.md support, and subagents, all features they'd expect from a CLI tool rather than an SDK. Which single mental model correctly accounts for all five features at once?","options":["The library independently reimplements each Claude Code feature on top of the plain Messages API, one at a time.","The library spawns the Claude Code CLI as a subprocess and drives it over a control protocol, inheriting its features.","Every one of these features comes directly from the Model Context Protocol specification that the SDK implements.","These features are inherited by the Agent SDK from the plain Anthropic SDK clients used for the Messages API."],"correct":1,"explain":"The SDK is a steering wheel bolted onto a car that already exists: it spawns and drives the Claude Code CLI, so every CLI feature shows up as an SDK option. It doesn't reimplement these features independently, they aren't mandated by MCP, and the plain Anthropic SDK clients for the Messages API have none of them to inherit. That single relationship explains all five features in the new hire's list at once."}]},{"id":"mt_loop_ownership","type":"CONCEPTUAL","cluster":"cl_foundations","name":"Mechanism vs. policy: what you stop writing","description":"In the hand-rolled loop you own stop_reason checks, tool dispatch, tool_result blocks, and the re-call. The SDK owns all of it and hands you a message stream. What you own shifts from mechanism to policy: which tools exist, who may call them, when to stop, what runs around each call.","core":true,"estMinutes":7,"evidence":["Name the four steps of the hand-rolled loop the SDK absorbs","Classify a given concern as mechanism (SDK's) or policy (yours)"],"assessmentPrompt":"You move a working hand-rolled agent onto the Agent SDK. Which of your code disappears, and which of it becomes configuration?","standards":["ccaf:d1"],"sources":[{"label":"Lesson · The loop is no longer yours","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene1"}],"centrality":1,"layer":0,"x":120,"y":46,"knowledgePoints":[{"id":"kp_loop_1","name":"The four steps that disappear","keyPrerequisites":[],"teach":"<p>In <em>Building with the Claude API</em> the agentic loop is yours, and it is always the same four steps:</p><pre class='kp-code'>while True:\n    resp = client.messages.create(...)          # 1. call\n    if resp.stop_reason != \"tool_use\": break     # 2. inspect stop_reason\n    result = run_tool(resp.content[-1])          # 3. dispatch the tool yourself\n    messages.append(tool_result_block(result))   # 4. append and go around again</pre><p>The Agent SDK runs that loop for you and hands you a stream of messages instead. All four steps vanish from your code.</p><p>What replaces them is not nothing. What you own shifts from <strong>mechanism</strong> — how the loop turns — to <strong>policy</strong>: which tools exist, who may call them, when to stop, and what runs around each call. Every option you will meet in this section is a policy knob.</p>","questions":[{"id":"q_loop_1a","q":"Your team maintains a hand-rolled support agent: a while loop that calls messages.create, checks resp.stop_reason for tool_use, dispatches the matched tool function, and appends a tool_result block before looping again. After porting this agent to the Claude Agent SDK and wrapping the same tool implementations and system prompt in a query() call, which part of the original code is safe to delete?","options":["The stop_reason check, the tool-dispatch logic, and the code that re-issues the request after appending each tool_result block.","The individual tool functions, since the SDK ships its own built-in replacements for any tool a hand-rolled agent might define.","The system prompt text and the chosen model id, since the SDK selects both automatically based on the conversation history.","The retry and exponential-backoff logic, which moves out of application code and down into the SDK's HTTP transport layer."],"correct":0,"explain":"The four mechanical steps — calling the API, inspecting stop_reason, dispatching the tool, and re-issuing the request — are exactly what the SDK's loop absorbs, so that code can go. The tool implementations, system prompt, and model choice are all policy decisions the SDK still expects the caller to supply, not mechanism it replaces. Retry and backoff are a separate concern from the loop this question describes, and porting to the SDK does not relocate them on its own."},{"id":"q_loop_1b","q":"A team migrating from a hand-rolled loop to the Agent SDK assumes that because the SDK now runs the loop, it also decides everything about how the agent behaves. During review, someone points out that one specific decision was never actually mechanism — it was always the caller's to make, both before and after the migration. Which decision is this?","options":["How the SDK formats and appends a tool_result block once a given tool implementation finishes running.","Which tools the agent is permitted to call at all, a decision the SDK never makes on the caller's behalf.","Detecting that resp.stop_reason has come back as tool_use rather than end_turn on a given API response.","Re-issuing the request to the model once every requested tool call has finished executing and returned."],"correct":1,"explain":"Tool permissioning is policy: the SDK exposes hooks and options for it, but it never decides on its own which tools exist or who may call them. Formatting tool_result blocks, checking stop_reason, and re-issuing the request are the three mechanical steps of the loop, and those are precisely what the SDK now performs internally. Confusing the two leads teams to think migrating to the SDK also migrated decisions that were always theirs."}]},{"id":"kp_loop_2","name":"Why the delta matters for the exam","keyPrerequisites":[],"teach":"<p>The hand-rolled loop is still tested. The SDK is the <em>delta</em> on top of it, not a replacement for knowing it.</p><p>A useful reading habit for exam questions: when a scenario describes something going wrong <em>inside</em> the loop — a tool never runs, a turn never ends, a result never comes back — ask whether the fix is a policy you failed to state, not a mechanism you failed to write. With the SDK, mechanism bugs are rarely yours.</p>","questions":[{"id":"q_loop_2a","q":"An SDK-based agent is configured with a file_search tool that your integration tests confirm works correctly in isolation, yet in production the agent never calls it, even on prompts that clearly require it. You confirm the subprocess is running normally and no exceptions are being thrown. Where should you focus your investigation first?","options":["Inside the loop, for a missing or misordered stop_reason branch that would prevent tool_use from being handled.","In the HTTP transport, for a tool_result block that is being silently dropped before it reaches the model.","In policy: whether the tool is registered, permitted for this session, and described clearly enough to be selected.","In the model configuration, for a temperature setting high enough to make tool selection unpredictable."],"correct":2,"explain":"With the SDK, the loop and the transport are mechanism the SDK owns and rarely the source of a bug like this. A tool that is technically available but never gets called is almost always a policy gap: it isn't registered for this session, permissions block it, or its description doesn't make clear when to use it. Temperature affects wording and creativity far more than whether a well-described tool gets selected."},{"id":"q_loop_2b","q":"A colleague who has only ever used the Agent SDK, and has never written a hand-rolled agentic loop, struggles to remember what options like maxTurns and outputFormat actually control, even after reading their descriptions. You suggest they first spend an afternoon studying the plain Messages API loop instead of rereading the SDK reference. What is the reasoning behind that suggestion?","options":["The SDK silently falls back to a hand-rolled loop whenever the Claude Code CLI executable cannot be found.","A hand-rolled loop must already exist and work correctly before any SDK-managed tool calls will function.","Python developers are expected to hand-roll the loop while TypeScript developers are not, per the SDK docs.","The SDK's options are named after loop concepts, so the vocabulary only makes sense once the loop is familiar."],"correct":3,"explain":"maxTurns names a limit on loop iterations, tool_result names a message block the loop used to append by hand, and stop_reason names a field the loop used to inspect — none of that vocabulary is self-explanatory without having seen the loop it replaced. The SDK doesn't fall back to a hand-rolled loop on a missing executable, and nothing about the SDK differs between Python and TypeScript on this point. Understanding the mechanism first makes the policy options legible rather than arbitrary."}]}],"reviewQuestions":[{"id":"r_loop_1","q":"A team lead is writing an internal onboarding doc and wants one sentence that captures what genuinely changes for a developer moving from a hand-rolled Messages API loop to the Claude Agent SDK, without overselling or underselling the shift. Which sentence best captures it?","options":["You stop owning the mechanism that turns the loop and start owning the policy that governs what it does.","You trade a slower, hand-written loop for a version of the same loop that simply executes faster.","You give up the ability to define and run your own tools in exchange for a fully managed streaming interface.","You move your agent's implementation from Python into TypeScript, since the SDK is TypeScript-first."],"correct":0,"explain":"Mechanism versus policy is the actual trade: the SDK absorbs the call-inspect-dispatch-append cycle, and what remains yours is which tools exist, who may call them, and when to stop. Speed is not the distinction the migration makes, and hand-rolled agents can already be reasonably fast. Custom tools remain fully available through the SDK, and the language you write in is an independent choice, not a consequence of adopting it."}]},{"id":"mt_entry_points","type":"FACTUAL","cluster":"cl_foundations","name":"Packages, entry points, and options types","description":"TypeScript `@anthropic-ai/claude-agent-sdk` with `query({ prompt, options })` and the `Options` type; Python `claude-agent-sdk` with `query(prompt=..., options=...)` and `ClaudeAgentOptions`. TypeScript adds `startup()` → `WarmQuery` to pre-spawn the subprocess (60,000 ms default initialize timeout).","core":true,"estMinutes":6,"evidence":["Recall both package names and both options type names without hedging","Say what `startup()` buys you and what it costs"],"assessmentPrompt":"Name the package and the options type for each language, and say what pre-warming does.","standards":["ccaf:d1"],"sources":[{"label":"Notes · Installation and entry points","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.651,"layer":1,"x":330,"y":108,"knowledgePoints":[{"id":"kp_entry_1","name":"Packages, calls, and options types","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>Two languages, same engine, different ignition keys:</p><table class='kp-table'><tr><th></th><th>TypeScript</th><th>Python</th></tr><tr><td>Package</td><td><code>@anthropic-ai/claude-agent-sdk</code></td><td><code>claude-agent-sdk</code></td></tr><tr><td>One-off call</td><td><code>query({ prompt, options })</code></td><td><code>query(prompt=..., options=...)</code></td></tr><tr><td>Options type</td><td><code>Options</code></td><td><code>ClaudeAgentOptions</code></td></tr><tr><td>Returns</td><td><code>Query</code> — an AsyncGenerator with extra methods</td><td><code>AsyncIterator[Message]</code></td></tr><tr><td>Multi-turn client</td><td>— (use <code>query</code> with <code>resume</code>)</td><td><code>ClaudeSDKClient</code></td></tr></table><p>The names are worth rote memorising. <code>@anthropic-ai/claude-code</code> is a different package; <code>claude-sdk</code> and <code>@anthropic/agent-sdk</code> are not packages at all.</p>","questions":[{"id":"q_entry_1a","q":"During code review, a pull request imports the Agent SDK using the package name @anthropic-ai/claude-code in a TypeScript project, and the reviewer notes this is actually the CLI package, not the SDK. What is the correct pair of package names for TypeScript and Python respectively?","options":["TypeScript @anthropic/agent-sdk and Python anthropic-claude-sdk, both maintained under the anthropic organization.","TypeScript @anthropic-ai/agent-sdk and Python claude-code-sdk, matching the naming used by the CLI package.","TypeScript @anthropic-ai/claude-agent-sdk and Python claude-agent-sdk, the two official Agent SDK packages.","TypeScript @anthropic-ai/claude-code and Python anthropic-agents, sharing a prefix with the CLI's own package."],"correct":2,"explain":"@anthropic-ai/claude-agent-sdk and claude-agent-sdk are the two official package names — same engine, different ignition keys for each language. @anthropic-ai/claude-code is the CLI package the reviewer flagged, not the SDK, which rules out the option built around it. The other two combinations invent plausible-sounding names that don't match either official package."},{"id":"q_entry_1b","q":"A Python developer writes result = query(prompt=\"...\", options=opts) expecting to get a final answer back synchronously, then is confused when result behaves like neither a string nor a completed value. What does query() actually return in the Python SDK, and how should it be consumed?","options":["A list of every message in the conversation, populated only once the entire run has completed executing.","A ClaudeSDKClient instance that the caller must separately call connect() on before any messages arrive.","A plain synchronous generator that yields decoded output strings as the underlying subprocess produces them.","An AsyncIterator[Message] that the caller iterates over with async for as the run produces each message."],"correct":3,"explain":"query() in Python returns an AsyncIterator[Message], meant to be consumed with async for as messages stream in — not collected into a list after the run finishes. ClaudeSDKClient is a separate, heavier entry point for multi-turn conversations, not what a one-off query() call returns. The iterator is asynchronous rather than a plain synchronous generator, which matters for how it must be consumed."}]},{"id":"kp_entry_2","name":"Pre-warming with startup()","keyPrerequisites":["mt_subprocess_model"],"teach":"<p>Spawning a subprocess and completing an initialize handshake takes real time. If the first thing a user does is wait for it, they feel it.</p><p><code>startup()</code> (TypeScript) pre-spawns the subprocess and finishes the handshake <em>before a prompt exists</em>, returning a <code>WarmQuery</code>. The first real <code>query()</code> then does not pay the spawn cost inline. The default initialize timeout is <strong>60,000 ms</strong>.</p><p>This option only exists because of the subprocess. There is nothing to pre-warm in an HTTP client.</p>","questions":[{"id":"q_entry_2a","q":"A voice assistant built on the TypeScript Agent SDK measures that the very first query() call in a session takes noticeably longer than every subsequent one, because it pays the cost of spawning the Claude Code subprocess and completing its initialize handshake inline. A teammate suggests calling startup() as soon as the app launches, before any user prompt exists. What problem does this actually solve?","options":["It pays the subprocess spawn and initialize-handshake cost ahead of time, so the first real query() responds faster.","It caches the system prompt on Anthropic's servers so subsequent requests are billed at a reduced token rate.","It preloads and validates every MCP tool schema so tool-call errors surface at startup instead of mid-conversation.","It verifies the configured API key is valid before the user's first prompt, avoiding a failed request later."],"correct":0,"explain":"startup() exists specifically to move the subprocess spawn and handshake — the slow part — earlier, so the first real query() doesn't pay that cost inline; the default initialize timeout is 60,000 ms. It has nothing to do with prompt-cache billing, MCP schema validation, or API key checks, even though those might sound like plausible startup tasks. The problem it solves is purely one of perceived latency on the first request."},{"id":"q_entry_2b","q":"A developer who has only ever built against the plain Messages API using a lightweight HTTP client asks why that client has no equivalent of the Agent SDK's startup() function for pre-warming a connection. What is the actual reason?","options":["Plain HTTP clients already establish and reuse a warm connection automatically the first time they are constructed.","There is no subprocess or handshake to pay for up front, since a plain HTTP client just issues a request directly.","The Messages API used by that HTTP client does not support streaming responses, making pre-warming unnecessary.","API keys used by plain HTTP clients are validated fresh on every single request, defeating any pre-warming benefit."],"correct":1,"explain":"Pre-warming only pays off when there's a subprocess to spawn and a handshake to complete before the first prompt — an HTTP client sending a request directly to the Messages API has neither. The Messages API does support streaming, so that isn't the explanation, and API key validation happening per-request doesn't prevent connection reuse either. The absence of startup() simply reflects the absence of the cost it exists to hide."}]}],"reviewQuestions":[{"id":"r_entry_1","q":"You're writing a code sample that needs to reference the type used to configure an Agent SDK call correctly in both languages, and a reviewer flags that you've used the same type name in both the TypeScript and Python versions of the snippet. What are the two distinct, correct type names?","options":["Both languages use a type named AgentOptions, since the SDK's naming is kept identical across languages.","Both languages call the configuration type ClaudeAgentOptions, matching the name of the SDK package itself.","TypeScript uses the bare name Options, while Python spells it out fully as ClaudeAgentOptions.","TypeScript uses ClaudeAgentOptions, while Python uses the shorter, bare name Options for the same concept."],"correct":2,"explain":"TypeScript keeps the bare name Options, while Python spells it out as ClaudeAgentOptions — the two languages genuinely diverge here, unlike the query() function name which stays consistent. Claiming both languages share one identical type name, in either direction, is the exact mistake the reviewer caught in the snippet. This pairing is worth drilling specifically because it's a common distractor on questions comparing the two SDKs."}]},{"id":"mt_query_vs_client","type":"PROCEDURAL","cluster":"cl_foundations","name":"query() vs. ClaudeSDKClient","description":"`query()` opens a new session per call, handles one exchange, and cannot be interrupted. `ClaudeSDKClient` (Python) reuses one session across exchanges, is manually connected, and supports interrupts. Rule of thumb: if the next action depends on Claude's response, or you need to interrupt, use the client.","core":true,"estMinutes":8,"evidence":["Pick the right entry point from a described workload","Name the two capabilities `query()` cannot provide"],"assessmentPrompt":"A support console needs to interrupt a long-running agent when the customer hangs up. Which entry point, and why?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Notes · query() versus ClaudeSDKClient","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.419,"layer":2,"x":540,"y":46,"knowledgePoints":[{"id":"kp_qvc_1","name":"The comparison table","keyPrerequisites":["mt_entry_points"],"teach":"<table class='kp-table'><tr><th>Feature</th><th><code>query()</code></th><th><code>ClaudeSDKClient</code></th></tr><tr><td>Session</td><td>New session by default</td><td>Reuses the same session</td></tr><tr><td>Conversation</td><td>Single exchange</td><td>Multiple exchanges, one context</td></tr><tr><td>Connection</td><td>Managed automatically</td><td>Manual control</td></tr><tr><td>Interrupts</td><td>Not supported</td><td>Supported</td></tr><tr><td>Continue chat</td><td>Manual via continue_conversation / resume</td><td>Automatic</td></tr><tr><td>Use case</td><td>One-off tasks</td><td>Continuous conversations</td></tr></table><p>Streaming input, hooks and custom tools work with both — those are not the discriminators.</p>","questions":[{"id":"q_qvc_1a","q":"A team compares query() and ClaudeSDKClient feature-by-feature and correctly notes that hooks, custom in-process tools, and streaming input are all available through either entry point. They're left with exactly one capability that only one of the two supports. Which capability is it, and which entry point has it?","options":["Hooks, which only ClaudeSDKClient can register, since query() runs each call in an isolated sandboxed process.","Custom in-process tools, which query() cannot use because it lacks a persistent connection to attach them to.","Streaming input, which is exclusive to query() because ClaudeSDKClient requires the full prompt up front.","Interrupts, which only ClaudeSDKClient supports, since query() offers no mechanism to cancel a run mid-turn."],"correct":3,"explain":"Interrupts are the one capability genuinely exclusive to ClaudeSDKClient — query() has no equivalent mechanism for cancelling a run partway through. Hooks, custom in-process tools, and streaming input all work identically with either entry point, which is exactly why the team ruled those three out already. The remaining capability, by elimination and by fact, is interrupts."},{"id":"q_qvc_1b","q":"A developer calls query() twice in a row from the same process, expecting the second call to remember what was discussed in the first, and is surprised when the agent responds as if it has never seen the earlier conversation. What is query()'s actual default behavior with respect to sessions?","options":["It opens a brand-new session on every call unless the caller explicitly passes resume with a prior session id.","It automatically reuses whichever session was most recently active in the current process, with no configuration.","It shares exactly one session across the entire lifetime of the process, regardless of how many times it's called.","It refuses to execute a second time within the same process unless a session id is passed on the first call."],"correct":0,"explain":"New session by default is the trap here: query() does not remember prior calls unless the caller explicitly passes resume, which is exactly what caught this developer out. It doesn't auto-resume the most recent session, doesn't share one session for the whole process lifetime, and doesn't refuse to run without a session id — it just quietly starts fresh. This default is precisely why an app expecting continuity needs ClaudeSDKClient or explicit resume handling instead."}]},{"id":"kp_qvc_2","name":"The rule of thumb, applied","keyPrerequisites":["mt_entry_points"],"teach":"<p>The rule worth memorising: <strong>if the next action depends on Claude's response, or you need to interrupt, use <code>ClaudeSDKClient</code>. Otherwise <code>query()</code>.</strong></p><p>Two worked cases:</p><ul><li><em>Nightly job that summarises yesterday's tickets and writes a file.</em> Nothing branches on the answer, nobody is watching, nothing to interrupt → <code>query()</code>.</li><li><em>Support console where an operator watches the agent work and can cancel it.</em> Interruption is a hard requirement → <code>ClaudeSDKClient</code>.</li></ul>","questions":[{"id":"q_qvc_2a","q":"A support console lets a human operator watch an agent work through a customer's request in real time, and the product spec requires that if the customer hangs up mid-conversation, the operator can immediately cancel the in-progress agent turn rather than waiting for it to finish naturally. Which entry point satisfies this requirement, and why?","options":["query(), paired with an AbortController to cancel the underlying HTTP request the moment the operator clicks cancel.","ClaudeSDKClient, since it is the only entry point of the two that supports interrupting a run in progress.","Either entry point works equally well here, since interrupt support is identical between query() and ClaudeSDKClient.","query(), configured with a deliberately low maxTurns value so any single turn cannot run long before stopping."],"correct":1,"explain":"Interrupts are the capability the comparison table draws a hard line on: only ClaudeSDKClient supports cancelling a run mid-turn, which is exactly what this operator console needs. An AbortController can end a request, but that's not the same interrupt facility, and a low maxTurns just shortens the run in advance rather than letting an operator cancel on demand. The two entry points are not equivalent here, which rules out treating them as interchangeable."},{"id":"q_qvc_2b","q":"A nightly batch job pulls yesterday's support tickets, runs one independent prompt per ticket asking for a summary, and writes each result to its own file — no ticket's summary should be influenced by another ticket's contents. Which entry point fits this job, and what's the risk of choosing the other one?","options":["ClaudeSDKClient, so every ticket in the batch shares one context and later tickets benefit from earlier summaries.","query(), because ClaudeSDKClient is architecturally unable to call any tools during a multi-turn conversation.","query(), since each ticket is a one-off exchange, and reusing a client risks leaking details between tickets.","ClaudeSDKClient, because batch jobs specifically require the manual connection control that only it provides."],"correct":2,"explain":"Each ticket here is an independent one-off exchange, which is exactly what query() is for; reusing one ClaudeSDKClient session across unrelated tickets risks one customer's details bleeding into another's summary through shared context. ClaudeSDKClient can call tools just fine, so that isn't a reason to avoid it, and nothing about batch processing specifically requires manual connection control. The isolation query() provides by default is the actual reason it's the right fit."}]}],"reviewQuestions":[{"id":"r_qvc_1","q":"A team is documenting an internal rule of thumb for choosing between query() and ClaudeSDKClient so future engineers don't have to re-derive it from the comparison table each time. Which statement correctly captures that rule?","options":["Use ClaudeSDKClient any time the agent is expected to call one or more tools at any point during its run.","Use query() specifically when the application happens to be written in TypeScript rather than in Python.","Use ClaudeSDKClient any time the agent's response needs to be streamed back to the caller incrementally.","Use ClaudeSDKClient if the next action depends on Claude's response or you need to interrupt; otherwise use query()."],"correct":3,"explain":"Dependence on the response and the need to interrupt are the two conditions that actually distinguish the entry points, per the comparison table. Tool use and streaming input work identically with either one, so neither is a valid basis for the choice, and the rule has nothing to do with which language the application is written in. This is the rule of thumb worth memorizing over the raw table."}]},{"id":"mt_system_prompt","type":"PROCEDURAL","cluster":"cl_foundations","name":"The system prompt is minimal by default","description":"Omitting `systemPrompt` gives a minimal prompt, not Claude Code's. `{ type: 'preset', preset: 'claude_code' }` opts into the full one; `append` extends it; `excludeDynamicSections: true` moves per-session context into the first user message so the prompt cache survives across machines.","core":true,"estMinutes":7,"evidence":["Predict the behaviour difference between an SDK agent and the Claude Code CLI on the same prompt","Choose the right systemPrompt shape for a custom product agent vs. a coding agent"],"assessmentPrompt":"Your SDK agent behaves nothing like Claude Code on the same task. What is the first option you check?","standards":["ccaf:d1","ccaf:d3"],"sources":[{"label":"Notes · The system prompt is minimal by default","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.209,"layer":2,"x":540,"y":170,"knowledgePoints":[{"id":"kp_sysprompt_1","name":"Minimal by default","keyPrerequisites":["mt_subprocess_model"],"teach":"<p>This one trips people constantly. <code>query()</code> does <strong>not</strong> give you Claude Code's system prompt unless you ask for it.</p><ul><li>Omit <code>systemPrompt</code> → a <em>minimal</em> prompt.</li><li><code>systemPrompt: \"…\"</code> → your custom prompt, and only yours.</li><li><code>systemPrompt: { type: 'preset', preset: 'claude_code' }</code> → Claude Code's full prompt.</li></ul><p>So an SDK agent that feels dumber than the CLI on the same task usually is not misconfigured in any exotic way — it just never opted into the preset.</p>","questions":[{"id":"q_sysprompt_1a","q":"A developer builds a coding-assistant prototype by calling query() with a custom prompt and never sets the systemPrompt field, assuming the SDK will automatically give the agent the same rich system prompt the Claude Code CLI itself uses. What does the agent actually receive?","options":["A minimal system prompt, not Claude Code's full prompt, since the CLI's preset must be explicitly requested.","Claude Code's full system prompt by default, since query() is built on top of the Claude Code CLI subprocess.","No system prompt at all, and the SDK raises a configuration error until systemPrompt is explicitly provided.","Whatever instructions are found in the nearest CLAUDE.md file, treated as a stand-in system prompt automatically."],"correct":0,"explain":"Omitting systemPrompt gives a minimal prompt, not Claude Code's — opting into the CLI's full prompt is explicit, via the claude_code preset, precisely because most SDK applications aren't building a coding agent. No error is raised for omitting it, and CLAUDE.md loading is a separate, unrelated mechanism governed by settingSources. This is one of the most common sources of an SDK agent that quietly underperforms compared to the CLI on the same task."},{"id":"q_sysprompt_1b","q":"An SDK-based agent given the exact same coding task the Claude Code CLI handles well instead produces vague, generic responses that ignore obvious project conventions. The model id, maxTurns, and hook configuration all match what the CLI would use. What should be checked first?","options":["Whether the model id passed into the SDK options actually matches the model the CLI defaults to using.","Whether systemPrompt is configured to opt into the claude_code preset rather than being left at its default.","Whether a PreToolUse hook is silently blocking every tool call the agent attempts to make during this run.","Whether maxTurns has been set too low for the agent to make meaningful progress on the coding task."],"correct":1,"explain":"The model id, maxTurns, and hooks are already confirmed to match, which leaves the system prompt as the remaining and by far most common explanation: without opting into the claude_code preset, the SDK agent runs on a minimal prompt instead of the CLI's full one. Same underlying engine, very different instructions is the actual cause far more often than anything else in this list. That's why it's the first thing worth checking, not the model configuration already ruled out."}]},{"id":"kp_sysprompt_2","name":"append and excludeDynamicSections","keyPrerequisites":["mt_subprocess_model"],"teach":"<p>Two modifiers sit on the preset form:</p><ul><li><code>append: \"…\"</code> — extends the preset rather than replacing it. This is how you add house rules without losing Claude Code's behaviour.</li><li><code>excludeDynamicSections: true</code> — moves per-session context (the parts of the prompt that differ per machine and per run) into the first user message. The point is <strong>prompt-cache reuse</strong>: a prompt prefix that is identical across machines can be cached and shared; one that embeds today's working directory cannot.</li></ul>","questions":[{"id":"q_sysprompt_2a","q":"An engineering team notices that their Agent SDK application's system prompt embeds the current working directory and machine hostname, which differ on every deployment, and as a result prompt caching almost never produces a cache hit across runs. What option addresses this, and how?","options":["Setting append to an empty string, which shrinks the overall system prompt and reduces the number of tokens sent.","Setting excludeDynamicSections: true, which strips every tool definition entirely out of the system prompt.","Setting excludeDynamicSections: true, which moves per-session context into the first message to stay cacheable.","Disabling CLAUDE.md loading via settingSources, which removes any machine-specific content from the loaded prompt."],"correct":2,"explain":"excludeDynamicSections: true moves the parts of the prompt that vary per machine or per run — like working directory — out of the system prompt and into the first user message, so the cacheable prefix stays identical across deployments and cache hits go up. It doesn't remove tool definitions, and an empty append string wouldn't touch the dynamic sections causing the problem. Disabling CLAUDE.md loading is unrelated to this specific cache-poisoning issue, since CLAUDE.md content isn't what's varying here."},{"id":"q_sysprompt_2b","q":"A team wants their SDK-based agent to behave exactly like the Claude Code CLI on coding tasks, but with three additional company-specific rules layered on top, without losing any of the CLI's existing instructions. Which systemPrompt configuration achieves this?","options":["A systemPrompt set to a plain string containing only the three company-specific rules, and nothing else at all.","Omitting systemPrompt entirely and instead placing the three rules inside the project's CLAUDE.md file.","Two sequential query() calls, one using the claude_code preset and a second one adding the three rules.","The claude_code preset with append set to the text of the three company-specific rules, extending rather than replacing it."],"correct":3,"explain":"append extends the preset instead of replacing it, which is exactly what's needed to keep Claude Code's full behavior while layering on three additional rules. A bare string discards the preset entirely rather than adding to it, and CLAUDE.md loading is a separate mechanism gated by settingSources that doesn't interact with systemPrompt directly. Two sequential calls would just run two independent, unrelated conversations rather than merging instructions into one."}]}],"reviewQuestions":[{"id":"r_sysprompt_1","q":"A reference doc needs one example that correctly demonstrates how to give an SDK agent Claude Code's exact full system prompt, as opposed to a minimal default or a fully custom one. Which systemPrompt value is the correct example?","options":["systemPrompt: { type: 'preset', preset: 'claude_code' }, which explicitly opts into the CLI's full default prompt.","Omitting the systemPrompt field entirely, which the doc wrongly assumes defaults to Claude Code's complete prompt.","systemPrompt set to any non-empty string, since the SDK treats a populated string as requesting the CLI preset.","systemPrompt: { type: 'cli', preset: true }, using the SDK's shorthand form for enabling the CLI's prompt."],"correct":0,"explain":"{ type: 'preset', preset: 'claude_code' } is the explicit, correct opt-in to Claude Code's full system prompt. Omitting the field gets a minimal prompt instead, and passing any string just becomes the agent's entire system prompt on its own — neither reaches the CLI's preset. { type: 'cli', preset: true } isn't a real shape the SDK recognizes; the preset object is the only way to request it."}]},{"id":"mt_setting_sources","type":"PROCEDURAL","cluster":"cl_foundations","name":"settingSources and CLAUDE.md loading","description":"`user` → ~/.claude/settings.json, `project` → .claude/settings.json, `local` → .claude/settings.local.json. Omitted loads all three; `[]` disables all three (endpoint-managed policy still loads). Project CLAUDE.md loads only when `\"project\"` is present. Python ≤ 0.1.59 treated `[]` as omitted.","core":true,"estMinutes":8,"evidence":["Diagnose \"the agent ignores our CLAUDE.md\" from an options object","Justify `settingSources: []` for a deployed product"],"assessmentPrompt":"An SDK app ignores the repo's CLAUDE.md. What is the most likely cause and the fix?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · It's ignoring our CLAUDE.md","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene2"}],"centrality":0.372,"layer":2,"x":540,"y":108,"knowledgePoints":[{"id":"kp_settings_1","name":"Three sources, three files","keyPrerequisites":["mt_subprocess_model"],"teach":"<p><code>SettingSource = \"user\" | \"project\" | \"local\"</code>:</p><table class='kp-table'><tr><th>Value</th><th>File</th></tr><tr><td><code>user</code></td><td><code>~/.claude/settings.json</code></td></tr><tr><td><code>project</code></td><td><code>.claude/settings.json</code> (version controlled)</td></tr><tr><td><code>local</code></td><td><code>.claude/settings.local.json</code> (gitignored)</td></tr></table><ul><li><strong>Omitted / undefined</strong> → loads all three, exactly like the CLI.</li><li><strong><code>[]</code></strong> → disables user, project and local. Endpoint-managed policy still loads regardless.</li></ul>","questions":[{"id":"q_settings_1a","q":"A developer configuring an Agent SDK application isn't sure what happens if the settingSources option is left out of the config entirely, and worries that leaving it unset might silently disable settings loading in production. What is the actual default behavior when settingSources is omitted?","options":["Every filesystem-based settings source is disabled by default, matching the behavior of passing an empty array.","It loads user, project, and local settings files, matching the Claude Code CLI's own default behavior exactly.","Only the project-level .claude/settings.json file is loaded, since that's the one most commonly checked in.","The SDK raises a configuration error at startup, since settingSources is a required field with no default."],"correct":1,"explain":"Omitting settingSources loads all three sources — user, project, and local — which is deliberately CLI parity; you have to opt out with an empty array, not opt in. It doesn't behave like an empty array, doesn't selectively load only the project file, and isn't a required field that errors when left unset. This default is exactly why the developer's worry about silent disabling is unfounded."},{"id":"q_settings_1b","q":"A security team deliberately configures their production SDK deployment with settingSources: [], intending to prevent any developer's personal settings file from affecting the deployed agent's behavior. They then ask whether this setting is strong enough to also block organization-wide policy from an endpoint-managed configuration. What actually still loads when settingSources is []?","options":["Nothing loads at all in this configuration; an empty array disables every possible source of settings, without exception.","User-level settings still load, since ~/.claude/settings.json is considered outside the scope of the project directory.","Endpoint-managed organization policy still loads regardless, since it isn't controlled by the settingSources array.","Local settings still load, since .claude/settings.local.json is gitignored and therefore treated separately from the array."],"correct":2,"explain":"Endpoint-managed policy loads regardless of settingSources, which is exactly the point of it — an individual application configuration shouldn't be able to override organization-wide policy. User and local settings are both disabled by an empty array just like project settings are, so neither of those survives it. This means settingSources: [] achieves the security team's goal for personal files while still respecting policy set above the application."}]},{"id":"kp_settings_2","name":"The CLAUDE.md bug, and the production default","keyPrerequisites":[],"teach":"<p>The common bug: <strong>project CLAUDE.md loads only when <code>\"project\"</code> is in <code>settingSources</code>.</strong> An SDK app that ignores CLAUDE.md has almost always passed <code>[]</code>, or a list that omitted <code>\"project\"</code>.</p><p>Two gotchas worth carrying:</p><ul><li><em>Python-only:</em> in SDK <strong>0.1.59 and earlier</strong>, <code>setting_sources=[]</code> behaved like omitting it, so it did <em>not</em> disable filesystem settings. TypeScript was never affected.</li><li><em>Production default:</em> for a deployed product you usually want <code>settingSources: []</code> — reproducible behaviour that does not change because a developer edited their personal settings file.</li></ul>","questions":[{"id":"q_settings_2a","q":"An SDK application is supposed to follow the coding conventions documented in the repository's CLAUDE.md file, but in testing, the agent consistently ignores everything CLAUDE.md says, even though the file itself is well-formed and sits at the project root as expected. The team has set systemPrompt to the claude_code preset. What is the most likely cause?","options":["CLAUDE.md needs to be renamed to a specific reserved filename before the SDK will recognize and load it.","The systemPrompt preset was set incorrectly, since only a custom string-based prompt can trigger CLAUDE.md loading.","CLAUDE.md is read only by the Claude Code CLI directly and can never be loaded through the SDK at all.","settingSources was left as an empty array or a list that omitted \"project\", so project CLAUDE.md never loaded."],"correct":3,"explain":"\"project\" specifically has to be present in settingSources for project-level CLAUDE.md to load — this is the single most common settings bug in SDK applications, and the preset systemPrompt being correctly set doesn't help if this is missing. Renaming the file and claiming CLAUDE.md is CLI-exclusive are both incorrect; the SDK does load it, just conditionally. A custom string systemPrompt has no bearing on whether CLAUDE.md loads, since that's governed by settingSources, not systemPrompt."},{"id":"q_settings_2b","q":"A platform team deploying a customer-facing SDK-based product debates whether to pass settingSources: [] in production, even though this means giving up automatic CLAUDE.md loading and other filesystem-based settings unless explicitly supplied elsewhere. Why is this the recommended default for a deployed product rather than leaving settingSources unset?","options":["So the deployed agent's behavior cannot silently drift because a developer or server edited a personal settings file.","Because endpoint-managed organization policy specifically requires settingSources to be set to an empty array.","Because CLAUDE.md files are not permitted to exist at all in a production deployment for compliance reasons.","Because it measurably speeds up each request, since no settings files are read from disk before the call."],"correct":0,"explain":"Reproducibility is the actual reason: settingSources: [] means the deployed behavior can't drift just because someone's personal ~/.claude/settings.json changed, at the cost of having to supply anything CLAUDE.md would have given you explicitly instead. Endpoint-managed policy loads regardless of this setting, so it isn't a requirement forcing the choice, and there's no rule against CLAUDE.md existing in production. Any speed difference from skipping a few file reads is negligible compared to the reproducibility benefit that actually motivates this."}]}],"reviewQuestions":[{"id":"r_settings_1","q":"A checklist for debugging an SDK app that ignores its repository's CLAUDE.md file needs to name exactly which settingSources value must be present for project-level CLAUDE.md to load at all. Which value is it?","options":["\"user\", since CLAUDE.md is treated by the SDK as a user-level settings concern rather than a project one.","\"project\", since project CLAUDE.md loading is specifically gated on this value being present in the array.","\"local\", since CLAUDE.md behaves like a gitignored local override rather than a version-controlled file.","Any single one of the three values is sufficient on its own, since all three sources load CLAUDE.md identically."],"correct":1,"explain":"\"project\" is specifically what gates project-level CLAUDE.md loading — it isn't a user-level or local-level concern, and the three source values aren't interchangeable for this purpose. Omitting settingSources entirely also works, since that loads all three sources including project. But if the array is present and \"project\" is left out, CLAUDE.md silently stops loading regardless of whether \"user\" or \"local\" are included."}]},{"id":"mt_budgets","type":"PROCEDURAL","cluster":"cl_foundations","name":"Budgets, stopping, and structured output","description":"`maxTurns` caps agentic round trips, `maxBudgetUsd` stops on a client-side cost estimate, `taskBudget: { total }` is an API-side token budget the model is told about so it can pace itself, `abortController` cancels, and `outputFormat: { type: 'json_schema', schema }` shapes the final result.","core":true,"estMinutes":8,"evidence":["Distinguish caps that cut the model off from budgets the model can plan against","Pick the right stop control for a described failure mode"],"assessmentPrompt":"Which budget option does the model itself know about, and why does that matter?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Lesson · What stops it?","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene3"}],"centrality":0.023,"layer":1,"x":330,"y":46,"knowledgePoints":[{"id":"kp_budget_1","name":"Guillotines vs. budgets the model can see","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>Four ways to stop a run, and they are not interchangeable:</p><ul><li><code>maxTurns</code> — maximum agentic turns (tool-use round trips). A guillotine: the run stops mid-thought when the count is hit.</li><li><code>maxBudgetUsd</code> — stops when the <em>client-side cost estimate</em> reaches this figure. Also a guillotine.</li><li><code>taskBudget: { total }</code> <em>(alpha)</em> — an API-side <strong>token</strong> budget, and the model is <strong>told its remaining budget</strong> so it can pace itself and wrap up deliberately.</li><li><code>abortController</code> (TS) — cancellation from your side.</li></ul><p>That third one is the distinctive fact. If a question asks which control lets the agent finish gracefully rather than be cut off, it is <code>taskBudget</code>.</p>","questions":[{"id":"q_budget_1a","q":"A long-running research agent needs to stop within some resource limit, but the team specifically wants the agent to be aware of how much budget remains so it can wrap up its findings deliberately instead of being cut off mid-sentence. Which of the following stopping mechanisms actually informs the model itself about its remaining budget?","options":["maxTurns, which counts agentic round trips and ends the run abruptly once that count is reached.","abortController, which lets calling code cancel a run from the outside at any point during its execution.","taskBudget, an API-side token budget where the model is told its remaining budget so it can pace itself.","maxBudgetUsd, which halts the run once a client-side dollar cost estimate reaches the configured limit."],"correct":2,"explain":"taskBudget is the distinctive one: it's an API-side token budget, and the model is actually told its remaining budget, which lets it pace itself and wrap up deliberately rather than being cut off. maxTurns and maxBudgetUsd are both guillotines — they stop the run at a threshold with no awareness on the model's part beforehand. abortController is caller-side cancellation and carries no budget information to the model at all."},{"id":"q_budget_1b","q":"A finance team wants to cap what a single agent run can cost and configures maxBudgetUsd, then later discovers that a run they expected to be blocked by this limit continued past it according to their actual invoice. What is maxBudgetUsd actually enforced against?","options":["A server-side billing meter that Anthropic tracks per API key and enforces at the moment of each request.","Wall-clock time elapsed during the run, converted into an approximate dollar figure using the current model's rate.","The same token budget that taskBudget uses, just expressed as a converted dollar amount instead of tokens.","A client-side cost estimate computed by the SDK, which is why it functions as a safety rail rather than a billing guarantee."],"correct":3,"explain":"maxBudgetUsd is enforced against a client-side cost estimate the SDK computes as the run progresses, not a server-side billing meter — which is exactly why it can diverge from the actual invoice and should be treated as a safety rail rather than a guarantee. It has nothing to do with wall-clock time, and it's a separate mechanism from taskBudget's token-based, model-visible budget. Understanding this distinction is what explains the finance team's surprise."}]},{"id":"kp_budget_2","name":"Structured results","keyPrerequisites":[],"teach":"<p>Stopping is half the problem; the other half is getting something your code can use.</p><p><code>outputFormat: { type: 'json_schema', schema }</code> makes the agent's final result conform to a schema, so a caller can consume it without parsing prose. It pairs naturally with budgets: a run that must end at some point should end with something structured rather than a half-written paragraph.</p>","questions":[{"id":"q_budget_2a","q":"A downstream service consumes an agent's final answer programmatically and needs it as an object with a fixed set of fields every single time, with no free-form prose to parse or guess at. Which option guarantees this shape most reliably?","options":["outputFormat: { type: 'json_schema', schema }, which makes the agent's final result conform to a declared schema.","A systemPrompt with a hand-written JSON example appended, asking the model to follow that format closely.","A PostToolUse hook that attempts to reformat whatever prose the agent produced into the desired object shape.","taskBudget set to a deliberately low total, on the theory that a short run naturally produces terser output."],"correct":0,"explain":"outputFormat with a json_schema is the declared, structural guarantee — the final result is made to conform to the schema rather than merely being asked to. A prompted JSON example is the probabilistic version of the same idea and can still drift from the intended shape. A hook that reformats prose after the fact is fragile and reactive, and a low taskBudget affects run length, not output structure, so it doesn't address the requirement at all."},{"id":"q_budget_2b","q":"An unattended nightly job must always produce something a downstream consumer can parse, even on days when the underlying task turns out to be unusually large, and the team wants the agent to end its run deliberately rather than being abruptly cut off partway through. Which pairing of options best serves both goals at once?","options":["abortController paired with plain free-text output, relying on the caller to interrupt the run when it runs long.","taskBudget paired with outputFormat: { type: 'json_schema', schema }, so the model paces itself and ends with structured output.","maxTurns paired with a much longer system prompt, so the agent has clearer instructions before it eventually stops.","maxBudgetUsd paired with streaming of the model's intermediate responses, so partial output stays visible as it runs."],"correct":1,"explain":"taskBudget lets the model pace itself against a budget it can actually see and wrap up deliberately, and outputFormat guarantees whatever it produces at the end has a shape the downstream consumer can rely on — together they address both stated goals directly. abortController requires the caller to actively decide to interrupt, which doesn't fit an unattended job, and free-text output isn't reliably parseable. Neither a longer system prompt nor streaming intermediate output changes what happens when the run actually needs to stop."}]}],"reviewQuestions":[{"id":"r_budget_1","q":"A team ranking maxTurns, taskBudget, and abortController by how gracefully each one ends a run wants to know which of the three lets the agent actually finish its current line of thought deliberately, rather than simply cutting execution off at a threshold. How should the three be ranked?","options":["abortController is the gentlest of the three, followed by maxTurns, with taskBudget being the most abrupt.","maxTurns is the gentlest of the three, since counting turns is inherently a coarser-grained limit than tokens.","taskBudget is the gentlest, since the model is told its remaining budget and can pace itself before ending.","All three mechanisms end a run in an identical way, simply triggered by different underlying metrics."],"correct":2,"explain":"Only taskBudget actually changes the model's behavior before the limit is hit, because the model is told its remaining budget and can choose to wrap up deliberately — that's what makes it the gentlest. maxTurns and abortController are both guillotines in comparison: one cuts off at a turn count, the other cancels from outside, neither with the model forewarned. Treating all three as identical ignores this distinction entirely, which is the actual point of the ranking exercise."}]},{"id":"mt_subagent_why","type":"CONCEPTUAL","cluster":"cl_subagents","name":"Why reach for a subagent","description":"Four reasons: context isolation (verbose exploration stays out of the main window), parallelization (independent subtasks finish in the time of the slowest), specialized instructions, and tool restriction. A tool left out of a subagent's `tools` isn't in its session at all.","core":true,"estMinutes":7,"evidence":["Give the reason a particular delegation is worth its overhead","Reject delegation when none of the four reasons applies"],"assessmentPrompt":"Name a case where delegating to a subagent makes the system worse, and a case where it is the only good option.","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Lesson · One agent, reading everything","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene4"}],"centrality":0.349,"layer":1,"x":330,"y":232,"knowledgePoints":[{"id":"kp_subwhy_1","name":"Four reasons, and only four","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>Delegation costs a round trip, a fresh context, and a summary in place of detail. It is worth paying for exactly four reasons:</p><ul><li><strong>Context isolation</strong> — verbose exploration stays out of the main window. A research agent can read fifty files without fifty files landing in your conversation.</li><li><strong>Parallelization</strong> — independent subtasks finish in the time of the slowest, not the sum.</li><li><strong>Specialized instructions</strong> — expertise that would be noise in the main prompt.</li><li><strong>Tool restrictions</strong> — a doc reviewer holding only <code>Read</code> and <code>Grep</code> cannot modify anything, structurally.</li></ul><p>If none of the four applies, a subagent is overhead with extra steps.</p>","questions":[{"id":"q_subwhy_1a","q":"A tech lead proposes delegating a formatting cleanup task to a subagent, arguing that subagents are billed at a discounted per-token rate compared to the main conversation. The task requires no isolation, no parallel work, no special expertise, and identical tool access to the main thread. How should this justification be evaluated?","options":["Justified — subagents are billed at a cheaper per-token rate than the main session, and that saving alone is worth the round trip.","Not justified — none of the four reasons applies here, and per-token pricing was never one of the four in the first place.","Justified — delegating always frees up the main conversation's context window, regardless of whether any of the four reasons apply.","Not justified — but only because the task is too small; a larger formatting job would justify the same delegation on cost grounds alone."],"correct":1,"explain":"None of the four reasons to delegate — context isolation, parallelization, specialized instructions, or tool restriction — applies here, and per-token pricing isn't one of the four in the first place. The claim that subagents are always billed at a discount is false, and 'delegation always frees context' misapplies a real benefit to a task too small to fill the window anyway. Reaching the right conclusion for the wrong reason (task size, rather than the four-reasons test) still misses the actual principle."},{"id":"q_subwhy_1b","q":"You configure a compliance-review subagent with tools: ['Read', 'Grep'] so it can audit code but never alter it. Mid-review, the coordinator's prompt asks it to 'fix the SQL injection issue you found.' What actually happens when the subagent attempts to make that fix?","options":["It falls back to using Bash to make the edit, since Bash was never explicitly disallowed.","The attempt triggers a PreToolUse hook that blocks the write and logs a denial event.","The editing tool simply is not present in its session, so no such call can even be attempted.","The coordinator gets a permission-denied error and must re-invoke it with Edit added to tools."],"correct":2,"explain":"Restricting tools to Read and Grep means Edit and Write never exist in that subagent's session — there is nothing to block or deny because there is no call to make. Bash isn't available either, since only the two listed tools are present. A hook-based denial or a permission error both imply the tool was reachable and then stopped, which isn't what tool restriction does."}]}],"reviewQuestions":[{"id":"r_subwhy_1","q":"A platform team designs a subagent so it structurally cannot delete production data, regardless of what its prompt says or how persuasive an injected instruction might be. Which of the four reasons to delegate does this design decision map to?","options":["Context isolation, since removing a tool also shrinks how much the subagent can read into its window.","Parallelization, since narrowing a subagent's tool list lets its smaller task finish faster.","Tool restriction, since withholding delete makes the action impossible, not merely discouraged.","Specialized instructions, since the prompt can simply tell the subagent never to delete anything."],"correct":2,"explain":"This is tool restriction specifically: a capability that doesn't exist in the session can't be exercised no matter what the prompt says or what an attacker injects. Relying on the prompt to forbid deletion is the specialized-instructions approach, which is exactly the probabilistic guarantee this design is trying to avoid. Isolation and parallelization describe unrelated benefits of delegation, not why the tool list itself was narrowed."}]},{"id":"mt_subagent_definition_sources","type":"FACTUAL","cluster":"cl_subagents","name":"Three ways to define a subagent","description":"Programmatically via the `agents` option (recommended for SDK apps), from markdown files in .claude/agents/, or the built-in `general-purpose` agent which needs no definition. Programmatic definitions take precedence over filesystem agents of the same name.","core":true,"estMinutes":5,"evidence":["State which definition wins on a name collision","Choose a definition source for a shipped product vs. a repo convention"],"assessmentPrompt":"A repo has .claude/agents/reviewer.md and the app passes an `agents: { reviewer: ... }`. Which one runs?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Three ways to define a subagent","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.326,"layer":2,"x":540,"y":232,"knowledgePoints":[{"id":"kp_subdef_1","name":"Three sources and the precedence rule","keyPrerequisites":["mt_subagent_why"],"teach":"<ol><li><strong>Programmatically</strong> — the <code>agents</code> option on <code>query()</code>. Recommended for SDK apps: the definition ships with the code.</li><li><strong>Filesystem</strong> — markdown files in <code>.claude/agents/</code>. Shared with anyone using the CLI in that repo.</li><li><strong>Built-in <code>general-purpose</code></strong> — Claude can spawn it with no definition at all.</li></ol><p class='kp-key'>Programmatic definitions take precedence over filesystem agents with the same name.</p><p>That precedence is what makes a shipped product predictable: a developer's local <code>.claude/agents/reviewer.md</code> cannot quietly replace the reviewer your app defines.</p>","questions":[{"id":"q_subdef_1a","q":"Your SDK app ships with agents: { reviewer: { description: '...', prompt: '...' } } passed to query(), but a developer's local checkout also has a leftover .claude/agents/reviewer.md with different instructions from an earlier experiment. When that developer runs the app, which reviewer definition actually executes?","options":["The filesystem definition, because .claude/agents files always take precedence over code.","The programmatic definition, since same-named programmatic definitions win over filesystem agents.","Both definitions run in sequence, with the filesystem one refining the programmatic one's output.","Neither runs at all — the SDK raises a name-collision error and query() fails to start immediately."],"correct":1,"explain":"Programmatic definitions win over a same-named filesystem agent, so the shipped reviewer definition runs unshadowed by whatever happens to sit in the developer's working copy. There's no merge and no collision error — precedence is resolved silently and deterministically. This is what makes a packaged app's agent behavior predictable regardless of what stray files exist on a given machine."},{"id":"q_subdef_1b","q":"A developer writes a coordinator prompt that says 'delegate exploratory research to a subagent' but never defines any agent named research-assistant, either programmatically or under .claude/agents/. When Claude tries to delegate, which agent can it fall back to without any definition existing anywhere?","options":["general-purpose, the built-in agent Claude can spawn with no definition at all.","research-assistant, since Claude auto-generates a definition from the prompt's wording.","None — delegation fails outright whenever the named agent has no definition.","reviewer, the other built-in agent that ships ready-made with the SDK by default."],"correct":0,"explain":"general-purpose is the one subagent that needs no definition anywhere — programmatic, filesystem, or otherwise — because it's built into the SDK itself. Claude doesn't invent a definition to match a name mentioned in prose; it falls back to the built-in agent instead. Nothing named 'reviewer' is built in the same way, so it isn't available without someone defining it."}]}],"reviewQuestions":[{"id":"r_subdef_1","q":"Your company is shipping a packaged SDK application to customers who will run it in their own environments, some of which may already contain unrelated .claude/agents/ files from other tools. Which definition source should you use for the app's own agents, and why?","options":["Programmatic definitions passed to query(), since they win over any same-named filesystem agent a customer's setup contains.","Filesystem definitions under .claude/agents/, because customers can then tune agent behavior without your team pushing a new release.","The built-in general-purpose agent for every single role, since it requires no ongoing maintenance as the product evolves.","Whichever source the CLI resolves first at startup, since precedence order doesn't matter once the app is deployed."],"correct":0,"explain":"Programmatic definitions ship with the code and take precedence over a same-named filesystem agent, so a stray .claude/agents/ file in a customer's environment can't quietly override the app's intended behavior. Relying on filesystem agents inverts that guarantee, and general-purpose can't substitute for a role-specific description or prompt. This is the same reproducibility concern that leads production apps to pass settingSources: [] as well."}]},{"id":"mt_agent_definition","type":"FACTUAL","cluster":"cl_subagents","name":"The AgentDefinition fields","description":"`description` and `prompt` are required — the description is what Claude matches against when choosing an agent. Optional: tools, disallowedTools, model, skills, memory, mcpServers, initialPrompt, maxTurns, background, effort, permissionMode.","core":true,"estMinutes":9,"evidence":["List the two required fields and say what each is for","Explain why a vague description causes an agent never to be selected"],"assessmentPrompt":"Claude never delegates to your `security-reviewer` agent. Which field do you fix first?","standards":["ccaf:d1"],"sources":[{"label":"Notes · AgentDefinition","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.256,"layer":3,"x":750,"y":46,"knowledgePoints":[{"id":"kp_agentdef_1","name":"The two required fields","keyPrerequisites":["mt_subagent_definition_sources"],"teach":"<p><code>AgentDefinition</code> requires exactly two fields:</p><ul><li><code>description</code> — natural language saying <em>when to use this agent</em>. <strong>This is what Claude matches against</strong> when deciding whether to delegate. Write it specifically, the way you would write a tool description.</li><li><code>prompt</code> — the subagent's system prompt.</li></ul><p>The failure mode follows directly: an agent that is never selected almost always has a vague description, not a broken prompt. \"Reviews things\" competes with everything; \"Reviews Python diffs for SQL injection and unsafe deserialization, before merge\" competes with nothing.</p>","questions":[{"id":"q_agentdef_1a","q":"You define a subagent named security-reviewer with description: 'Reviews things' and a detailed 40-line prompt covering SQL injection, unsafe deserialization, and secrets handling. In practice, Claude almost never delegates security reviews to it, even on PRs that clearly need one. Which field should you fix first?","options":["description, since that is the text Claude matches against when deciding whether to delegate.","prompt, since an agent that is never selected almost always has a poorly written system prompt.","model, since the agent may be assigned a model too small to ever be chosen for delegation.","tools, since Claude avoids delegating to agents whose tool list looks overly restrictive."],"correct":0,"explain":"Claude matches against description to decide whether an agent applies to the current situation, and the prompt is never even read until after that match succeeds. A vague description like 'Reviews things' competes with everything and wins nothing, no matter how detailed the prompt underneath it is. Model size and tool restrictions don't factor into selection at all."},{"id":"q_agentdef_1b","q":"You are writing a minimal AgentDefinition for a new subagent and want to know the smallest set of fields the SDK will actually accept. Which two fields are mandatory?","options":["name and prompt — every agent needs an identifying key and its own instructions.","description and tools — Claude needs to know when to use it and what it can touch.","prompt and model — instructions and a model must both be pinned explicitly.","description and prompt — what Claude matches on, and what the subagent runs with."],"correct":3,"explain":"description and prompt are the two required fields: one drives selection, the other drives execution once selected. tools, model, and the rest of the optional fields all have sensible defaults if omitted, so they aren't required. The registration key is a separate concern from the definition's own required contents."}]},{"id":"kp_agentdef_2","name":"The optional fields worth knowing","keyPrerequisites":["mt_subagent_definition_sources"],"teach":"<table class='kp-table'><tr><th>Field</th><th>Notes</th></tr><tr><td><code>tools</code></td><td>Allowed tool names. <strong>Omit → inherits every tool available to subagents.</strong></td></tr><tr><td><code>disallowedTools</code></td><td>Removes tools; accepts MCP patterns <code>mcp__server</code>, <code>mcp__server__*</code>, <code>mcp__*</code>.</td></tr><tr><td><code>model</code></td><td><code>opus</code> / <code>sonnet</code> / <code>haiku</code> / <code>inherit</code> / full id. Omitted → main model.</td></tr><tr><td><code>skills</code>, <code>memory</code>, <code>mcpServers</code></td><td>Preloaded skills; <code>user</code>/<code>project</code>/<code>local</code> memory; per-agent servers.</td></tr><tr><td><code>initialPrompt</code></td><td>Auto-submitted first user turn <strong>only when the agent runs as the main thread agent</strong>; ignored as a subagent.</td></tr><tr><td><code>maxTurns</code>, <code>background</code>, <code>effort</code>, <code>permissionMode</code></td><td>Per-agent turn cap, forced background execution, effort level, permission mode.</td></tr></table>","questions":[{"id":"q_agentdef_2a","q":"You set initialPrompt: 'Start by listing all TODO comments in the repo' on an AgentDefinition, intending to prime its very first turn. This particular agent is only ever invoked as a subagent via the Agent tool, never as the main thread agent. What actually happens to that initialPrompt value at runtime?","options":["It is submitted automatically as the subagent's first user turn, ahead of the Agent tool's own prompt string.","It replaces the prompt field entirely, so the subagent runs with only initialPrompt as its instructions.","The SDK raises a validation error at construction time, since initialPrompt requires a main-thread agent.","It is silently ignored, because initialPrompt only takes effect when the agent runs as the main thread agent."],"correct":3,"explain":"initialPrompt is only auto-submitted when the agent is the main thread agent; as a subagent it is silently ignored, with no error to signal the mismatch. If a subagent needs to start with specific content, that content has to go into the Agent tool's own prompt string instead. Nothing about this raises a construction-time error or touches the prompt field."},{"id":"q_agentdef_2b","q":"You are writing a reusable AgentDefinition template shared across several projects, each of which may run its main session on a different model. You want the subagent's model choice to explicitly and visibly track whatever the main session is using, rather than relying on an implicit default. Which value of model achieves that?","options":["'inherit', which states on purpose that the subagent should track the main session's model.","'auto', which lets the SDK pick the cheapest model capable of the task at hand.","'default', which pins the subagent to whatever model the CLI ships with by default.","Omitting the field is the only way to track the main session's model at all."],"correct":0,"explain":"'inherit' produces the same result as omitting the field — tracking the main session's model — but does so explicitly rather than by accident of a missing value. That distinction matters for a shared template, where a future edit could otherwise leave the field blank without anyone noticing why. 'auto' and 'default' aren't real values this field accepts."}]}],"reviewQuestions":[{"id":"r_agentdef_1","q":"A teammate argues that AgentDefinition.description is just internal documentation for the team, similar to a code comment, and suggests writing it loosely since 'the prompt is what actually matters.' What is description actually used for at runtime?","options":["Documentation for the team maintaining the agent, with no effect on runtime behavior.","It becomes the subagent's system prompt once Claude decides to delegate to it.","The label shown for that agent's entry in the CLI's own agent-selection menu.","The text Claude matches against when deciding whether to delegate to that agent."],"correct":3,"explain":"description is a selection mechanism, not a comment — it plays the same role a tool description plays when Claude decides which tool to call. Writing it loosely means the agent competes poorly against better-described agents and general-purpose fallback, regardless of how good the prompt underneath it is. The prompt field, not description, is what runs once selection has already happened."}]},{"id":"mt_agentdef_case_trap","type":"FACTUAL","cluster":"cl_subagents","name":"The Python camelCase trap","description":"`AgentDefinition` keeps camelCase field names even in Python (disallowedTools, permissionMode, maxTurns, mcpServers, initialPrompt) because they map to the wire format shared with TypeScript. `ClaudeAgentOptions` uses snake_case. Mixing them raises TypeError at construction.","core":false,"estMinutes":4,"evidence":["Spot the TypeError in a Python snippet that passes max_turns= to AgentDefinition"],"assessmentPrompt":"In Python, which object takes `max_turns` and which takes `maxTurns`?","standards":["ccaf:d1"],"sources":[{"label":"Notes · The Python camelCase trap","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":4,"x":960,"y":232,"knowledgePoints":[{"id":"kp_casetrap_1","name":"Two objects, two conventions, one file","keyPrerequisites":["mt_agent_definition"],"teach":"<p>In Python, <code>AgentDefinition</code> is a dataclass whose field names stay <strong>camelCase</strong> — <code>disallowedTools</code>, <code>permissionMode</code>, <code>maxTurns</code>, <code>mcpServers</code>, <code>initialPrompt</code> — because they map directly to the wire format shared with the TypeScript SDK.</p><p>Meanwhile <code>ClaudeAgentOptions</code> in the same file uses snake_case: <code>disallowed_tools</code>, <code>permission_mode</code>, <code>max_turns</code>.</p><pre class='kp-code'>AgentDefinition(description=..., prompt=..., maxTurns=5)     # correct\nAgentDefinition(description=..., prompt=..., max_turns=5)    # TypeError\n\nClaudeAgentOptions(max_turns=5)                              # correct\nClaudeAgentOptions(maxTurns=5)                               # wrong</pre><p>Passing the wrong one raises <code>TypeError</code> at construction, not at run time — which is the merciful version.</p>","questions":[{"id":"q_casetrap_1a","q":"You are writing Python code that both constructs an AgentDefinition for a subagent and passes ClaudeAgentOptions to configure the main query() call, and you want each object's turn cap set correctly using its own naming convention. Which pairing is correct?","options":["Both objects take max_turns, since Python conventionally uses snake_case throughout.","ClaudeAgentOptions takes max_turns, while AgentDefinition instead takes maxTurns.","AgentDefinition takes max_turns, while ClaudeAgentOptions instead takes maxTurns.","Both objects take maxTurns, since both map onto the exact same underlying wire format."],"correct":1,"explain":"ClaudeAgentOptions follows ordinary Python snake_case, so it wants max_turns, while AgentDefinition keeps camelCase because its fields map directly onto the wire format shared with TypeScript. Swapping the two, or assuming both use the same case, produces a TypeError at construction rather than a runtime surprise. Only AgentDefinition carries the wire-format naming — ClaudeAgentOptions does not."},{"id":"q_casetrap_1b","q":"A reviewer flags AgentDefinition's camelCase field names (disallowedTools, permissionMode, maxTurns) as inconsistent with the rest of the Python SDK's snake_case style and asks whether it should be 'fixed' in a wrapper. What is the actual reason those fields stay camelCase?","options":["AgentDefinition's fields map onto the wire format shared with the TypeScript SDK.","It is a known inconsistency that a future SDK release is already planning to correct.","Python dataclasses are simply not able to declare any fields using snake_case names.","The fields are named to match the front matter keys in .claude/agents markdown files."],"correct":0,"explain":"The camelCase naming is a serialization boundary showing through: AgentDefinition's fields travel over the same wire format the TypeScript SDK uses, so keeping the names aligned avoids a translation layer. It isn't a bug awaiting a fix, and Python dataclasses have no trouble declaring snake_case fields — this one just chose not to. The markdown front matter in .claude/agents is a separate, unrelated convention."}]}],"reviewQuestions":[{"id":"r_casetrap_1","q":"In Python, a developer writes AgentDefinition(description='...', prompt='...', permission_mode='plan'), intending to restrict the subagent to plan mode. What happens when this line runs?","options":["It works exactly as intended in this case, and the subagent starts up already in plan mode.","It constructs successfully, but the permission mode setting is silently ignored at runtime.","It raises a TypeError at construction, since AgentDefinition expects permissionMode instead.","It constructs without error, but raises a separate error later, once query() actually runs."],"correct":2,"explain":"AgentDefinition keeps camelCase field names, so passing permission_mode instead of permissionMode raises a TypeError immediately at construction — before the object is ever used. This is the merciful version of the bug: it fails loudly at the point of the mistake rather than silently dropping the setting or surfacing an error much later at query time. ClaudeAgentOptions is the object that actually wants permission_mode in snake_case."}]},{"id":"mt_subagent_tool_scope","type":"PROCEDURAL","cluster":"cl_subagents","name":"tools omitted vs. listed vs. empty","description":"Omitting `tools` inherits every tool available to subagents; listing them restricts to that set; `tools: []` leaves the agent with none. `disallowedTools` subtracts and accepts MCP patterns (mcp__server, mcp__server__*, mcp__*). Common sets: read-only Read/Grep/Glob; tests Bash/Read/Grep; edits Read/Edit/Write/Grep/Glob.","core":true,"estMinutes":7,"evidence":["Predict what a subagent can do from its definition","Write a tool set that makes a described capability structurally impossible"],"assessmentPrompt":"You define a doc-reviewer subagent and omit `tools` entirely. What can it do?","standards":["ccaf:d1","ccaf:d2"],"sources":[{"label":"Lesson · One agent, reading everything","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene4"}],"centrality":0,"layer":4,"x":960,"y":294,"knowledgePoints":[{"id":"kp_toolscope_1","name":"Omitted, listed, empty","keyPrerequisites":["mt_agent_definition"],"teach":"<p>Three states, three very different agents:</p><ul><li><code>tools</code> <strong>omitted</strong> → inherits <em>every</em> tool available to subagents. Silence means \"give them the whole keyring.\"</li><li><code>tools: ['Read', 'Grep']</code> → exactly those. Anything else is not in the session at all.</li><li><code>tools: []</code> → no tools. The agent can only think and answer.</li></ul><p>Common combinations worth recognising: read-only analysis <code>Read, Grep, Glob</code>; test execution <code>Bash, Read, Grep</code>; code modification <code>Read, Edit, Write, Grep, Glob</code>; full access → omit the field.</p><p><code>disallowedTools</code> subtracts, and accepts MCP patterns: <code>mcp__server</code>, <code>mcp__server__*</code>, <code>mcp__*</code>.</p>","questions":[{"id":"q_toolscope_1a","q":"You define a doc-reviewer AgentDefinition and, since documentation review only needs to read files, you leave the tools field out of the definition entirely rather than listing anything. What tool access does the resulting subagent actually have?","options":["None at all, until you explicitly list the tools it should have access to.","Only Read, Grep, and Glob, the conventional safe defaults for review agents.","Every tool available to subagents, since omitting the field is maximal, not minimal.","Only whatever subset the parent's own allowedTools happens to contain."],"correct":2,"explain":"Omitting tools is a maximal grant, not a minimal one — the subagent inherits every tool available to subagents, which is almost certainly more than a doc reviewer needs. Getting a read-only agent requires actively listing Read, Grep, and Glob rather than leaving the field blank. The parent's own allowedTools has no bearing on what a child subagent inherits by default."},{"id":"q_toolscope_1b","q":"A security team wants a code-audit subagent whose inability to modify the repository is guaranteed structurally, not just discouraged by policy or caught by a hook that could be misconfigured. Which tools configuration provides that guarantee?","options":["tools: ['Read', 'Grep', 'Glob', 'Bash'], since Bash output can be reviewed before any command executes.","tools: ['Read', 'Grep', 'Glob'], since no tool in that set can write to the filesystem at all.","Omitting tools entirely and setting permissionMode: 'plan' to block writes at the policy layer.","tools: ['Read', 'Edit'], guarded by a PreToolUse hook that rejects any write outside a scratch directory."],"correct":1,"explain":"Read, Grep, and Glob contain no tool capable of writing anything, so the guarantee holds structurally regardless of prompt content. Including Bash reopens every door a shell can open, no matter how carefully its output is reviewed. Plan mode and PreToolUse hooks are both policy layers sitting on top of a tool that still exists — they can be bypassed or misconfigured in a way a genuinely absent tool cannot."}]}],"reviewQuestions":[{"id":"r_toolscope_1","q":"Two AgentDefinitions differ only in one field: the first leaves tools out entirely, and the second sets tools: []. A developer assumes these behave the same way, since 'nothing was listed' in both cases. What is the actual difference in tool access between the two?","options":["Omitting the field inherits every tool available to subagents; tools: [] leaves the subagent with none at all.","There is no real difference — both configurations produce a subagent restricted to the same safe default set.","Omitting the field leaves the subagent with no tools; tools: [] inherits every tool there is.","Both configurations cause the SDK to raise a validation error before the subagent ever runs."],"correct":0,"explain":"Absence and emptiness mean opposite things here: an omitted tools field is a maximal grant, while an explicit empty array is a minimal one that leaves the subagent with nothing but its own reasoning. Assuming they behave the same way produces either an unintentionally over-privileged agent or an unintentionally powerless one. Neither configuration is invalid, so no error fires at construction."}]},{"id":"mt_agent_task_tool","type":"FACTUAL","cluster":"cl_subagents","name":"The tool that spawns subagents (Agent vs. Task)","description":"Delegation happens through a tool that must be auto-approved or the delegation silently fails. The tool is `Agent` in current SDKs, renamed from `Task` in Claude Code v2.1.63, and `\"Task\"` still appears in system:init and permission_denials. The CCA-F Exam Guide v1.0 predates the rename: answer \"Task\" on the exam, write \"Agent\" in code, match both when parsing.","core":true,"estMinutes":8,"evidence":["Give the exam answer and the shipping answer without confusing them","Diagnose a coordinator whose delegations never happen"],"assessmentPrompt":"A coordinator's subagents never run and nothing errors. What is missing from allowedTools — and what does the exam call it?","standards":["ccaf:d1","ccaf-glossary:t4"],"sources":[{"label":"Lesson · The tool with two names","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene5"}],"centrality":0.047,"layer":3,"x":750,"y":108,"knowledgePoints":[{"id":"kp_tasktool_1","name":"Delegation happens through a tool, and it must be approved","keyPrerequisites":["mt_subagent_definition_sources"],"teach":"<p>Claude does not delegate by magic. It calls a tool, and <strong>that tool's name must be auto-approved or the delegation silently fails</strong> — it falls through to <code>canUseTool</code>, or is denied outright in <code>dontAsk</code> mode.</p><p>So a coordinator whose subagents never run, with no error anywhere, is almost always missing the spawn tool from <code>allowedTools</code>.</p><p>The tool's name has changed:</p><ul><li><strong>Current SDK: the tool is <code>Agent</code>.</strong> Put <code>\"Agent\"</code> in <code>allowedTools</code>.</li><li>It was renamed from <code>Task</code> in Claude Code <strong>v2.1.63</strong>. Current releases still emit <code>\"Task\"</code> in the <code>system:init</code> tools list and in <code>result.permission_denials[].tool_name</code>, so detection code should match both.</li></ul>","questions":[{"id":"q_tasktool_1a","q":"A coordinator's prompt clearly instructs it to delegate research to a subagent, the AgentDefinition is well-formed, and yet in production no subagent ever actually spawns — with no error, warning, or denial appearing anywhere in the logs. What is the most likely cause?","options":["The subagent's own AgentDefinition has a subtle validation problem that silently voids it.","The spawn tool (Agent, or Task in older releases) isn't in the coordinator's allowedTools.","maxTurns on the subagent itself is set too low for it to ever complete useful work.","The subagent's tools list is completely empty, so Claude declines to delegate to it."],"correct":1,"explain":"An unapproved spawn tool doesn't error — it simply isn't in the session, so the delegation attempt never surfaces anywhere. A malformed AgentDefinition or a too-low maxTurns would typically fail loudly once the subagent actually starts, and an empty tools list still lets a subagent be spawned, just with nothing to do once it's running. Silence with no denial is the specific signature of a missing spawn tool."},{"id":"q_tasktool_1b","q":"You are writing a UI layer that watches the SDK message stream and shows 'delegating to research-assistant…' whenever a subagent spawns. To detect every delegation reliably across current releases, which tool_use names should your matcher check for?","options":["Only \"Agent\", since that is the current, correct tool name after the rename.","Only \"Task\", since that name is what the official documentation still uses.","\"Subagent\", the generic name the stream uses for any delegation event.","Both \"Agent\" and \"Task\", since the older name still appears in current releases."],"correct":3,"explain":"The spawn tool was renamed from Task to Agent in v2.1.63, but current releases still emit \"Task\" in the system:init tools list and in permission_denials, so matching only one name misses real delegation events. \"Subagent\" isn't a name the stream actually uses. A parser built for detection has to track both names even though allowedTools only needs the current one."}]},{"id":"kp_tasktool_2","name":"The exam answers Task; your code writes Agent","keyPrerequisites":[],"teach":"<p class='kp-key'>The CCA-F Exam Guide (v1.0, July 2026) predates the rename and names the <strong>Task</strong> tool.</p><p>Glossary term t4 asks what a coordinator's <code>allowedTools</code> must include and keys the answer to <code>\"Task\"</code>.</p><ul><li><strong>On the exam: answer \"Task.\"</strong></li><li><strong>In real code: write \"Agent\"</strong>, and match both when parsing.</li></ul><p>This is the single largest course-versus-exam divergence in Section 8. Hold both answers, and keep straight which context you are in.</p>","questions":[{"id":"q_tasktool_2a","q":"You are answering a CCA-F exam question about a coordinator that needs to spawn subagents, and the question asks which tool name must appear in that coordinator's allowedTools. The exam guide (v1.0, July 2026) was written before a certain SDK rename landed. What is the expected exam answer?","options":["\"Task\", the name the exam guide's glossary keys the answer to.","\"Agent\", the name that current SDK releases actually require.","\"Subagent\", a placeholder name used only in the guide's prose.","\"Delegate\", the name used throughout the guide's worked examples."],"correct":0,"explain":"The exam guide v1.0 predates the v2.1.63 rename and glossary term t4 keys its answer to \"Task,\" so that's the expected answer on the exam itself, even though it no longer matches current code. \"Agent\" is correct for production code but not for this exam question. Neither \"Subagent\" nor \"Delegate\" appear in the guide at all."},{"id":"q_tasktool_2b","q":"You are configuring allowedTools for a coordinator in a codebase running a current release of the SDK, well after the Task-to-Agent rename in v2.1.63. Which name must you list so the coordinator can actually spawn subagents?","options":["\"Agent\", the current tool name that current SDK releases require.","\"Task\", to stay consistent with the CCA-F exam guide's terminology.","Both \"Agent\" and \"Task\" together, since either name alone is unreliable.","Neither — the spawn tool is auto-approved regardless of allowedTools."],"correct":0,"explain":"Current releases use \"Agent\" as the actual spawn tool name, so that's what has to be in allowedTools for delegation to be approved. Listing \"Task\" as well is harmless but does no work, since only \"Agent\" is what current code checks. Matching both names matters for parsing the message stream, not for granting permission — that distinction is exactly what trips people up."}]}],"reviewQuestions":[{"id":"r_tasktool_1","q":"A study guide claims the subagent spawn tool has always been called the same thing in both the CCA-F exam guide and the current SDK. Is that true, and if not, what are the two correct names and where does each apply?","options":["True, but only one-way: \"Agent\" is correct on the exam, \"Task\" in production code.","False: the exam guide answer is \"Task\", while current production code requires \"Agent\".","True — both the exam guide and the current SDK consistently use \"Task\" in every case.","True — both the exam guide and the current SDK consistently use \"Agent\" in every case."],"correct":1,"explain":"The claim is false — the exam guide v1.0 predates the rename and its glossary keys the answer to \"Task\", while the shipping SDK renamed the tool to \"Agent\" in v2.1.63. Getting the exam question right means answering \"Task\" even though real code should write \"Agent\". Treating the two names as interchangeable in either direction gets one context or the other wrong."}]},{"id":"mt_context_isolation","type":"CONCEPTUAL","cluster":"cl_subagents","name":"Context isolation: what crosses inward","description":"A subagent's context starts fresh. It receives its own prompt, the Agent tool's prompt string, tool definitions, and project CLAUDE.md if settingSources loaded it. It does not receive the parent's conversation history, tool results, or system prompt, nor preloaded skills unless listed. The only content that crosses from parent to child is the Agent tool's prompt string.","core":true,"estMinutes":10,"evidence":["List what a subagent does and does not inherit","Rewrite a delegation prompt so a subagent has the file paths and findings it needs"],"assessmentPrompt":"How does a subagent receive context from prior research findings?","standards":["ccaf:d1","ccaf:d5","ccaf-glossary:t6"],"sources":[{"label":"Lesson · What actually crosses the boundary","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene6"}],"centrality":0.186,"layer":4,"x":960,"y":108,"knowledgePoints":[{"id":"kp_isolation_1","name":"The inheritance table","keyPrerequisites":["mt_agent_definition"],"teach":"<p>A subagent's context window <strong>starts fresh</strong>. It does not inherit the parent's conversation.</p><table class='kp-table'><tr><th>It receives</th><th>It does <em>not</em> receive</th></tr><tr><td>Its own <code>AgentDefinition.prompt</code></td><td>The parent's conversation history or tool results</td></tr><tr><td>The Agent tool's prompt string</td><td>The parent's system prompt</td></tr><tr><td>Project CLAUDE.md — only if loaded via <code>settingSources</code></td><td>Preloaded skill content, unless listed in <code>skills</code></td></tr><tr><td>Tool definitions (inherited, or the <code>tools</code> subset)</td><td></td></tr></table>","questions":[{"id":"q_isolation_1a","q":"A coordinator has spent its session accumulating conversation history — dozens of tool results, its own system prompt, and a running analysis — before delegating a follow-up task to a fresh subagent through the Agent tool. Which of the following does that subagent's context actually start with?","options":["The parent's conversation history and tool results, carried over automatically since delegation preserves session state.","The parent's own system prompt, layered underneath the subagent's own AgentDefinition.prompt for that run.","Definitions of the tools it can call, either the full inherited set or the narrower subset listed in its tools field.","Any skill content the parent had already preloaded into its window, even when the subagent's skills field leaves it out."],"correct":2,"explain":"Tool definitions cross from parent to subagent, either the full inherited set or the subset named in tools — that's the one item on this list a subagent actually starts with. Conversation history, tool results, and the parent's own system prompt all stay behind, since a subagent's context window starts fresh rather than continuing the parent's. Preloaded skill content likewise doesn't carry over unless the subagent's own skills field names it."},{"id":"q_isolation_1b","q":"Your project's CLAUDE.md contains repo-specific conventions, and a teammate assumes a subagent will pick it up automatically the same way the main thread does. Under what condition does a subagent actually see the project CLAUDE.md?","options":["Only when settingSources loads it into the subagent's context — the same rule the main thread follows.","Always, on every subagent run, since CLAUDE.md is applied globally across the whole SDK by default.","Only if the parent had already read the file into its own conversation before it delegated the task.","Never at all — CLAUDE.md is scoped only to the main thread and has no path to reach any subagent."],"correct":0,"explain":"CLAUDE.md reaches a subagent only through settingSources, the same mechanism that governs whether the main thread loads it — there's no separate always-on or never-on rule for subagents specifically. It isn't a global default applied to every run, and the parent having read it earlier doesn't transfer anything, since conversation history doesn't cross either. The file isn't main-thread-exclusive; it's settingSources-gated in both places."}]},{"id":"kp_isolation_2","name":"The one channel in","keyPrerequisites":[],"teach":"<p class='kp-key'>The only content that crosses from parent to subagent is the Agent tool's prompt string.</p><p>Everything the subagent needs — a file path, an error message, a prior finding, a decision already made — must be written into that prompt explicitly. Nothing else carries.</p><p>This is exam glossary <strong>t6</strong>, near verbatim: <em>\"How does a subagent receive context from prior research findings?\"</em> → <strong>the findings must be explicitly included in its prompt.</strong></p><p>Practical consequence: a coordinator's job is largely <em>writing good prompts for its workers</em>. A delegation prompt that says \"continue the analysis\" is a delegation to an agent that has no idea what the analysis was.</p>","questions":[{"id":"q_isolation_2a","q":"A research subagent spends its entire run investigating a bug and reaches a solid diagnosis. The coordinator then delegates a second subagent to write the fix, but writes only \"implement the fix based on the research\" as that subagent's prompt. How does the second subagent actually receive the first subagent's findings?","options":["It automatically receives the first subagent's final message, since the SDK links delegations made within the same coordinator turn.","It reads the first subagent's session transcript directly, using the session_id the coordinator had already captured.","It inherits the diagnosis through the coordinator's own conversation history, which both subagents share by default.","It receives nothing beyond that one sentence — the findings had to be written explicitly into its prompt to cross over at all."],"correct":3,"explain":"This is exam glossary t6: the only channel from parent to subagent is the Agent tool's prompt string, so a vague instruction like \"based on the research\" leaves the second subagent with no actual findings to work from. There's no automatic linking between sibling delegations, no transcript the second subagent can read on its own, and no shared conversation history between the two — each subagent's context starts fresh. Whatever the coordinator wants carried forward has to be written into the prompt in full."},{"id":"q_isolation_2b","q":"A coordinator delegates with the prompt \"continue the analysis and report back.\" What goes wrong?","options":["Nothing — the subagent resumes from the context","The subagent errors out on the empty prompt","The subagent has no idea what analysis is meant","It inherits the analysis but none of the tools"],"correct":2,"explain":"It will do something — that is the danger. It will just do it without any of the context the coordinator assumed it had."}]}],"reviewQuestions":[{"id":"r_isolation_1","q":"A subagent keeps behaving as if it has no idea what the coordinator already discovered earlier in the session, even though the coordinator's own context is full of relevant detail. What is the only channel through which content actually crosses from a coordinator to a subagent it spawns?","options":["The parent's most recent conversation turns, copied automatically into the subagent's context.","The prompt string passed to the Agent tool call that spawns that particular subagent instance.","The parent's own system prompt, which every subagent inherits alongside its own AgentDefinition.prompt.","Any file the parent has already read during the session, since file reads are cached at the session level."],"correct":1,"explain":"The Agent tool's prompt string is the single channel in — whatever the coordinator writes into that string is all the subagent has to work with. Nothing about recent turns, the parent's system prompt, or previously read files carries over automatically, because the subagent's context starts fresh rather than continuing the parent's. If the coordinator's discovery matters to the subagent, it has to be typed into that one prompt."}]},{"id":"mt_subagent_return","type":"CONCEPTUAL","cluster":"cl_subagents","name":"What comes back out","description":"The parent receives the subagent's final message as the Agent tool result. Intermediate tool calls and results stay inside the subagent — that is the point. The parent may summarize that final message; to keep it verbatim, instruct the main query to do so.","core":true,"estMinutes":6,"evidence":["Explain why fifty files read by a subagent cost the parent almost nothing","Preserve a subagent's exact output when a summary would lose detail"],"assessmentPrompt":"A research subagent reads 50 files. What lands in the parent's context?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Lesson · What actually crosses the boundary","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene6"}],"centrality":0.116,"layer":5,"x":1170,"y":108,"knowledgePoints":[{"id":"kp_return_1","name":"Only the final message comes back","keyPrerequisites":["mt_context_isolation"],"teach":"<p>Coming back the other way, the parent receives the subagent's <strong>final message</strong> as the Agent tool result. Intermediate tool calls and their results stay inside the subagent.</p><p>That is the entire point. A <code>research-assistant</code> can read fifty files without fifty files landing in the main context — the parent gets the conclusion, not the reading.</p><p>One wrinkle: the parent may <em>summarize</em> that final message in its own response. If you need it verbatim — a report, a diff, a list of findings — instruct the main query to preserve it.</p>","questions":[{"id":"q_return_1a","q":"A research-assistant subagent reads 50 files across a repository, running dozens of tool calls in the process, before finishing its investigation and returning to the coordinator. What actually lands in the coordinator's context as a result?","options":["All 50 files' contents, since intermediate tool results get appended to the parent's context.","The subagent's complete transcript, including every one of the dozens of tool calls it made.","Nothing at all, until the coordinator goes back and explicitly asks it to resend its findings.","Only the subagent's final message — its tool calls and their results stay inside its own context."],"correct":3,"explain":"Only the final message crosses back to the coordinator; the fifty file reads and every intermediate tool call stay inside the subagent's own context and never touch the parent's window. This is exactly why delegation is the primary lever for keeping a coordinator's context small — the reading happens, but the parent only sees the conclusion. Nothing is withheld pending a request, and nothing about the run gets appended automatically as it happens."},{"id":"q_return_1b","q":"Your coordinator keeps paraphrasing a subagent's carefully formatted report. What is the fix?","options":["Set the subagent's model to a bigger one","Tell the main query to preserve it verbatim","Increase maxTurns on the subagent itself","Return the report as a tool_result block"],"correct":1,"explain":"The parent is free to summarise unless told otherwise. Say otherwise."}]}],"reviewQuestions":[{"id":"r_return_1","q":"You're building a UI that displays the result of each subagent delegation as soon as it completes, reading directly off the Agent tool's result content. What does that result content actually contain?","options":["The subagent's full transcript, reconstructed from every message it produced during the run.","A structured summary the SDK generates by condensing the subagent's tool calls and results.","The subagent's final message — nothing about its intermediate tool calls or their results.","A session id the UI can use to separately fetch the subagent's complete results afterward."],"correct":2,"explain":"The Agent tool result is the subagent's final message and nothing more — every intermediate tool call and result stayed inside the subagent and never surfaces in this content. There's no SDK-generated summary standing in for it, no full transcript bundled alongside it, and no extra fetch step needed to retrieve it. Whatever the subagent decided to say at the end is exactly what the UI receives."}]},{"id":"mt_subagent_runtime","type":"FACTUAL","cluster":"cl_subagents","name":"Background execution and nesting depth","description":"Since v2.1.198 subagents run in the background by default; Claude sets run_in_background: false when it needs the result before continuing, and `background: true` on the definition forces background regardless. Subagents inherit the main session's extended thinking config and can nest three layers deep by default (CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH, 1 disables nesting).","core":false,"estMinutes":5,"evidence":["State the default nesting depth and how to turn nesting off","Predict whether a delegation blocks the parent"],"assessmentPrompt":"How deep can subagents nest by default, and what changes it?","standards":["ccaf:d1"],"sources":[{"label":"Notes · Behaviors that changed in v2.1.198","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":4,"x":960,"y":46,"knowledgePoints":[{"id":"kp_runtime_1","name":"Background by default, three layers deep","keyPrerequisites":["mt_agent_task_tool"],"teach":"<p>Behaviours that changed in <strong>v2.1.198</strong>:</p><ul><li><strong>Subagents run in the background by default.</strong> An Agent tool call that omits <code>run_in_background</code> launches a background subagent; Claude sets <code>run_in_background: false</code> when it needs the result before continuing. <code>background: true</code> on the definition forces background regardless.</li><li>A subagent inherits the main session's extended thinking configuration.</li></ul><p>Nesting: subagents can spawn subagents <strong>three layers deep by default</strong>. <code>CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH</code> changes it, and <code>1</code> turns nesting off.</p>","questions":[{"id":"q_runtime_1a","q":"Your orchestration design has a coordinator spawn a subagent, which itself spawns a further subagent to handle a narrower piece of work. You want to know how many layers deep this nesting is allowed to go with no special configuration, and how to change that limit if it becomes a problem.","options":["Three layers deep by default; the CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH variable changes that limit.","One layer deep by default, with no environment variable or setting able to enable deeper nesting.","Unlimited by default, effectively capped only indirectly by each subagent's own maxTurns setting.","Two layers deep by default, a limit controlled by the maxTurns field on the AgentDefinition itself."],"correct":0,"explain":"Subagents can nest three layers deep by default, and CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH is the setting that changes it — setting it to 1 is how you flatten the topology entirely. Nesting is neither unlimited nor capped at one or two layers out of the box, and maxTurns governs how long a single agent runs, not how many layers of delegation are permitted. The two concerns — turn budget and spawn depth — are controlled separately."},{"id":"q_runtime_1b","q":"Since v2.1.198, subagents run in the background by default whenever an Agent tool call omits run_in_background. Your coordinator needs a particular subagent's answer immediately, before it can decide what to do next. What does Claude actually do differently on that call to get a blocking result?","options":["It polls the subagent's session id repeatedly in a loop until a final message becomes available.","It sets background: true on that subagent's AgentDefinition to force synchronous execution.","Nothing — subagents always block the coordinator by default, regardless of any setting used.","It sets run_in_background: false on that specific Agent tool call, overriding the default."],"correct":3,"explain":"Claude sets run_in_background: false on the individual call when it needs the result before continuing, which overrides the background-by-default behavior for that one delegation. background: true on the definition does the opposite — it forces background regardless of what the call sets — so that wouldn't produce a blocking result here. There's no polling loop involved, and background is the default, not universal blocking."}]}],"reviewQuestions":[{"id":"r_runtime_1","q":"A developer who worked with an earlier version of the SDK assumes every subagent still blocks the coordinator until it finishes, the same way it always used to. Since v2.1.198, is that assumption still correct?","options":["Yes — every subagent still blocks the coordinator until it returns a final message, regardless of settings.","No — subagents run in the background by default now, and blocking only happens when explicitly requested.","Only when the subagent is nested inside another subagent rather than spawned directly by the coordinator.","Only when the subagent's own AgentDefinition sets its tools field to a restricted, non-empty list."],"correct":1,"explain":"The assumption is out of date: since v2.1.198, subagents run in the background by default, and blocking happens only when Claude deliberately sets run_in_background: false on that call. It has nothing to do with nesting depth or how a subagent's tools field is configured — those are separate concerns from background versus blocking behavior. The old always-blocks behavior no longer holds."}]},{"id":"mt_subagent_detect_resume","type":"PROCEDURAL","cluster":"cl_subagents","name":"Detecting and resuming a subagent","description":"Detect delegation by tool_use blocks named Agent (or Task); messages from inside carry parent_tool_use_id. Python reads message.content, TypeScript message.message.content. The Agent tool result carries `agentId`; capture it with session_id and re-query with resume plus the agent named in the prompt. Explore and Plan are one-shot and return no agentId.","core":false,"estMinutes":7,"evidence":["Write the detection predicate that works across the rename","Name the two identifiers needed to resume a subagent"],"assessmentPrompt":"Your UI needs to show \"delegating to research-assistant…\". What do you watch for in the message stream?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Notes · Detecting and resuming","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":4,"x":960,"y":170,"knowledgePoints":[{"id":"kp_detect_1","name":"Detecting delegation in the stream","keyPrerequisites":["mt_agent_task_tool"],"teach":"<p>To show \"delegating to research-assistant…\" in your own UI:</p><ul><li>Look for <code>tool_use</code> blocks named <code>\"Agent\"</code> — <em>or</em> <code>\"Task\"</code>, which still appears in current releases.</li><li>Messages produced <em>inside</em> a subagent carry <code>parent_tool_use_id</code>. That is how you attribute a message to the right agent.</li><li>Python reads blocks via <code>message.content</code>; TypeScript via <code>message.message.content</code> — the SDK message wraps the API message.</li></ul>","questions":[{"id":"q_detect_1a","q":"You're building a UI that renders a live tree of agent activity and need to attribute each incoming message to the correct subagent rather than the main thread. Which field on a message tells you it was produced inside a subagent?","options":["parent_session_id, which links every message back to the session that spawned it.","A boolean is_subagent flag set to true on messages produced inside a subagent.","parent_tool_use_id, which points back at the Agent tool call that spawned that subagent.","spawned_by_agent, an identifier naming which AgentDefinition produced the message."],"correct":2,"explain":"parent_tool_use_id points back at the specific Agent tool call that spawned the subagent, which is exactly the attribution a UI needs to build a tree of activity. There is no is_subagent boolean or spawned_by_agent field in the message stream, and parent_session_id isn't the mechanism the SDK actually uses for this. Tracking parent_tool_use_id per message is how you correctly nest activity under the right delegation."},{"id":"q_detect_1b","q":"You're writing a TypeScript handler that inspects an SDK message to pull out its content blocks, knowing the SDK message type wraps the underlying API message rather than exposing it directly. How do you reach those content blocks in TypeScript?","options":["message.content, the same flattened path that works in the Python SDK.","message.data.content, since the SDK wraps the API message inside a data field.","message.content.blocks, an array nested one level deeper than top-level content.","message.message.content, since the SDK message wraps the underlying API message."],"correct":3,"explain":"In TypeScript, message.message.content is correct because the SDK message wraps the API message as a nested field, unlike Python where message.content reaches it directly. There's no data field or a content.blocks sub-array in either SDK's message shape. Mixing up the two access patterns is a common source of bugs when porting detection code between the two SDKs."}]},{"id":"kp_detect_2","name":"Resuming a subagent","keyPrerequisites":[],"teach":"<p>The Agent tool result contains a text block with <code>agentId: &lt;id&gt;</code>. To go back to that same subagent later:</p><ol><li>Capture the <code>session_id</code> and the <code>agentId</code>.</li><li>Re-query with <code>resume: sessionId</code>, naming the agent in the prompt.</li></ol><p>Built-in <code>Explore</code> and <code>Plan</code> agents are one-shot and return <strong>no agentId</strong> — there is nothing to resume.</p><p>Beyond a few agents per turn, the <code>Workflow</code> tool (TS SDK v0.3.149+) moves orchestration into a script that runs outside the conversation context.</p>","questions":[{"id":"q_detect_2a","q":"You want to send a follow-up prompt to the exact same subagent instance that already ran once, rather than spawning a brand-new subagent with no memory of the first run. Which two identifiers do you need to have captured from that first run to do this?","options":["The session_id from the run and the agentId identifying that particular subagent within it.","The parent_tool_use_id from the first call together with the tool_use_id of its result.","The agentId alone — it is sufficient on its own to resume the correct subagent.","The session_id together with the human-readable name assigned to that agent."],"correct":0,"explain":"Resuming needs both identifiers: session_id to get back into the right conversation, and agentId to name which specific agent inside that session to resume. agentId alone can't locate the conversation it belongs to, and neither parent_tool_use_id/tool_use_id nor a human-readable name are what the resume mechanism actually keys on. Both pieces have to be captured at the time the first run completes, since they aren't retrievable later otherwise."},{"id":"q_detect_2b","q":"A developer tries to resume a previous Explore agent run the same way they'd resume a custom research-assistant subagent — capturing its session_id and looking for an agentId in the result. There's no agentId to find. Why can't a built-in Explore agent be resumed this way?","options":["Explore runs in a completely separate process that the main SDK session cannot reach afterward.","Explore's session data is discarded immediately and is never written to disk at all.","Explore is one-shot and its result contains no agentId, so there is nothing to resume.","Explore has no tools of its own, so there is no session state left behind to resume."],"correct":2,"explain":"Explore, like Plan, is a one-shot built-in agent whose result carries no agentId, which is precisely the identifier a resume attempt needs — so there is genuinely nothing to resume, not a bug in how the developer looked for it. It isn't about a separate process or discarded disk state, and Explore does use tools during its single run; the absence of an agentId is the actual reason. Custom subagents defined with an AgentDefinition are the ones that return an agentId and can be resumed."}]}],"reviewQuestions":[{"id":"r_detect_1","q":"You want to resume a specific subagent later, which means capturing its agentId at the time it first runs, before that information disappears. Where does agentId actually come from?","options":["The system:init message that Claude Code emits once at the very start of the session.","The AgentDefinition you wrote when configuring that subagent ahead of time.","A text block embedded in the Agent tool's result once that subagent's run completes.","The parent_tool_use_id field attached to messages produced inside the subagent."],"correct":2,"explain":"agentId is delivered inside a text block in the Agent tool result, which is exactly why it has to be captured at completion time — it isn't available anywhere before or after that point in a more durable form. It doesn't come from system:init, since that message fires at session startup before any subagent has run, and it isn't part of the AgentDefinition you authored, which has no knowledge of a specific run's id. parent_tool_use_id identifies the spawning call, not the agent instance itself."}]},{"id":"mt_hub_and_spoke","type":"CONCEPTUAL","cluster":"cl_subagents","name":"Hub-and-spoke coordination","description":"A coordinator owns all inter-subagent communication, error handling, and routing; subagents never talk to each other. Follows directly from context isolation — there is no channel between siblings.","core":true,"estMinutes":6,"evidence":["Name the pattern from a described topology","Explain why a mesh of subagents is not available to you"],"assessmentPrompt":"Three subagents each need a fact the first one discovered. How does it reach them?","standards":["ccaf:d1","ccaf-glossary:t3"],"sources":[{"label":"Lesson · Three shapes of decomposition","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene7"}],"centrality":0.047,"layer":5,"x":1170,"y":46,"knowledgePoints":[{"id":"kp_hub_1","name":"The topology isolation forces","keyPrerequisites":["mt_context_isolation"],"teach":"<p><strong>Hub-and-spoke</strong>: a coordinator owns all inter-subagent communication, error handling, and routing. Subagents do not talk to each other.</p><p>This is not a style preference — it is what context isolation leaves you with. There is no channel between siblings, because the only channel that exists is a parent writing a prompt string for a child.</p><p>So when subagent B needs something subagent A discovered, the sequence is always: A returns its final message → the coordinator reads it → the coordinator writes it into B's prompt. (Exam glossary <strong>t3</strong>.)</p>","questions":[{"id":"q_hub_1a","q":"You've split a research task across three subagents, and the second and third each need a fact the first one discovered while investigating. None of the three subagents can message each other directly. How does that fact actually reach the second and third subagents?","options":["The first subagent broadcasts the fact to its siblings through a shared event channel.","All three subagents share a common session, so the fact is visible to each automatically.","Each of the later subagents independently re-derives the same fact during its own investigation.","The coordinator reads the first subagent's final message and writes the fact into each later prompt."],"correct":3,"explain":"Hub-and-spoke is forced by context isolation: since subagents have no sibling channel, the coordinator has to read the first subagent's final message and explicitly write the fact into each subsequent prompt. There is no shared event channel or common session linking the three subagents together. Relying on each one to independently re-derive the fact wastes work and risks the three landing on inconsistent answers."},{"id":"q_hub_1b","q":"A teammate proposes letting subagents message each other directly in a mesh, rather than routing every exchange back through the coordinator, arguing it would cut a step out of multi-agent workflows. Why isn't that architecture actually available to build?","options":["The SDK technically allows it but rate-limits agent-to-agent calls heavily enough to make it impractical.","Mesh topologies are supported, but only through the separate Workflow tool rather than the Agent tool.","There is no channel between siblings at all — the only channel that exists is a parent's prompt to a child.","Only three subagents are permitted to run concurrently, which rules out a mesh of any useful size."],"correct":2,"explain":"There simply is no sibling-to-sibling channel to build a mesh on top of — the only channel that exists is a parent writing a prompt string for a child, so hub-and-spoke isn't a style choice but the only topology the architecture leaves available. There's no rate limit standing in the way, and the Workflow tool addresses orchestrating many delegations from a script, not direct sibling messaging. Concurrency limits on subagent count are a separate concern from whether a channel between them exists."}]}],"reviewQuestions":[{"id":"r_hub_1","q":"A multi-agent design routes every message between workers through a single coordinator, which also owns all error handling and routing decisions — no subagent ever addresses another directly. What is this coordination pattern called?","options":["Hub-and-spoke, where the coordinator owns every inter-subagent exchange.","Prompt chaining, a fixed sequence of steps known before the run starts.","Evaluator-optimizer, where one agent critiques and another revises in a loop.","Dynamic decomposition, where subtasks are generated from what earlier steps discover."],"correct":0,"explain":"This is exam glossary t3, hub-and-spoke: the coordinator owns all inter-subagent communication, error handling, and routing, which matches a design where no subagent ever addresses another directly. Prompt chaining and dynamic decomposition describe how work gets split up, not whether workers can talk to each other. Evaluator-optimizer describes a two-agent critique loop, which is a different pattern from a coordinator mediating many workers."}]},{"id":"mt_decomposition_shapes","type":"CONCEPTUAL","cluster":"cl_subagents","name":"Prompt chaining vs. dynamic decomposition","description":"Prompt chaining is fixed sequential decomposition — the steps are known up front, e.g. per-file analysis then a cross-file integration pass. Dynamic (adaptive) decomposition generates subtasks from what each step discovers, for open-ended work like adding tests to a legacy codebase.","core":true,"estMinutes":7,"evidence":["Choose the decomposition shape from how much is known before the run","Say what goes wrong when you chain an open-ended investigation"],"assessmentPrompt":"\"Add tests to this legacy codebase.\" Fixed chain or dynamic decomposition, and why?","standards":["ccaf:d1","ccaf-glossary:t12","ccaf-glossary:t13"],"sources":[{"label":"Lesson · Three shapes of decomposition","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene7"}],"centrality":0.023,"layer":6,"x":1380,"y":46,"knowledgePoints":[{"id":"kp_decomp_1","name":"Fixed sequence vs. discovered subtasks","keyPrerequisites":["mt_hub_and_spoke"],"teach":"<ul><li><strong>Prompt chaining</strong> (glossary t12) — fixed sequential decomposition. The steps are known up front: analyse each file, then run a separate cross-file integration pass. Use it when you can write the plan before the run starts.</li><li><strong>Dynamic / adaptive decomposition</strong> (t13) — subtasks generated from what each step discovers. Use it for open-ended investigation: \"add tests to a legacy codebase,\" where you cannot know what needs testing until you have looked.</li></ul><p>The discriminating question is simply: <em>could you have written the list of subtasks before starting?</em> If yes, chain. If no, decompose dynamically.</p>","questions":[{"id":"q_decomp_1a","q":"A team asks Claude to \"add tests to this legacy codebase,\" which has no existing coverage and no inventory yet of what actually needs testing. Before deciding how to structure this as a multi-agent task, which decomposition shape actually fits?","options":["Prompt chaining, since testing always follows the same fixed sequence of steps regardless of the codebase.","Prompt chaining, since the number of files involved can be counted before the run starts.","Neither — open-ended investigation like this should stay a single-agent task with no decomposition.","Dynamic decomposition, since the subtasks can only be identified once the code has actually been examined."],"correct":3,"explain":"This fits dynamic decomposition: you can't list what needs testing before looking at the code, so the subtasks have to be discovered as investigation proceeds rather than planned up front. Knowing the file count doesn't tell you which files need tests or what those tests should cover, so that alone doesn't make it a fixed chain. Ruling out decomposition entirely ignores that a legacy codebase of any real size benefits from splitting the discovery work across subagents."},{"id":"q_decomp_1b","q":"A task reads: \"summarize each of these 12 reports individually, then write one combined brief from the summaries.\" The full sequence of steps is knowable before any subagent starts running. Which decomposition shape does this fit?","options":["Dynamic decomposition, since each report might turn up findings that change the plan mid-run.","Hub-and-spoke, a third decomposition shape distinct from both chaining and dynamic decomposition.","Neither — twelve reports of this size should be handled by a single query with no delegation.","Prompt chaining, since the per-item summaries and the integration pass are both writable in advance."],"correct":3,"explain":"This is prompt chaining: the per-report summarization step and the final integration pass are both known before the run starts, which is exactly the fixed-sequence case the pattern is for. Nothing about the task depends on findings changing the plan mid-run, so dynamic decomposition doesn't fit. Hub-and-spoke describes how subagents communicate, not how the work gets split, so it isn't a competing decomposition shape here."}]}],"reviewQuestions":[{"id":"r_decomp_1","q":"You're deciding between prompt chaining and dynamic decomposition for a new multi-agent workflow and want a single question that reliably tells you which shape fits. What is that discriminating question?","options":["How many subagents the workflow ends up needing to involve by the time it finishes.","Whether the subagents assigned to the workflow are able to run in parallel with each other.","Whether the list of subtasks could have been written down before the run ever started.","Whether a coordinator is present at all, as opposed to subagents running independently."],"correct":2,"explain":"The discriminating question is whether the subtask list could have been written before the run started: if yes, it's a fixed chain; if the subtasks only emerge from what earlier steps discover, it's dynamic decomposition. Subagent count and parallelism are properties a workflow can have under either shape, so neither one distinguishes the two. A coordinator is present in both patterns — hub-and-spoke governs communication, not which decomposition shape is in use."}]},{"id":"mt_coordinator_defect","type":"PROCEDURAL","cluster":"cl_subagents","name":"Diagnosing multi-agent failure: look up, not down","description":"When every subagent succeeds and the aggregate is still wrong, the defect is the coordinator's task decomposition, not the workers. The exam's research scenario — three successful subagents, a report covering one sub-topic — resolves to decomposition that was too narrow.","core":true,"estMinutes":7,"evidence":["Locate the defect in a multi-agent run where all workers report success","Distinguish worker failure from decomposition failure from aggregation failure"],"assessmentPrompt":"Three subagents each return a good report, and the combined answer covers only one sub-topic. Where is the bug?","standards":["ccaf:d1"],"sources":[{"label":"Lesson · When every worker succeeds","href":"../lessons/0010-claude-agent-sdk-orchestration.html#scene8"}],"centrality":0,"layer":7,"x":1590,"y":46,"knowledgePoints":[{"id":"kp_coorddefect_1","name":"When every worker succeeds and the answer is still wrong","keyPrerequisites":["mt_decomposition_shapes"],"teach":"<p class='kp-key'>The coordinator is usually the defect.</p><p>The exam's multi-agent research scenario: three subagents each succeed, and the final report covers only one sub-topic. The tempting reads are \"a subagent failed silently\" or \"the aggregation step dropped content.\" Both are wrong.</p><p>The root cause is the <strong>coordinator's task decomposition being too narrow</strong> — it never asked for the other sub-topics, so nothing was there to aggregate.</p><p>The general rule: <strong>when every worker succeeds and the aggregate is wrong, look up, not down.</strong> Success at each node is evidence that the nodes are fine and the partition is not.</p>","questions":[{"id":"q_coorddefect_1a","q":"Three subagents each return a good report, and the combined answer covers only one sub-topic. Where is the bug?","options":["One of the subagents must have failed silently","The aggregation step truncated the results","Context isolation dropped the other findings","The coordinator's decomposition was too narrow"],"correct":3,"explain":"Every worker succeeded — so the workers are not the problem. What was never asked for cannot come back."},{"id":"q_coorddefect_1b","q":"A multi-agent research run finishes: every subagent reports success, no tool call returns an error, and the coordinator hasn't run out of turns. Yet the final combined report is noticeably thin, covering far less ground than the task called for. Which observation from this run points most strongly at the coordinator's task decomposition rather than a worker failure?","options":["One of the subagents took noticeably longer to finish its assigned piece than the other two did.","Every subagent succeeded, and the resulting whole is still thin — success everywhere, yet incomplete overall.","The coordinator's own turn budget was close to its configured maxTurns limit by the end of the run.","A tool call inside one of the subagents returned a result flagged with isError set to true."],"correct":1,"explain":"Universal success at every node combined with a thin aggregate is the specific signature of a bad partition — if the workers were the problem, at least one of them would show a failure, and none do. Runtime differences between subagents and a turn budget running close to its limit are both unremarkable operational details that don't point at decomposition specifically. An isError result would actually implicate a worker directly, which is the opposite of what's observed here."}]}],"reviewQuestions":[{"id":"r_coorddefect_1","q":"You're debugging a multi-agent pipeline where every individual subagent completed its assigned piece successfully, but the combined output is missing content the task clearly needed. What is the general diagnostic rule for tracing a failure like this?","options":["Always suspect whichever subagent happened to take the longest to finish its assigned piece.","Retry the entire run with a substantially larger turn budget before investigating anything else.","Assume context isolation dropped information somewhere before checking anything else about the run.","When every worker succeeds and the aggregate is wrong, the defect is in the coordinator itself."],"correct":3,"explain":"The rule is to look up, not down: the coordinator owns the task partition, and a bad partition — one that never asked for the missing sub-topics in the first place — survives perfectly successful workers untouched. Suspecting the slowest subagent or blaming context isolation both point at the workers, which the evidence has already cleared. Retrying with more turns doesn't fix a decomposition that was too narrow to begin with; it just repeats the same narrow plan."}]},{"id":"mt_determinism_principle","type":"CONCEPTUAL","cluster":"cl_hooks","name":"Programmatic enforcement vs. prompt instruction","description":"A hook is code that runs; a prompt is a request. Programmatic enforcement guarantees deterministic compliance, prompt instructions are probabilistic with a non-zero failure rate. If a rule must hold on every run including the unwatched ones, it belongs in a hook.","core":true,"estMinutes":8,"evidence":["Reject \"add few-shot examples\" as a fix for a compliance gap","State the principle in one sentence and apply it to a new scenario"],"assessmentPrompt":"An agent skips identity verification in 12% of refunds. What class of fix actually closes that gap?","standards":["ccaf:d1","ccaf-glossary:t9"],"sources":[{"label":"Lesson · A gate, not a reminder","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene1"}],"centrality":0.256,"layer":1,"x":330,"y":294,"knowledgePoints":[{"id":"kp_determinism_1","name":"Code that runs vs. a request that might be honoured","keyPrerequisites":["mt_loop_ownership"],"teach":"<p class='kp-key'>Programmatic enforcement guarantees deterministic compliance; prompt instructions are probabilistic and have a non-zero failure rate.</p><p>A hook is code that runs. A prompt is a request. If a rule must hold on <em>every</em> run — including the ones nobody is watching — it belongs in a hook.</p><p>This is the answer to the exam's most-quoted sample question. An agent skips <code>get_customer</code> in 12% of cases before issuing refunds. The fix is <strong>a programmatic prerequisite that blocks <code>lookup_order</code> and <code>process_refund</code> until identity is verified</strong> — not more few-shot examples, not a stronger instruction, not a better model.</p><p>Twelve percent is not a prompting problem. Twelve percent is what \"usually\" looks like at scale.</p>","questions":[{"id":"q_determinism_1a","q":"Production data shows that in 12% of cases, your customer support agent skips get_customer entirely before calling process_refund, using only the customer's stated name to identify the account. This has led to several refunds issued to the wrong customer. Which change would most reliably close this gap?","options":["Add a programmatic prerequisite that blocks process_refund until get_customer has returned a verified customer ID.","Update the system prompt to state explicitly that identity verification is mandatory before any refund is processed.","Add few-shot examples to the system prompt showing the agent always calling get_customer before process_refund.","Switch to a larger model with stronger instruction-following to reduce the rate of skipped steps."],"correct":0,"explain":"A programmatic prerequisite guarantees the sequence holds on every run, closing the gap entirely rather than reducing it. Options B and C are both prompt-based fixes that rely on probabilistic compliance — they might cut the 12% rate but can't eliminate it. Option D assumes model capability is the bottleneck, but the failure is a missing enforcement mechanism, not a comprehension problem."},{"id":"q_determinism_1b","q":"Your support agent's system prompt already states that identity must be verified via get_customer before any refund is authorized, but production logs still show occasional refunds processed without that call ever happening. The team is deciding which of several policies to enforce with a hook instead of leaving them in the prompt. Which policy is the best candidate for hook enforcement?","options":["Requiring the agent to keep a warm, empathetic tone whenever a customer's message reads as frustrated or dissatisfied.","Blocking any call to process_refund until get_customer has returned a verified customer identity for that account.","Requiring the agent to summarize any document longer than a few paragraphs before quoting specific passages from it.","Requiring the agent to ask a clarifying question whenever a customer's request is ambiguous about which order it concerns."],"correct":1,"explain":"Verifying identity before a refund is a hard invariant that must hold on every run, which is exactly what a hook enforces deterministically. Keeping a warm tone with frustrated customers, deciding when a document needs summarizing, and recognizing when a request is ambiguous are judgment calls the model is well suited to make, not invariants that need programmatic gating."}]}],"reviewQuestions":[{"id":"r_determinism_1","q":"A team is debating whether a new 'never log a customer's full card number' rule should live only in the agent's system prompt or also be enforced by a hook. One engineer summarizes the general distinction between the two mechanisms for the rest of the team. Which summary is accurate?","options":["Hooks execute in a separate process, so they run measurably faster than a prompt instruction the model reads on every turn.","Prompts should be phased out entirely in favor of hooks wherever a hook could technically be written to enforce the rule.","A hook is code that runs deterministically every time its event fires; a prompt is only a request the model may or may not follow.","Hooks exist only to gate tool calls, while prompts remain the sole mechanism for shaping what the model writes in its response."],"correct":2,"explain":"A hook is code that executes on every occurrence of its event, giving a guarantee a prompt cannot provide. The claim about execution speed conflates a different property with the real distinction, phasing prompts out entirely ignores that judgment calls still belong there, and restricting hooks to tool-call gating undersells what hooks can observe and enforce across other events like Stop or UserPromptSubmit."}]},{"id":"mt_hook_events","type":"FACTUAL","cluster":"cl_hooks","name":"The hook events","description":"Python HookEvent: PreToolUse, PostToolUse, PostToolUseFailure, UserPromptSubmit, Stop, SubagentStop, PreCompact, Notification, SubagentStart, PermissionRequest. TypeScript supports additional events. Each event names a moment in the loop where your code can run.","core":true,"estMinutes":7,"evidence":["Pick the event that fires at a described moment","Distinguish events that fire per tool call from events that fire per turn or session"],"assessmentPrompt":"You want to run code every time the agent tries to end its turn. Which event?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · Where hooks attach","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene2"}],"centrality":0.233,"layer":2,"x":540,"y":294,"knowledgePoints":[{"id":"kp_events_1","name":"The event list","keyPrerequisites":["mt_determinism_principle"],"teach":"<p>Python <code>HookEvent</code>: <code>PreToolUse</code>, <code>PostToolUse</code>, <code>PostToolUseFailure</code>, <code>UserPromptSubmit</code>, <code>Stop</code>, <code>SubagentStop</code>, <code>PreCompact</code>, <code>Notification</code>, <code>SubagentStart</code>, <code>PermissionRequest</code>. The TypeScript SDK supports additional events beyond these.</p><p>Group them by <em>what they are about</em> and the list stops being a list:</p><ul><li><strong>Per tool call</strong> — PreToolUse, PostToolUse, PostToolUseFailure, PermissionRequest</li><li><strong>Per turn</strong> — UserPromptSubmit, Stop, SubagentStop</li><li><strong>Per agent / session</strong> — SubagentStart, PreCompact, Notification</li></ul>","questions":[{"id":"q_events_1a","q":"You are building a compliance check that requires the agent to have cited at least one source before it is allowed to finish responding to the user, and the check needs to run every single time the agent attempts to end its turn. Which hook event should the check subscribe to?","options":["PostToolUse, so the check runs immediately after each individual tool result is returned to the model.","Notification, so the check runs whenever the SDK surfaces a user-facing notification during the session.","SessionEnd, so the check runs only once the entire multi-turn session with the user has fully concluded.","Stop, so the check runs when the agent attempts to end its turn, letting it require a citation before allowing that."],"correct":3,"explain":"Stop fires specifically when the agent attempts to end its turn, which is what makes it the right place to require a citation before that is allowed. PostToolUse fires per tool call rather than per turn, Notification is for informational events, and SessionEnd — even where it exists — fires only once the whole session is already over, too late to gate a single turn."},{"id":"q_events_1b","q":"A tool call to a payment gateway throws an exception, and you want a dedicated hook to run recovery logic specifically for that failure, without adding failure-handling branches to your existing PostToolUse success-path code. Which event should the recovery hook subscribe to?","options":["PostToolUse, since it fires for every tool call regardless of whether the call succeeded or failed.","PostToolUseFailure, a dedicated event that fires specifically when a tool call has already failed.","PermissionRequest, since it fires whenever a tool call requires explicit authorization before running.","UserPromptSubmit, since it fires before the model begins processing a new request from the user."],"correct":1,"explain":"PostToolUseFailure is a distinct event dedicated to calls that have already failed, so recovery logic can live in its own handler instead of being threaded through the success path. PostToolUse does not fire for failed calls, PermissionRequest concerns authorization rather than outcome, and UserPromptSubmit fires before the tool call happens at all."}]}],"reviewQuestions":[{"id":"r_events_1","q":"While reviewing the SDK's hook documentation, a colleague claims there is a Python HookEvent called ToolResultReceived that fires whenever a tool returns output. You check the actual list of events to confirm. Which statement is correct?","options":["ToolResultReceived exists and fires before PostToolUse specifically for read-only tools such as Read and Grep.","ToolResultReceived exists but only fires for tool calls that originate from within a subagent.","ToolResultReceived does not exist; PostToolUse is the actual event that fires when a tool call completes.","ToolResultReceived exists and replaced PostToolUse in the most recent version of the Python HookEvent list."],"correct":2,"explain":"ToolResultReceived is not one of the Python HookEvent values; PostToolUse is the actual event that fires once a tool call completes, whether by success or via the separate PostToolUseFailure event. The other claims all describe behavior for an event that was never part of the list to begin with."}]},{"id":"mt_hook_registration","type":"PROCEDURAL","cluster":"cl_hooks","name":"Registering a hook","description":"Hooks go in the `hooks` option keyed by event as a list of matchers. Python HookMatcher takes matcher (tool name or pattern like \"Write|Edit\"; omit to match all), hooks, and timeout (default 600s, UserPromptSubmit 30s). Callbacks are (input, tool_use_id, context) -> HookJSONOutput; input is a union discriminated on hook_event_name. Shell command hooks block with exit code 2 and feed stderr back to Claude; stderr from an exit-0 hook goes only to the debug log.","core":true,"estMinutes":9,"evidence":["Register a hook that fires only on Write and Edit","Explain why a hook's stderr never reached the model"],"assessmentPrompt":"Write the matcher that runs a hook on Write and Edit but not Read.","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Notes · Registering a hook","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.14,"layer":3,"x":750,"y":232,"knowledgePoints":[{"id":"kp_hookreg_1","name":"HookMatcher and the callback signature","keyPrerequisites":["mt_hook_events"],"teach":"<p>Hooks go in the <code>hooks</code> option, keyed by event, as a list of matchers:</p><pre class='kp-code'>HookMatcher(\n    matcher=\"Bash\",        # tool name or pattern, e.g. \"Write|Edit\"; omit to match all\n    hooks=[my_callback],\n    timeout=None,          # seconds; default 600, except UserPromptSubmit at 30\n)</pre><p>The callback is <code>(input, tool_use_id, context) -&gt; HookJSONOutput</code>. <code>input</code> is a discriminated union on <code>hook_event_name</code>.</p><p>All hook inputs share <code>session_id</code>, <code>transcript_path</code>, <code>cwd</code> and an optional <code>permission_mode</code>. <code>PreToolUseHookInput</code> adds <code>tool_name</code>, <code>tool_input</code>, <code>tool_use_id</code> — and, when firing inside a subagent, <code>agent_id</code> and <code>agent_type</code>.</p>","questions":[{"id":"q_hookreg_1a","q":"You want a hook registered under PreToolUse to run whenever the agent calls Write or Edit, but not when it calls Read, and you want to express that directly through the HookMatcher's matcher field rather than filtering inside the callback itself. How should you write the matcher?","options":["Set matcher to the list [\"Write\", \"Edit\"], since HookMatcher accepts an array of exact tool names.","Set matcher to the comma-separated string \"Write,Edit\" and let the SDK split it into individual tool names.","Leave matcher unset entirely and add an early return inside the callback whenever tool_name is not Write or Edit.","Set matcher to the pattern string \"Write|Edit\", which HookMatcher parses as matching either tool name."],"correct":3,"explain":"HookMatcher's matcher field accepts a pattern string like \"Write|Edit\", which the framework parses as matching either tool name for that hook. An array of exact names and a comma-separated string are not the formats the field expects, and leaving matcher unset would run the hook for every tool rather than expressing the restriction where it belongs."},{"id":"q_hookreg_1b","q":"A PreToolUse hook needs to apply a stricter rate limit to tool calls made by subagents than to calls made from the main thread, and it needs to tell the two apart purely from fields already present on the hook input. Which fields let it do that?","options":["The agent_id and agent_type fields, present on PreToolUseHookInput when the call originates inside a subagent.","The session_id field combined with the cwd field, since subagents always run from a completely different working directory.","The parent_tool_use_id field, which is populated only for calls issued from the main thread rather than a subagent.","The permission_mode field, since subagents always run under a noticeably stricter permission mode than the main thread does."],"correct":0,"explain":"agent_id and agent_type appear on PreToolUseHookInput specifically when a call originates inside a subagent, which is exactly the signal needed to apply a different rate limit there. session_id and cwd don't reliably distinguish the two contexts, parent_tool_use_id doesn't work the way described, and permission_mode is not tied to whether a call came from a subagent."}]},{"id":"kp_hookreg_2","name":"Command hooks and exit codes","keyPrerequisites":[],"teach":"<p>Hooks can also be shell commands configured in settings files rather than callbacks in your code. For those:</p><ul><li><strong>Exit code 2 blocks the action</strong> and feeds <code>stderr</code> back to Claude.</li><li>Stderr from a hook that exits <strong>0</strong> goes only to the debug log — <em>Claude never sees it.</em></li></ul><p>That second rule explains a common confusion: \"my hook printed a warning and the model ignored it.\" It did not ignore it. It never received it.</p>","questions":[{"id":"q_hookreg_2a","q":"A command hook configured for PreToolUse prints a warning to stderr reading 'this path is outside the project root' and then exits with code 0. The team expects Claude to see the warning and reconsider the file path, but across several transcripts the model never reacts to it. What explains this?","options":["The warning is delivered to Claude directly, but as a system message the model is trained to treat as low priority.","The tool call is blocked before it runs, so there is nothing left for the model to react to in the first place.","Exit code 0 tells the framework the hook succeeded, so stderr goes only to the debug log and Claude never sees it.","stderr from a command hook is injected beside the tool result only when the matcher targets exactly one tool."],"correct":2,"explain":"Exit code 0 tells the framework the hook finished normally, so any stderr it wrote is recorded only in the debug log and never reaches Claude. The call is not blocked, since blocking requires exit code 2, and stderr delivery to Claude does not depend on how narrowly the matcher is scoped."},{"id":"q_hookreg_2b","q":"Your command hook needs to stop a dangerous Bash command from executing at all, and it needs its stderr message delivered back to Claude so the model can adjust its approach on the next attempt. Which exit code should the hook script return?","options":["Exit code 0, which signals to the framework that the hook actively intervened in the tool call.","Exit code 1, which is reserved specifically for blocking actions along with a spoken reason.","Any non-zero exit code, since the framework treats all of them identically as a deliberate block.","Exit code 2, which blocks the action from running and feeds the hook's stderr back to Claude."],"correct":3,"explain":"Exit code 2 is the specific signal that blocks the action and feeds the hook's stderr back to Claude so it can adjust course. Exit 0 means the hook succeeded and the action proceeds, exit 1 has no special meaning here, and other non-zero codes are treated as the hook itself having failed rather than as a deliberate block."}]}],"reviewQuestions":[{"id":"r_hookreg_1","q":"You are configuring a HookMatcher and need to know how long the framework waits before treating your callback as timed out, given you set no explicit timeout value. Your hook is registered under PreToolUse, and a teammate's hook is registered under UserPromptSubmit. What are the applicable defaults?","options":["PreToolUse defaults to 600 seconds, while UserPromptSubmit defaults to 30 seconds since a user is waiting.","Both default to 30 seconds, since that is the single standard timeout applied across every hook event.","There is no default timeout at all; every HookMatcher must specify one explicitly or registration fails.","Both default to 600 seconds across every event, except Stop hooks, which fall back to 30 seconds instead."],"correct":0,"explain":"The default hook timeout is 600 seconds, except for UserPromptSubmit, which defaults to 30 seconds specifically because a user is sitting there waiting on the response. There is a default even when none is set explicitly, and it is not a flat 30 or 600 seconds across every event."}]},{"id":"mt_hook_direction","type":"CONCEPTUAL","cluster":"cl_hooks","name":"Which direction does the hook face?","description":"PreToolUse guards what goes out; PostToolUse cleans what comes back. Tool call interception (block a refund over $500) is PreToolUse. Result normalization (three MCP tools returning three timestamp formats) is PostToolUse. If a question says \"different tools return inconsistent formats,\" the answer is PostToolUse, not better prompting.","core":true,"estMinutes":8,"evidence":["Choose Pre vs. Post from the direction of the problem","Name both exam-named hook patterns and their canonical scenarios"],"assessmentPrompt":"Several MCP tools return timestamps in different formats. Where do you fix it?","standards":["ccaf:d1","ccaf:d2","ccaf-glossary:t10","ccaf-glossary:t11"],"sources":[{"label":"Lesson · Which direction does the hook face?","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene3"}],"centrality":0.14,"layer":3,"x":750,"y":294,"knowledgePoints":[{"id":"kp_direction_1","name":"Out vs. back","keyPrerequisites":["mt_hook_events"],"teach":"<p class='kp-key'>PreToolUse guards what goes <strong>out</strong>. PostToolUse cleans what comes <strong>back</strong>.</p><p>Direction is the whole distinction, and it decides almost every hook question on the exam. Two named patterns:</p><ul><li><strong>Tool call interception hook</strong> (glossary t11) — intercepts an <em>outgoing</em> call to enforce a business rule. Canonical case: block <code>process_refund</code> over $500 and redirect to human escalation. That is <code>PreToolUse</code> with <code>permissionDecision: \"deny\"</code> and a reason — or <code>updatedInput</code> to rewrite it.</li><li><strong>PostToolUse normalization</strong> (t10) — intercepts <em>results</em> to transform them before the model reads them. Canonical case: several MCP tools return timestamps in different formats (Unix epoch, ISO 8601, MM/DD/YYYY) and a hook normalises them so the model never has to reconcile the mismatch.</li></ul>","questions":[{"id":"q_direction_1a","q":"Three MCP tools your agent calls return timestamps in three different formats — one as Unix epoch, one as ISO 8601, and one as MM/DD/YYYY — and the model occasionally miscalculates date comparisons because it has to reconcile the mismatch itself. Where should you fix this?","options":["In the system prompt, listing each tool's timestamp format so the model can interpret every value correctly.","In a PostToolUse hook that normalizes each tool's returned timestamp into a single consistent format.","In a PreToolUse hook that rewrites the outgoing arguments before each of the three tool calls runs.","By merging the three tools into a single wrapper tool that always returns results as Unix epoch."],"correct":1,"explain":"Reconciling inconsistent tool output is exactly what PostToolUse normalization is for — the hook rewrites each returned timestamp into one consistent format so the model never has to compare formats itself. Explaining the formats in the system prompt still leaves the reconciliation work to the model, a PreToolUse hook only touches the outgoing request rather than what comes back, and merging the tools is a much larger change than the problem requires."},{"id":"q_direction_1b","q":"Your finance team requires that any process_refund call for more than $500 never actually execute — it must be intercepted and routed to a human reviewer before the payment provider is ever contacted. Which hook should implement this?","options":["A PostToolUse hook that checks the refund amount in the completed result and reverses it if it exceeds $500.","A UserPromptSubmit hook that rejects any user message mentioning refunds over $500 before the agent responds.","A Stop hook that refuses to let the agent end its current turn until a human has reviewed any large refund it issued.","A PreToolUse hook that denies the outgoing process_refund call and supplies a reason when the amount exceeds $500."],"correct":3,"explain":"Blocking the refund before it ever reaches the payment provider requires intercepting the outgoing call, which is what a PreToolUse hook denying process_refund with a reason accomplishes. A PostToolUse check would only notice the refund after it has already happened, a UserPromptSubmit hook can't inspect the tool call itself, and a Stop hook would let the refund go through and only block the agent from finishing afterward."}]}],"reviewQuestions":[{"id":"r_direction_1","q":"A new team member is trying to decide, as a general rule and without inspecting each case individually, whether a given hook requirement belongs on PreToolUse or PostToolUse. Which rule correctly captures the distinction?","options":["PreToolUse guards what goes out to a tool before it runs; PostToolUse handles what comes back from it afterward.","PreToolUse is reserved for cheap, fast checks; PostToolUse is for checks that are expensive to compute.","PreToolUse only applies to MCP tools; PostToolUse only applies to built-in tools like Read and Write.","PreToolUse can block a call outright, while PostToolUse can only observe the result and log it."],"correct":0,"explain":"PreToolUse sits before the tool runs and guards the outgoing call, while PostToolUse sits after and handles what comes back — that direction, not cost or tool type, is the actual distinction. PreToolUse isn't limited to MCP tools, and PostToolUse is not observe-only: it can also block via a decision field of its own."}]},{"id":"mt_hook_decisions","type":"PROCEDURAL","cluster":"cl_hooks","name":"Decision control, by event","description":"PreToolUse returns hookSpecificOutput.permissionDecision: allow / deny / ask / defer plus a reason. PostToolUse, PostToolUseFailure, UserPromptSubmit, Stop, SubagentStop and PreCompact use top-level `decision: \"block\"` with a reason. PermissionRequest uses hookSpecificOutput.decision.behavior. SessionStart and SubagentStart can only add context; Notification, SessionEnd, PostCompact and InstructionsLoaded are side-effect only.","core":true,"estMinutes":9,"evidence":["Write the right return shape for a given event","Say which events cannot block at all"],"assessmentPrompt":"Your PostToolUse hook returns permissionDecision: \"deny\". What happens?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · Blocking and redirecting","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene4"}],"centrality":0.116,"layer":4,"x":960,"y":356,"knowledgePoints":[{"id":"kp_decisions_1","name":"Two return shapes, split by event","keyPrerequisites":["mt_hook_registration"],"teach":"<table class='kp-table'><tr><th>Events</th><th>Pattern</th><th>Key fields</th></tr><tr><td><code>PreToolUse</code></td><td><code>hookSpecificOutput</code></td><td><code>permissionDecision</code>: <strong>allow / deny / ask / defer</strong>, plus <code>permissionDecisionReason</code></td></tr><tr><td><code>PostToolUse</code>, <code>PostToolUseFailure</code>, <code>UserPromptSubmit</code>, <code>Stop</code>, <code>SubagentStop</code>, <code>PreCompact</code></td><td>top-level <code>decision</code></td><td><code>decision: \"block\"</code>, <code>reason</code></td></tr><tr><td><code>PermissionRequest</code></td><td><code>hookSpecificOutput</code></td><td><code>decision.behavior</code>: allow / deny</td></tr><tr><td><code>SessionStart</code>, <code>SubagentStart</code></td><td>context only</td><td><code>additionalContext</code>; no blocking</td></tr><tr><td><code>Notification</code>, <code>SessionEnd</code>, <code>PostCompact</code>, <code>InstructionsLoaded</code></td><td>none</td><td>side effects only — logging, cleanup</td></tr></table><p>Getting the shape wrong is silent: a PostToolUse hook that returns <code>permissionDecision</code> is returning a field that event does not read.</p>","questions":[{"id":"q_decisions_1a","q":"You write a PostToolUse hook that inspects a completed database write and, wanting to prevent the model from seeing the result, returns hookSpecificOutput.permissionDecision: \"deny\". After deploying it, the questionable result still reaches the model unchanged. Why?","options":["The tool result is discarded silently before the model ever receives it, leaving the model working from stale data.","PostToolUse reads a top-level decision field, not hookSpecificOutput.permissionDecision, so the field is ignored.","The tool call is automatically retried once with the exact same arguments, producing the identical result again.","Returning a field that this particular event does not expect raises a validation error that halts the entire run."],"correct":1,"explain":"PostToolUse reads a top-level decision field rather than hookSpecificOutput.permissionDecision, so returning the PreToolUse-shaped field is simply ignored and the result reaches the model unchanged. Nothing is discarded, retried, or flagged as a validation error — the mismatch fails silently rather than loudly."},{"id":"q_decisions_1b","q":"You need to inject a short piece of context into the conversation right at the moment a new session or a new subagent begins, knowing these particular hooks cannot block anything since nothing has happened yet. Which pair of events are you working with?","options":["PreToolUse and PostToolUse, which both surround individual tool calls rather than whole sessions.","Stop and SubagentStop, which fire only once the current turn is already in the process of ending.","SessionStart and SubagentStart, which fire before anything exists yet and can only add additionalContext.","PermissionRequest and PreCompact, which both gate an action that is already underway in the current tool call."],"correct":2,"explain":"SessionStart and SubagentStart fire before anything has happened yet for that session or subagent, so all they can do is add additionalContext rather than block a call. PreToolUse and PostToolUse concern individual tool calls rather than session start, and Stop, SubagentStop, and PermissionRequest all fire at points where something is already underway."}]},{"id":"kp_decisions_2","name":"The four PreToolUse decisions","keyPrerequisites":[],"teach":"<p><code>permissionDecision</code> takes four values, and they are not two pairs:</p><ul><li><code>allow</code> — proceed, skipping the rest of the permission flow.</li><li><code>deny</code> — refuse, with <code>permissionDecisionReason</code> fed back so the model can adapt rather than retry blindly.</li><li><code>ask</code> — fall through to the normal prompt / <code>canUseTool</code>.</li><li><code>defer</code> — park the call and hand it to your application.</li></ul><p>Always supply <code>permissionDecisionReason</code> on a deny. A denial without a reason teaches the model nothing, and it will usually try again.</p>","questions":[{"id":"q_decisions_2a","q":"A PreToolUse hook denies a call to send_email because the recipient address is outside the allowed domain, but the hook's author leaves permissionDecisionReason empty. Over the following turns, the model repeatedly retries the same call with only cosmetic changes to the arguments. Why does supplying a reason on a deny matter?","options":["The reason is fed back to the model, giving it the information it needs to adapt instead of retrying blindly.","permissionDecisionReason is a required field, and omitting it causes the SDK to silently ignore the deny.","The reason text is recorded in billing and usage records so the team can audit denied calls later on.","Without a reason string present, the framework does not honor the exit code the hook returned."],"correct":0,"explain":"permissionDecisionReason is what gets fed back to the model, giving it the specific information needed to adapt its next attempt instead of retrying the same call with cosmetic changes. It is not a required field that would break the deny if omitted, it has nothing to do with billing records, and the exit code's effect does not depend on whether a reason string is present."},{"id":"q_decisions_2b","q":"A PreToolUse hook you are writing has no strong opinion on whether a particular Bash call should be allowed, and you want the decision to fall through to whatever the normal permission prompt or canUseTool callback would otherwise decide. Which permissionDecision value expresses that?","options":["allow, since it lets the tool call proceed immediately, skipping any further permission checks entirely.","ask, since it hands the decision back to the normal permission flow instead of deciding itself.","deny, since it forces the decision back to the user through an explanatory error message right away.","defer, since it parks the call for your own application to resolve at some later point in time."],"correct":1,"explain":"ask is the value that explicitly hands the decision back to the normal permission flow when the hook itself has no opinion. allow would let the call through unconditionally, deny would refuse it outright, and defer would park it for your own application to resolve rather than deferring to the built-in flow."}]}],"reviewQuestions":[{"id":"r_decisions_1","q":"You are writing a Stop hook that must prevent the agent from ending its turn until its response includes at least one citation. Which field on the hook's return value actually accomplishes that?","options":["Returning exit code 2 from a command-line script registered specifically on the Stop event.","Setting additionalContext to a string that explains a citation is required before finishing.","Setting a top-level decision field to \"block\" along with a reason, the pattern Stop shares with PostToolUse.","Setting hookSpecificOutput.permissionDecision to \"deny\", the field a PreToolUse hook would use."],"correct":2,"explain":"Stop belongs to the family of events that use a top-level decision field set to \"block\" with a reason, the same pattern PostToolUse uses. hookSpecificOutput.permissionDecision belongs to PreToolUse instead, additionalContext alone can't prevent the turn from ending, and a command hook's exit code is a mechanism for shell-based hooks rather than the field being asked about here."}]},{"id":"mt_hook_rewrite","type":"PROCEDURAL","cluster":"cl_hooks","name":"Rewriting instead of gating","description":"PreToolUse `updatedInput` (directly under hookSpecificOutput) replaces the tool's arguments before it runs. PostToolUse `additionalContext` injects a string next to the tool result, wrapped in a system reminder, capped at 10,000 characters — longer output is written to a file and replaced with a preview and path.","core":true,"estMinutes":7,"evidence":["Choose rewriting over denial when the call is nearly right","State the additionalContext size limit and what happens past it"],"assessmentPrompt":"An agent keeps calling a search tool without the tenant filter. Deny, or rewrite?","standards":["ccaf:d1","ccaf:d2"],"sources":[{"label":"Lesson · Blocking and redirecting","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene4"}],"centrality":0,"layer":5,"x":1170,"y":294,"knowledgePoints":[{"id":"kp_rewrite_1","name":"updatedInput and additionalContext","keyPrerequisites":["mt_hook_decisions"],"teach":"<p>Denial is not the only move. Two fields let a hook <em>fix</em> instead of refuse:</p><ul><li><strong><code>PreToolUse</code> → <code>updatedInput</code></strong> (directly under <code>hookSpecificOutput</code>) replaces the tool's arguments before it runs. Use it when the call is nearly right — a missing tenant filter, an unscoped path, a limit that is too high.</li><li><strong><code>PostToolUse</code> → <code>additionalContext</code></strong> injects a string into Claude's context, wrapped in a system reminder, placed next to the tool result. <strong>Capped at 10,000 characters</strong>; longer output is written to a file and replaced with a preview and the path.</li></ul><p>Rewriting is usually kinder than denying: the model gets a working result rather than a rejection it has to reason its way around.</p>","questions":[{"id":"q_rewrite_1a","q":"Your agent repeatedly calls a search_records tool without the required tenant_id filter, which would let it read across tenant boundaries if the results were ever returned to the model. The call is otherwise well-formed and clearly intends to search within the current tenant. Should a PreToolUse hook deny the call or rewrite it?","options":["Deny it with a reason so the model learns the rule and adds the filter itself on its next attempt.","Remove the search_records tool from the agent's available toolset entirely, so this can never recur.","Let the call proceed and catch the cross-tenant leak afterward inside a PostToolUse hook instead.","Rewrite the call with updatedInput, adding the missing tenant_id filter before it is allowed to proceed."],"correct":3,"explain":"The call is nearly right — it's missing one filter — so rewriting it with updatedInput closes the gap immediately and lets the search still complete successfully. Denying costs a turn while the model retries, removing the tool entirely is a disproportionate response to a fixable mistake, and catching the leak in PostToolUse would mean the cross-tenant read already happened."},{"id":"q_rewrite_1b","q":"A PostToolUse hook you wrote injects a long diagnostic report into additionalContext after every deploy-status check, and one particular report happens to run to roughly 40,000 characters. What happens to the injected context in that case?","options":["It is capped at 1,000 characters, and the remainder beyond that is truncated silently with no other notice.","It is capped at 10,000 characters, with the overflow written to a file and a preview left in context.","It is capped at 100,000 tokens, a limit well above the size of this particular report, so nothing unusual happens.","There is no cap on additionalContext at all, so the full 40,000-character report is passed through unchanged."],"correct":1,"explain":"additionalContext is capped at 10,000 characters, and content beyond that limit is written to a file with a preview and the file path left in context instead of being lost. The other caps and the claim of no cap at all don't match how the framework actually handles oversized context."}]}],"reviewQuestions":[{"id":"r_rewrite_1","q":"A PreToolUse hook needs to silently correct a tool call's arguments — for example, clamping a limit parameter that is set too high — rather than deny the call outright. Which field on the hook's response carries the corrected arguments?","options":["additionalContext, the same field a PostToolUse hook uses to inject extra information afterward.","updatedPermissions, a field that instead adjusts what the tool is allowed to do going forward.","updatedInput, nested directly under hookSpecificOutput, which replaces the tool's arguments before it runs.","permissionDecisionReason, which explains a deny but carries no replacement arguments of its own."],"correct":2,"explain":"updatedInput, nested under hookSpecificOutput on a PreToolUse response, is exactly the field that replaces a tool's arguments before it runs, which is what a silent correction like clamping a limit requires. additionalContext only applies after a tool has already run, updatedPermissions changes future authorization rather than this call's arguments, and permissionDecisionReason only explains a deny."}]},{"id":"mt_hook_defer","type":"PROCEDURAL","cluster":"cl_hooks","name":"Deferring a tool call for human approval","description":"A PreToolUse hook returning permissionDecision: \"defer\" produces stop_reason: \"tool_deferred\" with the pending call in deferred_tool_use. That is how you surface an approval request in your own UI and resume later with the same session_id.","core":false,"estMinutes":6,"evidence":["Build a human-in-the-loop approval step without blocking the process","Name the stop_reason and the field that carries the pending call"],"assessmentPrompt":"You need a manager to approve a refund in your own web UI before the tool runs. Which decision value?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Notes · Decision control, by event","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":5,"x":1170,"y":232,"knowledgePoints":[{"id":"kp_defer_1","name":"Parking a call for a human","keyPrerequisites":["mt_hook_decisions"],"teach":"<p>A <code>PreToolUse</code> hook returning <code>permissionDecision: \"defer\"</code> produces:</p><ul><li><code>stop_reason: \"tool_deferred\"</code></li><li>the pending call in <code>deferred_tool_use</code></li></ul><p>That is how you surface an approval request in <em>your own</em> UI — a manager approving a refund in your web app, not in a terminal — and resume later with the same <code>session_id</code>.</p><p>Contrast with <code>ask</code>, which hands the decision to the SDK's own permission flow, and with <code>deny</code>, which ends the matter.</p>","questions":[{"id":"q_defer_1a","q":"Your company requires that any refund over $1,000 be approved by a manager inside your own web application — not through a terminal prompt — before the refund tool actually executes, with the run resuming automatically once approval is granted. Which permissionDecision value should the PreToolUse hook return?","options":["deny, along with a clear reason explaining that manager approval is required before this can proceed.","ask, which routes the decision to the SDK's own built-in interactive permission flow instead.","allow, and then reverse the refund transaction afterward if the manager ends up rejecting it.","defer, which hands the pending call to your application to surface and resolve on its own."],"correct":3,"explain":"defer is built for exactly this situation: it parks the pending call and hands it to your own application to surface and resolve, resuming the run once approval comes back. deny would end the matter without a path to approval, ask would route to the SDK's own permission UI rather than your web app, and allow-then-reverse would let the refund execute before anyone had approved it."},{"id":"q_defer_1b","q":"After a PreToolUse hook returns permissionDecision: \"defer\" for a pending refund call, your backend needs to detect that the call is now waiting on external approval rather than assuming the turn simply ended normally. What should it check for?","options":["A stop_reason value of \"tool_deferred\", with the pending call itself available in deferred_tool_use.","A stop_reason value of \"end_turn\", the same value used whenever the agent finishes a turn normally.","A stop_reason value of \"max_turns\", indicating the run has hit its configured turn limit.","A stop_reason value of \"tool_use\", the generic value returned whenever any tool call occurs at all."],"correct":0,"explain":"A defer decision produces a distinct stop_reason of \"tool_deferred\", with the pending call itself available in deferred_tool_use so the backend knows exactly what is waiting on approval. \"end_turn\", \"max_turns\", and \"tool_use\" are all used for other situations and would not signal that a call is parked awaiting external approval."}]}],"reviewQuestions":[{"id":"r_defer_1","q":"A manager has just approved a refund that was parked by a defer decision, and you need the agent's run to continue exactly where it left off rather than starting the conversation over from scratch. How do you resume it?","options":["Start an entirely new query using the same prompt text that originally triggered the deferred call.","Re-run the PreToolUse hook that produced the defer decision so it can re-evaluate the call again.","Resume the run using the same session_id that was active at the moment the call was deferred.","Call the underlying refund tool yourself and inject its result directly into the transcript."],"correct":2,"explain":"Resuming with the same session_id that was active when the call was deferred picks the run back up exactly where it stopped. Starting a new query loses that state, re-running the hook would just produce another defer decision, and calling the tool yourself bypasses the approval flow the defer was designed to enforce."}]},{"id":"mt_can_use_tool","type":"CONCEPTUAL","cluster":"cl_hooks","name":"canUseTool vs. a PreToolUse hook","description":"canUseTool replaces the interactive permission prompt and fires only when the permission flow resolves to a prompt — calls already approved by allowedTools, a settings rule, or a permissive mode never reach it. To gate every call unconditionally, use PreToolUse. Exceptions that reach canUseTool anyway: AskUserQuestion, MCP tools marked requiresUserInteraction, and connector tools an org set to ask. It returns { behavior: \"allow\", updatedInput?, updatedPermissions? } or { behavior: \"deny\", message, interrupt? }.","core":true,"estMinutes":8,"evidence":["Explain why a canUseTool audit log has holes in it","Choose between canUseTool and PreToolUse for a stated requirement"],"assessmentPrompt":"You log every tool call from canUseTool and the log is missing calls. Why?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · Six modes, one badly-named option","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene6"}],"centrality":0,"layer":5,"x":1170,"y":170,"knowledgePoints":[{"id":"kp_canuse_1","name":"canUseTool only fires on a prompt","keyPrerequisites":["mt_permission_modes"],"teach":"<p><code>canUseTool</code> is the SDK replacement for the <em>interactive permission prompt</em>. It is invoked <strong>only when the permission flow resolves to a prompt.</strong></p><p>Calls already approved by <code>allowedTools</code>, by a settings allow rule, or by a permissive <code>permissionMode</code> (<code>acceptEdits</code>, <code>bypassPermissions</code>) <strong>never reach it</strong>.</p><p class='kp-key'>To gate every tool call unconditionally, use a PreToolUse hook, not canUseTool.</p><p>Exceptions that reach <code>canUseTool</code> even when otherwise allowed: <code>AskUserQuestion</code>, MCP tools marked <code>requiresUserInteraction</code>, and connector tools an organisation set to <code>ask</code>. In <code>dontAsk</code> mode those are denied instead of prompting.</p><p>It returns a <code>PermissionResult</code>: <code>{ behavior: \"allow\", updatedInput?, updatedPermissions? }</code> or <code>{ behavior: \"deny\", message, interrupt? }</code>.</p>","questions":[{"id":"q_canuse_1a","q":"You add logging inside your canUseTool callback expecting it to capture every tool call the agent makes, but comparing the log against the full transcript afterward shows a large number of calls are missing. What explains the gap?","options":["canUseTool calls are sampled rather than invoked on every single call once tool-call volume gets high enough.","Only MCP tools are ever routed through the canUseTool callback, while built-in tools bypass it entirely.","Logging placed inside canUseTool has to be made asynchronous, or entries can get silently dropped somewhere.","Calls already auto-approved by allowedTools, a settings rule, or a permissive mode never reach canUseTool."],"correct":3,"explain":"Calls that are already auto-approved — by allowedTools, a settings allow rule, or a permissive mode like bypassPermissions — never reach canUseTool at all, which is exactly why the log has gaps. canUseTool isn't sampled, it isn't restricted to MCP tools, and an async logging bug wouldn't produce gaps that line up with which calls were pre-approved."},{"id":"q_canuse_1b","q":"Your agent runs under bypassPermissions mode, and you assume canUseTool will never fire for anything since the mode is designed to skip prompts entirely. During testing, though, it does still fire for one particular kind of call. Which of these correctly reaches canUseTool even when otherwise allowed?","options":["An AskUserQuestion call, which reaches canUseTool in any mode, including bypassPermissions.","A Bash call, whenever Bash happens to be explicitly listed in the agent's allowedTools configuration.","A Read call, specifically whenever it happens to run under acceptEdits mode instead of default mode.","Any tool call at all, simply because the agent happens to be running under bypassPermissions mode."],"correct":0,"explain":"AskUserQuestion is one of the exceptions that reaches canUseTool in every permission mode, including bypassPermissions, regardless of what would otherwise be allowed. Being listed in allowedTools or running under acceptEdits or bypassPermissions is exactly what causes other calls to skip canUseTool rather than reach it."}]}],"reviewQuestions":[{"id":"r_canuse_1","q":"You are deciding where to put audit logging that must record every single tool call the agent makes with no gaps, and a teammate suggests putting it inside canUseTool since that is the SDK's permission callback. Why is that the wrong place, given when each mechanism fires?","options":["canUseTool fires on every tool call without exception, while PreToolUse only fires for file-write tools.","canUseTool fires only when the permission flow resolves to a prompt, while a PreToolUse hook fires on every call.","They fire at exactly the same point for exactly the same set of calls, so either placement works equally well.","PreToolUse only fires under default permission mode, while canUseTool fires regardless of the mode in use."],"correct":1,"explain":"canUseTool only fires when the permission flow actually resolves to a prompt, so any call that gets auto-approved never reaches it — a PreToolUse hook, by contrast, fires on every call regardless of how it will ultimately be decided. The claims that canUseTool fires on everything, that the two fire identically, or that PreToolUse is mode-restricted all get the relationship backwards."}]},{"id":"mt_permission_modes","type":"FACTUAL","cluster":"cl_hooks","name":"The permission modes","description":"default (standard prompting), acceptEdits (auto-accept file edits), plan (read-only, propose without editing), dontAsk (never prompt — deny anything not pre-approved), bypassPermissions (skip checks; explicit ask rules still prompt; requires allowDangerouslySkipPermissions), auto (a model classifier resolves what would have been prompts).","core":true,"estMinutes":8,"evidence":["Pick the mode for an unattended production run","Distinguish bypassPermissions from dontAsk"],"assessmentPrompt":"A nightly unattended agent must never prompt and never do anything you didn't pre-approve. Which mode?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · Six modes, one badly-named option","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene6"}],"centrality":0.14,"layer":2,"x":540,"y":356,"knowledgePoints":[{"id":"kp_modes_1","name":"Six modes","keyPrerequisites":["mt_subprocess_model"],"teach":"<table class='kp-table'><tr><th>Mode</th><th>Behaviour</th></tr><tr><td><code>default</code></td><td>Standard prompting behaviour</td></tr><tr><td><code>acceptEdits</code></td><td>Auto-accept file edits</td></tr><tr><td><code>plan</code></td><td>Read-only; explore and propose without editing</td></tr><tr><td><code>dontAsk</code></td><td>Never prompt — deny anything not pre-approved</td></tr><tr><td><code>bypassPermissions</code></td><td>Skip checks (explicit ask rules still prompt); requires <code>allowDangerouslySkipPermissions: true</code></td></tr><tr><td><code>auto</code></td><td>A model classifier approves or denies what would have been prompts</td></tr></table><p>The pair worth drilling is <code>dontAsk</code> versus <code>bypassPermissions</code>: one denies everything unapproved, the other allows almost everything. They sound similar and are opposites.</p>","questions":[{"id":"q_modes_1a","q":"You are configuring an unattended agent that runs every night with no one watching it, and the requirement is strict: it must never prompt for permission, and it must never perform any action that was not explicitly pre-approved in advance. Which permission mode satisfies both requirements?","options":["auto, since a model classifier approves or denies actions on your behalf without ever prompting.","acceptEdits, since it silently approves file edits without ever showing an interactive prompt.","bypassPermissions, since it skips permission checks and lets nearly everything through unprompted.","dontAsk, since it never prompts and denies anything that was not explicitly pre-approved."],"correct":3,"explain":"dontAsk is built for exactly this case: it never prompts, and anything that was not explicitly pre-approved is denied rather than allowed. auto still lets a classifier approve actions on its own judgment, acceptEdits only concerns file edits specifically, and bypassPermissions also never prompts but does so by allowing nearly everything rather than denying it."},{"id":"q_modes_1b","q":"Your team enables bypassPermissions for a coding agent to avoid interrupting it with prompts, but a scoped disallow rule like \"Bash(rm *)\" still blocks a destructive command the agent later attempts. This surprises a teammate who assumed bypass mode skipped every check. What does bypassPermissions actually still honor?","options":["Explicit ask rules and scoped disallow rules, both of which still take effect even under bypass mode.","Nothing at all; bypassPermissions is designed to skip every permission check without any exception.","Only the allowedTools list, which continues to auto-approve the specific tools it names.","Hooks still fire under bypassPermissions, but none of the ordinary permission rules apply anymore."],"correct":0,"explain":"bypassPermissions still honors explicit ask rules and scoped disallow rules like \"Bash(rm *)\", which is why the destructive command was still blocked. It does not skip every check without exception, its effect isn't limited to policing the allowedTools list, and hooks continuing to fire is a separate mechanism from the permission rules being described here."}]}],"reviewQuestions":[{"id":"r_modes_1","q":"Two modes, dontAsk and bypassPermissions, both run without ever showing an interactive prompt, and a teammate assumes that makes them interchangeable. Which statement correctly distinguishes their default behavior toward an unapproved action?","options":["dontAsk allows everything through by default, while bypassPermissions denies anything unapproved.","dontAsk denies anything that was not pre-approved, while bypassPermissions allows it through instead.","Both modes skip prompts and allow whatever action is requested, with no real difference between them.","They are two configuration names that both map to exactly the same underlying permission behavior."],"correct":1,"explain":"dontAsk denies anything that wasn't pre-approved, while bypassPermissions allows nearly everything through — same silence, opposite defaults. Neither the reversed version nor the claim that both allow everything nor the claim that they're identical describes the actual difference between the two modes."}]},{"id":"mt_allowed_vs_disallowed","type":"CONCEPTUAL","cluster":"cl_hooks","name":"allowedTools does not restrict","description":"allowedTools auto-approves the tools it lists; unlisted tools fall through to permissionMode and canUseTool rather than being blocked. disallowedTools actually blocks: a bare \"Bash\" removes the tool from Claude's context entirely, while a scoped rule like \"Bash(rm *)\" leaves the tool available and denies matching calls in every mode, including bypassPermissions.","core":true,"estMinutes":8,"evidence":["Predict what an agent can still do given only an allowedTools list","Write the rule that survives bypassPermissions"],"assessmentPrompt":"You put only Read and Grep in allowedTools. Can the agent write a file?","standards":["ccaf:d1","ccaf:d4"],"sources":[{"label":"Lesson · Six modes, one badly-named option","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene6"}],"centrality":0.093,"layer":3,"x":750,"y":170,"knowledgePoints":[{"id":"kp_allow_1","name":"allowedTools auto-approves; it does not restrict","keyPrerequisites":["mt_permission_modes"],"teach":"<p class='kp-key'>allowedTools does not restrict.</p><p>It <em>auto-approves</em> the tools it lists. Unlisted tools do not become unavailable — they fall through to <code>permissionMode</code> and <code>canUseTool</code>. Depending on the mode, an unlisted tool may prompt, be denied, or simply run.</p><p>To actually block, use <code>disallowedTools</code>, which has two modes of its own:</p><ul><li>A bare <code>\"Bash\"</code> <strong>removes the tool from Claude's context entirely</strong> — it is not in the tool list, so it is never considered.</li><li>A scoped rule like <code>\"Bash(rm *)\"</code> leaves the tool available and denies matching calls <strong>in every mode, including <code>bypassPermissions</code>.</strong></li></ul>","questions":[{"id":"q_allow_1a","q":"You configure allowedTools to contain only Read and Grep, running the agent under default permission mode, expecting this to prevent it from ever writing a file. Partway through a session, the agent successfully calls Write after a permission prompt appears and you approve it. What does this reveal about allowedTools?","options":["allowedTools should have acted as a strict whitelist, so this behavior indicates a bug in the permission system.","allowedTools should have removed Write from the agent's available tools entirely, so this call should have been impossible.","allowedTools only auto-approves the tools it lists; Write, being unlisted, correctly fell through to the normal permission mode.","A hook must have explicitly allowed the write, since allowedTools alone can never let an unlisted call through."],"correct":2,"explain":"allowedTools only auto-approves the tools it names; an unlisted tool like Write doesn't become unavailable, it simply falls through to whatever the active permission mode does with it, which in default mode is a prompt. It is not a whitelist, it does not remove unlisted tools from the agent's context, and no hook was needed to explain a prompt that the permission mode itself would produce."},{"id":"q_allow_1b","q":"You need a rule that keeps the Bash tool available to the agent for legitimate use, but reliably denies any Bash call matching rm *, even in configurations where the agent runs under bypassPermissions mode for everything else. Which configuration achieves that?","options":["disallowedTools: [\"Bash(rm *)\"], a scoped rule keeping Bash available but denying matches in every mode.","allowedTools: [\"Read\"], since leaving Bash off the allow list should be sufficient to block its destructive calls entirely.","permissionMode: \"plan\", which prevents any tool anywhere from executing changes regardless of which tools are listed.","A canUseTool callback that returns deny for matching commands, since it fires for every single Bash call no matter the mode."],"correct":0,"explain":"A scoped rule like disallowedTools: [\"Bash(rm *)\"] leaves Bash available for legitimate use while still denying matching calls in every mode, including bypassPermissions. Leaving Bash out of allowedTools wouldn't remove it under a permissive mode, permissionMode: \"plan\" is a blanket read-only mode rather than a targeted rule, and canUseTool would never even fire for a call already allowed under bypassPermissions."}]}],"reviewQuestions":[{"id":"r_allow_1","q":"You are choosing between disallowedTools: [\"Bash\"] and disallowedTools: [\"Bash(rm *)\"] to restrict a coding agent, and need to know exactly how the two differ in effect before picking one. Which statement correctly describes the difference?","options":["There is no real difference between them at all; the scoped version is simply a more specific way to write the same rule.","The bare tool name removes Bash from the agent's context entirely, while the scoped rule keeps it available but polices calls.","The bare tool name causes a prompt on every single Bash call, while the scoped rule denies matching calls outright without ever prompting.","The scoped rule only takes effect when the agent runs under default permission mode, unlike the bare tool-name rule."],"correct":1,"explain":"The bare tool name removes Bash from the agent's context entirely, so it is never available to call, while the scoped rule keeps Bash usable but denies specifically the matching calls. Neither one merely prompts instead of denying, they are not equivalent to each other, and the scoped rule's effect isn't limited to default permission mode."}]},{"id":"mt_inprocess_mcp","type":"PROCEDURAL","cluster":"cl_tools","name":"In-process MCP servers","description":"createSdkMcpServer() / create_sdk_mcp_server() builds an MCP server that runs in your application's own process — no subprocess, no stdio transport, no separate deployment. It is the SDK's answer to \"I need custom tools,\" and tools are then referenced as mcp__<server>__<tool>.","core":true,"estMinutes":8,"evidence":["Choose in-process over a stdio server and justify it","Write the allowedTools entry for an in-process tool"],"assessmentPrompt":"Your agent needs three custom tools that call functions already in your app. What do you build?","standards":["ccaf:d2"],"sources":[{"label":"Lesson · MCP servers that don't leave the process","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene7"}],"centrality":0.163,"layer":1,"x":330,"y":356,"knowledgePoints":[{"id":"kp_inproc_1","name":"A server with no server","keyPrerequisites":["mt_loop_ownership"],"teach":"<p><code>createSdkMcpServer()</code> / <code>create_sdk_mcp_server()</code> builds an MCP server that runs <strong>in the same process as your application</strong> — no subprocess, no stdio transport, no separate deployment, no serialization boundary between your tool and the rest of your app.</p><pre class='kp-code'>const support = createSdkMcpServer({ name: \"support\", tools: [lookupOrder] });</pre><p>This is the SDK's answer to \"I need custom tools,\" and it is the main thing the MCP course does not cover — that course builds <em>external</em> servers over stdio.</p><p>Tools from it are referenced as <code>mcp__&lt;server&gt;__&lt;tool&gt;</code> — so the example above gives you <code>mcp__support__lookup_order</code> in <code>allowedTools</code>.</p>","questions":[{"id":"q_inproc_1a","q":"Your support agent needs three new tools — lookup_order, issue_credit, and flag_ticket — that each call functions already defined in your Node.js backend. A teammate proposes spinning up a separate stdio MCP server process so the tools follow the standard MCP pattern used by your other integrations. Before implementing that, what should you consider building instead?","options":["Build an in-process SDK MCP server via createSdkMcpServer(), since the functions already run inside your app and a subprocess would add needless transport and deployment overhead.","Build the stdio MCP server your teammate proposed, so every tool integration the agent uses follows one consistent transport pattern across the codebase.","Build an HTTP MCP server behind your load balancer, matching how the external services your agent already calls are deployed and reached in production.","Skip MCP altogether and register the three functions as plain tool definitions passed straight into the agent's tools option, bypassing the protocol."],"correct":0,"explain":"An in-process server keeps the tools in the same process as the functions they call, avoiding the subprocess, transport, and deployment overhead a stdio or HTTP server would add for no benefit here. Matching transport styles for consistency isn't a reason to add process boundaries you don't need. An HTTP server makes sense for tools that are actually external services, which these are not. And skipping MCP entirely loses the standard tool-description and content-block conventions the rest of the agent relies on."},{"id":"q_inproc_1b","q":"You register an in-process SDK MCP server with name: \"support\" that exposes a tool called lookup_order. When you add this tool to allowedTools so the agent can call it, which identifier should you use?","options":["\"support.lookup_order\", using a dot to join the server and tool names the way most RPC frameworks do.","\"mcp__support__lookup_order\", using the double-underscore mcp__<server>__<tool> naming that applies to every MCP server, in-process or not.","\"mcp:support/lookup_order\", using a colon-and-slash form similar to a URI scheme.","\"support__lookup_order\", dropping the mcp prefix since the server already runs in-process rather than as a separate transport."],"correct":1,"explain":"Every MCP tool, whether it comes from an in-process SDK server or an external stdio/HTTP one, is addressed with the double-underscore mcp__<server>__<tool> form — the in-process case gets no special-cased naming. A dot-joined name resembles common RPC conventions but isn't what MCP uses. A colon-and-slash form looks like a URI but again isn't the actual naming scheme. Dropping the mcp prefix because the server is in-process is a natural guess, but the prefix is part of the fixed naming convention regardless of transport."}]}],"reviewQuestions":[{"id":"r_inproc_1","q":"Your team currently runs a stdio MCP server as a separate process to expose three internal tools, and it adds a noticeable amount of deployment and operational overhead. A teammate suggests rewriting it as an in-process SDK MCP server instead. What would that change actually eliminate, and what would stay the same?","options":["It would only eliminate the mcp__ prefix on each tool's name, since the two forms otherwise behave identically to the agent.","It would eliminate almost nothing — an in-process server is essentially a thin convenience alias over the same stdio transport.","It would eliminate the subprocess, the transport layer, and the separate deployment, while the mcp__<server>__<tool> naming and tool descriptions stay exactly the same.","It would eliminate the need to write tool descriptions, since in-process tools can rely on your application's existing function signatures instead."],"correct":2,"explain":"An in-process server removes the process boundary itself — no subprocess to run, no stdio transport to maintain, and no separate deployment — but the tools are still addressed with the same mcp__<server>__<tool> naming and still need the same descriptions, since selection still depends on them. It is not a no-op alias; the overhead it removes is real. And tool descriptions aren't optional in either form — they're still how the model chooses the right tool."}]},{"id":"mt_tool_shape","type":"FACTUAL","cluster":"cl_tools","name":"tool() has two shapes","description":"TypeScript: a function returning a definition — tool(name, description, zodSchema, handler, extras?). Python: a decorator on an async function — @tool(name, description, input_schema, annotations?) where the decorated function is the handler. TypeScript-only extras carry searchHint and alwaysLoad. Both return { content: [{ type: \"text\", text }] }.","core":true,"estMinutes":7,"evidence":["Write a tool definition in either language without mixing the shapes","Say where annotations go in each language"],"assessmentPrompt":"In Python, where does the handler live in a @tool definition?","standards":["ccaf:d2"],"sources":[{"label":"Notes · tool() has a different shape","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.116,"layer":2,"x":540,"y":418,"knowledgePoints":[{"id":"kp_toolshape_1","name":"Function in TypeScript, decorator in Python","keyPrerequisites":["mt_inprocess_mcp"],"teach":"<p><code>tool()</code> has a different shape in each language, and the docs are easy to skim past:</p><table class='kp-table'><tr><th></th><th>TypeScript</th><th>Python</th></tr><tr><td>Form</td><td>A function returning a definition</td><td>A <strong>decorator</strong> on an async function</td></tr><tr><td>Signature</td><td><code>tool(name, description, inputSchema, handler, extras?)</code></td><td><code>@tool(name, description, input_schema, annotations?)</code></td></tr><tr><td>Schema</td><td>Zod (3 or 4)</td><td>Type map <code>{\"a\": float}</code> or full JSON Schema</td></tr><tr><td>Handler</td><td>The 4th argument</td><td>The decorated function itself</td></tr><tr><td>Annotations</td><td>Inside <code>extras.annotations</code></td><td>The 4th positional argument</td></tr></table><p>Both return <code>{\"content\": [{\"type\": \"text\", \"text\": ...}]}</code> from the handler.</p>","questions":[{"id":"q_toolshape_1a","q":"You're porting a TypeScript tool definition, where the handler function is passed as the fourth argument to tool(), into an equivalent Python @tool-decorated tool for the same agent. In the Python version, where does the handler function actually live?","options":["As the fourth positional argument to the @tool decorator, mirroring the TypeScript signature exactly.","Registered separately afterward with a register_handler() call tied to the tool's name.","Inside the input_schema dictionary, alongside the field definitions for each parameter.","It is the async function that @tool decorates — there's no separate handler argument to pass."],"correct":3,"explain":"Python's @tool is a decorator, so the function it decorates is the handler itself — there's nothing to pass positionally the way TypeScript's tool() takes a handler as its fourth argument. Treating the decorator's fourth argument as a handler slot confuses the two languages' shapes; in Python that slot is annotations, not a handler. There's no register_handler() step, and the input_schema dictionary only describes parameters, not behavior."},{"id":"q_toolshape_1b","q":"A tool definition needs an idempotentHint annotation. In the TypeScript version it goes inside extras.annotations, the fifth argument to tool(). Where does the equivalent annotation go in the Python @tool version?","options":["As the fourth positional argument to @tool, the same slot TypeScript reserves for the handler function.","Inside extras.annotations, mirroring TypeScript exactly, since annotations belong to the MCP protocol rather than to either language binding.","Inside the input_schema type map, listed alongside the declared parameter type for each field.","There's no equivalent at all — Python's @tool provides no way to set annotations at definition time."],"correct":0,"explain":"Python's @tool takes annotations as its fourth positional argument — the same slot where TypeScript's tool() puts the handler function, which is exactly the kind of mismatch worth memorizing deliberately. Annotations aren't shared protocol-level state that lives in one universal location; each language binding places them differently. They don't belong in input_schema, which only describes parameters. And Python does support setting them — just not through an extras object."}]},{"id":"kp_toolshape_2","name":"The TypeScript-only extras","keyPrerequisites":[],"teach":"<p>TypeScript's <code>extras</code> carries two things Python has no equivalent for:</p><ul><li><code>searchHint</code> — a one-line capability phrase shown in the deferred-tool list when tool search is active. It is how a tool advertises itself before its full schema is loaded.</li><li><code>alwaysLoad: true</code> — keep this tool's full schema in the initial prompt instead of deferring it. For the two or three tools an agent needs constantly.</li></ul><p>Both exist because a large tool surface is expensive to describe up front, so schemas get deferred until needed.</p>","questions":[{"id":"q_toolshape_2a","q":"Your agent has dozens of MCP tools registered, and most of their full schemas are deferred out of the initial prompt to save context. When the model is deciding whether a particular deferred tool is worth loading in full, what does the TypeScript-only searchHint extra actually provide it?","options":["A ranking signal the SDK uses internally to sort the deferred-tool list before it's ever shown to the model at all.","A rewritten version of the search query the model is expected to send whenever it wants to reach for that tool.","A one-line capability phrase, letting the tool advertise itself before its full schema is loaded.","Internal documentation meant for your own team, stripped out before the tool list ever reaches the model."],"correct":2,"explain":"searchHint is what the model sees for a deferred tool before it pays the cost of loading that tool's full schema — a short phrase describing what the tool can do, so the model can decide whether it's worth loading. It isn't an internal sorting signal, and it doesn't rewrite the model's search queries; it's the thing being searched over. It also isn't team-only documentation — it's shown directly to the model as part of tool selection."},{"id":"q_toolshape_2b","q":"Your agent calls a particular tool on nearly every turn, and you've noticed the model occasionally has to issue an extra tool-search step just to load that tool's schema before it can be used. Which TypeScript-only extra should you set on that tool's definition to avoid the extra step?","options":["searchHint, so the model finds the tool faster the next time it searches for it.","annotations.idempotentHint, so the model trusts repeated calls enough to skip re-checking the schema.","There is no such option — every deferred tool must be searched for before its first use each session.","alwaysLoad: true, so the tool's full schema stays in the initial prompt instead of being deferred."],"correct":3,"explain":"alwaysLoad: true keeps a tool's schema in the initial prompt rather than deferring it, which is exactly what removes the extra search step for a tool used on nearly every turn — worth doing for the handful of tools an agent needs constantly, since it's context spent every turn. searchHint only helps a deferred tool get found faster, it doesn't remove the deferral itself. idempotentHint is unrelated; it's a behavioral hint about retries, not about loading. And the option does exist — it's just meant to be used sparingly."}]}],"reviewQuestions":[{"id":"r_toolshape_1","q":"You're writing an integration test that calls a tool handler directly, once for a TypeScript tool() definition and once for its Python @tool equivalent, and asserting on the shape of the return value. What shape should both handlers return, regardless of language?","options":["{ result: ..., isError: false, meta } — a result envelope with a boolean status flag and metadata.","{ content: [{ type: \"text\", text }] } — the MCP content-block shape, the same in both languages.","A Zod-validated plain object matching the tool's declared output schema.","A plain string containing the output text, with no surrounding structure."],"correct":1,"explain":"Both TypeScript and Python handlers return the same MCP content-block shape — a content array of typed blocks, most commonly a single text block — because that shape is defined by the protocol, not the language binding. isError is a real field but it sits alongside this shape on the result rather than replacing it. Zod validation only applies to TypeScript's input schema, not the return value. And a bare string would drop the structure MCP clients expect to parse."}]},{"id":"mt_tool_annotations","type":"FACTUAL","cluster":"cl_tools","name":"ToolAnnotations are hints, not controls","description":"title, readOnlyHint (default false), destructiveHint (default true), idempotentHint (default false), openWorldHint (default true). They are behavioural hints — clients must not rely on them for security decisions. Real restriction comes from tool sets, disallowedTools, and hooks.","core":false,"estMinutes":5,"evidence":["Reject readOnlyHint as a safety mechanism","Recall the two annotations that default to true"],"assessmentPrompt":"Is `readOnlyHint: true` enough to guarantee a tool cannot modify anything?","standards":["ccaf:d2"],"sources":[{"label":"Notes · ToolAnnotations","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":3,"x":750,"y":480,"knowledgePoints":[{"id":"kp_annot_1","name":"Hints, and their surprising defaults","keyPrerequisites":["mt_tool_shape"],"teach":"<p><code>ToolAnnotations</code> are behavioural <strong>hints</strong>, not security controls. Clients should not rely on them for security decisions.</p><ul><li><code>title</code></li><li><code>readOnlyHint</code> — default <strong>false</strong></li><li><code>destructiveHint</code> — default <strong>true</strong></li><li><code>idempotentHint</code> — default <strong>false</strong></li><li><code>openWorldHint</code> — default <strong>true</strong></li></ul><p>The defaults are deliberately pessimistic: an undeclared tool is assumed to be destructive and to touch the outside world. Two of the five default to <code>true</code>, and both of those are the cautious answer.</p>","questions":[{"id":"q_annot_1a","q":"A tool you're integrating declares readOnlyHint: true in its ToolAnnotations, and a teammate wants to skip writing a permission rule for it since the tool \"already says it's read-only.\" Is that annotation alone sufficient to guarantee the tool cannot modify anything?","options":["No — it's a hint, not a boundary, so restriction still has to come from tool sets, disallowedTools, or hooks.","Yes — the SDK enforces every declared ToolAnnotations hint automatically at the moment a tool call is attempted.","Yes, but only for tools registered through an in-process SDK MCP server rather than an external stdio or HTTP one.","It depends on whether the agent is currently running in plan mode versus its normal, unrestricted execution mode."],"correct":0,"explain":"ToolAnnotations are explicitly hints, not enforced guarantees — clients aren't supposed to make security decisions based on them, so readOnlyHint: true is information for the model, not a constraint the SDK checks before letting a call through. The SDK doesn't enforce declared hints automatically. Nothing about in-process versus external MCP servers changes that. And plan mode restricts what the agent attempts, but it doesn't retroactively make an annotation load-bearing."},{"id":"q_annot_1b","q":"You're auditing a tool that declares no ToolAnnotations at all, relying entirely on the SDK's defaults. Which two hints does that tool implicitly carry as true, meaning it will be treated as capable of causing damage and of reaching outside your system?","options":["readOnlyHint and idempotentHint, since an undeclared tool defaults to being treated as safe to call repeatedly without side effects.","destructiveHint and openWorldHint — undeclared, a tool is assumed destructive and reaching outside your system.","destructiveHint and idempotentHint, since an undeclared tool defaults to being treated as unsafe to retry automatically.","readOnlyHint and openWorldHint, since an undeclared tool defaults to being treated as safe internally but reaching externally."],"correct":1,"explain":"The defaults are deliberately pessimistic: destructiveHint defaults to true and openWorldHint defaults to true, so a tool with no declared annotations is treated as if it might break things and as if it reaches outside your system, until told otherwise. readOnlyHint and idempotentHint both default to false, the opposite of being assumed safe. Mixing destructiveHint with idempotentHint, or readOnlyHint with openWorldHint, pairs one true default with one false one, which isn't how the actual defaults land."}]}],"reviewQuestions":[{"id":"r_annot_1","q":"A tool in your agent declares destructiveHint: true, and you want to make sure it genuinely cannot run in your staging environment rather than just being flagged as risky to the model. Since ToolAnnotations are hints rather than enforced controls, where should the actual restriction come from?","options":["From the tool's own description and input schema, written strictly enough that the model won't choose to call it.","From the model's own judgment at call time, informed by the destructiveHint annotation.","From tool sets, disallowedTools, or hooks — the mechanisms that actually withhold, block, or gate a tool's availability.","From the outputFormat declared on the tool, which can be set to reject destructive calls."],"correct":2,"explain":"Real restriction has to come from mechanisms that actually control availability or interception — tool sets that withhold the tool, disallowedTools that blocks it outright, or hooks that gate the call — since annotations are only advisory. A strict description can discourage a call but can't prevent one the model still decides to make. Relying on the model's judgment is exactly the probabilistic approach annotations already represent, not a fix for it. outputFormat governs response shape, not whether a call is permitted."}]},{"id":"mt_mcp_wiring","type":"PROCEDURAL","cluster":"cl_tools","name":"Wiring MCP servers into a query","description":"mcpServers takes a record of configs in four shapes: stdio, SSE, HTTP, and in-process SDK. strictMcpConfig: true uses only what you passed, ignoring project .mcp.json, user settings, plugin servers and claude.ai connectors — reach for it when you need a reproducible tool surface. toolAliases maps a built-in name onto an MCP tool. AgentDefinition.mcpServers scopes servers per subagent.","core":false,"estMinutes":7,"evidence":["Guarantee that production runs see exactly the tools you shipped","Redirect a built-in tool name to your own implementation"],"assessmentPrompt":"Your staging agent picks up a developer's personal MCP server. Which option stops that?","standards":["ccaf:d2","ccaf:d4"],"sources":[{"label":"Notes · Wiring servers in","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":2,"x":540,"y":480,"knowledgePoints":[{"id":"kp_wiring_1","name":"Four shapes, and reproducibility","keyPrerequisites":["mt_inprocess_mcp"],"teach":"<ul><li><code>mcpServers</code> — a record of configs, in four shapes: <strong>stdio, SSE, HTTP, and the in-process SDK server</strong>.</li><li><code>strictMcpConfig: true</code> — use <em>only</em> the servers passed in <code>mcpServers</code>, ignoring project <code>.mcp.json</code>, user settings, plugin servers, and claude.ai connectors. Reach for this when you need a reproducible tool surface in production.</li><li><code>toolAliases</code> — map a built-in tool name onto an MCP tool so Claude calls yours instead, e.g. <code>{ Bash: 'mcp__workspace__bash' }</code>.</li><li><code>AgentDefinition.mcpServers</code> — servers scoped to a single subagent.</li></ul>","questions":[{"id":"q_wiring_1a","q":"Your staging agent is configured with its own mcpServers entry, but during a run it also picks up a developer's personal MCP server that was never declared anywhere in the staging config — it's coming in through that developer's local project .mcp.json. Which option, set on the agent, stops any server outside the declared mcpServers from being loaded?","options":["settingSources: [], which controls which settings files get read but leaves .mcp.json server discovery running independently of it.","toolAliases on the options, which remaps specific tool names onto others rather than restricting which servers get loaded at all.","disallowedTools: [\"mcp__*\"], which blocks calls to MCP tools after they've already loaded rather than preventing the server from loading.","strictMcpConfig: true, restricting the agent to only the servers in mcpServers and ignoring every other source."],"correct":3,"explain":"strictMcpConfig: true is the option that shuts out every other server source — project .mcp.json, user settings, plugin servers, and claude.ai connectors — leaving only the servers explicitly passed in mcpServers, which is exactly the reproducible surface staging needs. settingSources governs which settings files are read, not MCP server discovery specifically. toolAliases remaps a tool name onto another tool, it doesn't restrict which servers load. And blocking mcp__* with disallowedTools would hide the tools after the fact rather than stopping the unwanted server from being picked up at all."},{"id":"q_wiring_1b","q":"You want every call the model makes to the built-in Bash tool to actually run through your own sandboxed mcp__workspace__bash implementation instead, without changing any prompts or telling the model to call a differently named tool. Which mechanism lets you redirect calls this way?","options":["toolAliases, mapping Bash onto mcp__workspace__bash so the model's ordinary calls redirect to your implementation.","Renaming your MCP tool to literally be called \"Bash\" when you register the server, so the built-in name collides with it and the model resolves to whichever one loads last.","Adding a shorthand entry for it inside allowedTools that points the built-in tool name at the sandboxed implementation instead of its default handler.","Aliasing the mcp__workspace server itself onto the name of a different, unrelated server so its tools inherit that server's routing."],"correct":0,"explain":"toolAliases is built for exactly this — mapping a built-in tool name like Bash onto an MCP tool name like mcp__workspace__bash, so the model's ordinary call to Bash is transparently redirected to your sandboxed implementation with no prompt changes needed. Renaming your MCP tool to literally collide with the built-in name isn't how the mapping works and risks other conflicts. allowedTools controls which tools are available, not how one name routes to another. And aliasing one server onto another server doesn't touch individual tool routing at all."}]}],"reviewQuestions":[{"id":"r_wiring_1","q":"You're documenting the mcpServers option for your team and need to list every transport shape it supports, so a new engineer knows what's possible before choosing one for a new integration. What are the four shapes?","options":["stdio, WebSocket, gRPC, and in-process — WebSocket and gRPC as lower-latency alternatives to HTTP.","stdio, SSE, HTTP, and the in-process SDK server, the last existing only inside the SDK itself.","local, remote, hosted, and embedded, categorized by where the server process physically runs on your infrastructure.","stdio and HTTP only — SSE and in-process servers are just handled as variations of the plain HTTP shape."],"correct":1,"explain":"The four shapes mcpServers actually supports are stdio, SSE, HTTP, and the in-process SDK server — the first three are ordinary MCP transports usable outside the SDK too, while the in-process shape is unique to it, with no subprocess or transport layer at all. WebSocket and gRPC aren't among the supported shapes. Categorizing by physical location conflates several of the real shapes rather than naming them. And SSE and in-process are distinct entries, not variations folded into HTTP."}]},{"id":"mt_tool_descriptions","type":"CONCEPTUAL","cluster":"cl_tools","name":"Tool description design","description":"The description is the primary mechanism the model uses to select a tool. When an agent confuses two similar tools, the highest-leverage fix is expanding each description with input formats, example queries, edge cases and boundary explanations — not adding few-shot examples and not merging the tools.","core":true,"estMinutes":8,"evidence":["Rewrite a thin tool description into a selecting one","Reject merging or few-shot as the fix for tool confusion"],"assessmentPrompt":"An agent keeps calling `search_orders` when it should call `search_customers`. What is the highest-leverage fix?","standards":["ccaf:d2","ccaf-glossary:t14"],"sources":[{"label":"Notes · Tool description design","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0,"layer":3,"x":750,"y":418,"knowledgePoints":[{"id":"kp_tooldesc_1","name":"The description is the selection mechanism","keyPrerequisites":["mt_tool_shape"],"teach":"<p class='kp-key'>The tool description is the primary mechanism the model uses to select a tool.</p><p>So when an agent confuses two similar tools, the highest-leverage fix is <strong>expanding each description</strong> — input formats, example queries, edge cases, and explicit boundary statements about when <em>not</em> to use it. Not few-shot examples in the system prompt. Not merging the two tools into one.</p><p>A description doing its job:</p><pre class='kp-code'>\"Look up an order by ID. Accepts a 12-character alphanumeric order ID\n (e.g. NS4417XQ2210). Returns status, line items, and refund eligibility.\n Use get_customer first if you only have an email address.\"</pre><p>Note the last sentence. Half of good description design is telling the model what the <em>other</em> tool is for.</p>","questions":[{"id":"q_tooldesc_1a","q":"Your support agent frequently calls search_orders when the customer's request should route to search_customers instead, and this happens often enough that customers are getting responses about the wrong entity. A teammate suggests adding several few-shot examples to the system prompt showing the correct tool for each case. What's the highest-leverage fix instead?","options":["Add the few-shot examples to the system prompt as proposed, since that's the standard way to correct a recurring selection mistake.","Merge search_orders and search_customers into a single tool that infers which entity the user meant.","Expand both tools' descriptions with input formats, example queries, and explicit statements of when each tool should and shouldn't be used.","Remove search_orders from allowedTools entirely so the agent has no choice but to call search_customers."],"correct":2,"explain":"The tool description is the primary mechanism the model uses to select between tools, so the highest-leverage fix is expanding both descriptions — formats, example queries, and explicit boundaries about when not to use each one — directly addressing the thing driving selection. Few-shot examples in the system prompt can help but don't fix the root cause the way clearer descriptions do. Merging the tools removes the distinction the agent needs to make rather than teaching it to make that distinction correctly. And removing search_orders from allowedTools breaks every legitimate case where it was the right tool."},{"id":"q_tooldesc_1b","q":"A tool description for lookup_order ends with the line: \"Use get_customer first if you only have an email address.\" The rest of the description already explains the input format and what the tool returns. What does that final sentence add that the rest of the description doesn't?","options":["It documents the API for other engineers who might read the tool's source code later on.","It shortens the JSON schema the model otherwise has to parse before it can make a call.","It satisfies an MCP requirement that every description cross-reference at least one other tool.","It tells the model when not to reach for this tool, ruling out a neighbor the rest wouldn't."],"correct":3,"explain":"A description that only explains what a tool does still competes for selection against other tools that could plausibly fit the same request — the boundary line is what actually disambiguates, by telling the model when to reach for a different tool instead. It isn't aimed at engineers reading source later; it's read by the model at call time. It doesn't affect schema size, which is a separate part of the tool definition. And MCP doesn't require cross-references in descriptions — this is a design choice, not a protocol rule."}]}],"reviewQuestions":[{"id":"r_tooldesc_1","q":"You're rewriting a tool description specifically to help the model stop confusing it with a similar tool it's routinely misused instead of. Besides what the tool returns, which four kinds of detail should the description add to actually resolve that confusion?","options":["Input formats, example queries, edge cases, and explicit boundaries about when not to use it.","Author, version number, changelog entries, and the license the tool happens to be released under.","Rate limits, expected latency, per-call cost, and the region the backing service physically runs in.","A JSON schema for the inputs, and essentially nothing else beyond what that schema already covers."],"correct":0,"explain":"Resolving confusion between similar tools takes formats, example queries, edge cases, and explicit boundaries about when the tool doesn't apply — the schema already says what's accepted, so the description has to carry when to reach for it. Authorship and versioning metadata doesn't help the model choose between tools at call time. Operational details like rate limits and cost matter for other reasons but don't disambiguate selection. And a schema alone is exactly the baseline that wasn't enough to prevent the confusion in the first place."}]},{"id":"mt_is_error","type":"FACTUAL","cluster":"cl_tools","name":"isError — the one real field","description":"isError is an actual MCP protocol field on a tool result. It is how a tool signals failure back to the calling agent, as opposed to returning a success payload whose text happens to describe a failure.","core":true,"estMinutes":5,"evidence":["Identify isError as protocol, not convention","Explain why a failure returned as ordinary text misleads the agent"],"assessmentPrompt":"Which of isError, errorCategory and isRetryable is defined by MCP?","standards":["ccaf:d2","ccaf-glossary:t15"],"sources":[{"label":"Lesson · Three fields, one of which is real","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene8"}],"centrality":0.047,"layer":3,"x":750,"y":356,"knowledgePoints":[{"id":"kp_iserror_1","name":"The one field that is actually in the protocol","keyPrerequisites":["mt_tool_shape"],"teach":"<p><code>isError</code> is a <strong>real MCP field</strong>. It is how a tool result signals failure back to the calling agent.</p><p>The alternative — returning a success payload whose text happens to say \"could not find that order\" — is worse than it looks. The agent has to infer failure from prose, which it will sometimes do wrong, and any code you write around the result cannot branch on it at all.</p><p>Exam glossary <strong>t15</strong>. When the question asks which of the three named error fields belongs to the protocol, this is the one.</p>","questions":[{"id":"q_iserror_1a","q":"You're reviewing a tool's error-handling code and see it setting isError, errorCategory, and isRetryable on every failed result. A teammate asks which of these three fields the MCP protocol itself actually defines, versus which ones are just naming conventions your team invented. What's the answer?","options":["All three are defined by the MCP protocol, since they all describe aspects of a failed tool call.","Only isError is defined by MCP; errorCategory and isRetryable are conventions you define yourself in your own error payload.","None of the three are defined by MCP — all three are conventions various teams have adopted independently.","isError and isRetryable are defined by MCP; errorCategory is the only one that's a convention."],"correct":1,"explain":"isError is the one real MCP field — it's how a tool result signals failure back to the calling agent. errorCategory and isRetryable are both design conventions you define yourself in your own error payload; nothing in the MCP or SDK types requires or defines them. Claiming all three or none are protocol fields gets the actual split wrong in opposite directions. And pairing isRetryable with isError as protocol fields, while singling out errorCategory as the convention, still misclassifies isRetryable."},{"id":"q_iserror_1b","q":"A tool currently reports failures by returning a normal success result whose text field just happens to say \"could not find that order.\" Your code that processes the result, and the agent reasoning about it, both have to treat this the same as any other successful call. What's the concrete problem with this approach compared to setting isError: true?","options":["It costs noticeably more tokens per call than an equivalent isError-flagged result would, since prose is verbose.","MCP itself rejects any result that returns a success status alongside text that happens to describe a failure.","The agent has to infer failure from prose, and downstream code has no field to branch on at all.","It silently breaks prompt caching for every subsequent turn in the rest of the conversation."],"correct":2,"explain":"Both consumers lose when failure is buried in prose: the agent has to infer failure from text it might read as an ordinary answer, and code processing the result has no field to branch on since the result is marked successful. MCP doesn't reject this shape — it's syntactically valid, just semantically misleading. It isn't a meaningful token-cost difference driving the problem. And it has no particular relationship to prompt caching, which depends on message structure, not on how a single result signals failure."}]}],"reviewQuestions":[{"id":"r_iserror_1","q":"You're implementing a tool handler and need to decide what isError is actually for, as opposed to the SDK's own retry logic or a tool's destructiveHint annotation. What is its actual purpose?","options":["Telling the SDK's internal call logic to automatically retry the failed call.","Marking the tool itself as destructive so the model treats future calls more cautiously.","Recording the failure to your team's logging or observability system.","Signalling the failure of a specific call result back to the calling agent."],"correct":3,"explain":"isError signals failure on that specific result back to the agent — it's how the agent learns the call didn't succeed. It has nothing to do with retry behavior; whether to retry is a separate, author-defined isRetryable convention, not something isError triggers. It's also unrelated to destructiveHint, which describes the tool's general behavior rather than one call's outcome. And logging is something your own code does around the result — isError itself is just the protocol-level signal."}]},{"id":"mt_error_conventions","type":"CONCEPTUAL","cluster":"cl_tools","name":"errorCategory and isRetryable are your conventions","description":"Neither is an SDK or MCP field. errorCategory is a design convention — you classify failures (transient / validation / business / permission) in your own payload so the agent picks the right recovery. isRetryable is your boolean saying whether the operation can safely be retried; isRetryable: false on a business error means explain the rule, do not retry. The exam names both as concepts, so learn them as vocabulary.","core":true,"estMinutes":8,"evidence":["Design an error payload an agent can act on","Say which of the three named fields you will not find in the SDK types"],"assessmentPrompt":"A refund is denied by policy. What should isRetryable be, and what should the agent do?","standards":["ccaf:d2","ccaf-glossary:t16","ccaf-glossary:t17"],"sources":[{"label":"Lesson · Three fields, one of which is real","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene8"}],"centrality":0.023,"layer":4,"x":960,"y":418,"knowledgePoints":[{"id":"kp_errconv_1","name":"errorCategory and isRetryable are yours","keyPrerequisites":["mt_is_error"],"teach":"<p>Read this carefully, because the exam names three things and only one is a protocol field.</p><ul><li><strong><code>errorCategory</code></strong> — <em>not</em> an SDK or MCP field. It is a <strong>design convention</strong>: you classify failures in your own error payload (transient / validation / business / permission) so the agent can pick the right recovery instead of guessing from prose. (Glossary t16.)</li><li><strong><code>isRetryable</code></strong> — likewise <strong>your own field</strong>, a boolean saying whether the operation can safely be retried. <code>isRetryable: false</code> on a business error means <em>don't retry — explain the business-rule violation instead.</em> (t17.)</li></ul><p>Learn them as vocabulary; just don't go looking for them in the SDK types.</p>","questions":[{"id":"q_errconv_1a","q":"A process_refund call fails because the refund amount exceeds a policy-defined limit — not because of a transient network issue or a malformed request. You're setting isRetryable on the error payload and deciding what the agent should do next. What should isRetryable be, and what should the agent's next move look like?","options":["isRetryable: false — the limit won't change on a second try, so explain the rule instead of retrying.","isRetryable: true — retry the call automatically after silently substituting in a smaller refund amount on the customer's behalf.","isRetryable: true — treat the denial as though the policy engine were experiencing a transient blip worth retrying past.","Leave isRetryable unset entirely and let the SDK infer the correct retry behavior from the error message's text."],"correct":0,"explain":"A business-rule violation like a policy limit is stable across attempts — retrying burns turns and looks broken to the customer without changing the outcome, so isRetryable: false paired with an explanation of the rule is the right response. Substituting a smaller amount changes what the customer asked for without their input, which isn't the same as retrying the same request. Treating a policy denial as a transient blip misclassifies a business error as a transient one. And there's no SDK inference step — isRetryable is your own field, so leaving it unset just leaves the signal missing."},{"id":"q_errconv_1b","q":"A new engineer on your team is browsing the SDK's type definitions looking for where errorCategory is declared, expecting to find it alongside fields like isError. Where will they actually find it?","options":["Defined on the ToolResult type, next to the isError field it's meant to complement.","Nowhere in the SDK type definitions — errorCategory is a convention your own code defines in its error payload.","Defined on the HookJSONOutput type union, since categorization happens inside a hook.","Defined on the PermissionResult type union, alongside the fields that govern tool permissions."],"correct":1,"explain":"errorCategory doesn't appear in the SDK's type definitions at all — it's exam vocabulary for a pattern you implement yourself, not a field the SDK or MCP protocol declares. It isn't on ToolResult next to isError, since isError is the one field that actually is real. It has no connection to HookJSONOutput, which describes hook outputs, not error payloads. And it isn't part of PermissionResult either, which governs permission decisions rather than error classification."}]},{"id":"kp_errconv_2","name":"The four categories, used","keyPrerequisites":[],"teach":"<p>The point of a category is that each one implies a <em>different</em> recovery:</p><ul><li><strong>transient</strong> — retry, probably with backoff.</li><li><strong>validation</strong> — fix the arguments and call again.</li><li><strong>business</strong> — do not retry; explain the rule to the user.</li><li><strong>permission</strong> — do not retry; escalate or ask for authorisation.</li></ul><p>Without the category, all four look the same to the model: a string that says something went wrong. With it, the agent's next move is determined rather than guessed.</p>","questions":[{"id":"q_errconv_2a","q":"A tool call to search_orders fails because the date range argument was formatted incorrectly, not because anything about the underlying system is unavailable or forbidden. Which errorCategory value best captures this failure, given that the implied recovery is simply to fix the argument and call again?","options":["transient, since retrying the identical call again is likely to succeed once the underlying issue clears on its own.","business, since the failure reflects a rule about which well-formed requests are actually allowed to succeed.","validation, since the call was malformed and fixing the arguments before calling again resolves it.","permission, since the caller apparently needs additional authorization before the request can proceed at all."],"correct":2,"explain":"validation is the category for a malformed call — the request itself was wrong, and the recovery is to fix the arguments and call again, which is a different move from retrying unchanged or escalating for authorization. transient implies the same call would succeed later without changes, which isn't true of a malformed date range. business implies a rule was violated even though the request was well-formed, which doesn't match a formatting error. And permission implies an authorization problem, not an argument problem."},{"id":"q_errconv_2b","q":"Your team is debating whether it's worth the extra implementation effort to add errorCategory to every tool's error payload, given that isError already tells the agent a call failed. What's the actual argument in favor of adding the category?","options":["It makes your team's log output noticeably easier for engineers to scan when triaging a batch of failures.","MCP requires every error result to carry a category field for the tool response to be considered spec-compliant.","It meaningfully reduces the token usage of each failed tool call by shortening the error text.","Each category implies a different recovery, so without it every failure looks the same to the model."],"correct":3,"explain":"Without a category, transient, validation, business, and permission failures all collapse into one undifferentiated 'something went wrong' string, giving the model the same reaction to four situations that actually call for different next moves — retrying, fixing arguments, explaining a rule, or escalating. Readability of logs is a side benefit at best, not the actual argument. MCP doesn't require a category field at all, since it isn't a protocol field to begin with. And categorization doesn't meaningfully change token usage either way."}]}],"reviewQuestions":[{"id":"r_errconv_1","q":"Your team is writing an internal style guide that lists isError, errorCategory, and isRetryable together as \"error fields.\" Someone points out that grouping them like that is misleading. Which one of the three is the only one actually defined by the MCP protocol itself?","options":["isError — the other two are conventions your own code defines, not protocol fields.","errorCategory — it classifies failures in a way the other two fields depend on.","isRetryable — it determines whether the SDK will attempt an automatic retry.","All three are defined by MCP, since they all commonly appear together on error results."],"correct":0,"explain":"isError is the one field the MCP protocol actually defines; errorCategory and isRetryable are both conventions you invent in your own error payload, which is exactly why grouping all three as equivalent 'error fields' is misleading. errorCategory doesn't have some special protocol status the other two depend on. isRetryable doesn't trigger SDK-level automatic retries — that logic, if it exists, is something you write yourself. And the fact that all three commonly appear together doesn't mean all three are protocol-defined."}]},{"id":"mt_error_propagation","type":"PROCEDURAL","cluster":"cl_tools","name":"Propagating subagent failure to the coordinator","description":"When a subagent times out, the best result for coordinator recovery is a structured error naming what failed and how far it got — not an empty result marked successful, and not a generic \"unavailable\" string after silent retries. A coordinator can only make an intelligent recovery decision from information it actually receives.","core":true,"estMinutes":7,"evidence":["Rank four failure payloads by how well a coordinator can recover from each","Explain why silent retries make the coordinator worse, not better"],"assessmentPrompt":"A subagent times out halfway through. What should the coordinator receive?","standards":["ccaf:d1","ccaf:d2","ccaf:d5"],"sources":[{"label":"Lesson · Three fields, one of which is real","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene8"}],"centrality":0,"layer":6,"x":1380,"y":108,"knowledgePoints":[{"id":"kp_errprop_1","name":"What a coordinator can actually recover from","keyPrerequisites":["mt_error_conventions","mt_subagent_return"],"teach":"<p>A subagent times out halfway through. Four things it could hand back:</p><ol><li>An empty result marked successful. <em>Worst.</em> The coordinator now believes a sub-topic was covered and was found to contain nothing.</li><li>A generic \"unavailable\" string after silent retries. The coordinator learns that something failed, and nothing else.</li><li>Nothing at all. The coordinator hangs or times out itself.</li><li><strong>A structured error naming what failed and how far it got.</strong> <em>Best.</em></li></ol><p class='kp-key'>A coordinator can only make an intelligent recovery decision from information it actually receives.</p><p>And recall why this is sharp with subagents specifically: only the final message crosses back. Whatever partial progress the subagent made is invisible unless the final message says so.</p>","questions":[{"id":"q_errprop_1a","q":"Your research subagent times out while it's two-thirds through gathering sources for a report, having already found several usable citations before the timeout hit. You're designing what the subagent's final message hands back to the coordinator. What should it contain?","options":["An empty result marked as successful, so the coordinator continues the workflow without special-casing the failure.","A structured error naming what failed, how far the subagent got, and the citations it had already found before timing out.","A generic \"unavailable\" status returned only after the subagent has silently retried the search several times.","Nothing at all — let the coordinator's own timeout handling detect that the subagent never returned."],"correct":1,"explain":"A structured error that names the failure and reports how far the subagent got — including the citations already found — is the only version of this that gives the coordinator enough information to decide whether to retry, proceed with the partial results, or try a different approach. Marking an empty result as successful is worse than any error, since the coordinator will believe the topic was covered when it wasn't. A generic status after silent retries hides both the partial progress and the specific failure. And returning nothing forces the coordinator to rely on its own timeout, discarding everything the subagent found."},{"id":"q_errprop_1b","q":"Of the four ways a timed-out subagent could report back, an empty result marked as successful is considered the most dangerous, worse than even returning nothing. Why is that specific option worse than the others?","options":["It wastes noticeably more tokens than any of the other three reporting options would across the run.","It causes the SDK to retry the subagent call indefinitely, over and over, until it eventually succeeds.","It turns a failure into what looks like a legitimate finding, so nothing downstream questions it.","It breaks the context isolation boundary that normally separates the subagent from the coordinator."],"correct":2,"explain":"Marking an empty result successful converts a failure into something that reads as a real finding — the coordinator has no reason to question it, so it proceeds as if the sub-topic was covered and found to contain nothing, which is worse than an obvious error it would at least investigate. This isn't about token cost, which doesn't distinguish the four options in any meaningful way. It has no effect on the SDK's retry behavior, since nothing here triggers automatic retries. And it doesn't touch context isolation at all — the two agents' contexts stay separate regardless of what the final message says."}]}],"reviewQuestions":[{"id":"r_errprop_1","q":"A subagent is implemented to retry a failing search internally, without reporting anything to the coordinator until either the search succeeds or all retries are exhausted. Once retries are exhausted, why does this design specifically hurt the coordinator's ability to recover well?","options":["Silent retries are inherently slower overall than just failing on the very first attempt would be.","Silent retries violate a specific requirement in the MCP spec about how failures must be reported.","Silent retries break the session boundary that the subagent is otherwise running inside of.","Silent retries hide the specific failure and any partial progress made along the way."],"correct":3,"explain":"Retrying isn't the problem — retrying invisibly is, because by the time the coordinator hears anything, it only gets a final generic failure with no visibility into what was tried, what partially worked, or why it failed, which is exactly the information an intelligent recovery decision needs. Speed isn't the issue being described here. MCP has no specification requirement being violated by internal retry logic. And none of this touches the subagent's session boundary, which is a separate concern from what gets reported in the final message."}]},{"id":"mt_session_continuity","type":"PROCEDURAL","cluster":"cl_sessions","name":"Resuming and continuing sessions","description":"resume: \"<id>\" resumes a specific session; continue / continue_conversation picks up the most recent; resumeSessionAt resumes at a message UUID; sessionId supplies your own; persistSession: false keeps it off disk and unresumable. Utilities: listSessions, getSessionMessages, getSessionInfo, renameSession, tagSession, plus sessionStore for mirroring transcripts to an external backend.","core":true,"estMinutes":8,"evidence":["Resume a specific named prior conversation","Say what persistSession: false costs you"],"assessmentPrompt":"You want to continue one specific prior conversation, not the most recent one. Which option?","standards":["ccaf:d5","ccaf-glossary:t8"],"sources":[{"label":"Lesson · Resume, or branch","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene9"}],"centrality":0.023,"layer":2,"x":540,"y":542,"knowledgePoints":[{"id":"kp_sess_1","name":"The continuity options","keyPrerequisites":["mt_entry_points"],"teach":"<table class='kp-table'><tr><th>Option</th><th>Effect</th></tr><tr><td><code>resume: \"&lt;id&gt;\"</code></td><td>Resume a specific session by ID</td></tr><tr><td><code>continue</code> / <code>continue_conversation</code></td><td>Continue the most recent conversation</td></tr><tr><td><code>resumeSessionAt</code></td><td>Resume at a specific message UUID</td></tr><tr><td><code>sessionId</code></td><td>Use a specific UUID instead of auto-generating one</td></tr><tr><td><code>persistSession: false</code></td><td>Don't write to disk; the session cannot be resumed later</td></tr></table><p>Glossary <strong>t8</strong> is the plain case: <code>--resume &lt;session-name&gt;</code> continues a specific named prior conversation — as distinct from <code>continue</code>, which just grabs the latest.</p>","questions":[{"id":"q_sess_1a","q":"A support engineer has three separate open conversations with your CLI-based agent, each about a different customer ticket. She wants her next command to continue specifically the ticket-142 conversation, not whichever conversation last received a message. Which option should the query pass?","options":["Set continue to true, which reopens whichever conversation last received a message, not necessarily ticket-142.","Set sessionId to a freshly generated UUID, which creates a brand-new session rather than reopening ticket-142.","Set resume to the ticket-142 conversation's session id, reopening that exact conversation no matter what ran last.","Set forkSession to true on the query with no resume value specified at all, which has no baseline session to branch from."],"correct":2,"explain":"Setting resume to ticket-142's own session id targets that exact conversation directly, independent of whatever ran most recently. Setting continue instead grabs whichever conversation last received a message, which may not be ticket-142 at all. Assigning a fresh sessionId creates a new, empty session rather than reopening an existing one. And forkSession only has meaning alongside resume — used alone it has no baseline session to branch from."},{"id":"q_sess_1b","q":"Your agent handles one-off runs over files containing customer PII, so you've set persistSession: false to avoid writing transcripts to disk for compliance reasons. A teammate later asks you to resume one of these runs to review its reasoning. What actually happens?","options":["The run cannot be resumed at all, because persistSession: false means no transcript was ever written to disk.","PostToolUse hooks stop firing entirely for the remainder of that particular run once this is set.","Nothing changes in practice at all; persistSession is purely a performance tuning setting with no other effect.","Tool results are still returned to the model but are withheld from the final response shown to the user."],"correct":0,"explain":"persistSession: false means the transcript for that run is never written to disk, so there is nothing on disk to resume from — the teammate's request simply cannot be fulfilled. Hooks like PostToolUse continue firing normally regardless of this setting; it doesn't touch hook execution. It is not a mere performance knob either — it permanently forecloses resuming or forking that session later. And it has no bearing on what tool results reach the model versus the user, which is an unrelated distinction."}]},{"id":"kp_sess_2","name":"Session utilities and external storage","keyPrerequisites":[],"teach":"<p>Around the options sit a set of utilities: <code>listSessions()</code>, <code>getSessionMessages()</code>, <code>getSessionInfo()</code>, <code>renameSession()</code>, <code>tagSession()</code>.</p><p>And <code>sessionStore</code> mirrors transcripts to an external backend, so <strong>any host can resume them</strong> — the difference between sessions that live on one machine's disk and sessions that live in your system.</p><p>That distinction matters the moment you have more than one worker process. Without a session store, \"resume session X\" only works if the request lands on the machine that created it.</p>","questions":[{"id":"q_sess_2a","q":"Your agent API runs on four stateless instances behind a round-robin load balancer. Customers report that resuming a previous conversation usually works, but roughly one in four attempts comes back as a brand-new, empty session instead. What's the most likely fix?","options":["Set persistSession: true on every one of the four instances so transcripts are written to disk.","Configure sticky sessions on the load balancer so that each customer is always routed to the exact same instance.","Set forkSession: true on every resume call so each instance creates its own independent branch.","Add a sessionStore that mirrors transcripts to a shared backend, so any instance can resume any session."],"correct":3,"explain":"A sessionStore mirrors transcripts to a backend all four instances share, so resume succeeds no matter which instance handles the request — the actual cause of the intermittent failures. Sticky routing only works around the symptom by pinning each customer to the instance that created their session, and breaks the moment that instance restarts or the balancer rebalances. Turning on persistSession changes nothing about cross-instance access; it only controls whether a transcript is written to disk at all, not where other instances can find it. And forkSession has nothing to do with retrieving an existing session across machines — it only branches a new id off an already-resumed baseline."},{"id":"q_sess_2b","q":"You're building a dashboard page that lists every past conversation a given user has had with the agent, showing titles and timestamps, before they click into any specific one to see its content. Which utility should populate that list?","options":["Call getSessionMessages() once per known session id to assemble each conversation's full message history.","Call listSessions() to retrieve the index of the user's sessions before any specific one is opened.","Call tagSession() on each conversation so the dashboard can group entries by label instead of by time.","Call renameSession() on the most recent conversations so their titles are current before they're listed."],"correct":1,"explain":"listSessions() returns the index of a user's sessions, which is exactly the lightweight list a dashboard needs before anyone opens a specific conversation. getSessionMessages() instead pulls the full transcript of one already-known session, which is far more than an index view requires and wasteful to call for every row. tagSession() only attaches metadata labels to sessions; it doesn't enumerate them. renameSession() changes a session's stored title, which doesn't help populate a list in the first place."}]}],"reviewQuestions":[{"id":"r_sess_1","q":"A teammate keeps passing continue when they want to reopen yesterday's conversation about a specific bug, but several other conversations have happened since then, so continue keeps landing them somewhere else. How would you explain the fix to them?","options":["continue only works from the CLI, while resume is only available when calling the SDK directly.","resume called with no session id specified at all always starts a brand-new, completely empty session.","continue reopens whichever conversation ran most recently; resume reopens the one you name by id specifically.","continue and resume are interchangeable aliases that both target the exact same underlying flag."],"correct":2,"explain":"continue always grabs whichever conversation ran most recently, which explains why it keeps landing your teammate somewhere other than yesterday's bug discussion; resume, given that conversation's own session id, reopens it specifically regardless of what ran afterward. They aren't interchangeable aliases — passing an id to each produces different results only for resume. And the CLI-versus-SDK split described in one option isn't the actual distinction; both options exist in each surface."}]},{"id":"mt_fork_session","type":"CONCEPTUAL","cluster":"cl_sessions","name":"forkSession: branching from a shared baseline","description":"forkSession only means something alongside resume. Without it, resuming appends to the original session; with it you get an independent branch from the same baseline. The exam's case: comparing two implementation strategies from the same expensive codebase analysis — fork so both branches share the analysis and diverge without contaminating each other.","core":true,"estMinutes":7,"evidence":["Distinguish resume from resume + fork on a described goal","Name the cost forking saves"],"assessmentPrompt":"You want two independent explorations that both start from one expensive analysis. What do you pass?","standards":["ccaf:d5","ccaf-glossary:t7"],"sources":[{"label":"Lesson · Resume, or branch","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene9"}],"centrality":0,"layer":3,"x":750,"y":542,"knowledgePoints":[{"id":"kp_fork_1","name":"Fork only means something with resume","keyPrerequisites":["mt_session_continuity"],"teach":"<p class='kp-key'>forkSession only means something alongside resume.</p><ul><li><code>resume</code> alone → you append to the original session. One timeline.</li><li><code>resume</code> + <code>forkSession</code> → you branch to a <strong>new session ID</strong> from the same baseline. Two timelines that share a past and do not share a future.</li></ul><p>The exam's framing (glossary <strong>t7</strong>): comparing two implementation strategies from the same codebase analysis → <strong>fork the session</strong>, so both branches inherit the expensive analysis and diverge afterwards without contaminating each other.</p><p>Contrast t8: <code>--resume &lt;session-name&gt;</code> simply continues a specific named prior conversation. Same verb, different intent.</p>","questions":[{"id":"q_fork_1a","q":"After a subagent finishes a lengthy codebase analysis, you want to explore two different implementation strategies based on that analysis, without either exploration's dead ends leaking into the other. What should the two follow-up queries pass?","options":["resume set to the analysis session's id together with forkSession: true, run once per strategy.","forkSession: true on its own, run once per strategy, with no resume value specified.","resume set to the analysis session's id alone, run once per strategy, with no forkSession value.","Two fresh queries that each paste the full text of the codebase analysis into a new prompt."],"correct":0,"explain":"Pairing resume with forkSession lets each follow-up branch a new session id off the same analysis baseline, so both strategies inherit the expensive analysis while their explorations stay isolated from each other. forkSession on its own has no resumed session to branch from, so it accomplishes nothing. resume alone would have both follow-ups append to the same single timeline, letting one strategy's dead ends contaminate the other's context. And pasting the analysis text into fresh prompts discards the session mechanism entirely and re-spends the tokens that made the analysis expensive."},{"id":"q_fork_1b","q":"Two teammates both pass resume with the same session id to continue exploring a shared analysis, but neither sets forkSession. After both have sent several follow-up messages, what has happened to their two lines of investigation?","options":["The second resume call is rejected outright because the session is already locked by the first caller's process.","Each resume call is treated as copy-on-write by default, so the two investigations stayed separate.","The original session was deleted and replaced the moment the second teammate's resume call landed.","Both sets of follow-up messages landed in the same timeline, so each investigation is now part of the other's."],"correct":3,"explain":"Without forkSession, resume just appends to the one existing timeline, so both teammates' follow-up messages ended up interleaved in the same session — each investigation's dead ends and detours are now part of the other's context. resume isn't exclusive or locking; a second caller can resume the same id without being rejected. Sessions also aren't copy-on-write by default — that isolation only happens when forkSession is explicitly set. And nothing about resuming a session deletes or replaces it; the original history is preserved, just shared."}]}],"reviewQuestions":[{"id":"r_fork_1","q":"You're documenting the SDK's session options for new team members and need one accurate sentence describing what forkSession does and what it requires to have any effect. Which description is correct?","options":["It runs two entirely separate agents against the same prompt at the same time, in parallel.","It only takes effect combined with resume, branching a new session id off that resumed baseline.","It copies all of a session's associated files into a freshly created directory on disk.","It compacts a session's history immediately, before the accompanying resume call takes effect."],"correct":1,"explain":"forkSession only means something alongside resume — used together, it branches a brand-new session id off the resumed baseline, giving two timelines that share a past but not a future. Used alone, without resume, there's no baseline to branch from, so it does nothing observable. It has nothing to do with running parallel agents against one prompt, copying files to a new directory, or triggering compaction — those are separate mechanisms entirely."}]},{"id":"mt_compaction","type":"CONCEPTUAL","cluster":"cl_sessions","name":"Compaction and the context window","description":"The SDK compacts automatically; PreCompact fires before it and a compact-boundary message appears in the stream. /compact is the manual Claude Code equivalent. SDKControlGetContextUsageResponse reports totalTokens against maxTokens, where the window is the model's context window or the lower auto-compaction window when one applies.","core":true,"estMinutes":7,"evidence":["Name the hook that fires before compaction and what it is good for","Read a context-usage response correctly"],"assessmentPrompt":"You need to save a summary of state before the SDK compacts. Which hook?","standards":["ccaf:d5","ccaf-glossary:t60"],"sources":[{"label":"Notes · Context management","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.047,"layer":1,"x":330,"y":418,"knowledgePoints":[{"id":"kp_compact_1","name":"Automatic compaction and PreCompact","keyPrerequisites":["mt_loop_ownership"],"teach":"<p>The SDK compacts automatically as the context fills. Two things you can observe and act on:</p><ul><li><code>PreCompact</code> fires <em>before</em> it happens — your chance to persist anything the compaction might blur.</li><li>A <strong>compact-boundary message</strong> appears in the stream, so your code can tell that history before that point has been summarised.</li></ul><p><code>/compact</code> is the manual Claude Code equivalent (glossary <strong>t60</strong>).</p><p><code>SDKControlGetContextUsageResponse</code> reports <code>totalTokens</code> against <code>maxTokens</code>, where the window is the model's context window — or the lower auto-compaction window when one applies. So a number that looks like it is far from the limit may be closer than you think.</p>","questions":[{"id":"q_compact_1a","q":"You want to write a summary of the agent's current plan to an external log immediately before the SDK automatically compacts the conversation, so nothing important gets lost in the summarization. Which hook fires at the right moment for this?","options":["SessionEnd, which fires only once the entire session has fully and finally concluded.","PostCompact, which fires right after compaction has already finished summarizing.","PreCompact, which fires before compaction happens, while full history is still present.","Stop, which fires when the agent's turn is ending for reasons unrelated to compaction."],"correct":2,"explain":"PreCompact fires before compaction happens, which is the last moment the full, unsummarized history is still available to read and log. SessionEnd fires far too late, after the whole session — not just this compaction — has concluded. A hook firing after compaction has already run would only see the summarized version, defeating the purpose of preserving detail. And Stop is about the end of a turn, an unrelated event from compaction entirely."},{"id":"q_compact_1b","q":"Your monitoring dashboard reads totalTokens and maxTokens from an SDKControlGetContextUsageResponse and the ratio looks comfortably low, yet the agent triggers compaction sooner than that ratio would suggest it should. What does maxTokens actually represent here?","options":["The effective ceiling in force, possibly a lower auto-compaction window, not the model's full window.","Always the model's full advertised context window, no matter what else is configured for the run.","Whatever numeric value was passed to a separate, unrelated maxTokens option in the query itself.","Whatever budget currently remains under the taskBudget setting that was specifically configured for this run."],"correct":0,"explain":"maxTokens in that response reflects the effective ceiling actually in force, which can be a lower auto-compaction window rather than the model's theoretical full context — explaining why compaction triggers before the ratio looks alarming. It is not always the model's full advertised window; that's exactly the assumption the dashboard's confusion is built on. It also isn't tied to a separate query-level maxTokens option, which governs something else entirely. And taskBudget tracks spend, not the token ceiling that governs compaction."}]}],"reviewQuestions":[{"id":"r_compact_1","q":"You're building tooling that watches the message stream to determine when history before some point has already been summarized by compaction. What should your code look for?","options":["A second system:init message, matching the one normally seen only at conversation start.","A tool_result entry marked isError: true appearing at the exact point compaction ran.","Nothing; compaction happens invisibly with no corresponding entry in the message stream.","A compact-boundary message in the stream, marking where the summarized history ends."],"correct":3,"explain":"A compact-boundary message appears directly in the stream, giving code a concrete marker for where summarized history ends and unsummarized history begins. There is no second system:init message involved — that message type only appears once, at the very start of a conversation. Compaction also isn't reported as a failed tool call, so watching for isError: true on a tool_result would never catch it. And it isn't invisible either; the whole point of the boundary message is to make it observable to downstream code."}]},{"id":"mt_delegation_context_lever","type":"CONCEPTUAL","cluster":"cl_sessions","name":"Delegation as the primary context lever","description":"Delegation, not compaction, is the main tool for keeping a long run inside its window. The Explore subagent exists so a verbose multi-phase discovery does not exhaust the main context — findings come back as a summary, the fifty files do not. enableFileCheckpointing separately allows rewinding file changes.","core":true,"estMinutes":6,"evidence":["Choose delegation over compaction for a long discovery phase","Explain what Explore is for in one sentence"],"assessmentPrompt":"A multi-phase discovery keeps blowing the context window. What is the first structural fix?","standards":["ccaf:d5","ccaf-glossary:t40"],"sources":[{"label":"Notes · Context management","href":"../08-claude-agent-sdk/notes.md"}],"centrality":0.023,"layer":6,"x":1380,"y":170,"knowledgePoints":[{"id":"kp_lever_1","name":"Delegation beats compaction","keyPrerequisites":["mt_subagent_return","mt_compaction"],"teach":"<p class='kp-key'>Delegation is the primary context lever.</p><p>Compaction is damage control — it summarises what already filled the window. Delegation prevents the filling: the verbose part happens somewhere else, and only the conclusion comes back.</p><p>That is exactly why the <strong>Explore</strong> subagent exists (glossary <strong>t40</strong>): a verbose multi-phase discovery would otherwise exhaust the main window. The findings return as a summary; the fifty files never arrive.</p><p>Separately, <code>enableFileCheckpointing</code> lets you rewind file changes — a different kind of safety net, not a context lever.</p>","questions":[{"id":"q_lever_1a","q":"A code-review workflow's discovery phase reads roughly fifty files across the repository, and this alone regularly fills most of the context window before the actual review work even begins. Lowering maxTokens just makes compaction trigger earlier without fixing the underlying problem. What's the structural fix?","options":["Enable file checkpointing for the whole run so any file changes can be rewound afterward if needed.","Delegate the discovery phase to a subagent, so only its findings return to the main context, not the fifty files.","Call /compact by hand between each phase of the discovery to keep the window from filling as much.","Lower maxTokens further still, so that automatic compaction triggers even earlier and more often during discovery."],"correct":1,"explain":"Delegating discovery to a subagent means the verbose reading of fifty files happens somewhere else entirely, and only the conclusions return to the main context — this prevents the window from filling rather than reacting once it already has. Manually compacting between phases and lowering maxTokens are both still forms of damage control that respond to a window that's already filling, not fixes for the filling itself. File checkpointing is an unrelated safety net for rewinding filesystem changes, not a context-management tool at all."},{"id":"q_lever_1b","q":"After a long agent run modifies a dozen files, you discover the edits took a wrong turn and want to roll the filesystem back to its state before the run started, without touching the conversation history at all. Which setting supports this?","options":["sessionStore, which mirrors the conversation's transcript to a shared external backend.","PreCompact, which fires before automatic compaction and lets you persist extra state.","enableFileCheckpointing, which lets you rewind the file changes the agent made during the run.","forkSession, which branches a new session id off an already-resumed baseline session."],"correct":2,"explain":"enableFileCheckpointing is a safety net specifically for the filesystem, letting you rewind the file changes an agent made — exactly what's needed to undo a run that took a wrong turn without touching the conversation itself. sessionStore and PreCompact both operate on the conversation transcript and context, not on files on disk. forkSession branches conversation history into a new session id; it has no relationship to filesystem state at all."}]}],"reviewQuestions":[{"id":"r_lever_1","q":"A teammate argues that since the SDK already compacts automatically once context fills up, there's no real need to bother delegating verbose discovery work to a subagent. How would you push back on that reasoning?","options":["Compaction summarizes a window that's already full, while delegation keeps it from filling up in the first place.","Delegation is simply a faster mechanism to execute than compaction, with no other meaningful difference between them.","Compaction has been removed entirely from the current SDK release, leaving delegation as the only option.","Subagents are automatically granted a much larger context window than the main agent loop receives."],"correct":0,"explain":"Compaction is damage control — it summarizes a window only after it's already full — while delegation is prevention, keeping the verbose work from filling the main window at all, which is why it beats compaction as a strategy rather than merely duplicating it. It isn't just a speed difference; the two operate at different points in the process entirely. Compaction hasn't been removed from the SDK, and subagents don't receive some inherently larger context window — the benefit comes from what returns to the caller, not from a bigger window."}]},{"id":"mt_stop_hook_gate","type":"PROCEDURAL","cluster":"cl_sessions","name":"Gate the outcome with a Stop hook","description":"A Stop hook that runs the test suite and returns decision: \"block\" with a reason turns \"did you run the tests?\" into a condition the agent cannot end its turn without satisfying. Gate the outcome; do not ask for it.","core":true,"estMinutes":7,"evidence":["Convert a request in the system prompt into an enforced end-of-turn condition","Name the event and the return shape"],"assessmentPrompt":"The agent keeps declaring done without running tests. What do you build?","standards":["ccaf:d1","ccaf:d5"],"sources":[{"label":"Lesson · Evidence, not assurance","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene10"}],"centrality":0.023,"layer":5,"x":1170,"y":356,"knowledgePoints":[{"id":"kp_stopgate_1","name":"Gate the outcome, don't ask for it","keyPrerequisites":["mt_hook_decisions","mt_determinism_principle"],"teach":"<p>\"Always run the tests before you say you're done\" in a system prompt is a request. Most of the time it works. <em>Most of the time</em> is the problem.</p><p>A <code>Stop</code> hook that runs the test suite and returns <code>decision: \"block\"</code> with a reason turns that request into a <strong>condition Claude cannot end its turn without satisfying</strong>. The agent does not get to decide whether the tests ran; the gate decides whether the turn ends.</p><p>This is the determinism principle applied to the end of a turn rather than to a tool call. Same idea, different attachment point.</p>","questions":[{"id":"q_stopgate_1a","q":"Despite a system-prompt instruction telling the agent to always run the test suite before declaring a task complete, code review keeps finding pull requests where the agent claimed success without running tests at all. What should you build to close this gap deterministically?","options":["A PostToolUse hook that inspects each tool result for the presence of recognizable test output.","A stronger, more emphatic version of the instruction placed at the very top of the system prompt.","A PreToolUse hook on the Bash tool that checks whether the invoked command specifically looks like a test runner.","A Stop hook that runs the test suite itself and returns decision: \"block\" with a reason if it hasn't passed."],"correct":3,"explain":"A Stop hook that runs the suite itself and returns decision: \"block\" turns the instruction into a condition the agent cannot end its turn without satisfying, which is what deterministic compliance requires. A stronger instruction is still just a request the model can skip under pressure, the same failure mode already observed. A PreToolUse hook on Bash can only check whether some command resembling a test runner was invoked, not whether the suite actually passed. And a PostToolUse hook inspecting for test output is reactive and easy to satisfy without a genuine, passing test run."},{"id":"q_stopgate_1b","q":"A colleague asks why you replaced the 'always run tests before finishing' line in the system prompt with a Stop hook, instead of just rewording the instruction to sound more forceful. What's the real advantage of the hook?","options":["The hook consumes noticeably fewer tokens than the equivalent system-prompt instruction did.","The hook gates whether the turn is allowed to end at all, rather than merely asking the agent to comply.","The hook executes on a noticeably smaller and faster model than the main agent loop uses.","The hook bypasses permission checks that a system-prompt instruction would otherwise be subject to."],"correct":1,"explain":"The hook's advantage is that it gates the end of the turn itself, making compliance a condition rather than a request — the same determinism principle applied one level up from a single tool call. Token savings and running on a different model aren't the mechanism at work here; the hook and the instruction it replaced aren't being compared on cost or speed. And permission checks are a separate system entirely — the hook isn't bypassing anything, it's enforcing something the prompt could only ask for."}]}],"reviewQuestions":[{"id":"r_stopgate_1","q":"You're implementing a Stop hook meant to prevent the agent's turn from ending until the test suite has passed. Which return shape actually accomplishes that?","options":["hookSpecificOutput.permissionDecision set to \"deny\", the same shape used to block a tool call.","updatedInput containing corrected arguments for the tool call that originally triggered the hook.","A top-level decision field set to \"block\", together with a reason string explaining why.","additionalContext describing to the agent what work still needs to be done before finishing."],"correct":2,"explain":"Stop hooks use a top-level decision: \"block\" plus a reason, which is a different return shape from the tool-call-blocking family — using the wrong shape here fails silently and lets the turn end anyway. permissionDecision belongs to hooks that gate individual tool calls, like PreToolUse, not the end of a turn. updatedInput only makes sense for a hook that's modifying a tool call's arguments, which Stop isn't doing. additionalContext can add information but doesn't itself prevent the turn from ending."}]},{"id":"mt_reliability_posture","type":"CONCEPTUAL","cluster":"cl_sessions","name":"Reliability posture and honest metrics","description":"Cap the loop with maxTurns, maxBudgetUsd and taskBudget; set fallbackModel for primary-model failure; put escalation criteria in the system prompt with few-shot examples demonstrating them, because judgment about when to hand off is a prompting problem, not a wiring one. And beware aggregates: 97% overall accuracy can hide a category that fails most of the time — evaluate per category.","core":true,"estMinutes":8,"evidence":["Separate failures that hooks fix from failures that prompting fixes","Explain why per-category evaluation changes a decision that aggregate accuracy would not"],"assessmentPrompt":"First-contact resolution is low because the agent hands off too late. Hook, or prompt?","standards":["ccaf:d1","ccaf:d3","ccaf:d5"],"sources":[{"label":"Lesson · Evidence, not assurance","href":"../lessons/0011-claude-agent-sdk-hooks-and-reliability.html#scene10"}],"centrality":0,"layer":6,"x":1380,"y":232,"knowledgePoints":[{"id":"kp_posture_1","name":"The whole posture, in order","keyPrerequisites":["mt_budgets","mt_stop_hook_gate"],"teach":"<ol><li><strong>Cap the loop</strong> — <code>maxTurns</code>, <code>maxBudgetUsd</code>, <code>taskBudget</code>.</li><li><strong>Survive model failure</strong> — <code>fallbackModel</code>.</li><li><strong>Gate the outcome, don't ask for it</strong> — a <code>Stop</code> hook that demands evidence.</li><li><strong>Prompt for judgment</strong> — escalation criteria belong in the system prompt <em>with few-shot examples demonstrating them</em>.</li><li><strong>Measure per category</strong> — 97% overall accuracy can mask a category that fails most of the time.</li></ol><p>Points 3 and 4 are the pair that gets tested. A gate is right when the failure is <em>compliance</em>. Prompting is right when the failure is <em>judgment</em> — like deciding when a conversation should be handed to a human. You cannot hook your way to good taste about when to escalate.</p>","questions":[{"id":"q_posture_1a","q":"Your support agent's first-contact resolution rate is low because it keeps trying to resolve issues on its own well past the point where a human should have taken over, not because it's skipping a procedural step. Should you address this with a hook or with the system prompt?","options":["The system prompt, expanded with escalation criteria and few-shot examples showing when to hand off.","A Stop hook that blocks the agent's turn from ending until an escalation check has explicitly run.","A PreToolUse hook that guards the escalation tool so it can only be called under narrow conditions.","A lower maxTurns setting that forces the conversation to hand off sooner regardless of its content."],"correct":0,"explain":"The failure here is judgment — knowing when a conversation should be handed to a human — and hooks can only enforce invariants, not supply taste; the fix is a system prompt with escalation criteria and worked examples showing the judgment call being made correctly. A Stop hook could force some escalation check to run, but it can't make that check reach the right conclusion about a genuinely ambiguous case. A PreToolUse guard on the escalation tool restricts when it's callable rather than teaching when it should be called. And a blunt maxTurns cap hands off based on turn count, not on whether handoff actually makes sense for that conversation."},{"id":"q_posture_1b","q":"Your agent shows 97% overall accuracy across all interactions, and your manager wants to ship it broadly on the strength of that single number. Why might that aggregate figure still hide a serious problem?","options":["A 97% figure sits below the threshold published by every comparable industry benchmark for this domain.","Accuracy simply isn't a meaningful metric for any agent system at all, no matter how it happens to be computed.","The aggregate figure says nothing at all about the agent's response latency under real production load.","One category of requests can fail most of the time while everything else performing well averages it away."],"correct":3,"explain":"Aggregates hide their worst slice — a single category can be failing most of the time and still get buried under everything else that performs well, producing a headline number that looks fine while a real subset of users is being badly served. Whether 97% clears some industry benchmark is beside the point; the problem is what the average conceals, not where it ranks. Accuracy is a meaningful metric when measured correctly — the issue here is granularity, not the metric's validity. And latency is a genuinely separate concern from the per-category masking problem being illustrated."}]}],"reviewQuestions":[{"id":"r_posture_1","q":"You're deciding whether a newly discovered reliability gap should be closed with a hook or addressed through the system prompt instead. What's the general rule the team follows for making that call?","options":["Hooks handle every reliability gap; prompting is considered inherently too unreliable to depend on.","Hooks enforce compliance invariants, while prompting teaches judgment calls, like when to escalate.","Prompting governs tool-use decisions while hooks apply to anything that involves free text.","Hooks are reserved for one part of the curriculum, and prompting belongs to an entirely separate one."],"correct":1,"explain":"The dividing line is what kind of failure you're dealing with: hooks enforce compliance invariants deterministically, while prompting — with escalation criteria and examples — teaches judgment calls like when to hand a conversation to a human, since no hook can supply that kind of taste. Treating hooks as the answer to every gap ignores that some failures simply aren't rule violations. The tool-use-versus-free-text split and the curriculum-domain split both describe distinctions that don't actually correspond to how this choice gets made."}]}],"dependencies":[{"topicId":"mt_subprocess_model","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"The subprocess explanation only means something once you know which loop the SDK took over"},{"topicId":"mt_entry_points","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"You cannot judge what query() gives you without knowing what it replaced"},{"topicId":"mt_query_vs_client","prerequisiteId":"mt_entry_points","strength":"hard","reason":"The comparison is between two entry points you must be able to name first"},{"topicId":"mt_system_prompt","prerequisiteId":"mt_subprocess_model","strength":"hard","reason":"The claude_code preset is only intelligible if you know Claude Code is running underneath"},{"topicId":"mt_system_prompt","prerequisiteId":"mt_entry_points","strength":"soft","reason":"systemPrompt is an options field, so it helps to have seen the options object"},{"topicId":"mt_setting_sources","prerequisiteId":"mt_subprocess_model","strength":"hard","reason":"Reading settings off disk is a Claude Code behaviour, inherited because of the subprocess"},{"topicId":"mt_budgets","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"A turn cap is meaningless until you know what a turn is in the loop the SDK runs"},{"topicId":"mt_budgets","prerequisiteId":"mt_entry_points","strength":"soft","reason":"Budgets are set through the options object"},{"topicId":"mt_subagent_why","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"Delegation is a policy decision, and policy is what you own once the SDK owns the loop"},{"topicId":"mt_subagent_why","prerequisiteId":"mt_query_vs_client","strength":"soft","reason":"Knowing how one agent runs frames why you would want a second"},{"topicId":"mt_subagent_definition_sources","prerequisiteId":"mt_subagent_why","strength":"hard","reason":"Define agents only after you can say why one is worth its overhead"},{"topicId":"mt_subagent_definition_sources","prerequisiteId":"mt_setting_sources","strength":"soft","reason":"Filesystem agents in .claude/agents are subject to the same on-disk loading rules"},{"topicId":"mt_agent_definition","prerequisiteId":"mt_subagent_definition_sources","strength":"hard","reason":"The field list is the content of the programmatic definition form"},{"topicId":"mt_agentdef_case_trap","prerequisiteId":"mt_agent_definition","strength":"hard","reason":"You have to know the field names before the casing of the field names can bite"},{"topicId":"mt_subagent_tool_scope","prerequisiteId":"mt_agent_definition","strength":"hard","reason":"tools and disallowedTools are AgentDefinition fields"},{"topicId":"mt_agent_task_tool","prerequisiteId":"mt_subagent_definition_sources","strength":"hard","reason":"A defined agent still needs a tool call to spawn it"},{"topicId":"mt_agent_task_tool","prerequisiteId":"mt_allowed_vs_disallowed","strength":"soft","reason":"\"Must be auto-approved\" only lands once you know what allowedTools actually does"},{"topicId":"mt_context_isolation","prerequisiteId":"mt_agent_definition","strength":"hard","reason":"What crosses inward is defined against the fields that could have carried it"},{"topicId":"mt_context_isolation","prerequisiteId":"mt_system_prompt","strength":"soft","reason":"The parent's system prompt is one of the things that does not cross"},{"topicId":"mt_subagent_return","prerequisiteId":"mt_context_isolation","strength":"hard","reason":"The return path is the second half of the same boundary"},{"topicId":"mt_subagent_runtime","prerequisiteId":"mt_agent_task_tool","strength":"hard","reason":"run_in_background is an argument on the tool that spawns the agent"},{"topicId":"mt_subagent_detect_resume","prerequisiteId":"mt_agent_task_tool","strength":"hard","reason":"Detection means matching that tool's name in the stream"},{"topicId":"mt_subagent_detect_resume","prerequisiteId":"mt_subagent_return","strength":"soft","reason":"agentId arrives inside the tool result you learn to read there"},{"topicId":"mt_hub_and_spoke","prerequisiteId":"mt_context_isolation","strength":"hard","reason":"Hub-and-spoke is forced by isolation: there is no sibling channel"},{"topicId":"mt_decomposition_shapes","prerequisiteId":"mt_hub_and_spoke","strength":"hard","reason":"Both shapes describe how the hub splits work"},{"topicId":"mt_coordinator_defect","prerequisiteId":"mt_decomposition_shapes","strength":"hard","reason":"The defect being diagnosed is a decomposition defect"},{"topicId":"mt_coordinator_defect","prerequisiteId":"mt_subagent_return","strength":"soft","reason":"You reason about what the coordinator saw, which is only the final messages"},{"topicId":"mt_determinism_principle","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"Hooks are the policy half of the mechanism/policy split"},{"topicId":"mt_hook_events","prerequisiteId":"mt_determinism_principle","strength":"hard","reason":"Learn why code beats a request before memorising where the code attaches"},{"topicId":"mt_hook_events","prerequisiteId":"mt_subprocess_model","strength":"soft","reason":"The event list is Claude Code's, exposed through the SDK"},{"topicId":"mt_hook_registration","prerequisiteId":"mt_hook_events","strength":"hard","reason":"You register against an event you can name"},{"topicId":"mt_hook_direction","prerequisiteId":"mt_hook_events","strength":"hard","reason":"Direction is the Pre/Post distinction inside the event list"},{"topicId":"mt_hook_decisions","prerequisiteId":"mt_hook_registration","strength":"hard","reason":"The return shape belongs to the callback you just learned to write"},{"topicId":"mt_hook_decisions","prerequisiteId":"mt_hook_direction","strength":"hard","reason":"Which return shape is legal depends on which event, and direction organises the events"},{"topicId":"mt_hook_rewrite","prerequisiteId":"mt_hook_decisions","strength":"hard","reason":"updatedInput and additionalContext are alternatives to the decision fields"},{"topicId":"mt_hook_defer","prerequisiteId":"mt_hook_decisions","strength":"hard","reason":"defer is one of the four permissionDecision values"},{"topicId":"mt_permission_modes","prerequisiteId":"mt_subprocess_model","strength":"hard","reason":"Permission modes are a Claude Code concept the SDK inherits"},{"topicId":"mt_can_use_tool","prerequisiteId":"mt_permission_modes","strength":"hard","reason":"canUseTool fires only when the mode resolves to a prompt"},{"topicId":"mt_can_use_tool","prerequisiteId":"mt_hook_decisions","strength":"hard","reason":"The whole point is the contrast with a PreToolUse hook's decision"},{"topicId":"mt_allowed_vs_disallowed","prerequisiteId":"mt_permission_modes","strength":"hard","reason":"Unlisted tools fall through to the permission mode, so the modes come first"},{"topicId":"mt_inprocess_mcp","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"Custom tools are the tool half of the policy you now own"},{"topicId":"mt_inprocess_mcp","prerequisiteId":"mt_entry_points","strength":"soft","reason":"Servers are wired in through the options object"},{"topicId":"mt_tool_shape","prerequisiteId":"mt_inprocess_mcp","strength":"hard","reason":"tool() defines the tools an in-process server holds"},{"topicId":"mt_tool_annotations","prerequisiteId":"mt_tool_shape","strength":"hard","reason":"Annotations are an argument to tool(), in a different position per language"},{"topicId":"mt_tool_annotations","prerequisiteId":"mt_allowed_vs_disallowed","strength":"soft","reason":"\"Hints, not controls\" needs a picture of where real controls live"},{"topicId":"mt_mcp_wiring","prerequisiteId":"mt_inprocess_mcp","strength":"hard","reason":"In-process is one of the four mcpServers shapes"},{"topicId":"mt_mcp_wiring","prerequisiteId":"mt_setting_sources","strength":"soft","reason":"strictMcpConfig excludes exactly the on-disk sources settingSources governs"},{"topicId":"mt_tool_descriptions","prerequisiteId":"mt_tool_shape","strength":"hard","reason":"The description is the second argument you just learned to pass"},{"topicId":"mt_is_error","prerequisiteId":"mt_tool_shape","strength":"hard","reason":"isError is a field on the result your handler returns"},{"topicId":"mt_error_conventions","prerequisiteId":"mt_is_error","strength":"hard","reason":"The two conventions are defined against the one real field"},{"topicId":"mt_error_propagation","prerequisiteId":"mt_error_conventions","strength":"hard","reason":"A structured error is an error payload designed the way this topic teaches"},{"topicId":"mt_error_propagation","prerequisiteId":"mt_subagent_return","strength":"hard","reason":"What propagates to the coordinator is the subagent's final message"},{"topicId":"mt_session_continuity","prerequisiteId":"mt_entry_points","strength":"hard","reason":"resume and continue are options on the entry point"},{"topicId":"mt_session_continuity","prerequisiteId":"mt_query_vs_client","strength":"soft","reason":"The client manages sessions for you, so the contrast sharpens both"},{"topicId":"mt_fork_session","prerequisiteId":"mt_session_continuity","strength":"hard","reason":"forkSession only means anything alongside resume"},{"topicId":"mt_compaction","prerequisiteId":"mt_loop_ownership","strength":"hard","reason":"Automatic compaction is one of the things the SDK now does for you"},{"topicId":"mt_compaction","prerequisiteId":"mt_hook_events","strength":"soft","reason":"PreCompact is on the event list"},{"topicId":"mt_delegation_context_lever","prerequisiteId":"mt_subagent_return","strength":"hard","reason":"The lever works because only the final message comes back"},{"topicId":"mt_delegation_context_lever","prerequisiteId":"mt_compaction","strength":"hard","reason":"It is the alternative to compaction, so both have to be on the table"},{"topicId":"mt_stop_hook_gate","prerequisiteId":"mt_hook_decisions","strength":"hard","reason":"A Stop hook uses the top-level decision: block shape"},{"topicId":"mt_stop_hook_gate","prerequisiteId":"mt_determinism_principle","strength":"hard","reason":"It is the principle applied to the end of a turn instead of a tool call"},{"topicId":"mt_reliability_posture","prerequisiteId":"mt_budgets","strength":"hard","reason":"Capping the loop is the first line of the posture"},{"topicId":"mt_reliability_posture","prerequisiteId":"mt_stop_hook_gate","strength":"hard","reason":"Gating the outcome is the second line"},{"topicId":"mt_reliability_posture","prerequisiteId":"mt_delegation_context_lever","strength":"soft","reason":"Context exhaustion is one of the failure modes the posture has to cover"}],"encompassings":[{"topicId":"mt_subprocess_model","encompassedId":"mt_loop_ownership","coverage":0.35,"reason":"Naming what the subprocess does re-states what the SDK took over"},{"topicId":"mt_query_vs_client","encompassedId":"mt_entry_points","coverage":0.7,"reason":"You cannot compare the two entry points without naming both and their options types"},{"topicId":"mt_system_prompt","encompassedId":"mt_subprocess_model","coverage":0.5,"reason":"Choosing the claude_code preset re-derives that Claude Code is the thing underneath"},{"topicId":"mt_setting_sources","encompassedId":"mt_subprocess_model","coverage":0.5,"reason":"Loading settings off disk is the subprocess model in concrete form"},{"topicId":"mt_budgets","encompassedId":"mt_loop_ownership","coverage":0.4,"reason":"Counting turns exercises the shape of the loop the SDK runs"},{"topicId":"mt_subagent_definition_sources","encompassedId":"mt_subagent_why","coverage":0.4,"reason":"Choosing a definition source restates why the agent exists"},{"topicId":"mt_agent_definition","encompassedId":"mt_subagent_definition_sources","coverage":0.6,"reason":"The field list is the programmatic form of the definition"},{"topicId":"mt_agentdef_case_trap","encompassedId":"mt_agent_definition","coverage":0.5,"reason":"The casing trap is drilled by writing the fields out"},{"topicId":"mt_subagent_tool_scope","encompassedId":"mt_agent_definition","coverage":0.6,"reason":"Reasoning about tools and disallowedTools exercises the definition as a whole"},{"topicId":"mt_agent_task_tool","encompassedId":"mt_subagent_definition_sources","coverage":0.5,"reason":"Spawning an agent implies having defined one"},{"topicId":"mt_context_isolation","encompassedId":"mt_agent_definition","coverage":0.5,"reason":"The inheritance table is read against the definition's fields"},{"topicId":"mt_subagent_return","encompassedId":"mt_context_isolation","coverage":0.7,"reason":"The return path is the same boundary, walked the other way"},{"topicId":"mt_subagent_runtime","encompassedId":"mt_agent_task_tool","coverage":0.45,"reason":"run_in_background is an argument on that tool"},{"topicId":"mt_subagent_detect_resume","encompassedId":"mt_agent_task_tool","coverage":0.6,"reason":"Detection is matching the tool's two names in the stream"},{"topicId":"mt_subagent_detect_resume","encompassedId":"mt_subagent_return","coverage":0.4,"reason":"agentId is read out of the tool result"},{"topicId":"mt_hub_and_spoke","encompassedId":"mt_context_isolation","coverage":0.6,"reason":"The topology is a consequence of isolation, re-derived each time"},{"topicId":"mt_decomposition_shapes","encompassedId":"mt_hub_and_spoke","coverage":0.5,"reason":"Both shapes describe how the hub splits work"},{"topicId":"mt_coordinator_defect","encompassedId":"mt_decomposition_shapes","coverage":0.8,"reason":"Diagnosing the defect means judging the decomposition"},{"topicId":"mt_coordinator_defect","encompassedId":"mt_hub_and_spoke","coverage":0.55,"reason":"You reason about the coordinator's role in the topology"},{"topicId":"mt_coordinator_defect","encompassedId":"mt_subagent_return","coverage":0.45,"reason":"The diagnosis turns on what the coordinator actually received"},{"topicId":"mt_hook_registration","encompassedId":"mt_hook_events","coverage":0.5,"reason":"Registering means keying the hook by event"},{"topicId":"mt_hook_direction","encompassedId":"mt_hook_events","coverage":0.6,"reason":"Direction sorts the event list into two halves"},{"topicId":"mt_hook_decisions","encompassedId":"mt_hook_registration","coverage":0.7,"reason":"The return shape is the body of the callback you registered"},{"topicId":"mt_hook_decisions","encompassedId":"mt_hook_direction","coverage":0.6,"reason":"Which shape is legal is decided by which side of the call you are on"},{"topicId":"mt_hook_decisions","encompassedId":"mt_hook_events","coverage":0.5,"reason":"The decision table is organised by event"},{"topicId":"mt_hook_rewrite","encompassedId":"mt_hook_decisions","coverage":0.65,"reason":"Rewriting lives in the same hookSpecificOutput you use to decide"},{"topicId":"mt_hook_defer","encompassedId":"mt_hook_decisions","coverage":0.5,"reason":"defer is one of the four permissionDecision values"},{"topicId":"mt_can_use_tool","encompassedId":"mt_permission_modes","coverage":0.6,"reason":"When canUseTool fires is stated in terms of the modes"},{"topicId":"mt_can_use_tool","encompassedId":"mt_hook_decisions","coverage":0.4,"reason":"The contrast keeps the PreToolUse decision shape in play"},{"topicId":"mt_allowed_vs_disallowed","encompassedId":"mt_permission_modes","coverage":0.65,"reason":"Unlisted tools fall through to the mode, so the modes get re-exercised"},{"topicId":"mt_stop_hook_gate","encompassedId":"mt_determinism_principle","coverage":0.8,"reason":"It is the principle applied verbatim to the end of a turn"},{"topicId":"mt_stop_hook_gate","encompassedId":"mt_hook_decisions","coverage":0.55,"reason":"The Stop hook uses the top-level block shape"},{"topicId":"mt_tool_shape","encompassedId":"mt_inprocess_mcp","coverage":0.5,"reason":"Every tool you write is registered on an in-process server"},{"topicId":"mt_tool_annotations","encompassedId":"mt_tool_shape","coverage":0.5,"reason":"Annotations are an argument whose position differs by language"},{"topicId":"mt_mcp_wiring","encompassedId":"mt_inprocess_mcp","coverage":0.6,"reason":"In-process is one of the four server shapes being wired"},{"topicId":"mt_tool_descriptions","encompassedId":"mt_tool_shape","coverage":0.45,"reason":"The description is the second argument to tool()"},{"topicId":"mt_is_error","encompassedId":"mt_tool_shape","coverage":0.4,"reason":"isError sits on the result your handler returns"},{"topicId":"mt_error_conventions","encompassedId":"mt_is_error","coverage":0.8,"reason":"The conventions are defined by contrast with the real field"},{"topicId":"mt_error_propagation","encompassedId":"mt_error_conventions","coverage":0.7,"reason":"A structured error is exactly this payload design"},{"topicId":"mt_error_propagation","encompassedId":"mt_is_error","coverage":0.5,"reason":"Signalling the failure at all is the isError part"},{"topicId":"mt_error_propagation","encompassedId":"mt_subagent_return","coverage":0.4,"reason":"What propagates is the subagent's final message"},{"topicId":"mt_fork_session","encompassedId":"mt_session_continuity","coverage":0.8,"reason":"Forking is resume plus one flag, so resume is re-practised every time"},{"topicId":"mt_compaction","encompassedId":"mt_loop_ownership","coverage":0.3,"reason":"Automatic compaction is one of the things the SDK now does for you"},{"topicId":"mt_delegation_context_lever","encompassedId":"mt_subagent_return","coverage":0.6,"reason":"The lever works precisely because only the summary returns"},{"topicId":"mt_delegation_context_lever","encompassedId":"mt_compaction","coverage":0.5,"reason":"Choosing it means ruling compaction out on the merits"},{"topicId":"mt_reliability_posture","encompassedId":"mt_budgets","coverage":0.7,"reason":"Capping the loop is the first line of the posture"},{"topicId":"mt_reliability_posture","encompassedId":"mt_stop_hook_gate","coverage":0.5,"reason":"Gating the outcome is the second line"},{"topicId":"mt_reliability_posture","encompassedId":"mt_determinism_principle","coverage":0.4,"reason":"Sorting hook problems from prompt problems re-applies the principle"}],"layout":{"width":1710,"height":588}};
