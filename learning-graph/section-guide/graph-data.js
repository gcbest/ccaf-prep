/* GENERATED FILE — do not edit.
   Source of truth: learning-graph/section-guide/data/*.json
   Rebuild with: node learning-graph/scripts/build-web-data.mjs section-guide */
window.CCAF_GRAPH = {"version":1,"builtAt":"2026-08-31","subject":"Community Guide: Claude Certified Architect","section":"Community Guide · Claude Certified Architect","sectionShort":"Community Guide","storageKey":"ccaf_learning_graph_guide_v1","notesPath":"reference/claude-certified-architect-guide.md","clusters":[{"id":"cl_api_fundamentals","name":"The Messages API, precisely","subject":"Community Guide: Claude Certified Architect","summary":"The request shape every call sends, the roles that populate it (including why tool results ride inside a user-role message), the stop_reason values that drive an agentic loop, and the three specific ways a context window degrades under load.","color":"#3f6b86","colorDark":"#5f9cc0","notesPart":"Chapter 1 — Claude API: Fundamentals of Model Interaction"},{"id":"cl_tool_use_guide","name":"tool_use and schema discipline","subject":"Community Guide: Claude Certified Architect","summary":"Why a tool's description — not its name — is what Claude actually chooses between, the three tool_choice modes, and the schema-design rules (nullable fields, 'other'/'unclear' enums) that eliminate syntax errors but leave semantic ones for you to catch.","color":"#5f6b3a","colorDark":"#8ba055","notesPart":"Chapter 2 — Tools and tool_use"},{"id":"cl_agent_sdk_guide","name":"The agentic loop and hub-and-spoke","subject":"Community Guide: Claude Certified Architect","summary":"The loop's only real branch (stop_reason, not parsed text), AgentDefinition's least-privilege allowed_tools, the coordinator/subagent topology with its isolated-context rule, spawning via Task, and the determinism test that decides hook versus prompt.","color":"#8f3f3f","colorDark":"#c67878","notesPart":"Chapter 3 — Claude Agent SDK: Building Agentic Systems"},{"id":"cl_mcp_guide","name":"MCP: tools, resources, and honest errors","subject":"Community Guide: Claude Certified Architect","summary":"MCP's three primitives, project (.mcp.json) versus personal (~/.claude.json) server configuration, and the difference between a structured isError response a coordinator can act on and a generic one it can't.","color":"#7a4a6b","colorDark":"#a8779a","notesPart":"Chapter 4 — Model Context Protocol (MCP)"},{"id":"cl_claude_code_guide","name":"Configuring Claude Code","subject":"Community Guide: Claude Certified Architect","summary":"The three-level CLAUDE.md hierarchy and @path imports, .claude/rules/'s path-scoped loading, commands and skills as the on-demand counterpart to always-loaded CLAUDE.md, planning mode versus direct execution, /compact and /memory, and running headless in CI.","color":"#96652a","colorDark":"#ae7531","notesPart":"Chapter 5 — Claude Code: Configuration and Workflows"},{"id":"cl_prompt_eng_advanced","name":"Prompt engineering, made explicit","subject":"Community Guide: Claude Certified Architect","summary":"Few-shot examples over vague description, explicit falsifiable criteria in place of 'be conservative,' prompt chaining versus dynamic decomposition, the interview pattern, and validation/retry loops that catch what a schema can't.","color":"#55578f","colorDark":"#7b7db0","notesPart":"Chapter 6 — Prompt Engineering: Advanced Techniques"},{"id":"cl_batches","name":"The Batches API","subject":"Community Guide: Claude Certified Architect","summary":"The 50%-cheaper, up-to-24-hour, no-multi-turn trade-off, custom_id-based partial-failure recovery, and planning a submission window against a real deadline.","color":"#3f8f6f","colorDark":"#6bc4a0","notesPart":"Chapter 7 — Message Batches API"},{"id":"cl_decomposition","name":"Decomposing a task","subject":"Community Guide: Claude Certified Architect","summary":"Fixed pipelines for predictable, template-shaped work versus dynamic decomposition for open-ended investigation, worked through with multi-pass code review as the concrete example.","color":"#8f6f3f","colorDark":"#c4a06b","notesPart":"Chapter 8 — Task Decomposition Strategies"},{"id":"cl_escalation","name":"Escalation and human-in-the-loop","subject":"Community Guide: Claude Certified Architect","summary":"Which triggers are reliable (an explicit request, a policy gap, a financial threshold) and which aren't (sentiment, self-rated confidence), the three escalation patterns, a self-contained structured handoff, and calibrated confidence routing with stratified auditing.","color":"#a84e31","colorDark":"#c96549","notesPart":"Chapter 9 — Escalation and Human-in-the-Loop"},{"id":"cl_error_handling_guide","name":"Error handling across agents","subject":"Community Guide: Claude Certified Architect","summary":"The transient/validation/business/permission taxonomy, the four anti-patterns that starve a coordinator of the information it needs, and the structured-error-plus-coverage-annotation shape that replaces them.","color":"#6b4a7a","colorDark":"#9a77a8","notesPart":"Chapter 10 — Error Handling in Multi-agent Systems"},{"id":"cl_context_mgmt_guide","name":"Context management in production","subject":"Community Guide: Claude Certified Architect","summary":"A facts block that survives summarization, PostToolUse trimming of oversized results, position-aware layout against lost-in-the-middle, scratchpads and subagent delegation to protect the main context, and structured state files for crash recovery.","color":"#3a6b5f","colorDark":"#63a08f","notesPart":"Chapter 11 — Context Management in Production Systems"},{"id":"cl_provenance","name":"Preserving provenance","subject":"Community Guide: Claude Certified Architect","summary":"Why a claim without a source, date, and confidence has lost what makes it checkable, preserving conflicting values instead of silently picking one, and rendering by content type once dates resolve an apparent contradiction into a trend.","color":"#8f3f6f","colorDark":"#c478a0","notesPart":"Chapter 12 — Preserving Provenance"},{"id":"cl_builtin_tools_guide","name":"Claude Code's built-in tools","subject":"Community Guide: Claude Certified Architect","summary":"Matching Glob, Grep, Read, Write, Edit, and Bash to the task at hand, investigating incrementally rather than reading everything up front, and the Read-then-Write fallback for when Edit can't find a unique match.","color":"#4a6b8f","colorDark":"#7896c4","notesPart":"Chapter 13 — Claude Code Built-in Tools"}],"standards":[{"id":"ccaf:d1","family":"ccaf","code":"Domain 1","name":"Agentic Architecture & Orchestration","weight":0.27,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d2","family":"ccaf","code":"Domain 2","name":"Tool Design & MCP Integration","weight":0.18,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d3","family":"ccaf","code":"Domain 3","name":"Prompt Engineering & Structured Output","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d4","family":"ccaf","code":"Domain 4","name":"Claude Code Configuration & Workflows","weight":0.2,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"},{"id":"ccaf:d5","family":"ccaf","code":"Domain 5","name":"Context Management & Reliability","weight":0.15,"source":"CCA-F Exam Guide v1.0 (effective July 2026)"}],"topics":[{"id":"mt_api_request_structure","type":"FACTUAL","cluster":"cl_api_fundamentals","name":"The Messages API request shape","description":"The fields every Claude Messages API call sends together — model, max_tokens, system, messages, tools, tool_choice — and why the full message history rides along on every single call.","core":true,"estMinutes":8,"evidence":["List the top-level fields a Messages API request carries and what each one controls.","Explain why a request must carry the full conversation history rather than just the newest turn."],"assessmentPrompt":"A teammate's client only sends the newest user message on each call to save bandwidth. What breaks, and which field's job was that?","standards":["ccaf:d1"],"sources":[{"label":"Guide §1.1 The Messages API request shape","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L120-L146"}],"centrality":1,"layer":0,"x":120,"y":46,"knowledgePoints":[{"id":"kp_api_request_1","name":"A request is stateless, so it repeats itself","keyPrerequisites":[],"teach":"<p class='kp-key'>Every Messages API call is independent — the model retains nothing between requests, so <code>messages</code> must carry the entire conversation so far, not just the newest turn.</p><p>A request bundles <code>model</code>, <code>max_tokens</code>, <code>system</code>, <code>messages</code>, <code>tools</code>, and <code>tool_choice</code> together. <code>system</code> sits outside <code>messages</code> because it's a standing instruction rather than part of the back-and-forth; <code>tools</code> and <code>tool_choice</code> exist only to let the model ask your code to do something instead of answering directly.</p><p>Drop the history and the model isn't being forgetful — it's being handed a different, shorter conversation than the one the user thinks they're in.</p>","questions":[{"id":"q_api_request_1a","q":"A client is rewritten to send only the user's newest message on each API call, to cut request size. On turn 4, the assistant responds as if turns 1–3 never happened. What is the most accurate diagnosis?","options":["max_tokens was set too low on turn 4, truncating the response before the model could reference earlier turns.","The API silently caches prior turns server-side, and the cache expired sooner than the client assumed it would.","The system prompt was not resent on turn 4, so the model lost the persona and constraints defined earlier.","Each request is independent and stateless, so omitting turns 1–3 from messages means the model never received them."],"correct":3,"explain":"The API holds no state between calls — every prior turn the model is supposed to know about has to be re-sent inside messages on every single request. Turn 4 not referencing turns 1–3 is exactly what omitting them from messages produces, not a caching expiry, a truncated response, or a missing system prompt (which would drop persona/constraints, not turn-by-turn content)."},{"id":"q_api_request_1b","q":"Which pairing of a request field to its purpose is correct?","options":["max_tokens sets the maximum number of tokens the response may contain, and can cause a truncated reply if too low.","tool_choice selects which model variant (Opus, Sonnet, Haiku) handles the request based on task complexity.","tools specifies the maximum context window size available for the current conversation's messages array.","system defines the conversational back-and-forth history that must be resent on every call to the API."],"correct":0,"explain":"max_tokens caps how long the response can be, and setting it too low truncates the reply — that's the correct pairing. tool_choice controls tool-selection behavior, not model routing; system is the standing instruction outside the conversational history, not the history itself; and tools defines available functions, not a context-window limit."}]}],"reviewQuestions":[{"id":"r_api_request_1","q":"A teammate argues that since the system prompt 'is loaded once,' it doesn't need to be included in every individual API call, only messages does. Is this accurate?","options":["Yes — system is cached server-side after the first call and persists automatically for the rest of the session.","No — the API is stateless per call, so system must be passed in every request alongside messages, just like the history.","Yes — only the first request in a session needs the system field; later calls only need to grow the messages array.","No — system only needs to be resent every fifth call to refresh the model's persona without wasting tokens each turn."],"correct":1,"explain":"There's no cross-call memory: the API doesn't persist system, messages, or anything else between requests. system has to ride along on every call exactly like the message history does, not once, not periodically — every time."}]},{"id":"mt_message_roles","type":"CONCEPTUAL","cluster":"cl_api_fundamentals","name":"Roles: user, assistant, and system-as-a-role","description":"Why there are only two conversational roles plus one instructional role, why tool results travel inside a user-role message rather than a dedicated 'tool' role, and the placement rules that govern a mid-conversation system message.","core":true,"estMinutes":8,"evidence":["State which role a tool_result content block is sent under, and why there is no separate tool role.","Give the placement rule that keeps a mid-conversation system message from splitting a tool_use/tool_result pair."],"assessmentPrompt":"A tool result gets sent back as `{\"role\": \"tool\", ...}` and the API rejects it. What role should it have used instead, and where does the tool_result block live inside that message?","standards":["ccaf:d1"],"sources":[{"label":"Guide §1.2 Roles: user, assistant, and system-as-a-role","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L147-L176"}],"centrality":0.92,"layer":1,"x":330,"y":46,"knowledgePoints":[{"id":"kp_message_roles_1","name":"Tool results live inside a user turn","keyPrerequisites":["mt_api_request_structure"],"teach":"<p class='kp-key'>There is no <code>\"role\": \"tool\"</code>. A tool's result is sent back as a <code>tool_result</code> content block inside a <code>user</code>-role message.</p><p><code>user</code> and <code>assistant</code> are the two conversational roles; <code>system</code> can also appear as a role directly inside <code>messages</code> (not just the top-level field), letting you inject an instruction mid-conversation without invalidating the cached prefix from the top-level <code>system</code> field — but it has placement rules: it must immediately follow a <code>user</code> turn or an <code>assistant</code> turn ending in server tool use, must precede an <code>assistant</code> turn or end the array, and can never sit between a <code>tool_use</code> block and its matching <code>tool_result</code> — that returns a 400 error.</p>","questions":[{"id":"q_message_roles_1a","q":"Where does a tool's output belong in the next request sent back to the API?","options":["As a top-level `tool_output` field alongside `system` and `messages`, separate from the conversation array.","As a message with `\"role\": \"tool\"`, appended immediately after the assistant's `tool_use` message.","As a `tool_result` content block inside a message with `\"role\": \"user\"` — there is no dedicated tool role.","As a `tool_result` content block inside the same assistant message that originally issued the `tool_use` call."],"correct":2,"explain":"Tool results travel inside a user-role message as a tool_result content block — the API has no separate tool role. A top-level tool_output field doesn't exist, a literal role: \"tool\" message is rejected, and the result can't live in the same assistant message that made the call, since that message is what the result is a response to."},{"id":"q_message_roles_1b","q":"An engineer wants to inject a mid-conversation instruction as an inline `system` role message, placed directly between an `assistant` message's `tool_use` block and the `user` message carrying its `tool_result`. What happens?","options":["It succeeds silently, and the inline system message simply takes precedence over the top-level system field going forward.","It succeeds, but only for the remainder of that single turn — later turns revert to the top-level system field.","The API ignores the inline system message entirely and processes the surrounding tool_use/tool_result pair normally.","The API returns a 400 error, because a system message cannot sit between a tool_use block and its tool_result."],"correct":3,"explain":"One of the explicit placement rules for an inline system-role message is that it cannot separate a tool_use block from its tool_result — the API returns a 400 error rather than silently accepting, partially honoring, or ignoring the insertion."}]}],"reviewQuestions":[{"id":"r_message_roles_1","q":"Which statement about the messages array's roles is correct?","options":["user and assistant are the two conversational roles, and system can also appear inline in messages under its own placement rules.","There are three conversational roles — user, assistant, and a dedicated tool role — each one carrying its own distinct kind of content block.","Only user and assistant may appear in messages; system may only ever be set via the top-level system field.","assistant is the only role that can carry a tool_result block, since it represents the model's own tool activity."],"correct":0,"explain":"user and assistant are the two conversational roles, and system can additionally appear inline within messages (subject to its placement rules), not just at the top level. There is no third 'tool' role, and tool_result blocks are carried by user-role messages, not assistant-role ones."}]},{"id":"mt_stop_reason","type":"FACTUAL","cluster":"cl_api_fundamentals","name":"Reading stop_reason correctly","description":"The four stop_reason values a Messages API response can carry, what each one means for your code, and why tool_use and end_turn are the two that actually drive an agentic loop.","core":true,"estMinutes":6,"evidence":["Match each stop_reason value to the action a caller should take.","Explain why max_tokens and stop_sequence do not belong in a loop's completion check."],"assessmentPrompt":"A response comes back with stop_reason 'max_tokens' and your code treats it the same as 'end_turn', showing the (truncated) text to the user. What should have happened instead?","standards":["ccaf:d1"],"sources":[{"label":"Guide §1.3 Reading stop_reason correctly","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L177-L189"}],"centrality":0.68,"layer":2,"x":540,"y":46,"knowledgePoints":[{"id":"kp_stop_reason_1","name":"Two of four values actually drive the loop","keyPrerequisites":["mt_message_roles"],"teach":"<p class='kp-key'><code>stop_reason</code> has four values — <code>end_turn</code>, <code>tool_use</code>, <code>max_tokens</code>, <code>stop_sequence</code> — but an agentic loop only branches on the first two.</p><table><tr><th>Value</th><th>Action</th></tr><tr><td><code>end_turn</code></td><td>Show the result to the user</td></tr><tr><td><code>tool_use</code></td><td>Execute the tool, append the result, loop again</td></tr><tr><td><code>max_tokens</code></td><td>Response is truncated — you may need a higher limit</td></tr><tr><td><code>stop_sequence</code></td><td>Handle per your application's own logic</td></tr></table><p>Treating <code>max_tokens</code> like <code>end_turn</code> means silently shipping a cut-off response as if it were complete.</p>","questions":[{"id":"q_stop_reason_1a","q":"A response comes back with stop_reason \"max_tokens\", and the calling code displays the (truncated) text to the user exactly as it would for \"end_turn\". What's wrong with that, and what should happen instead?","options":["Nothing is wrong — max_tokens and end_turn both signal the model is finished, so identical handling is correct either way.","The response is incomplete because it hit the token ceiling; the caller should recognize this and likely raise max_tokens and retry.","The response should be discarded entirely and re-sent with tool_choice set to \"any\" to force a shorter, complete answer.","The truncation only ever matters for tool_use responses specifically, so treating a max_tokens response the same as end_turn is actually correct behavior here."],"correct":1,"explain":"max_tokens means the model was cut off mid-generation, not that it finished — treating it like end_turn ships a truncated response as if it were complete. The fix is recognizing the truncation and likely increasing the token limit and retrying, not discarding-and-forcing a tool call (unrelated to the token ceiling) or assuming it's harmless."},{"id":"q_stop_reason_1b","q":"For an agentic loop deciding whether to keep calling tools or hand a final answer to the user, which two stop_reason values actually need a branch in the loop's control flow?","options":["max_tokens and stop_sequence, since both represent a form of the model stopping generation early.","end_turn and stop_sequence, since both indicate the model chose to stop rather than being cut off.","tool_use and end_turn, since together they cover every outcome the loop needs to act differently on.","tool_use and max_tokens, since both indicate the response requires some form of follow-up action."],"correct":2,"explain":"tool_use tells the loop to execute a tool and continue; end_turn tells it the task is done and the result can be shown. Those are the two branches an agentic loop's control flow actually needs — max_tokens and stop_sequence are edge cases handled separately, not the loop's core branching logic."}]}],"reviewQuestions":[{"id":"r_stop_reason_1","q":"Why are max_tokens and stop_sequence considered unreliable as the primary signal for ending an agentic loop's iteration, compared to end_turn and tool_use?","options":["They aren't unreliable — all four values are equally valid primary signals for ending a loop's iteration.","They fire far more often than end_turn or tool_use in practice, making them statistically the dominant signal.","They only ever appear in Message Batches API responses, and never in the synchronous Messages API, so they're irrelevant to a synchronous agentic loop's control flow.","They represent the response being cut off or bounded by a fixed pattern, not the model's own decision that it's done or needs a tool."],"correct":3,"explain":"max_tokens and stop_sequence reflect an external limit being hit, not the model concluding it's finished or that it needs a tool — which is exactly why they don't belong in the loop's core branch. They aren't equally valid signals, they aren't the dominant case in practice, and they aren't specific to the batch API."}]},{"id":"mt_context_window_problems","type":"CONCEPTUAL","cluster":"cl_api_fundamentals","name":"Three ways the context window fails you","description":"The lost-in-the-middle effect, tool-result accumulation from over-wide tool outputs, and the way progressive summarization quietly turns exact numbers into vague qualifiers like 'about' or 'a few'.","core":true,"estMinutes":10,"evidence":["Name which of the three context-window problems a given symptom matches.","Explain why a 40-field tool response is a context problem even when every field is technically accurate."],"assessmentPrompt":"After a long session gets compacted, a report that used to say '42% growth' now says 'growth of roughly forty percent, though this should be verified.' Which context-window problem produced that, and why?","standards":["ccaf:d5"],"sources":[{"label":"Guide §1.5 Three ways the context window fails you","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L200-L217"}],"centrality":0.12,"layer":2,"x":540,"y":108,"knowledgePoints":[{"id":"kp_context_window_1","name":"Three distinct failure modes, not one","keyPrerequisites":["mt_message_roles"],"teach":"<p class='kp-key'>The context window fails in three specific ways, and naming which one you're looking at determines the fix.</p><ul><li><strong>Lost-in-the-middle</strong> — models read the start and end of a long input more reliably than its middle; put key information at one of the edges.</li><li><strong>Tool-result accumulation</strong> — a tool returning 40+ fields when only 5 matter fills the window with noise on every single call.</li><li><strong>Progressive summarization</strong> — compressing history loses exact numbers, percentages, and dates first, replacing them with vague qualifiers like 'about' or 'a few'.</li></ul><p>All three shrink how much of the window is actually useful, but a fix for one (trimming a tool response) does nothing for another (a number going vague after a summary).</p>","questions":[{"id":"q_context_window_1a","q":"After a session runs a history-compressing summarization step, a report that previously read \"revenue grew 42% year over year\" now reads \"revenue grew by a notable amount, though the exact figure should be re-verified.\" Which context-window problem does this illustrate?","options":["Progressive summarization — compressing history dropped the exact numeric value and replaced it with a vague qualifier.","Lost-in-the-middle — the 42% figure was originally placed in the middle of a long document and got skipped over.","Tool-result accumulation — an earlier tool call returned too many fields, crowding out the original percentage figure.","None of the three — this is expected behavior any time a session's context is compressed to save space."],"correct":0,"explain":"This is the textbook progressive-summarization failure: an exact number ('42%') degraded into a vague phrase ('a notable amount') specifically because of a compression step, not because of where it sat in the input or a verbose tool response. It's a named, avoidable failure mode, not something to shrug off as expected."},{"id":"q_context_window_1b","q":"A tool call returns a 40-field customer record when the current task only needs 5 of those fields. On its own, which context-window problem does this most directly cause?","options":["Progressive summarization — the extra fields will eventually be compressed away and lose their precision when that happens.","Tool-result accumulation — the unused 35 fields still occupy context on every subsequent turn that includes this tool result.","Lost-in-the-middle — the 5 relevant fields are statistically likely to land in the middle of the 40-field response.","None — a single oversized tool response has no measurable effect on the context window unless it repeats many times."],"correct":1,"explain":"This is tool-result accumulation by definition: an oversized single response, most of it unused, sitting in context and consuming budget on every turn it's referenced. It isn't a summarization problem (nothing has been compressed yet), isn't specifically about middle-of-input placement, and isn't negligible even on a single occurrence, since that one response already carries the excess."}]}],"reviewQuestions":[{"id":"r_context_window_1","q":"A long vulnerability report places its single most important finding in paragraph 30 of 60, with introductory material before it and a conclusion after. Which context-window problem does this risk, and why?","options":["Progressive summarization — a finding placed mid-document is statistically more likely to be compressed away than one at the edges.","Tool-result accumulation — a 60-paragraph report is inherently too long regardless of where its key finding sits within it.","Lost-in-the-middle — content placed near the middle of a long input is read less reliably than content near the start or end.","None — placement within a document has no bearing on how reliably a model processes information it contains."],"correct":2,"explain":"This is the lost-in-the-middle effect: models process the start and end of a long input more reliably than the middle, so a critical finding buried at paragraph 30 of 60 is at real risk of being missed or under-weighted. It isn't a summarization or tool-output problem, and placement within an input does measurably affect how reliably it's processed."}]},{"id":"mt_tool_use_basics","type":"FACTUAL","cluster":"cl_tool_use_guide","name":"What tool_use actually is","description":"tool_use is Claude emitting a structured request for your code to run — the model never executes anything itself; it names a tool and supplies arguments, and your application decides whether and how to run it.","core":true,"estMinutes":6,"evidence":["State what the model does and does not do when it 'calls a tool.'","Identify who is responsible for actually executing a tool_use request."],"assessmentPrompt":"A candidate answer claims 'the model runs the tool code sandboxed inside the API.' What is actually true, and who runs the tool?","standards":["ccaf:d2"],"sources":[{"label":"Guide §2.1 What tool_use actually is","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L222-L225"}],"centrality":0.4,"layer":3,"x":750,"y":46,"knowledgePoints":[{"id":"kp_tool_use_basics_1","name":"A structured request, not a remote execution","keyPrerequisites":["mt_stop_reason"],"teach":"<p class='kp-key'>The model never runs code. It emits a <code>tool_use</code> block naming a tool and its arguments; your application decides whether, and how, to actually execute it.</p><p>That separation is what makes the pattern safe to build on — the model proposes, your code disposes, and nothing happens that your application didn't choose to run.</p>","questions":[{"id":"q_tool_use_basics_1a","q":"A junior engineer describes tool_use as 'Claude running the requested function inside a secure sandbox on Anthropic's servers.' What's the accurate correction?","options":["Claude does run the function, but only for read-only tools — write operations are the only ones requiring the caller to execute them.","Claude only emits a structured request naming a tool and its arguments; the calling application decides whether and how to execute it.","The description is accurate for the Agent SDK specifically, though the plain Messages API does require the caller to execute tools.","Claude runs the function in a sandbox only when tool_choice is set to \"any\"; under \"auto\" the caller must execute it manually."],"correct":1,"explain":"In every case, the model only produces a tool_use request — it never executes anything itself, sandboxed or otherwise. That's true across the plain Messages API and the Agent SDK, for read-only and write tools alike, and regardless of the tool_choice setting; execution is always the calling application's job."},{"id":"q_tool_use_basics_1b","q":"After the model emits a tool_use block, what is the calling application's role in the exchange?","options":["To forward the tool_use block unchanged to the user's browser, which executes the tool client-side via JavaScript.","To validate the tool_use block's JSON syntax only — actual execution happens automatically on Anthropic's infrastructure.","To decide whether to execute the named tool with the given arguments, then return the result for the model to continue from.","Nothing — tool_use blocks are informational logging only and do not require any response from the calling application."],"correct":2,"explain":"The calling application is where execution actually happens: it chooses whether to run the named tool with the supplied arguments and then feeds the result back so the loop can continue. Nothing about tool_use forwards execution to a browser, happens automatically server-side, or can be safely ignored as mere logging."}]}],"reviewQuestions":[{"id":"r_tool_use_basics_1","q":"Why does the model-proposes, application-disposes split matter for building agentic systems safely?","options":["It ensures the model's tool calls are always syntactically correct, which is actually guaranteed by the schema, not this split.","It doesn't matter much in practice, since most applications execute every tool_use request the model emits without review.","It only really matters for tools that modify data; read-only tools are assumed to bypass the application and execute directly from the model's own request.","It guarantees nothing unsafe happens unless the application itself chooses to run it — the model can request but never act unilaterally."],"correct":3,"explain":"The split means the model can only ever propose an action — the application retains full control over whether that action actually happens, which is what makes it safe to add gating, validation, or permission checks. It isn't about schema-guaranteed syntax, isn't something applications are free to ignore, and applies to read-only tools exactly as much as to ones that modify data."}]},{"id":"mt_tool_description_selection","type":"CONCEPTUAL","cluster":"cl_tool_use_guide","name":"The description is the selection mechanism","description":"Claude picks which tool to call based on its description, not its name — so two tools with overlapping descriptions get confused for each other, and a minimal description like 'retrieves customer information' invites exactly that.","core":false,"estMinutes":10,"evidence":["Explain why two tools with near-identical descriptions get misrouted even when their names differ.","List what a strong tool description should include beyond a one-line summary."],"assessmentPrompt":"Production logs show requests meant for a document-analysis tool routing to a web-search tool 45% of the time. Both tools' descriptions say roughly 'analyzes content and extracts key information.' What is the root cause, and what fixes it?","standards":["ccaf:d2"],"sources":[{"label":"Guide §2.2 The description is the selection mechanism","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L226-L258"}],"centrality":0,"layer":4,"x":960,"y":170,"knowledgePoints":[{"id":"kp_tool_description_1","name":"Description, not name, drives selection","keyPrerequisites":["mt_tool_use_basics"],"teach":"<p class='kp-key'>Claude chooses a tool by reading its description, not by its function name. Two tools with near-identical descriptions get confused for each other regardless of how different their names are.</p><p>A strong description states what the tool does and returns, its input formats with example values, its edge cases, and — critically — when to use it instead of a similar alternative. 'Retrieves customer information' invites exactly the ambiguity a longer, differentiated description would avoid.</p>","questions":[{"id":"q_tool_description_1a","q":"Production logs show requests meant for a document-analysis tool routing to a web-search tool 45% of the time. Both tools' descriptions read roughly 'analyzes content and extracts key information.' What is the root cause?","options":["The two tools' descriptions overlap almost completely, so the model has no textual basis for telling them apart.","The tools' names are too similar, and the model is matching on name similarity rather than reading either description.","The coordinator's system prompt lacks a routing table, which is the only mechanism that can disambiguate similar tools.","The document-analysis tool's input_schema is missing required fields, causing the model to fall back to the other tool."],"correct":0,"explain":"Selection is driven by description text, and two descriptions that both say roughly 'analyzes content and extracts key information' give the model nothing to distinguish between — that overlap, not tool naming, a missing routing table, or a schema defect, is the root cause the guide points to."},{"id":"q_tool_description_1b","q":"Which addition to a tool's description most directly helps the model choose correctly between it and a similar alternative?","options":["A shorter description, since brevity reduces the chance the model over-analyzes minor wording differences between tools.","An explicit statement of when to use this tool versus the similar alternative, alongside its purpose and input formats.","Renaming the tool to something more distinctive, since the model primarily disambiguates tools by their function names.","Moving the description into the system prompt instead of the tool definition, since system-level text carries more weight."],"correct":1,"explain":"Explicitly stating when to use this tool over a similar one gives the model the exact distinguishing signal it needs, on top of what the tool does and its input formats. A shorter description removes detail rather than adding disambiguation, renaming doesn't address the selection mechanism the guide identifies, and relocating text to the system prompt isn't how tool selection works."}]}],"reviewQuestions":[{"id":"r_tool_description_1","q":"A team notices their custom MCP tool is rarely chosen even though it offers functionality no built-in tool has. What does the guide suggest as the likely fix?","options":["Nothing to fix — agents inherently prefer built-in tools over MCP tools regardless of description quality, by design.","Disable the competing built-in tools entirely, since the model can't be taught to prefer an MCP tool through description alone.","Strengthen the MCP tool's description to highlight its concrete, unique advantages that built-in tools can't provide.","Switch the MCP tool's name to exactly match a built-in tool's name, so the model treats them as interchangeable options."],"correct":2,"explain":"Agents can default to a built-in tool over a similar MCP one, and the countermeasure is strengthening the MCP tool's description to spell out concrete advantages or unique data the built-in tool can't match — description quality is the lever, not disabling built-ins or aliasing names, and it isn't an unfixable default."}]},{"id":"mt_tool_choice_modes","type":"FACTUAL","cluster":"cl_tool_use_guide","name":"auto, any, and forced tool_choice","description":"The three tool_choice values — auto (model decides), any (must call some tool), and a named tool (must call that specific one) — and which scenario each is built for.","core":false,"estMinutes":8,"evidence":["Match 'auto', 'any', and a forced tool name to the scenario each is designed to guarantee.","Explain why tool_choice: any still guarantees structured output even though it doesn't pick which tool."],"assessmentPrompt":"A pipeline needs `extract_metadata` to run before any other step, every time, with no exceptions. Which tool_choice setting guarantees that, and why would 'auto' not be reliable enough?","standards":["ccaf:d2"],"sources":[{"label":"Guide §2.3 auto, any, and forced tool_choice","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L259-L272"}],"centrality":0,"layer":4,"x":960,"y":46,"knowledgePoints":[{"id":"kp_tool_choice_1","name":"auto, any, or a named tool","keyPrerequisites":["mt_tool_use_basics"],"teach":"<p class='kp-key'>Three tool_choice values, three guarantees:</p><ul><li><code>{\"type\": \"auto\"}</code> — the model decides whether to call a tool at all. Default for most cases.</li><li><code>{\"type\": \"any\"}</code> — the model must call some tool, but picks which one. Use when you need guaranteed structured output but any of several extraction tools would do.</li><li><code>{\"type\": \"tool\", \"name\": \"X\"}</code> — the model must call exactly tool X. Use when a specific first step is mandatory, like running extraction before enrichment.</li></ul>","questions":[{"id":"q_tool_choice_1a","q":"A pipeline must run extract_metadata as its first step on every single request, with no exceptions and no possibility the model answers in plain text instead. Which tool_choice setting guarantees this?","options":["{\"type\": \"any\"}, since it forces some tool call, and extract_metadata is the only one likely to be chosen first.","{\"type\": \"auto\"}, combined with a strongly worded system-prompt instruction telling the model to call it first.","{\"type\": \"any\"}, paired with removing every other tool definition so extract_metadata is the only one the model can pick.","{\"type\": \"tool\", \"name\": \"extract_metadata\"}, since it forces exactly that tool to be called, with no other option available."],"correct":3,"explain":"Only the named-tool form — {\"type\": \"tool\", \"name\": \"extract_metadata\"} — forces exactly that specific tool with no alternative. \"any\" guarantees some tool call but not which one, \"auto\" plus a prompt instruction is still probabilistic, and removing every other tool is a workaround rather than the mechanism the API actually provides for this guarantee."},{"id":"q_tool_choice_1b","q":"A task has three interchangeable extraction tools, and any one of them would produce acceptable structured output. Which tool_choice setting fits, and why?","options":["{\"type\": \"any\"}, since it guarantees some tool is called while still letting the model pick the best-fitting one of the three.","{\"type\": \"tool\", \"name\": ...} on whichever tool is defined first, since forcing one avoids ambiguity in the model's choice.","{\"type\": \"auto\"}, since it's the lowest-friction default and works whenever more than one valid tool exists.","None of the three — with multiple interchangeable tools, tool_choice must be omitted entirely to avoid over-constraining the call."],"correct":0,"explain":"\"any\" is built for exactly this case: it guarantees structured output by forcing a tool call, while leaving the choice of which of several acceptable tools to the model. Forcing one specific tool removes the model's ability to pick the best fit, \"auto\" doesn't guarantee a tool gets called at all, and omitting tool_choice entirely just falls back to auto's same uncertainty."}]}],"reviewQuestions":[{"id":"r_tool_choice_1","q":"Why does {\"type\": \"any\"} still count as guaranteeing structured output, even though it doesn't pin down which tool gets called?","options":["It doesn't guarantee structured output — only a named-tool tool_choice actually guarantees the response is structured.","Because it forces a tool_use response rather than free text, and every tool's output is structured by its input_schema.","It guarantees structured output only when exactly one tool is defined, which makes the 'any' choice effectively forced anyway.","Because the API automatically converts any free-text response into JSON matching the first tool's schema when 'any' is set."],"correct":1,"explain":"\"any\" forces the model to emit some tool_use call rather than plain text, and because every tool's arguments are shaped by its own input_schema, whichever tool gets picked still produces structured output — that's the guarantee, independent of which specific tool is chosen. It isn't limited to the single-tool case, and the API doesn't silently convert free text into JSON."}]},{"id":"mt_schema_design_errors","type":"CONCEPTUAL","cluster":"cl_tool_use_guide","name":"Schema design, and what schemas can't fix","description":"Nullable fields instead of forced fabrication, 'other'/'unclear' enum escapes so data isn't lost or forced into the wrong bucket, required-vs-optional discipline, and the hard line between syntax errors (a schema eliminates) and semantic errors (a schema cannot catch).","core":true,"estMinutes":12,"evidence":["Explain why marking a field required when its data is sometimes absent causes fabrication.","Give an example of a semantic error that a valid JSON schema response can still contain."],"assessmentPrompt":"An extraction schema marks `discount_percentage` as required, and on documents with no discount the model starts inventing a value like 0 or a plausible-looking guess. What schema change fixes this, and why doesn't tightening the field's type help?","standards":["ccaf:d2","ccaf:d3"],"sources":[{"label":"Guide §2.4-2.5 Schema design, and what schemas can't fix","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L273-L326"}],"centrality":0.14,"layer":4,"x":960,"y":108,"knowledgePoints":[{"id":"kp_schema_design_1","name":"Nullable, not required, when data can be absent","keyPrerequisites":["mt_tool_use_basics"],"teach":"<p class='kp-key'>Marking a field required when its data is sometimes missing pushes the model to fabricate a value rather than admit the information isn't there.</p><p><code>\"type\": [\"string\", \"null\"]</code> lets the model return <code>null</code> honestly. An enum with an <code>\"other\"</code> option (plus a detail string) keeps data that doesn't fit your predefined categories instead of losing it, and an <code>\"unclear\"</code> option is honest where a forced guess would be a coin flip dressed up as a classification.</p>","questions":[{"id":"q_schema_design_1a","q":"An extraction schema marks discount_percentage as required. On documents with no discount mentioned, the model starts returning invented values like 0 or a plausible-sounding guess. What change fixes this?","options":["Add a discount_present boolean field alongside the existing required discount_percentage field to flag ambiguous cases.","Keep discount_percentage required as it already is, but add a separate confidence field so the model can flag whenever the invented value feels uncertain.","Change discount_percentage's type to [\"number\", \"null\"] and drop it from the required list, so absence can be represented honestly.","Change discount_percentage's type to a string instead of a number, so the model can write \"not mentioned\" as free text."],"correct":2,"explain":"The fabrication is a direct symptom of requiring a field whose data isn't always present — making it nullable and non-required removes the pressure to invent a value. Adding a flag or a confidence score alongside a still-required field doesn't remove that pressure, and switching to a string just relocates the same problem into unstructured text."},{"id":"q_schema_design_1b","q":"A classification schema's category enum has five fixed values. On a batch of real documents, roughly 8% don't fit any of the five. Which schema change addresses this without losing data?","options":["Force the model to pick whichever of the five existing values is closest, since a five-way enum should already be exhaustive.","Remove the enum constraint entirely and let category be free-text, so no document is ever forced into the wrong bucket.","Lower the required threshold so category becomes optional, letting the model omit it for documents that don't fit.","Add an \"other\" value to the enum, paired with a category_detail string field for documents that don't fit the five values."],"correct":3,"explain":"An \"other\" value paired with a detail string keeps every document classifiable while preserving the specifics of the ones that don't fit the five predefined categories — no data is lost or force-fit. Forcing the closest of five wrong buckets loses information, dropping the enum constraint sacrifices the structure you need, and making the field merely optional discards the classification instead of capturing it."}]},{"id":"kp_schema_design_2","name":"What a schema catches, and what it can't","keyPrerequisites":["mt_tool_use_basics"],"teach":"<p class='kp-key'>tool_use with a JSON schema guarantees syntactically valid JSON and the required structure — but it says nothing about whether the values inside are correct.</p><table><tr><th>Error type</th><th>Example</th><th>Mitigation</th></tr><tr><td>Syntax</td><td>Invalid JSON, wrong field type</td><td>tool_use + schema (eliminates it)</td></tr><tr><td>Semantic</td><td>Totals don't add up, hallucinated value</td><td>Validation checks, retry with feedback, self-correction</td></tr></table>","questions":[{"id":"q_schema_design_2a","q":"A tool_use response passes JSON schema validation cleanly — every required field is present with the correct type — but the invoice's line_items sum to $145 while total reads $150. What does this demonstrate?","options":["A semantic error slipped through: the JSON is syntactically valid and well-typed, but a value inside it is wrong.","The schema itself is broken, since a correctly designed schema would have caught this kind of arithmetic mismatch.","The response should have failed schema validation, so this is evidence the tool_use call was constructed incorrectly.","Nothing unusual — small arithmetic discrepancies like this are expected and don't require any further handling."],"correct":0,"explain":"This is exactly what a semantic error looks like: structurally and syntactically the response is fine, but a value is wrong in a way no JSON schema check can detect. Schemas were never designed to verify arithmetic consistency, so this isn't a broken schema, a validation failure, or something to leave unhandled."},{"id":"q_schema_design_2b","q":"Which of the following is a syntax error that tool_use with a JSON schema is specifically designed to eliminate?","options":["A price field that is technically a valid number but is off by a factor of ten from the actual value on the invoice.","A response missing a closing brace, or a field typed as a string where the schema requires a number.","A category field containing a value that isn't hallucinated but doesn't match the true category on the source document.","A summary field that is well-formed and correctly typed but omits an important caveat from the source text."],"correct":1,"explain":"Malformed JSON (a missing brace) or a field with the wrong type is precisely the class of syntax error a JSON schema enforces against. A price that's numerically wrong, a mismatched category, or an incomplete-but-well-typed summary are all semantic problems — the values are wrong or incomplete, not the structure."}]}],"reviewQuestions":[{"id":"r_schema_design_1","q":"A team believes that once their extraction schema validates cleanly against every response, they no longer need any downstream validation logic. What's wrong with that conclusion?","options":["Nothing is wrong — schema validation is a complete correctness check, so downstream validation would only be redundant.","Schema validation is unreliable enough that syntax errors can still slip through even after it reports success.","Schema validation only guarantees valid structure and types, not that the values themselves are semantically correct.","Downstream validation is only needed for numeric fields; string and enum fields are fully covered by schema validation."],"correct":2,"explain":"A schema guarantees syntactic validity and structural completeness, but it has no way to check whether the values inside are actually correct — that gap is exactly what downstream semantic validation exists to close. Schema validation isn't unreliable at catching syntax errors, and the gap it leaves applies to numeric, string, and enum fields alike."}]},{"id":"mt_agentic_loop_guide","type":"CONCEPTUAL","cluster":"cl_agent_sdk_guide","name":"The agentic loop's only real branch","description":"Send, receive, check stop_reason, act — and the three tempting-but-wrong completion signals (parsing assistant text for 'done,' an iteration cap, or checking for textual output) that all fail where stop_reason == end_turn does not.","core":true,"estMinutes":10,"evidence":["Trace the four steps of the agentic loop and where stop_reason is checked.","Explain why an iteration-limit stop condition is an anti-pattern rather than a safety net."],"assessmentPrompt":"An agent implementation stops looping once the assistant's response contains the string 'Task completed.' Why is this unreliable, and what should it check instead?","standards":["ccaf:d1"],"sources":[{"label":"Guide §3.1 The agentic loop's only real branch","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L331-L352"}],"centrality":0.24,"layer":3,"x":750,"y":108,"knowledgePoints":[{"id":"kp_agentic_loop_1","name":"One real branch, three tempting fakes","keyPrerequisites":["mt_stop_reason"],"teach":"<p class='kp-key'>The loop is: send, receive, check stop_reason, act, repeat. The only reliable completion signal is <code>stop_reason == \"end_turn\"</code>.</p><p>Three anti-patterns masquerade as completion checks: scanning assistant text for a phrase like 'Task completed,' capping iterations at an arbitrary number and treating the cap itself as done, and checking whether the assistant produced any textual content at all. Each one can fire while real work is still outstanding, or fail to fire when the model has genuinely finished.</p>","questions":[{"id":"q_agentic_loop_1a","q":"An implementation stops its loop as soon as the assistant's response text contains the string \"Task completed.\" Why is this an anti-pattern rather than a reasonable shortcut?","options":["The model could say that phrase mid-task while still intending further tool calls, or finish without ever saying it — text isn't the signal.","It's inefficient but not wrong — parsing assistant text for a completion phrase is slower than checking stop_reason, but in careful testing it reliably produces the exact same result.","It only fails when the model is instructed not to narrate its own completion, which is rare enough to not matter in practice.","It's an anti-pattern only when combined with an iteration cap; used alone, text-matching is a fully reliable completion signal."],"correct":0,"explain":"Text content is not a structured signal — the model can produce that phrase without actually being done, or finish the task without ever producing it, so the parsing can both false-positive and false-negative independent of an iteration cap. It isn't merely slower than the reliable check; it's a different and unreliable check entirely."},{"id":"q_agentic_loop_1b","q":"Which stop_reason value is the loop's only reliable signal that a task is genuinely finished and the result can be shown to the user?","options":["max_tokens, since a truncated response means generation stopped and no further tool calls will be issued.","end_turn, since it specifically indicates the model concluded the turn with nothing further to do.","Any stop_reason value paired with an iteration count above the configured cap for that loop.","stop_sequence, since encountering a defined sequence is the model's way of signaling task completion."],"correct":1,"explain":"end_turn is the model's own signal that it's done — that's the loop's one reliable branch. max_tokens signals truncation (not completion), stop_sequence is an application-defined stopping point handled per your own logic rather than a completion signal, and an iteration cap is an arbitrary limit, not a signal the model produced at all."}]}],"reviewQuestions":[{"id":"r_agentic_loop_1","q":"A loop implementation caps iterations at 5 and treats hitting the cap as 'task complete' if no explicit error occurred. What's the risk in this design?","options":["There is no real risk — 5 iterations is generous enough in practice that legitimate tasks essentially never need more.","The risk only applies to tasks involving subagents; single-agent loops are unaffected by an iteration cap of this kind.","The cap can be reached with real, unfinished work still pending, and treating that as completion silently ships a partial result.","The cap is fine as a completion signal as long as it's combined with a check for whether the assistant happened to produce any textual content at all."],"correct":2,"explain":"An arbitrary iteration cap has no relationship to whether the underlying task is actually done — hitting it with genuine work still outstanding and treating that as success ships an incomplete result without any signal that something was cut short. The risk isn't scoped to subagents only, isn't solved by pairing it with a text-presence check, and doesn't disappear just because 5 sounds generous — it depends entirely on the task."}]},{"id":"mt_agent_definition","type":"FACTUAL","cluster":"cl_agent_sdk_guide","name":"AgentDefinition's four load-bearing fields","description":"name and description identify an agent, system_prompt gives it instructions, and allowed_tools enforces least privilege — the same object shapes both a coordinator (allowed_tools includes Task) and a subagent (it doesn't).","core":false,"estMinutes":8,"evidence":["Name the AgentDefinition field that enforces least privilege and explain how.","Explain what distinguishes a coordinator's AgentDefinition from a subagent's."],"assessmentPrompt":"Two AgentDefinitions look almost identical, but only one has \"Task\" in allowed_tools. What role does that one play, and why can't the other one spawn subagents?","standards":["ccaf:d1"],"sources":[{"label":"Guide §3.2 AgentDefinition's four load-bearing fields","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L353-L372"}],"centrality":0.04,"layer":4,"x":960,"y":232,"knowledgePoints":[{"id":"kp_agent_definition_1","name":"allowed_tools is where least privilege lives","keyPrerequisites":["mt_agentic_loop_guide"],"teach":"<p class='kp-key'><code>AgentDefinition</code> has four load-bearing fields: <code>name</code> and <code>description</code> identify the agent, <code>system_prompt</code> gives it instructions, and <code>allowed_tools</code> enforces least privilege by listing exactly what it may call.</p><p>A coordinator's <code>allowed_tools</code> includes <code>\"Task\"</code> so it can spawn subagents; a subagent's typically doesn't, since spawning further subagents isn't its job.</p>","questions":[{"id":"q_agent_definition_1a","q":"Two AgentDefinitions are nearly identical except one has \"Task\" in allowed_tools and the other doesn't. What role does the one with \"Task\" play, and why can't the other spawn subagents?","options":["It's a subagent; the other is the coordinator, since coordinators are generally defined as whichever AgentDefinition has the narrowest allowed_tools list of the two.","Both are coordinators; \"Task\" only controls whether an agent can read files, not whether it can spawn other agents.","The distinction is cosmetic — any AgentDefinition can spawn subagents at runtime regardless of its allowed_tools list.","It's the coordinator; the other can't spawn subagents because allowed_tools is precisely what enforces which tools an agent may call."],"correct":3,"explain":"allowed_tools is the least-privilege mechanism — an agent without \"Task\" in that list simply cannot invoke it, which is exactly what keeps it from spawning subagents. Coordinators are the ones granted Task, not the ones with the narrowest tool list, and allowed_tools genuinely gates capability rather than being cosmetic."},{"id":"q_agent_definition_1b","q":"Which AgentDefinition field carries the instructions that shape how an agent behaves once invoked?","options":["system_prompt, since it holds the instructions that define the agent's role, tone, and constraints.","description, since it's read by the model at runtime to determine the agent's own behavioral guidelines.","name, since a well-chosen identifier is what primarily determines an agent's behavior in practice.","allowed_tools, since restricting which tools are available is what shapes an agent's overall behavior."],"correct":0,"explain":"system_prompt is where an agent's actual instructions live. description and name are identification metadata rather than behavioral instructions, and allowed_tools constrains capability, not the guidance an agent follows within that capability."}]}],"reviewQuestions":[{"id":"r_agent_definition_1","q":"Why does giving every subagent in a system the same broad allowed_tools list (including Task) undermine the principle AgentDefinition is built around?","options":["It doesn't undermine anything — a broader allowed_tools list is always safer because it gives every agent more flexibility to recover from errors.","It undermines least privilege: each subagent should only have the tools its specific job requires, not everything available system-wide.","It's only a problem if the subagents actually use Task; simply having it available in allowed_tools carries no risk on its own.","It only matters for cost, since unused tools in allowed_tools slightly increase token usage but have no other effect."],"correct":1,"explain":"allowed_tools is meant to scope each agent to exactly what its job requires — handing every subagent the coordinator's full tool list, Task included, defeats that principle by letting agents do far more than their role calls for. Broader access isn't inherently safer, unused permissions still represent real risk even if not immediately exercised, and the concern is about scope of capability, not just token cost."}]},{"id":"mt_hub_and_spoke_guide","type":"CONCEPTUAL","cluster":"cl_agent_sdk_guide","name":"Hub-and-spoke and isolated context","description":"A coordinator decomposes, delegates, aggregates, and handles errors; subagents do not automatically inherit its conversation history, do not share memory with each other, and route every result back through the hub rather than to one another directly.","core":true,"estMinutes":10,"evidence":["List the five responsibilities that belong to the coordinator, not the subagents.","Explain why a subagent that 'seems to know' something from an earlier call is a sign of a bug, not a feature."],"assessmentPrompt":"A colleague proposes letting the document-analysis subagent send its results straight to the synthesis subagent, skipping the coordinator, to save a round trip. What does the hub-and-spoke topology lose if you do that?","standards":["ccaf:d1"],"sources":[{"label":"Guide §3.3 Hub-and-spoke and isolated context","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L373-L397"}],"centrality":0.02,"layer":5,"x":1170,"y":46,"knowledgePoints":[{"id":"kp_hub_and_spoke_1","name":"Isolated context forces everything through the hub","keyPrerequisites":["mt_agent_definition"],"teach":"<p class='kp-key'>Subagents do not automatically inherit the coordinator's conversation history, do not share memory with each other, and route every result back through the coordinator rather than to one another directly.</p><p>The coordinator decomposes the task, decides which subagents are needed, delegates, aggregates and validates results, handles errors and retries, and talks to the user. That's the price and the payoff of the topology: nothing crosses between spokes except through the hub, which is exactly what keeps errors and information flow observable.</p>","questions":[{"id":"q_hub_and_spoke_1a","q":"A colleague proposes letting the document-analysis subagent send its results directly to the synthesis subagent, bypassing the coordinator, to save a round trip. What does the hub-and-spoke topology lose if this is adopted?","options":["Nothing meaningful — the coordinator's role is only to spawn subagents, not to observe what happens between them afterward.","Only latency changes; the coordinator would still see both results eventually once the synthesis subagent finishes its work.","The coordinator's ability to observe every interaction, apply uniform error handling, and control what each subagent receives.","The subagents would need to share memory to communicate directly, which the SDK does not technically support at all."],"correct":2,"explain":"Routing every exchange through the coordinator is what gives it visibility into all interactions, a single place to apply error handling uniformly, and control over exactly what each subagent sees — a direct subagent-to-subagent path bypasses all of that, not just latency. The coordinator's role is broader than spawning, and the failure mode here is architectural, not a missing-memory technical limitation."},{"id":"q_hub_and_spoke_1b","q":"A subagent's response references a fact from an earlier, unrelated subagent call in the same session that was never included in its own prompt. What does this most likely indicate?","options":["Normal behavior — subagents share the coordinator's full conversation history by design, so this is expected.","The two subagents share memory directly with each other, which is intended when they're part of the same coordinator's system.","This is fine as long as the fact happens to be correct — isolation only matters when the leaked information is wrong.","The subagent hallucinated the fact, or the isolated-context guarantee was violated by leaking context it shouldn't have received."],"correct":3,"explain":"Subagents are supposed to receive only what's explicitly passed in their own prompt — a fact from an unrelated call appearing anyway means either fabrication or an isolation violation, and either one is a bug worth investigating. It isn't expected behavior, subagents don't share memory with each other, and correctness of the leaked fact doesn't make the isolation break acceptable."}]}],"reviewQuestions":[{"id":"r_hub_and_spoke_1","q":"Which responsibility belongs to the coordinator rather than to any individual subagent in a hub-and-spoke system?","options":["Aggregating and validating results from multiple subagents, and deciding how to handle a failure among them.","Executing the specific, narrowly scoped task it was delegated, using only the tools listed in its own allowed_tools.","Maintaining its own isolated context, receiving only what was explicitly included in its own prompt.","Returning a result formatted according to whatever structure its own assigned task required."],"correct":0,"explain":"Aggregating results across subagents and deciding how to respond to a failure is squarely the coordinator's job — it's the one component with visibility across all of them. Executing a narrow delegated task, working from an isolated context, and returning results in the expected format are all subagent-level responsibilities, not the coordinator's."}]},{"id":"mt_task_tool_context","type":"PROCEDURAL","cluster":"cl_agent_sdk_guide","name":"Spawning with the Task tool: explicit context, parallel calls","description":"A Task prompt must carry every piece of context the subagent needs — it inherits nothing — and a coordinator can issue several Task calls in one response to run subagents concurrently rather than one at a time.","core":false,"estMinutes":10,"evidence":["Explain why 'Analyze the document' is an incomplete Task prompt even though it's a valid instruction.","Describe how a coordinator runs three subagents in parallel rather than sequentially."],"assessmentPrompt":"A Task prompt reads only \"Summarize the findings.\" The subagent has no document, no prior search results, and no output format. What specifically is missing, and what principle does it violate?","standards":["ccaf:d1"],"sources":[{"label":"Guide §3.4 Spawning with the Task tool: explicit context, parallel calls","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L398-L431"}],"centrality":0,"layer":6,"x":1380,"y":46,"knowledgePoints":[{"id":"kp_task_tool_1","name":"A Task prompt has to be self-contained","keyPrerequisites":["mt_hub_and_spoke_guide"],"teach":"<p class='kp-key'>Because a subagent inherits nothing, a Task prompt like \"Analyze the document\" is broken — it has no document, no prior context, no output format. A working one spells out the document text, prior findings, and the expected output shape.</p><p>A coordinator can issue several Task calls in one response, and those subagents run concurrently rather than one at a time — parallel spawning is a single-response affordance, not a sequence of separate calls.</p>","questions":[{"id":"q_task_tool_1a","q":"A Task prompt reads only: \"Summarize the findings.\" The subagent has no document, no prior search results, and no output format specified. What principle does this violate?","options":["Least privilege — the prompt should have restricted the subagent's allowed_tools rather than given it more context.","Explicit context passing — a subagent inherits nothing automatically, so every piece of needed context must be in the prompt itself.","The determinism principle — the prompt should have specified whether to use a hook or a prompt instruction for the summary format.","Isolated context — the fix is to let this subagent inherit the coordinator's full conversation history instead of writing a longer prompt."],"correct":1,"explain":"Because subagents receive nothing beyond what's explicitly in their Task prompt, an instruction with no document, no prior results, and no output format is missing exactly the context it needed to carry — that's an explicit-context-passing failure, not a tools, hooks, or inheritance issue. Isolation is the very reason context must be passed explicitly, not something to be overridden here."},{"id":"q_task_tool_1b","q":"A coordinator's single response contains three separate Task calls: one to search, one to analyze a document, and one to search a different topic. How do these three subagents execute?","options":["Sequentially, in the order the Task calls appear within that one response, each waiting for the previous to finish.","Only the first Task call in the response actually spawns a subagent; the other two are silently ignored by the SDK.","Concurrently — multiple Task calls within one coordinator response run the corresponding subagents in parallel.","It depends on whether allowed_tools includes \"Task\" more than once — otherwise only one subagent can run at a time."],"correct":2,"explain":"Multiple Task calls issued within a single coordinator response spawn their subagents to run concurrently — that's exactly what parallel spawning means in this pattern. They don't run sequentially, they aren't silently dropped, and duplicate entries in allowed_tools has nothing to do with concurrency."}]}],"reviewQuestions":[{"id":"r_task_tool_1","q":"What distinguishes a well-formed Task prompt from a broken one like \"Analyze the document\"?","options":["A well-formed prompt is shorter, since brevity is what makes a Task prompt easy for a subagent to act on quickly.","A well-formed prompt names the target subagent explicitly by its AgentDefinition name rather than by task description.","A well-formed prompt sets tool_choice to a forced value so the subagent's very first action is fully determined.","A well-formed prompt includes the actual content, any prior relevant results, and the expected output format inline."],"correct":3,"explain":"Since a subagent inherits nothing, everything it needs — the document text itself, whatever upstream results matter, and the shape the output should take — has to be spelled out in the prompt. Length isn't the deciding factor, naming a target agent isn't how Task prompts work, and tool_choice governs tool selection, not the context problem this pattern addresses."}]},{"id":"mt_hooks_vs_prompts_guide","type":"CONCEPTUAL","cluster":"cl_agent_sdk_guide","name":"Hooks are code that runs; prompts are requests","description":"A hook (PostToolUse to normalize data, PreToolUse to block a policy violation) guarantees its behavior on every single occurrence; a system-prompt instruction is honored at high but non-zero probability — the deciding question is whether a failure has financial, legal, or safety consequences.","core":true,"estMinutes":10,"evidence":["State the one-sentence test for choosing a hook over a prompt instruction.","Give an example of a rule that belongs in a hook and one that belongs in a prompt, and say why."],"assessmentPrompt":"A refund-limit rule ('never approve a refund over $500 without approval') currently lives only in the system prompt, and it's occasionally skipped in production. Why would moving it to a PreToolUse hook actually close the gap, where a stronger prompt wouldn't?","standards":["ccaf:d1"],"sources":[{"label":"Guide §3.5 Hooks are code that runs; prompts are requests","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L432-L466"}],"centrality":0.08,"layer":4,"x":960,"y":294,"knowledgePoints":[{"id":"kp_hooks_vs_prompts_1","name":"Consequences decide: hook or prompt","keyPrerequisites":["mt_agentic_loop_guide"],"teach":"<p class='kp-key'>A hook — like a PreToolUse check blocking refunds above $500 — is deterministic: it runs the same way, every time, 100% of occurrences. A system-prompt instruction is probabilistic: honored at high but non-zero probability, never exactly 100%.</p><p>The deciding question: does a failure here have financial, legal, or safety consequences? If yes, use a hook. If it's a general preference or formatting nicety, a prompt instruction is enough — and cheaper to write and change.</p>","questions":[{"id":"q_hooks_vs_prompts_1a","q":"A refund-limit rule ('never approve a refund over $500 without approval') lives only in the system prompt today, and it's occasionally skipped in production. Why would a PreToolUse hook close the gap where a stronger prompt wouldn't?","options":["A hook is code that runs deterministically on every occurrence of its event; a prompt instruction is honored with high but non-zero probability.","A hook and a stronger prompt are functionally equivalent; the gap closes only because hooks execute with lower latency than a prompt check.","A hook works because it uses a larger, more capable model internally to evaluate the refund amount than the main conversation does.","A hook only helps because it adds more few-shot examples to the effective context the model sees before approving a refund."],"correct":0,"explain":"The core distinction is the guarantee: a hook runs as code on every single occurrence, while even a strongly worded prompt instruction remains probabilistic and can still be skipped some fraction of the time. It has nothing to do with latency, a larger internal model, or effectively adding few-shot examples — those don't change the probabilistic nature of prompt-based compliance."},{"id":"q_hooks_vs_prompts_1b","q":"Which rule is the better candidate for hook enforcement rather than a prompt instruction, based on the guide's stated criterion?","options":["Formatting all monetary values in responses consistently as dollars-and-cents rather than mixing formats within one reply.","Blocking any call to process_refund until a verified customer identity has been established via get_customer.","Keeping a warm, empathetic tone in responses whenever a customer's message reads as frustrated or dissatisfied.","Preferring concise responses over long ones when the customer's question can be answered in a sentence or two."],"correct":1,"explain":"Blocking a refund until identity is verified is a hard invariant with real financial consequences if skipped — exactly the profile the guide says belongs in a hook. Formatting consistency, tone, and response length are general preferences with no comparable stakes, and are well suited to a prompt instruction instead."}]}],"reviewQuestions":[{"id":"r_hooks_vs_prompts_1","q":"A team is deciding whether a new 'always redact card numbers in logs' rule should live only in the system prompt or also be enforced with a hook. Which framing correctly applies the guide's criterion?","options":["It should be a prompt instruction only, since logging concerns are operational rather than user-facing and don't need determinism.","It should be neither — redaction is best handled by the logging infrastructure itself, entirely outside the agent's own instructions.","It should be a hook, since a redaction failure carries real compliance and safety consequences that a probabilistic instruction can't fully rule out.","It should be a prompt instruction with several carefully chosen few-shot examples of correctly redacted logs, since enough examples reliably raises compliance to effectively 100%."],"correct":2,"explain":"A redaction failure has real compliance and safety consequences, which is exactly the profile the guide says calls for a hook rather than relying on a prompt's non-zero failure rate. Calling it purely operational understates the stakes, ruling it out of the agent's responsibility ignores that the agent is what's producing the log content, and no amount of few-shot examples turns a prompt instruction into a 100% guarantee."}]},{"id":"mt_mcp_overview","type":"FACTUAL","cluster":"cl_mcp_guide","name":"MCP's three primitives","description":"Tools (actions the agent can take), resources (data the agent can read for context), and prompts (predefined templates) are the three things a Model Context Protocol server can expose, and connecting to a server makes all of them available at once.","core":true,"estMinutes":8,"evidence":["Name MCP's three primitive types and what each is for.","State what happens automatically when you connect to a new MCP server."],"assessmentPrompt":"A team wants an agent to read a database schema for context without taking any action on it. Which MCP primitive is that, and how is it different from a tool?","standards":["ccaf:d2"],"sources":[{"label":"Guide §4.1 MCP's three primitives","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L471-L478"}],"centrality":0.18,"layer":4,"x":960,"y":356,"knowledgePoints":[{"id":"kp_mcp_overview_1","name":"Tools, resources, prompts — and all-at-once discovery","keyPrerequisites":["mt_tool_use_basics"],"teach":"<p class='kp-key'>MCP defines three primitive types: <strong>tools</strong> (actions the agent can call — CRUD, API calls, commands), <strong>resources</strong> (data the agent can read for context, without taking an action), and <strong>prompts</strong> (predefined templates for common tasks).</p><p>Connecting to a server discovers all of its tools automatically, and tools from every connected server become available to the model at once — description quality across all of them is what determines how reliably they get used.</p>","questions":[{"id":"q_mcp_overview_1a","q":"A team wants an agent to read a database schema for context, without the agent taking any action on the database. Which MCP primitive is this, and how does it differ from a tool?","options":["A prompt — prompts are templates that can include schema information as part of their predefined structure for the model.","A resource — resources supply data for context without the agent invoking an action, unlike a tool, which performs one.","A tool with no side effects — a 'read-only tool' is the correct term for what MCP calls a resource in this scenario.","Neither — schema access in MCP always requires a dedicated schema-inspection tool rather than a resource of any kind."],"correct":1,"explain":"A resource is exactly this: data the agent can read for context — like a schema — without it representing an action being taken. A prompt is a template rather than raw contextual data, calling it a 'read-only tool' blurs a distinction MCP draws deliberately, and resources are precisely how schema access without action-taking is meant to work."},{"id":"q_mcp_overview_1b","q":"What happens automatically the moment an agent connects to a new MCP server?","options":["Only the server's resources become available; its tools require a separate explicit registration step by the developer.","The agent must manually enumerate the server's tools one at a time before any of them can be called.","The server's tools and resources are discovered automatically and become available for the model to use right away.","Nothing changes until the agent's system prompt is updated to explicitly list the new server's tool names."],"correct":2,"explain":"Discovery happens automatically on connection — tools (and resources) from the server become available immediately, with no separate registration step, manual enumeration, or system-prompt update required to surface them."}]}],"reviewQuestions":[{"id":"r_mcp_overview_1","q":"Why does connecting to several MCP servers at once raise the same tool-description concerns that Chapter 2 describes for tool_use in general?","options":["It doesn't — MCP servers are assumed to namespace their tools automatically behind the scenes, so overlapping descriptions across different servers can never actually cause confusion.","Because MCP tools are selected by server name rather than by description, so this concern is specific to non-MCP tools only.","Because MCP resources, not MCP tools, are the only primitive affected by description quality when multiple servers are connected.","Because tools from every connected server are available to the model together, and description overlap between any two of them causes the same misrouting."],"correct":3,"explain":"Since tools from all connected servers sit in the same pool available to the model, two similarly described tools — whether from the same server or different ones — create the exact same selection ambiguity Chapter 2 warns about. There's no automatic namespacing that prevents this, selection is still description-driven rather than server-name-driven, and the concern applies to tools, not just resources."}]},{"id":"mt_mcp_server_config","type":"PROCEDURAL","cluster":"cl_mcp_guide","name":"Project vs. user MCP configuration","description":"`.mcp.json` at the project root is version-controlled and shared with every contributor, using `${VAR}` expansion so secrets never get committed; `~/.claude.json` is personal, unshared, and meant for experimentation.","core":false,"estMinutes":8,"evidence":["State which of the two configuration files is checked into version control and which is not.","Explain how `.mcp.json` supplies secrets without committing them."],"assessmentPrompt":"A new team member clones the repo and an MCP server that everyone else has doesn't show up for them. Given that `.mcp.json` is present in the repo, what's the most likely reason, and where would you look?","standards":["ccaf:d2"],"sources":[{"label":"Guide §4.3 Project vs. user MCP configuration","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L486-L524"}],"centrality":0,"layer":5,"x":1170,"y":170,"knowledgePoints":[{"id":"kp_mcp_server_config_1","name":"Shared project config vs. personal experiments","keyPrerequisites":["mt_mcp_overview"],"teach":"<p class='kp-key'><code>.mcp.json</code> lives at the project root, is checked into version control, and uses <code>${VAR}</code> expansion so a token never gets committed alongside the config that needs it. Every contributor who clones the repo gets the same servers.</p><p><code>~/.claude.json</code> is personal — not shared via version control — and is where individual experimentation and one-off servers belong. Prefer existing community MCP servers for standard integrations (Jira, GitHub, Slack); build your own only for unique, team-specific workflows.</p>","questions":[{"id":"q_mcp_server_config_1a","q":"A new team member clones the repo, and an MCP server that everyone else has doesn't show up for them, even though .mcp.json in the repo lists it. What's the most likely explanation?","options":["Their environment is missing the environment variable .mcp.json's `${VAR}` expansion depends on for that server's credentials.",".mcp.json only applies to the contributor who originally created the file; it isn't actually shared across a team by design.","MCP servers must be re-registered manually by each new contributor even when .mcp.json already lists them correctly.","The server was defined in ~/.claude.json originally, and cloning a repository never copies another user's home directory files."],"correct":0,"explain":".mcp.json is shared and version-controlled by design, so the file itself being present is expected — a missing environment variable that its `${VAR}` expansion depends on is the most likely reason a listed server still fails to come up for a new contributor. It isn't scoped to one person, doesn't require manual re-registration, and the question already states it came from .mcp.json, not from someone's personal config."},{"id":"q_mcp_server_config_1b","q":"Where should a developer add an MCP server they're personally experimenting with, that isn't ready to be used by the rest of the team yet?","options":["In .mcp.json, since any new server should go through the shared, version-controlled configuration regardless of its maturity.","In ~/.claude.json, since it's personal, unshared configuration suited to individual experimentation before something is team-ready.","In .mcp.json, but commented out, so it's visible to the team as a preview without actually being loaded by anyone yet.","Neither file — experimental servers require a separate staging configuration file that isn't part of the documented setup."],"correct":1,"explain":"~/.claude.json is exactly the personal, unshared location meant for experimentation before something is ready for the whole team. Putting it in .mcp.json — commented out or not — pushes an unfinished server into shared, version-controlled configuration prematurely, and there's no separate staging file in this setup."}]}],"reviewQuestions":[{"id":"r_mcp_server_config_1","q":"Why does .mcp.json use `${GITHUB_TOKEN}`-style expansion instead of embedding the token value directly in the file?","options":["It's purely a stylistic convention with no functional benefit — either form works identically once the file is loaded.","Because JSON doesn't support string values longer than a certain length, so tokens must be referenced rather than inlined.","So the token itself never gets committed to version control, even though the config file that references it does.","So that different team members can each supply a different, personal value for the same variable name automatically."],"correct":2,"explain":"The whole point of ${VAR} expansion is that the secret itself lives in the environment, not the committed file — so the shared, version-controlled .mcp.json never contains a real credential. It isn't a stylistic choice with no effect, it isn't a JSON length limitation, and while different environments can supply different values, that's a side effect of the mechanism, not its stated purpose."}]},{"id":"mt_mcp_iserror","type":"CONCEPTUAL","cluster":"cl_mcp_guide","name":"isError: structured vs. generic MCP failures","description":"A structured `isError: true` response carries an error category, a retryable flag, the attempted input, and any partial results; a generic `{\"isError\": true, \"content\": \"Operation failed\"}` gives the agent nothing to decide with.","core":false,"estMinutes":8,"evidence":["List the fields a structured MCP error response should carry beyond isError itself.","Explain what decision a generic error message leaves an agent unable to make."],"assessmentPrompt":"An MCP tool fails and returns `{\"isError\": true, \"content\": \"Operation failed\"}`. What can't the calling agent decide because of this shape, and what would fix it?","standards":["ccaf:d2"],"sources":[{"label":"Guide §4.4 isError: structured vs. generic MCP failures","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L525-L554"}],"centrality":0.12,"layer":5,"x":1170,"y":108,"knowledgePoints":[{"id":"kp_mcp_iserror_1","name":"A generic error tells the agent nothing to act on","keyPrerequisites":["mt_mcp_overview"],"teach":"<p class='kp-key'>A structured error — <code>errorCategory</code>, <code>isRetryable</code>, a descriptive <code>message</code>, the <code>attempted_query</code>, and any <code>partial_results</code> — gives the agent what it needs to decide whether to retry, change its approach, or escalate.</p><p><code>{\"isError\": true, \"content\": \"Operation failed\"}</code> gives none of that. The flag says something went wrong; it says nothing about what, or what to do about it.</p>","questions":[{"id":"q_mcp_iserror_1a","q":"An MCP tool fails and returns exactly {\"isError\": true, \"content\": \"Operation failed\"}. What can't the calling agent decide because of this shape?","options":["Nothing is actually missing — isError: true is sufficient on its own for any downstream agent to proceed correctly.","Only whether the failure was transient; every other recovery decision is still fully supported by this response shape.","Only whether to escalate to a human; retry and query-adjustment decisions don't depend on the response's structure at all.","Whether to retry, adjust the request, or escalate — none of that is decidable without a category, a retryable flag, or context."],"correct":3,"explain":"Without a category, a retryable flag, the attempted input, or partial results, the agent has no basis for choosing between retrying, adjusting its approach, or escalating — the bare flag rules nothing in and nothing out. It isn't sufficient on its own, and the missing information affects every one of those recovery decisions, not just one."},{"id":"q_mcp_iserror_1b","q":"Which addition to an MCP error response most directly helps the coordinator decide whether it's worth retrying automatically?","options":["An isRetryable boolean alongside an errorCategory, explicitly signaling whether this class of failure is safe to retry.","A partial_results field on its own, since simply seeing whatever partial output exists is what determines whether attempting a retry is worthwhile.","A longer, more detailed message string describing the failure in plain language for a human reader.","The attempted_query field alone, since knowing what was attempted implies whether retrying makes sense."],"correct":0,"explain":"An explicit isRetryable flag paired with the error category is the direct signal for a retry decision — it tells the caller, not just describes the failure. A longer message helps a human read the failure but doesn't structurally signal retryability, partial_results is useful for salvaging value rather than deciding on retry, and attempted_query alone doesn't say whether retrying is safe."}]}],"reviewQuestions":[{"id":"r_mcp_iserror_1","q":"A well-structured MCP error response includes partial_results even though the call ultimately failed. What is that field for?","options":["It's a debugging artifact only, intended purely for developers reading logs later, and never meant to influence the coordinator's actual runtime decisions.","It preserves whatever useful output was produced before the failure, so the coordinator can decide to use it instead of discarding everything.","It duplicates the message field in a different format, mainly to satisfy schema validation requirements.","It exists only for failures categorized as transient; other categories never populate a partial_results field."],"correct":1,"explain":"partial_results lets a failed call still hand over whatever it managed to produce, so the coordinator can choose to salvage that value rather than treat the whole call as a total loss. It's meant to inform runtime decisions, not just debugging, isn't a redundant restatement of message, and isn't restricted to any one error category."}]},{"id":"mt_mcp_resources","type":"FACTUAL","cluster":"cl_mcp_guide","name":"Resources as a map, not a search","description":"An MCP resource — a content catalog, a database schema, a documentation set — hands the agent an immediate picture of what data exists, so it doesn't have to spend exploratory tool calls discovering the same thing.","core":false,"estMinutes":6,"evidence":["Explain what an agent has to do instead when a resource isn't available for a given dataset.","Give an example of data suited to a resource rather than a tool."],"assessmentPrompt":"Without a resource exposing the database schema, how does an agent typically end up discovering table and column names, and what does that cost compared to just reading the resource?","standards":["ccaf:d2"],"sources":[{"label":"Guide §4.5 Resources as a map, not a search","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L555-L567"}],"centrality":0,"layer":5,"x":1170,"y":232,"knowledgePoints":[{"id":"kp_mcp_resources_1","name":"A map instead of exploratory calls","keyPrerequisites":["mt_mcp_overview"],"teach":"<p class='kp-key'>A resource — a content catalog, a database schema, a documentation set, an issue summary — hands the agent an immediate picture of what data exists. Without one, the agent has to spend exploratory tool calls discovering the same structure the resource could have handed over directly.</p>","questions":[{"id":"q_mcp_resources_1a","q":"Without a resource exposing a project's database schema, how does an agent typically end up learning the table and column names it needs?","options":["It can't — without a resource, schema information is permanently inaccessible to the agent under the MCP model.","The model already has the schema memorized from its training data, so no additional discovery is required at all.","Through a series of exploratory tool calls that probe the database piece by piece to reconstruct the same structure.","It reads the schema directly from the .mcp.json configuration file, which always documents the full database structure."],"correct":2,"explain":"Absent a resource, the agent has to spend a series of exploratory tool calls piecing together the same structural picture a resource would have handed over immediately. It isn't permanently inaccessible, the model has no memorized knowledge of a specific project's live schema, and .mcp.json configures servers rather than documenting database structure."},{"id":"q_mcp_resources_1b","q":"A documentation set describing an internal API is available to an agent. Why does the guide frame this as a resource rather than a tool?","options":["Because documentation is always too large to fit a tool's input_schema, forcing it to be modeled as a resource instead.","Because tools are reserved exclusively for write operations under MCP's model, and any read-only capability must always be modeled as a resource instead.","There's no real distinction — 'resource' and 'tool' are interchangeable terms MCP uses for the same underlying concept.","Because it's data the agent reads for context rather than an action the agent takes — that distinction is what defines a resource."],"correct":3,"explain":"The tool/resource line is drawn by whether something is an action to take versus data to read for context — documentation the agent consults is squarely the latter. It isn't a matter of size, tools aren't reserved for writes only (a read-only lookup can be a tool too), and the two terms name genuinely distinct primitives."}]}],"reviewQuestions":[{"id":"r_mcp_resources_1","q":"A hierarchical list of every project task is exposed to an agent. Which MCP primitive does this best fit, and why?","options":["A resource — it's a content catalog the agent can read for orientation, not an action it takes on the project.","A tool, since 'listing tasks' sounds like an action even though nothing about the underlying data changes when it's read.","A prompt, since a hierarchical structure resembles a predefined template more closely than a data set does.","None of the three — task lists fall outside what MCP's primitives are designed to represent in any form."],"correct":0,"explain":"A content catalog like a hierarchical task list is precisely a resource — data for the agent to read and orient itself with, not an action it performs. Framing it as a tool just because 'list' sounds action-like misses that nothing is being changed, it isn't a predefined prompt template, and it fits comfortably within what MCP's resource primitive is for."}]},{"id":"mt_claudemd_hierarchy_guide","type":"FACTUAL","cluster":"cl_claude_code_guide","name":"CLAUDE.md's three levels","description":"User-level (`~/.claude/CLAUDE.md`, personal, not shared), project-level (`.claude/CLAUDE.md` or a root CLAUDE.md, version-controlled, applies to every contributor), and directory-level (scoped to that subtree) — and the classic mistake of putting shared standards at the wrong level.","core":true,"estMinutes":8,"evidence":["Match each CLAUDE.md level to whether it's shared via version control.","Diagnose why a new teammate isn't receiving project conventions that live in someone else's ~/.claude/CLAUDE.md."],"assessmentPrompt":"A new hire says they aren't seeing the team's coding standards even though a senior engineer swears they wrote them into CLAUDE.md. What level did the senior engineer most likely use by mistake?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.1 CLAUDE.md's three levels","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L572-L593"}],"centrality":0.06,"layer":2,"x":540,"y":170,"knowledgePoints":[{"id":"kp_claudemd_hierarchy_1","name":"Three levels, one classic misplacement","keyPrerequisites":["mt_message_roles"],"teach":"<p class='kp-key'>User-level (<code>~/.claude/CLAUDE.md</code>) is personal and not shared via version control. Project-level (<code>.claude/CLAUDE.md</code> or a root <code>CLAUDE.md</code>) is version-controlled and applies to every contributor. Directory-level applies only within that subtree.</p><p>The classic mistake: a team member writes project-wide standards into their own user-level file. It works for them and silently doesn't exist for anyone else, since it was never committed.</p>","questions":[{"id":"q_claudemd_hierarchy_1a","q":"A new hire says they aren't seeing the team's coding standards, even though a senior engineer insists they wrote them into CLAUDE.md. What level did the senior engineer most likely use by mistake?","options":["Directory-level, scoped to a subdirectory the new hire's work happens not to touch.","Project-level, but in a branch that hasn't been merged into the one the new hire checked out.","User-level (~/.claude/CLAUDE.md), which is personal and never shared via version control.","There's no mistake here — CLAUDE.md content is always shared automatically regardless of which level it's written at."],"correct":2,"explain":"This is the guide's named common mistake: standards meant for the whole team end up in the senior engineer's personal, unshared ~/.claude/CLAUDE.md instead of the version-controlled project level, so nobody else ever receives them. A directory-level mismatch or an unmerged branch would at least be visible in version control somewhere; a purely personal file never is."},{"id":"q_claudemd_hierarchy_1b","q":"Which CLAUDE.md level is checked into version control and applies to every contributor on the project?","options":["User-level (~/.claude/CLAUDE.md), since it's the level meant for team-wide conventions.","Only directory-level CLAUDE.md files are version-controlled; the other two levels are always personal.","None of the levels are version-controlled by default; that requires a separate, manual setup step.","Project-level (.claude/CLAUDE.md or a root CLAUDE.md)."],"correct":3,"explain":"Project-level CLAUDE.md is exactly the version-controlled, shared-with-every-contributor level. User-level is personal by definition, directory-level files are also committed but scoped narrower than 'every contributor,' and version control isn't an opt-in extra step for the project level — it's how that level is meant to work."}]}],"reviewQuestions":[{"id":"r_claudemd_hierarchy_1","q":"A convention needs to apply only when working within a specific subdirectory of a large monorepo, not to the whole project. Which CLAUDE.md level fits?","options":["Directory-level — a CLAUDE.md placed in that subdirectory applies specifically when working with files there.","User-level, since personal files can be scoped to any directory the individual chooses to work in.","Project-level, since a root CLAUDE.md is the only place conventions can be written at all.","None — subdirectory-specific conventions require .claude/rules/ with a paths glob instead of any CLAUDE.md level."],"correct":0,"explain":"Directory-level CLAUDE.md is exactly the mechanism for a convention scoped to one subtree. User-level files aren't dynamically scoped by directory, project-level is the wrong grain for a subdirectory-only rule, and while .claude/rules/ is a valid alternative organization, directory-level CLAUDE.md already covers this case directly."}]},{"id":"mt_path_imports","type":"PROCEDURAL","cluster":"cl_claude_code_guide","name":"@path imports and their limits","description":"`@path` pulls another file's content into CLAUDE.md at that point, resolved relative to the file containing the import (not the working directory), with a maximum nesting depth of five — the mechanism that keeps standards from being duplicated across every package's CLAUDE.md.","core":false,"estMinutes":6,"evidence":["State what an @path is resolved relative to.","State the maximum import nesting depth."],"assessmentPrompt":"A CLAUDE.md at `packages/api/CLAUDE.md` writes `@./standards/testing.md`. Relative to which directory does Claude Code resolve that path?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.2 @path imports and their limits","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L594-L613"}],"centrality":0,"layer":3,"x":750,"y":232,"knowledgePoints":[{"id":"kp_path_imports_1","name":"@path is resolved relative to its own file","keyPrerequisites":["mt_claudemd_hierarchy_guide"],"teach":"<p class='kp-key'><code>@path</code> pulls another file's content into CLAUDE.md at that point. It's written with <code>@</code> immediately before the path (no space), resolved <strong>relative to the file that contains the import</strong> — not the working directory — and nesting can go up to five levels deep.</p><p>This is what keeps standards from being duplicated across every package's CLAUDE.md — one canonical file, imported wherever it's needed.</p>","questions":[{"id":"q_path_imports_1a","q":"A CLAUDE.md at packages/api/CLAUDE.md contains the line `Test requirements: @./standards/testing.md`. Relative to which location does Claude Code resolve that path?","options":["Relative to the repository root, regardless of which file the @path line actually appears in.","Relative to packages/api/ — the directory containing the file where the @path import itself appears.","Relative to the current working directory the terminal happened to be in when Claude Code was launched.","Relative to ~/.claude/, since imports are always resolved against the user-level configuration directory."],"correct":1,"explain":"@path resolves relative to the file that contains the import — here, packages/api/CLAUDE.md — so ./standards/testing.md means packages/api/standards/testing.md. It isn't anchored to the repo root, the terminal's working directory, or the user-level config directory."},{"id":"q_path_imports_1b","q":"What is the maximum nesting depth for @path imports?","options":["Three levels, matching the three levels of the CLAUDE.md hierarchy itself.","There is no limit — imports may nest as deeply as the file structure requires.","Five levels.","One level — an imported file may not itself contain further @path imports."],"correct":2,"explain":"The guide states a maximum import nesting depth of five. It isn't tied to the three-level hierarchy count, it isn't unlimited, and imported files are allowed to contain their own further imports up to that five-level cap, not zero."}]}],"reviewQuestions":[{"id":"r_path_imports_1","q":"What problem does @path solve that writing the same standards text directly into every package's CLAUDE.md would create?","options":["Performance — @path imports load faster than inline text, which is the primary reason the syntax exists.","Security — @path is specifically required to prevent one package's CLAUDE.md file from ever being able to access files located outside its own directory tree.","Nothing structural — @path is purely a stylistic shorthand with no functional difference from inline text.","Duplication — the same content would need to be kept in sync across every file that repeats it, rather than living in one canonical place."],"correct":3,"explain":"Writing the same standards into every package's file means every future edit has to be repeated everywhere in sync — @path avoids that by keeping one canonical file and pulling it in wherever needed. It isn't primarily about load performance, it isn't a security boundary, and it does change behavior, not just style."}]},{"id":"mt_claude_rules_guide","type":"CONCEPTUAL","cluster":"cl_claude_code_guide","name":".claude/rules/ and path-scoped loading","description":"A rule file's YAML frontmatter `paths` glob determines when it loads — only when Claude Code touches a matching file — which saves context on irrelevant rules and lets one rule apply across scattered files (like every `*.test.ts`) regardless of directory, unlike a directory-scoped CLAUDE.md.","core":false,"estMinutes":10,"evidence":["Explain what triggers a `.claude/rules/` file to load, versus a directory-level CLAUDE.md.","Choose between `.claude/rules/` with a `paths` glob and directory-level CLAUDE.md for a convention that applies to test files scattered across many directories."],"assessmentPrompt":"Test conventions need to apply to every `*.test.tsx` file no matter which directory it lives in. Why does a `.claude/rules/testing.md` with a `paths` glob fit better here than a directory-level CLAUDE.md, and what would the directory-level version fail to cover?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.3 .claude/rules/ and path-scoped loading","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L614-L655"}],"centrality":0,"layer":3,"x":750,"y":170,"knowledgePoints":[{"id":"kp_claude_rules_1","name":"A paths glob decides whether a rule loads at all","keyPrerequisites":["mt_claudemd_hierarchy_guide"],"teach":"<p class='kp-key'>Each file under <code>.claude/rules/</code> carries YAML frontmatter with a <code>paths</code> glob. The rule loads <strong>only</strong> when Claude Code is editing a file matching that pattern — saving context on rules that don't apply right now.</p><p>Choose <code>.claude/rules/</code> with <code>paths</code> when a convention applies to files scattered across many directories by type (every <code>*.test.ts</code>, wherever it lives); choose directory-level CLAUDE.md when the convention is tied to one specific place.</p>","questions":[{"id":"q_claude_rules_1a","q":"A testing convention needs to apply to every *.test.tsx file no matter which directory it lives in. Why does .claude/rules/testing.md with a paths glob fit better than a directory-level CLAUDE.md?","options":["Because a glob-scoped rule loads by file pattern regardless of location, while a directory-level file only covers one subtree.","Because .claude/rules/ files always take priority over CLAUDE.md files whenever both happen to apply to the same file.","Because directory-level CLAUDE.md files cannot contain YAML frontmatter, which testing conventions specifically require.","There's no real difference; either mechanism covers scattered test files equally well in current versions of Claude Code."],"correct":0,"explain":"A paths glob matches by file pattern across the whole project, which is exactly what scattered *.test.tsx files need — a directory-level CLAUDE.md, by contrast, would need to exist (and be duplicated) in every directory that happens to contain a test file. It isn't about priority ordering, frontmatter isn't a CLAUDE.md limitation being worked around, and the two mechanisms are not interchangeable for this scattered case."},{"id":"q_claude_rules_1b","q":"What triggers a .claude/rules/ file to actually load into context?","options":["It loads once per session automatically, the same way project-level CLAUDE.md does, regardless of which files are touched.","Claude Code edits a file that matches the rule's paths glob in its frontmatter — otherwise the rule stays unloaded.","The user explicitly invokes it as a slash command, the same way a custom command or skill would be invoked.","It loads whenever any file in the .claude/rules/ directory itself is opened, regardless of its own paths pattern."],"correct":1,"explain":"Loading is conditional on a path match — Claude Code only pulls a rule in when it's working with a file the rule's paths glob covers, which is what saves context on irrelevant rules. It doesn't load unconditionally like a project-level CLAUDE.md, it isn't invoked like a slash command, and opening files under .claude/rules/ itself isn't what triggers loading."}]}],"reviewQuestions":[{"id":"r_claude_rules_1","q":"A convention only ever applies to files inside src/api/, and nowhere else in the project. Which is the more natural fit: directory-level CLAUDE.md in src/api/, or .claude/rules/ with a paths glob?","options":[".claude/rules/ with a paths glob — glob-based rules are always preferred over directory-level CLAUDE.md in current practice.","Neither fits; a convention scoped to one directory can only be expressed via a root-level CLAUDE.md with conditional logic.","Directory-level CLAUDE.md in src/api/ — the convention is tied to one specific place, which is exactly what that level is for.","Both are functionally impossible for this case, since neither mechanism supports scoping by directory path in this way."],"correct":2,"explain":"Since the convention is tied to one specific directory rather than a file pattern scattered across the project, directory-level CLAUDE.md in src/api/ is the more natural fit — that's precisely the distinction the guide draws. .claude/rules/ isn't universally preferred, a root CLAUDE.md with conditional logic isn't how this scoping is done, and directory-scoped conventions are exactly what directory-level CLAUDE.md supports."}]},{"id":"mt_commands_skills_guide","type":"CONCEPTUAL","cluster":"cl_claude_code_guide","name":"Commands, skills, and always-on vs. on-demand","description":"Slash commands (`.claude/commands/`) and skills (`.claude/skills/`, configured via SKILL.md frontmatter) both create `/name` invocations for on-demand tasks; CLAUDE.md stays always-loaded for standards that apply on every turn — the choice between them is invocation frequency, not capability.","core":false,"estMinutes":10,"evidence":["State the difference in when a skill's instructions load versus CLAUDE.md's.","Decide whether a one-off 'generate a test file' workflow belongs in CLAUDE.md or a skill, and why."],"assessmentPrompt":"A team keeps adding 'when generating tests, do X' instructions into their root CLAUDE.md, and the file is getting long and diluting the rules that matter every turn. What's the better home for that instruction, and why?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.4-5.5 Commands, skills, and always-on vs. on-demand","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L656-L715"}],"centrality":0,"layer":3,"x":750,"y":356,"knowledgePoints":[{"id":"kp_commands_skills_1","name":"On-demand vs. always-loaded","keyPrerequisites":["mt_claudemd_hierarchy_guide"],"teach":"<p class='kp-key'>Slash commands (<code>.claude/commands/</code>) and skills (<code>.claude/skills/</code>, configured via SKILL.md frontmatter) both create <code>/name</code> invocations for on-demand tasks. CLAUDE.md stays always-loaded for standards that should apply on every single turn.</p><p>Skill frontmatter adds real capability: <code>context: fork</code> runs the skill in an isolated subagent so its verbose output doesn't pollute the main session, <code>allowed-tools</code> restricts what it can call, and <code>argument-hint</code> prompts for a parameter when one is expected but missing.</p>","questions":[{"id":"q_commands_skills_1a","q":"A team keeps adding 'when generating tests, do X' instructions into their root CLAUDE.md, and the file is growing long enough to dilute the rules that matter on every turn. What's the better home for that instruction?","options":["A directory-level CLAUDE.md placed specifically inside the test directory, so it only loads there instead of at the root.","The user-level ~/.claude/CLAUDE.md, so it stops counting against the project-level file's growing length.","It should stay in CLAUDE.md exactly where it is; skills are only appropriate for tasks that never touch project code.","A skill invoked as /generate-tests, since test generation is an on-demand task rather than something needed on every turn."],"correct":3,"explain":"Test generation is invoked when needed, not required on every single turn — exactly the on-demand profile a skill fits, which also stops it from diluting the always-loaded rules in CLAUDE.md. Relocating it to a test directory's CLAUDE.md still keeps it always-loaded whenever that directory is touched, moving it to the user level just changes which file grows, and skills are routinely used for project-code tasks."},{"id":"q_commands_skills_1b","q":"A skill's SKILL.md frontmatter sets context: fork. What does this specifically achieve?","options":["It runs the skill in an isolated subagent so its verbose output doesn't pollute the main session's context.","It restricts which built-in tools the skill is permitted to call during its execution.","It prompts the user for a required argument whenever the skill is invoked without one.","It shares the skill with every team member automatically, regardless of where the skill file is stored."],"correct":0,"explain":"context: fork is specifically what isolates a skill's execution into a subagent so its output stays out of the main session. Restricting tools is allowed-tools' job, prompting for a missing parameter is argument-hint's job, and sharing has nothing to do with context: fork — it depends on whether the skill lives under the project's .claude/skills/ at all."}]}],"reviewQuestions":[{"id":"r_commands_skills_1","q":"What is the argument-hint field in a SKILL.md frontmatter for?","options":["It restricts the skill to a specific list of allowed tools, the same role allowed-tools plays.","It's a hint shown asking for a needed parameter when the skill is invoked without one supplied.","It documents, for other developers reading the file, what the skill's overall purpose is.","It forces the skill to always run in an isolated subagent context, the same role context: fork plays."],"correct":1,"explain":"argument-hint's job is prompting for a parameter the skill expects but wasn't given at invocation. Restricting tools is a separate field (allowed-tools), documenting purpose for other developers isn't what this frontmatter key does, and isolating execution is context: fork's role, not argument-hint's."}]},{"id":"mt_planning_mode_guide","type":"CONCEPTUAL","cluster":"cl_claude_code_guide","name":"Planning mode vs. direct execution","description":"Planning mode investigates with read-only tools and produces an approved plan before any change is made — suited to large, ambiguous, or architecturally significant changes — while direct execution suits a single-file fix with a clear cause; the Explore subagent keeps the investigation's verbose output out of the main context either way.","core":true,"estMinutes":10,"evidence":["List two situations where planning mode is warranted and one where direct execution is enough.","Explain what the Explore subagent protects the main session from during a planning-mode investigation."],"assessmentPrompt":"A request will touch roughly 45 files as part of migrating away from a deprecated library. Should this run under planning mode or direct execution, and what's the argument for that choice specifically at this scale?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.6 Planning mode vs. direct execution","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L716-L745"}],"centrality":0.06,"layer":4,"x":960,"y":418,"knowledgePoints":[{"id":"kp_planning_mode_1","name":"Investigate-only, versus investigate-and-act","keyPrerequisites":["mt_agentic_loop_guide"],"teach":"<p class='kp-key'>Planning mode investigates with read-only tools (Read, Grep, Glob) and produces a plan the user approves before anything changes — suited to large, ambiguous, or architecturally significant work, or an unfamiliar codebase. Direct execution suits a single-file fix with a clear cause.</p><p>The Explore subagent isolates a planning-mode investigation's verbose output from the main context, returning only a summary — preventing context exhaustion during a multi-phase investigation.</p>","questions":[{"id":"q_planning_mode_1a","q":"A request will touch roughly 45 files as part of migrating away from a deprecated library. Should this run under planning mode or direct execution?","options":["Direct execution — since the target library to migrate away from is already known, there's nothing left worth planning regardless of how many files end up affected.","Direct execution — planning mode is reserved specifically for cases involving unfamiliar codebases, not migrations in familiar ones.","Planning mode — the scale (45 files) is exactly the kind of large, multi-file change that warrants investigation and an approved plan first.","Neither is clearly better; the choice makes no practical difference once the target library to migrate away from is already decided."],"correct":2,"explain":"A 45-file library migration is exactly the scale the guide flags for planning mode — large changes benefit from investigation and an approved plan before anything is touched, regardless of whether the target library is already decided. Planning mode isn't limited to unfamiliar codebases specifically, and the scale here does make a practical difference to how safely the change can proceed."},{"id":"q_planning_mode_1b","q":"What does the Explore subagent specifically protect during a planning-mode investigation?","options":["It protects the codebase from being modified by ensuring only read-only tools are ever available anywhere in the session.","It protects the user from needing to approve a plan, by making planning mode fully autonomous end to end.","It protects against merge conflicts by locking the files under investigation until the plan is approved.","It protects the main session's context from being filled with the verbose output of a multi-phase investigation."],"correct":3,"explain":"The Explore subagent's specific job is keeping the main session's context from being consumed by an investigation's verbose output, returning only a summary instead. Read-only tool restriction is a planning-mode-wide property, not specifically the Explore subagent's contribution; user approval still remains part of the workflow; and nothing here involves file locking."}]}],"reviewQuestions":[{"id":"r_planning_mode_1","q":"A bug report includes a clear stack trace pointing to a single line in one file, and the fix is an obvious one-line change. Which approach fits?","options":["Direct execution — a single-file fix with a clear, well-understood cause doesn't need an investigation phase first.","Planning mode, since any change to production code should always go through an investigation-and-approval step first.","Planning mode, specifically to invoke the Explore subagent so the fix's verbose diff doesn't pollute the session.","Neither — a change this small should bypass both planning mode and direct execution via a raw shell command instead."],"correct":0,"explain":"A single-file fix with a clear cause is exactly the profile the guide gives for direct execution — no ambiguity to investigate, no architectural decision to weigh. Requiring planning mode for every change overstates its purpose, invoking Explore for a one-line diff misapplies a tool meant for larger investigations, and there's no third 'raw shell command' pathway being described here."}]},{"id":"mt_compact_memory","type":"FACTUAL","cluster":"cl_claude_code_guide","name":"/compact and /memory","description":"`/compact` summarizes prior history to reclaim context window space, at the risk of losing exact numbers and dates in the process; `/memory` opens CLAUDE.md itself for editing so notes and preferences persist automatically into future sessions.","core":false,"estMinutes":6,"evidence":["State what /compact risks losing when it summarizes history.","Explain what /memory actually edits and why that makes it persist across sessions."],"assessmentPrompt":"After a long investigation session runs /compact to free up context, a fact that was previously '42% of requests' now reads as 'a large share of requests.' What happened, and which command caused it?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.7-5.8 /compact and /memory","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L746-L760"}],"centrality":0,"layer":3,"x":750,"y":294,"knowledgePoints":[{"id":"kp_compact_memory_1","name":"/compact frees space; /memory persists it","keyPrerequisites":["mt_context_window_problems"],"teach":"<p class='kp-key'><code>/compact</code> summarizes prior history to reclaim context window space during long sessions — at the cost of the same progressive-summarization risk from Chapter 1: exact numbers and dates can go vague.</p><p><code>/memory</code> opens CLAUDE.md itself for editing, so notes, preferences, and current work context persist and are automatically loaded into future sessions — an alternative to re-explaining the same instructions every time.</p>","questions":[{"id":"q_compact_memory_1a","q":"After a long investigation session runs /compact to free up context, a fact that used to read '42% of requests' now reads 'a large share of requests.' What happened?","options":["/memory silently rewrote the fact when it saved the session's notes into CLAUDE.md for future reference.","/compact's history summarization lost the exact figure, the same progressive-summarization risk the guide warns about.","This indicates a bug — /compact is guaranteed never to alter any specific numeric values during summarization.","The vague phrasing came from the original tool call, not from any compression step run afterward in the session."],"correct":1,"explain":"This is exactly the risk /compact carries — summarizing history can drop exact figures and replace them with vague language, which is a known trade-off, not a bug. /memory edits CLAUDE.md rather than summarizing session history, and the scenario specifically attributes the change to running /compact, not to how the original figure was first produced."},{"id":"q_compact_memory_1b","q":"What does /memory actually do, and why does that make it persist across sessions?","options":["It saves a snapshot of the current conversation to a temporary file, which the next session may optionally choose to load.","It compresses the current session's history the same way /compact does, but stores the result instead of discarding it.","It opens CLAUDE.md for editing, and since CLAUDE.md is loaded automatically every session, whatever's written there persists.","It sends the current context to Anthropic's servers for long-term storage, retrievable by name in any future session."],"correct":2,"explain":"/memory's mechanism is direct: it opens CLAUDE.md itself for editing, and because CLAUDE.md is automatically loaded at the start of every session, anything written there is available going forward. It isn't a temporary snapshot, it isn't the same operation as /compact with different storage, and nothing about it involves server-side conversation storage."}]}],"reviewQuestions":[{"id":"r_compact_memory_1","q":"A developer wants a project convention they keep re-explaining every session to instead persist automatically. Which command addresses this, and why not the other one?","options":["/compact, since freeing up context space is what allows more room for standing instructions to be honored going forward.","Either command works equally well here, since both /compact and /memory ultimately affect what content ends up available to the model in future sessions.","Neither — persisting a standing convention requires a dedicated configuration file outside of both /compact and /memory.","/memory, since it edits CLAUDE.md directly, which loads automatically every session — /compact only summarizes existing history, it doesn't persist anything new."],"correct":3,"explain":"/memory is the right tool because it edits CLAUDE.md, which is exactly what's auto-loaded every session — /compact only summarizes what's already in the current session's history and has no role in carrying a new standing instruction forward. The two commands aren't interchangeable, and CLAUDE.md itself, edited via /memory, is that dedicated persistence mechanism."}]},{"id":"mt_cli_ci_sessions","type":"PROCEDURAL","cluster":"cl_claude_code_guide","name":"Headless CI usage and session forking","description":"`-p`/`--print` runs Claude Code non-interactively for CI, `--output-format json` plus `--json-schema` makes the result machine-parseable for automated PR comments, `--resume` continues a named session (risking stale tool results if files changed since), and `fork_session` branches a shared investigation into independently diverging approaches.","core":false,"estMinutes":12,"evidence":["Explain why `-p` is the only correct way to run Claude Code inside a CI pipeline.","Distinguish when to use --resume versus when to start a fresh session with a short summary instead."],"assessmentPrompt":"A CI pipeline needs a PR reviewed and the result posted as inline comments automatically, with no human present to respond to prompts. Which flags make this possible, and what does each one contribute?","standards":["ccaf:d4"],"sources":[{"label":"Guide §5.9-5.10 Headless CI usage and session forking","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L761-L821"}],"centrality":0,"layer":5,"x":1170,"y":294,"knowledgePoints":[{"id":"kp_cli_ci_1","name":"Headless mode's flags, and what each contributes","keyPrerequisites":["mt_planning_mode_guide"],"teach":"<p class='kp-key'><code>-p</code>/<code>--print</code> is the only correct way to run Claude Code in CI: non-interactive, processes the prompt, prints to stdout, exits — it never waits for input. <code>--output-format json</code> plus <code>--json-schema</code> makes the result machine-parseable, so a pipeline can post inline PR comments automatically.</p><p>Reviewing your own generated code with the same session is weaker than an independent instance, since the original session is less likely to challenge its own reasoning — and re-reviews should include prior results so only new or unresolved issues get reported.</p>","questions":[{"id":"q_cli_ci_1a","q":"A CI pipeline needs a PR reviewed with the result posted as inline comments automatically, with no human present to respond to any prompts. Which flags make this possible?","options":["-p (or --print) for non-interactive execution, combined with --output-format json and --json-schema for a parseable result.","--resume paired with a named session, so the same reviewing session can be reused across every subsequent CI run.","fork_session, so the review can branch off from the code-generation session that produced the pull request originally.","--output-format json alone; -p is only needed for local interactive use and has no bearing on CI-specific behavior."],"correct":0,"explain":"-p is what makes the run non-interactive at all — required for any unattended CI use — and --output-format json with --json-schema is what turns the result into something a script can parse and post as inline comments. --resume and fork_session are session-continuation tools, not what enables headless execution or structured output, and --output-format alone doesn't address the interactive-vs-headless problem -p solves."},{"id":"q_cli_ci_1b","q":"Why does the guide recommend reviewing generated code with an independent Claude Code instance rather than the same session that wrote it?","options":["Because the same session runs measurably slower on a review pass than a freshly started one would.","Because the same session retains its original reasoning and is less likely to challenge decisions it already made.","Because a single session is technically limited to one file per invocation, which a full PR review would exceed.","There's no real reason given — the recommendation is a stylistic preference with no effect on review quality."],"correct":1,"explain":"The concern is that the same session already committed to its own reasoning while writing the code, making it less likely to critically re-examine those same decisions — an independent instance has no such attachment. It isn't about execution speed, isn't a file-count limitation, and the guide gives this as a real quality concern, not a stylistic preference."}]}],"reviewQuestions":[{"id":"r_cli_ci_1","q":"A CI pipeline re-reviews a pull request after new commits are pushed, and the same issues keep getting reported as inline comments every time, even ones already acknowledged. What should the pipeline change?","options":["Switch from -p to --resume, so the review session remembers what it already flagged across every CI run automatically.","Nothing — repeating the same findings on every commit is expected and intentional behavior for a CI review pipeline.","Include the prior review's results in the new prompt's context and instruct Claude to report only new or unresolved issues.","Disable --json-schema, since structured output is what's causing the same issues to be re-serialized on every run."],"correct":2,"explain":"The fix is exactly what the guide describes: feed the prior review's results into the new prompt's context and instruct the model to report only what's new or still unresolved. Switching to --resume conflates session continuation with this filtering problem, repeating identical comments isn't the intended behavior, and --json-schema controls output shape, not which issues get re-reported."}]},{"id":"mt_few_shot_patterns","type":"CONCEPTUAL","cluster":"cl_prompt_eng_advanced","name":"Few-shot examples over vague descriptions","description":"Two to four input/output examples remove the ambiguity a phrase like 'be more precise' leaves open — especially for informal measurements, mixed citation formats, and acceptable-vs-flagged code — and normalization rules in the prompt (dates to ISO 8601, currency to amount+code) prevent the JSON from being syntactically valid but semantically inconsistent.","core":false,"estMinutes":12,"evidence":["Explain why an example resolves ambiguity that a text instruction leaves open.","Give an example of a normalization rule that prevents a semantic (not syntax) error."],"assessmentPrompt":"An extraction prompt keeps returning dates in whatever format the source text used ('next Tuesday,' 'March 5th,' '03/05'). What kind of prompt addition fixes this, and why is a stronger instruction alone less reliable than showing worked examples?","standards":["ccaf:d3"],"sources":[{"label":"Guide §6.1 Few-shot examples over vague descriptions","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L827-L907"}],"centrality":0.1,"layer":5,"x":1170,"y":356,"knowledgePoints":[{"id":"kp_few_shot_1","name":"Examples remove ambiguity a description leaves open","keyPrerequisites":["mt_schema_design_errors"],"teach":"<p class='kp-key'>2–4 input/output examples show, rather than describe, expected behavior — and the model generalizes the pattern rather than repeating the examples verbatim. They're especially strong for informal measurements ('a pinch of salt'), mixed citation formats, and acceptable-vs-flagged code, where a rule alone is too varied to state cleanly.</p><p>Pair examples with explicit normalization rules (dates to ISO 8601, currency to amount+code) so the output stays syntactically valid <em>and</em> semantically consistent — a normalization rule prevents the specific class of semantic error a schema alone can't catch.</p>","questions":[{"id":"q_few_shot_1a","q":"An extraction prompt keeps returning dates in whatever format the source text used ('next Tuesday,' 'March 5th,' '03/05'). What kind of prompt addition fixes this most directly?","options":["A stronger single-sentence instruction: 'Always use standard date formatting,' left otherwise unchanged.","A normalization rule plus worked examples showing informal date phrases converted to ISO 8601 explicitly.","Switching the date field's JSON schema type from string to a dedicated 'date' type, which doesn't exist in JSON Schema.","Removing the date field from the schema entirely, since informal date phrasing can't be reliably normalized at all."],"correct":1,"explain":"A concrete normalization rule paired with worked examples is exactly what resolves this — it shows the model the expected transformation rather than leaving 'standard formatting' open to interpretation. A vague instruction alone repeats the ambiguity, JSON Schema has no native date type, and dropping the field discards the data instead of normalizing it."},{"id":"q_few_shot_1b","q":"Why are few-shot examples especially effective for extracting informal measurements like 'a pinch of salt' or 'two handfuls of rice'?","options":["Because the model cannot process any numeric quantity unless it first sees a worked example demonstrating the format.","Because JSON Schema requires an explicit enum of every possible measurement phrase to validate the extracted output.","Because informal measurement units are too diverse and inconsistent for a rule-based instruction to enumerate cleanly.","Because informal measurements only ever appear in cooking-related text, which the model handles as a special case."],"correct":2,"explain":"The diversity of informal phrasing is exactly why a rule can't cover every case cleanly — examples let the model generalize the pattern (approximate value + original text + a precision flag) instead. The model can handle numeric quantities without an example present at all, JSON Schema doesn't require enumerating every phrase, and this isn't restricted to cooking-domain text specifically."}]}],"reviewQuestions":[{"id":"r_few_shot_1","q":"A prompt provides three examples distinguishing acceptable code from code that should be flagged in review. What does the model do with these examples, according to the guide?","options":["It repeats the examples' exact code back whenever a similar-looking snippet appears in the file being reviewed.","It ignores the examples unless the reviewed code is byte-for-byte identical to one of the three shown.","It treats the examples purely as documentation and does not factor them into its actual review decisions.","It generalizes the underlying decision logic the examples demonstrate, applying it to new code it hasn't seen before."],"correct":3,"explain":"The guide is explicit that the model generalizes the pattern the examples demonstrate rather than just repeating them — that generalization to new, unseen code is the whole value of few-shot prompting. It doesn't require an exact match to apply what it learned, and it does factor the examples into its actual decisions rather than treating them as inert documentation."}]},{"id":"mt_explicit_criteria","type":"CONCEPTUAL","cluster":"cl_prompt_eng_advanced","name":"Explicit criteria replace vague instructions","description":"'Be conservative, report only high-confidence findings' is not a criterion; a numbered list of exactly what qualifies (and an explicit not-this list) is — the same discipline extends to severity levels, where each tier gets a concrete example rather than a one-word label.","core":true,"estMinutes":10,"evidence":["Rewrite a vague review instruction as a numbered, falsifiable criterion.","Explain why pairing each severity tier with a concrete example reduces disagreement between runs."],"assessmentPrompt":"A code-review prompt says 'flag comments that seem outdated.' Two runs on the same file disagree about which comments qualify. What's missing from the instruction, and what would an explicit-criteria version look like?","standards":["ccaf:d3"],"sources":[{"label":"Guide §6.2 Explicit criteria replace vague instructions","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L908-L946"}],"centrality":0.08,"layer":6,"x":1380,"y":108,"knowledgePoints":[{"id":"kp_explicit_criteria_1","name":"A criterion is falsifiable; a vibe isn't","keyPrerequisites":["mt_few_shot_patterns"],"teach":"<p class='kp-key'>'Be conservative, report only high-confidence findings' sounds like a rule but isn't one — nothing in it tells you whether a specific finding qualifies. A numbered list of exactly what qualifies, paired with an explicit not-this list, is a criterion. The same discipline applies to severity: pairing each tier with a concrete example (a null pointer during payment vs. a stylistic nit) removes the ambiguity a bare label like 'critical' leaves open.</p>","questions":[{"id":"q_explicit_criteria_1a","q":"A code-review prompt says 'flag comments that seem outdated.' Two separate runs on the same file disagree on which comments qualify. What's missing from the instruction?","options":["A numbered, falsifiable list of exactly what makes a comment outdated, plus an explicit list of what doesn't qualify.","A larger context window, so the model can see more of the file at once before making its judgment on each comment.","A few-shot example is unnecessary here; the fix is purely a matter of running the same prompt multiple times and averaging.","A stricter JSON schema for the output, since the disagreement is a structural problem rather than a criteria problem."],"correct":0,"explain":"'Seems outdated' has no falsifiable boundary — the fix is exactly what explicit criteria supplies: a numbered list of what qualifies and an explicit not-this list, removing the ambiguity that produced disagreement between runs. Context window size isn't the bottleneck here, averaging multiple runs papers over the ambiguity rather than resolving it, and the output's JSON structure isn't what's in question — its judgment criteria are."},{"id":"q_explicit_criteria_1b","q":"Why does pairing each severity tier (CRITICAL, HIGH, MEDIUM, LOW) with a concrete example reduce disagreement between separate review runs?","options":["It doesn't reduce disagreement measurably; severity labeling is inherently subjective regardless of any examples given.","Because a concrete example anchors an otherwise abstract label to a specific, checkable case the model can compare against.","Because the examples are counted toward a numeric score that mechanically determines severity, bypassing judgment entirely.","Because listing examples lets the model skip severity classification whenever the input doesn't exactly match one of them."],"correct":1,"explain":"A worked example ('CRITICAL: a null pointer during payment processing') gives the model a concrete anchor to compare a new case against, instead of interpreting an abstract word like 'critical' from scratch each time — that's what narrows disagreement between runs. It isn't a scoring mechanism bypassing judgment, and it doesn't license skipping classification for non-matching cases."}]}],"reviewQuestions":[{"id":"r_explicit_criteria_1","q":"Which instruction is an example of explicit criteria rather than a vague instruction?","options":["\"Be thorough but reasonable when flagging issues in the code under review.\"","\"Use good judgment about which findings are worth reporting to the team.\"","\"Flag a comment as problematic ONLY if it contradicts the actual code, references a non-existent function, or is a resolved TODO.\"","\"Try to focus mainly on the more serious issues rather than minor stylistic ones, where possible.\""],"correct":2,"explain":"The numbered list of exactly what qualifies is falsifiable — a given comment either matches one of the three conditions or it doesn't. 'Thorough but reasonable,' 'good judgment,' and 'mainly... where possible' are all open to interpretation in the same way the guide's 'be conservative' example is, with nothing concrete to check a specific case against."}]},{"id":"mt_prompt_chaining_vs_dynamic","type":"CONCEPTUAL","cluster":"cl_prompt_eng_advanced","name":"Prompt chaining vs. dynamic decomposition","description":"Chaining runs a fixed sequence of focused steps (one file at a time, then an integration pass) to avoid attention dilution on predictable, repeatable tasks; dynamic decomposition generates its next subtask from the current one's results, for open-ended investigations where the scope isn't known up front.","core":false,"estMinutes":10,"evidence":["Explain what attention dilution is and why splitting a task into a chain avoids it.","Decide between chaining and dynamic decomposition for a task whose subtasks only become clear mid-investigation."],"assessmentPrompt":"A 14-file pull request gets reviewed in a single prompt, and the review comments a clear issue in one file while missing an equally clear one in another. What phenomenon is this, and which strategy — chaining or dynamic decomposition — is the fix, and why that one specifically?","standards":["ccaf:d3"],"sources":[{"label":"Guide §6.3 Prompt chaining vs. dynamic decomposition","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L947-L970"}],"centrality":0.04,"layer":7,"x":1590,"y":46,"knowledgePoints":[{"id":"kp_prompt_chaining_1","name":"Attention dilution is why chaining exists","keyPrerequisites":["mt_explicit_criteria"],"teach":"<p class='kp-key'>Reviewing many files in one prompt causes attention dilution: deep analysis on some files, shallow commentary on others, inconsistent flagging of the same pattern across files. Chaining — one focused step per file, then a separate integration pass for cross-file issues — keeps quality consistent because each step only has one thing to look at.</p><p>Chaining suits predictable, repeatable tasks where all steps are known up front; dynamic decomposition suits open-ended investigations where the next subtask only becomes clear from the current one's results.</p>","questions":[{"id":"q_prompt_chaining_1a","q":"A 14-file pull request gets reviewed in a single prompt. The review flags a clear issue in one file but approves the identical pattern in another. What phenomenon is this, and which strategy fixes it?","options":["Model drift — the fix is re-running the exact same single prompt a second time and taking the more thorough of the two results.","A schema defect — the fix is tightening the output JSON schema so inconsistent findings can't be represented at all.","Insufficient context window — the fix is upgrading to a model with a larger context window and keeping the single-prompt approach.","Attention dilution — the fix is prompt chaining: one focused pass per file, then a separate integration pass for cross-file issues."],"correct":3,"explain":"Inconsistent flagging of the same pattern across files is the textbook symptom of attention dilution, and prompt chaining — one file per focused pass, then integration — is exactly the guide's fix, since each pass only has one thing to look at. Re-running the same prompt doesn't address the underlying cause, this isn't a schema representation problem, and a bigger context window doesn't fix inconsistent attention across a wide single prompt."},{"id":"q_prompt_chaining_1b","q":"Which task is a better fit for prompt chaining than for dynamic decomposition?","options":["A recurring, template-shaped code review where the same sequence of steps applies to every pull request.","An open-ended investigation into an unfamiliar codebase where the scope isn't known until discovery is underway.","A task whose next subtask can only be identified from what the current step happens to find.","A task where the full scope of work only becomes clear midway through the investigation itself."],"correct":0,"explain":"A recurring, template-shaped task with a known, repeatable sequence of steps is exactly what prompt chaining is built for — stability and reproducibility from a fixed plan. The other three all describe scope becoming clear only during execution, which is the profile dynamic decomposition is suited to instead."}]}],"reviewQuestions":[{"id":"r_prompt_chaining_1","q":"Why does a fixed pipeline like prompt chaining offer more reproducibility than dynamic decomposition, according to the guide?","options":["It doesn't — both strategies produce equally reproducible results regardless of how the subtasks are determined.","Because the steps are all defined in advance, so the same sequence runs every time rather than being generated on the fly per run.","Because dynamic decomposition is deprecated in favor of chaining for all but the smallest possible tasks.","Because chaining always ends up using fewer total steps than dynamic decomposition would, regardless of the task's actual underlying complexity."],"correct":1,"explain":"Reproducibility comes from every step being fixed up front, so the same sequence of prompts runs every time rather than being freshly generated based on intermediate results — that's the trade chaining makes for stability. The two strategies are not equally reproducible, dynamic decomposition isn't deprecated, and step count isn't what drives the reproducibility difference."}]},{"id":"mt_interview_pattern_guide","type":"PROCEDURAL","cluster":"cl_prompt_eng_advanced","name":"The interview pattern","description":"Before implementing, Claude asks clarifying questions when the domain is unfamiliar, the implications are non-obvious (cache invalidation strategy, failure-mode tolerance), or multiple viable approaches exist and the right one depends on context it doesn't have yet.","core":false,"estMinutes":8,"evidence":["List two conditions under which asking clarifying questions before implementing is warranted.","Explain why the interview pattern is inappropriate for a well-understood, single-approach task."],"assessmentPrompt":"A request to 'add caching to the API' arrives with no detail on invalidation strategy, staleness tolerance, or scope (per-user vs. global). Why does the interview pattern fit here, and what would asking nothing upfront risk?","standards":["ccaf:d3"],"sources":[{"label":"Guide §6.4 The interview pattern","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L971-L987"}],"centrality":0,"layer":7,"x":1590,"y":108,"knowledgePoints":[{"id":"kp_interview_pattern_1","name":"Ask before implementing when the answer isn't yours to guess","keyPrerequisites":["mt_explicit_criteria"],"teach":"<p class='kp-key'>Before implementing, Claude asks clarifying questions when the domain is unfamiliar (fintech, healthcare), the implications are non-obvious (cache invalidation strategy, acceptable staleness), or several viable approaches exist and the right one depends on context it hasn't been given.</p><p>It's the wrong move for a well-understood, single-approach task — asking questions there just adds a round trip with nothing genuinely uncertain to resolve.</p>","questions":[{"id":"q_interview_pattern_1a","q":"A request to 'add caching to the API' arrives with no detail on invalidation strategy, staleness tolerance, or scope (per-user vs. global). Why does the interview pattern fit here?","options":["It doesn't fit — caching is a well-understood, single-approach problem that should be implemented directly without questions.","Because the interview pattern is required by policy for any request involving performance-related infrastructure changes.","Because several viable approaches exist here and the right one depends on context (invalidation strategy, staleness tolerance) not yet supplied.","Because asking clarifying questions first is always the safer choice than implementing anything, regardless of how well-specified the original request already is."],"correct":2,"explain":"Caching genuinely has multiple viable designs, and the right one depends on specifics (invalidation strategy, tolerance for stale data, scope) the request hasn't supplied — exactly the condition the guide gives for asking first. It isn't a single-approach problem, there's no blanket policy tied to 'performance' as a category, and the guide doesn't treat asking-first as universally safer regardless of how well-specified a request already is."},{"id":"q_interview_pattern_1b","q":"A bug report includes a clear stack trace and an obvious one-line fix. Why would applying the interview pattern here be a poor fit?","options":["Because the interview pattern can only be used for architectural decisions, never for anything classified as a bug fix.","Because interview-style questions are exclusively reserved for greenfield projects, not fixes to existing codebases.","Because a one-line fix is too small in scope for the interview pattern's clarifying-questions mechanism to apply at all.","Because there is nothing genuinely uncertain to resolve — asking questions here just adds a round trip without added value."],"correct":3,"explain":"The interview pattern earns its cost by resolving real ambiguity — a well-understood fix with an obvious cause has none, so asking first just delays the fix without buying any better outcome. It isn't restricted to architectural work, isn't restricted to greenfield projects, and there's no hard size threshold the pattern is gated on."}]}],"reviewQuestions":[{"id":"r_interview_pattern_1","q":"Which condition, on its own, most clearly warrants using the interview pattern before implementing?","options":["The task involves an unfamiliar domain (e.g., healthcare compliance) with non-obvious implications the requester hasn't addressed.","The requester happened to use a slightly informal tone when phrasing their request, rather than writing out a fully detailed specification.","The task is expected to take more than a few minutes of implementation time once the approach has been decided.","The requester is a new team member rather than someone who has worked with the codebase for a long time."],"correct":0,"explain":"An unfamiliar domain with non-obvious implications is exactly the condition the guide gives for asking clarifying questions first — the risk of guessing wrong is real and consequential. Informal phrasing, raw implementation time, and the requester's tenure are not the criteria the guide ties the interview pattern to."}]},{"id":"mt_validation_retry_selfcorrect","type":"CONCEPTUAL","cluster":"cl_prompt_eng_advanced","name":"Validate, retry with feedback, self-correct","description":"A retry that includes the original document, the prior (wrong) extraction, and the specific error ('total=150 but line items sum to 145') fixes format and structural errors and arithmetic slips; it cannot fix data that's genuinely absent from the source or context that lives in a document never provided — and a self-correction pattern that asks the model to compute and compare both a stated and calculated value catches the same class of error before you even retry.","core":false,"estMinutes":12,"evidence":["List what a retry-with-feedback prompt must include beyond just 'try again.'","Give an example of a validation failure that retrying will not fix, and explain why."],"assessmentPrompt":"Extraction validation fails because a total doesn't match the sum of line items. A retry prompt is built that just says 'the previous answer was wrong, try again.' What's missing from it, and why would that omission make the retry no more likely to succeed than the first attempt?","standards":["ccaf:d3"],"sources":[{"label":"Guide §6.5-6.6 Validate, retry with feedback, self-correct","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L988-L1036"}],"centrality":0,"layer":5,"x":1170,"y":418,"knowledgePoints":[{"id":"kp_validation_retry_1","name":"Retry with the specific error, not just 'try again'","keyPrerequisites":["mt_schema_design_errors"],"teach":"<p class='kp-key'>A useful retry includes the original document, the previous (wrong) extraction, and the specific error ('total=150 but line items sum to 145') — enough for the model to actually fix what went wrong. Retry works for format errors, structural errors, and arithmetic slips the model can re-check; it does not work when the needed information is genuinely absent from the source or lives in a document that was never provided.</p><p>Self-correction extracts both a stated value and a separately computed one, flagging <code>conflict_detected</code> when they differ — catching the same class of arithmetic error before a human, or a retry loop, ever has to.</p>","questions":[{"id":"q_validation_retry_1a","q":"A total doesn't match the sum of line items. A retry prompt is built that says only 'the previous answer was wrong, try again.' Why is this retry no more likely to succeed than the first attempt?","options":["Retries never help with arithmetic inconsistencies under any circumstances, regardless of what context is included with them.","It's missing the original document, the prior extraction, and the specific discrepancy — without them, the model has no new information to act on.","The retry should have used a different model entirely; the same model repeating a task never produces a different result.","The prompt is actually fine as written; the real issue is that retries always require a JSON schema, which this extraction task didn't originally specify at all."],"correct":1,"explain":"A bare 'try again' gives the model nothing beyond what it already had — it needs the source document, its own prior (wrong) output, and the concrete discrepancy to actually diagnose and fix the error. Arithmetic slips are precisely the kind of thing a well-formed retry *can* fix, switching models isn't the guide's answer to this problem, and the schema's presence or absence isn't what's missing here."},{"id":"q_validation_retry_1b","q":"Which validation failure will a retry-with-feedback loop most likely fail to resolve, no matter how well the retry prompt is constructed?","options":["A date field returned in the wrong format relative to the schema's expected ISO 8601 structure.","A required total that doesn't match the sum of the line items provided in the same extraction.","A field whose value is genuinely absent from the source document and was never supplied anywhere.","A field placed at the wrong nesting level relative to where the schema expects it to appear."],"correct":2,"explain":"No amount of re-prompting can produce a value the source document simply never contained — that's the one class of failure retries can't fix by design. Format errors, arithmetic mismatches, and structural misplacement are all things the model can genuinely re-check and correct given the right feedback."}]}],"reviewQuestions":[{"id":"r_validation_retry_1","q":"A self-correction pattern has the model output both a stated_total and a separately calculated_total field, plus a conflict_detected boolean. What does this achieve?","options":["It duplicates work the schema's required-field validation already performs, adding no benefit that plain validation didn't provide.","It guarantees the extraction is now fully correct, since the model has effectively double-checked its own output.","It only matters for currency fields; the pattern provides no benefit for other kinds of numeric extraction.","It surfaces an internal arithmetic inconsistency the moment it's produced, before a retry loop or a human even needs to catch it."],"correct":3,"explain":"Having the model compute and compare two independently derived values surfaces a conflict at generation time, catching the same class of arithmetic error earlier than waiting for a downstream retry or human review to notice it. It isn't redundant with schema validation (which only checks structure/type), it doesn't guarantee full correctness, and nothing restricts the pattern to currency specifically."}]},{"id":"mt_batches_overview","type":"FACTUAL","cluster":"cl_batches","name":"The Batches API's trade-offs","description":"A 50% cost saving against a processing window of up to 24 hours with no latency SLA, no multi-turn tool calling (one request in, one response out), correlated to your own data via `custom_id` — and choosing it comes down to whether anyone is waiting on the result right now.","core":false,"estMinutes":8,"evidence":["State the batch API's cost saving and its processing-time ceiling.","Decide between the Batch API and the synchronous API for a task where a developer is waiting on the result."],"assessmentPrompt":"A weekly security audit and a pre-merge PR check both need Claude's help. Which one uses the Batch API, and what property of the other one rules batching out?","standards":["ccaf:d3"],"sources":[{"label":"Guide §7.1-7.2 The Batches API's trade-offs","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1041-L1061"}],"centrality":0.04,"layer":1,"x":330,"y":108,"knowledgePoints":[{"id":"kp_batches_overview_1","name":"50% cheaper, up to 24 hours, no one waiting","keyPrerequisites":["mt_api_request_structure"],"teach":"<p class='kp-key'>The Batch API trades latency for cost: 50% savings against a processing window of up to 24 hours with no latency SLA, and no multi-turn tool calling — one request in, one response out. The deciding question is whether anyone is waiting on the result right now.</p>","questions":[{"id":"q_batches_overview_1a","q":"A weekly security audit and a pre-merge PR check both need Claude's involvement. Which one uses the Batch API, and what property of the other rules batching out?","options":["The PR check uses batching; the weekly audit is ruled out because it needs multi-turn tool calling the Batch API doesn't support.","Both use batching equally well; cost savings apply the same way regardless of whether a human is waiting on the result.","Neither uses batching; the Batch API is reserved for workloads with no near-term deadline of any kind, which excludes both cases.","The weekly audit uses batching; the PR check is ruled out because a developer is actively waiting and can't accept a 24-hour window."],"correct":3,"explain":"The weekly audit isn't time-sensitive, so the 50% saving is worth accepting a window that could take up to 24 hours; the pre-merge check has a developer actively waiting, and that's exactly the property (someone waiting right now) that rules batching out. The 24-hour ceiling, not a tool-calling limitation specifically, is what makes it wrong for the PR check, and a 'no deadline of any kind' framing goes further than what actually disqualifies a workload."},{"id":"q_batches_overview_1b","q":"Which limitation is specific to the Message Batches API compared to the synchronous Messages API?","options":["It does not support multi-turn tool calling — each batched request is one request in, one response out.","It cannot process more than a handful of requests in a single submission, unlike the synchronous API's higher throughput.","It cannot use tool_use at all, restricting batched requests to plain text generation only.","It requires a different model family entirely, since batch-compatible models are versioned separately from synchronous ones."],"correct":0,"explain":"The batch API's stated multi-turn limitation is exactly this: one request produces one response, with no back-and-forth tool-calling loop within a single batched item. It's actually built for high-volume submissions rather than being limited to a handful, it does support tool_use as a single-turn request, and it doesn't require a separate model family."}]}],"reviewQuestions":[{"id":"r_batches_overview_1","q":"Processing 10,000 documents overnight for a report needed by morning is a commonly cited use case for which API, and why?","options":["The synchronous API, since only it can process a volume as large as 10,000 documents within a single night.","The Batch API, since the deadline (by morning) comfortably fits its up-to-24-hour window and the 50% saving is worth taking.","The synchronous API, since the Batch API's lack of an SLA guarantee makes it unsuitable for any deadline-bound task.","Neither — a workload of this size and cadence should be split across multiple providers rather than one Batch API call."],"correct":1,"explain":"An overnight-to-morning deadline comfortably fits within the batch API's up-to-24-hour window, and with nobody waiting in real time, the 50% cost saving is the natural choice — this is one of the guide's own example use cases. The synchronous API isn't required to handle this volume, the lack of an SLA guarantee is a planning consideration rather than a blanket disqualifier, and nothing here calls for splitting across providers."}]},{"id":"mt_batches_custom_id_failures","type":"PROCEDURAL","cluster":"cl_batches","name":"custom_id and partial batch failure","description":"Every batch request carries a `custom_id` linking it back to your source document; when some requests in a batch fail (say, from exceeding the context limit), you identify them by that id, adjust your approach for just those, and resubmit only the failures rather than the whole batch.","core":false,"estMinutes":8,"evidence":["Explain what custom_id lets you avoid doing after a partial batch failure.","Describe the recovery sequence for 5 failed documents out of a batch of 100."],"assessmentPrompt":"95 of 100 batched documents succeed; 5 fail on context-limit errors. What does custom_id let you avoid re-doing, and what's the right next step for just the 5?","standards":["ccaf:d3"],"sources":[{"label":"Guide §7.3-7.4 custom_id and partial batch failure","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1062-L1087"}],"centrality":0.02,"layer":2,"x":540,"y":232,"knowledgePoints":[{"id":"kp_batches_custom_id_1","name":"custom_id turns a partial failure into a small fix","keyPrerequisites":["mt_batches_overview"],"teach":"<p class='kp-key'>Every batch request carries a <code>custom_id</code> linking it to your own source document. When 95 of 100 documents succeed and 5 fail (say, from exceeding the context limit), you identify the 5 by their <code>custom_id</code>, adjust your approach for just those (like chunking a long document), and resubmit only the failures — the 95 successes never need to be redone.</p>","questions":[{"id":"q_batches_custom_id_1a","q":"95 of 100 batched documents succeed; 5 fail with context-limit errors. What does custom_id let you avoid doing next?","options":["Avoid needing to know which specific documents failed, since the Batch API automatically retries them without any input.","Avoid splitting the failing documents into smaller chunks, since custom_id handles context-limit errors automatically.","Avoid re-processing the 95 successful documents — you can identify and resubmit only the 5 failures by their custom_id.","Avoid submitting a second batch at all — custom_id causes failed requests to be silently completed on a delayed schedule."],"correct":2,"explain":"custom_id's specific value is correlation — it lets you tell exactly which 5 of the 100 failed so only those need resubmission, sparing the 95 that already succeeded. It doesn't trigger automatic retries, doesn't chunk documents on its own, and doesn't silently complete failures on a delay — it's an identifier, not a recovery mechanism by itself."},{"id":"q_batches_custom_id_1b","q":"What does custom_id in a batch request correlate the response back to?","options":["The specific model version used to process that particular request, mainly for auditing purposes when reviewing which model generated which output later.","The submission timestamp, allowing results to be sorted chronologically once the batch completes.","The billing account responsible for that particular request, for itemized cost tracking purposes.","The original source document or item the request represents, so a result can be matched back to what produced it."],"correct":3,"explain":"custom_id is explicitly the link back to your own source document or item — that's what lets you match a result (or a failure) to what it came from. It isn't a model-version tag, a timestamp, or a billing identifier."}]}],"reviewQuestions":[{"id":"r_batches_custom_id_1","q":"Why is chunking a long document into smaller pieces a sensible response to a context-limit failure identified via custom_id, rather than simply resubmitting the same request unchanged?","options":["Because a context-limit failure means the request itself was too large, so an unchanged resubmission would fail the same way again.","It isn't sensible — resubmitting the identical request a second time is the correct response to any batch failure, including this one.","Because chunking is required by the Batch API for every submission, regardless of whether a prior attempt failed or succeeded.","Because custom_id only accepts requests below a certain size, so chunking is a workaround for that identifier's own limitation."],"correct":0,"explain":"A context-limit failure means the input itself exceeded what a single request can handle, so resubmitting it unchanged would simply reproduce the same failure — chunking actually changes the thing that caused the error. Batch submissions aren't required to be chunked in general, and custom_id is an identifier with no size constraint of its own."}]},{"id":"mt_batches_sla","type":"PROCEDURAL","cluster":"cl_batches","name":"Planning a batch submission window against a deadline","description":"Given a deadline and the batch API's up-to-24-hour window with no SLA guarantee, the latest safe submission time is deadline minus 24 hours — and a recurring deadline needs a matching recurring submission cadence carved out of that same margin.","core":false,"estMinutes":8,"evidence":["Compute the latest safe submission time for a given deadline and the batch API's window.","Explain why the batch API's lack of an SLA guarantee changes how that margin should be planned."],"assessmentPrompt":"A result is needed within 30 hours of a nightly trigger. Given the batch API's up-to-24-hour window, what is the latest point in that 30-hour window the batch can still be submitted?","standards":["ccaf:d3"],"sources":[{"label":"Guide §7.5 Planning a batch submission window against a deadline","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1088-L1096"}],"centrality":0,"layer":3,"x":750,"y":418,"knowledgePoints":[{"id":"kp_batches_sla_1","name":"The submission window is the deadline minus 24 hours","keyPrerequisites":["mt_batches_custom_id_failures"],"teach":"<p class='kp-key'>Given a deadline and the batch API's up-to-24-hour window with no SLA guarantee, the latest safe submission time is <strong>deadline − 24 hours</strong>. A recurring deadline needs a recurring submission cadence carved out of that same margin — for a nightly trigger with a 30-hour deadline, that's a 6-hour submission window, not a single fixed instant.</p>","questions":[{"id":"q_batches_sla_1a","q":"A result is needed within 30 hours of a nightly trigger. Given the Batch API's up-to-24-hour window, what is the latest point in that 30-hour window the batch can still be safely submitted?","options":["At the 30-hour mark itself, since the batch API's window only starts counting once the deadline has already passed.","At the 6-hour mark — submitting any later leaves less than the full 24-hour window before the 30-hour deadline arrives.","At the 24-hour mark, since the batch only needs a small buffer after its own 24-hour processing window completes.","There is no safe cutoff — the lack of an SLA guarantee means no submission time can reliably meet a fixed deadline."],"correct":1,"explain":"Working backward from the 30-hour deadline by the up-to-24-hour window gives a 6-hour submission cutoff — submitting later than that risks the batch still being in flight when the deadline arrives. Submitting at hour 30 or hour 24 leaves no margin at all, and while the lack of an SLA guarantee means margin should be generous, it doesn't mean no cutoff can be planned."},{"id":"q_batches_sla_1b","q":"Why does the Batch API's lack of a latency SLA specifically affect how a submission window should be planned, compared to an API with a guaranteed response time?","options":["It doesn't affect planning at all — an SLA and its absence lead to identical scheduling regardless of the deadline involved.","Because the lack of an SLA only matters for batches larger than 10,000 requests, below which timing is effectively guaranteed.","Because without a guarantee, 'up to 24 hours' should be treated as the planning assumption rather than an expected average.","Because an SLA's absence means the Batch API cannot be used for any task that has a hard deadline attached to it."],"correct":2,"explain":"With no guaranteed latency, the safe planning assumption is the full 'up to 24 hours,' not some shorter typical-case average — that's exactly why the margin is calculated against the ceiling rather than an expected value. It does change how scheduling should be done, it isn't scoped to a request-count threshold, and it doesn't rule out the Batch API for deadline-bound work — it just requires planning around the ceiling."}]}],"reviewQuestions":[{"id":"r_batches_sla_1","q":"A team wants results delivered by 9am and plans to submit their batch at 8am the same day. What's wrong with this plan?","options":["Nothing — a one-hour buffer is more than sufficient given the Batch API's typical actual turnaround time in practice.","Nothing is wrong structurally, but the team should switch to the synchronous API instead to guarantee the 9am deadline.","The problem is unrelated to timing — batches submitted in the morning are processed with lower priority than evening submissions.","The plan leaves almost no margin against a window that can take up to 24 hours; the batch should be submitted roughly a day earlier."],"correct":3,"explain":"An 8am submission for a 9am deadline leaves essentially no margin against a window that can legitimately take up to 24 hours — the batch should go out roughly a full day ahead of the deadline instead. Relying on typical turnaround rather than the stated ceiling is exactly the planning mistake the guide warns against, switching APIs sidesteps rather than fixes the scheduling error, and there's no morning-versus-evening priority effect described anywhere in the guide."}]},{"id":"mt_decomposition_strategies","type":"CONCEPTUAL","cluster":"cl_decomposition","name":"Fixed pipelines vs. dynamic adaptive decomposition","description":"A fixed pipeline defines every step up front for predictable, template-shaped work and buys stability and reproducibility; dynamic decomposition generates the next subtask from what the current one just found, for open-ended work whose full scope isn't known at the start.","core":false,"estMinutes":10,"evidence":["Decide between a fixed pipeline and dynamic decomposition for a task whose steps are all known in advance.","Trace an example where a discovery mid-task (an unexpected external dependency) forces the plan to adapt."],"assessmentPrompt":"'Add tests for a legacy codebase' turns up an undocumented dependency on an external API partway through. Was this ever a candidate for a fixed pipeline, and what does the mid-task discovery tell you about which strategy actually fits?","standards":["ccaf:d1"],"sources":[{"label":"Guide §8.1-8.2 Fixed pipelines vs. dynamic adaptive decomposition","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1099-L1129"}],"centrality":0.02,"layer":8,"x":1800,"y":46,"knowledgePoints":[{"id":"kp_decomposition_strategies_1","name":"Known-up-front vs. discovered-along-the-way","keyPrerequisites":["mt_prompt_chaining_vs_dynamic"],"teach":"<p class='kp-key'>A fixed pipeline (document → metadata → data → validation → enrichment → output) defines every step in advance, for predictable, template-shaped work where stability matters. Dynamic decomposition generates each next subtask from what the current one found — map the structure, discover 3 untested modules, prioritize the risky one, discover a dependency mid-work, adapt the plan — for open-ended work whose scope isn't known at the start.</p>","questions":[{"id":"q_decomposition_strategies_1a","q":"'Add tests for a legacy codebase' turns up an undocumented dependency on an external API partway through the work. What does this mid-task discovery tell you about which strategy actually fits?","options":["It confirms a fixed pipeline was right all along — the discovery is just a minor detail the original plan can absorb unchanged.","It means the task should be aborted and restarted as a fixed pipeline once every dependency has first been fully documented.","It shows dynamic decomposition fits — the next step (mocking the dependency) only became apparent from what was found mid-task.","It's irrelevant to strategy choice; both fixed pipelines and dynamic decomposition would have proceeded identically here."],"correct":2,"explain":"The need to add a mock for the external API only became visible once work was already underway — that's dynamic decomposition's defining trait: the plan adapts based on what execution actually reveals. A fixed pipeline defined up front wouldn't have this step, restarting after full documentation defeats the point of adaptive discovery, and the two strategies clearly diverge here rather than behaving identically."},{"id":"q_decomposition_strategies_1b","q":"Which project is the better fit for a fixed pipeline rather than dynamic decomposition?","options":["An open-ended investigation into why a production system intermittently fails, with no known cause yet.","A first exploration of an entirely unfamiliar codebase with no existing documentation to work from.","A research task whose specific subtopics can genuinely only be identified once the first round of search results actually comes back.","A recurring document-review workflow that always follows the same metadata-then-extraction-then-validation template."],"correct":3,"explain":"A recurring workflow that always follows the same known sequence is exactly what a fixed pipeline is built for — stability and reproducibility from steps defined in advance. The other three all involve genuine unknowns that can only be resolved by executing and observing what turns up, which is dynamic decomposition's territory."}]}],"reviewQuestions":[{"id":"r_decomposition_strategies_1","q":"What does a fixed pipeline buy you that dynamic decomposition does not, according to the guide?","options":["Stability and reproducibility, since every step is defined up front rather than generated from intermediate results.","Higher quality output on every single task, regardless of whether that task's true scope was actually known in advance or not.","The ability to handle discoveries mid-task without needing to adjust the plan at all.","Lower cost in all cases, since fixed pipelines never require more steps than dynamic decomposition would."],"correct":0,"explain":"The guide's specific claim for fixed pipelines is stability and reproducibility, precisely because every step is known in advance rather than generated on the fly. It doesn't claim universally higher quality, it explicitly handles mid-task discoveries worse than dynamic decomposition (not better), and it makes no general cost claim either way."}]},{"id":"mt_multipass_review","type":"PROCEDURAL","cluster":"cl_decomposition","name":"Multi-pass review for large pull requests","description":"A per-file first pass (one file per prompt) followed by a separate integration pass for cross-file issues avoids the attention dilution, inconsistent flagging, and missed bugs that come from reviewing 10+ files in a single pass.","core":false,"estMinutes":8,"evidence":["List the two passes a multi-pass review splits into and what each one catches.","Explain why reviewing 14 files in one prompt produces inconsistent flagging across files."],"assessmentPrompt":"A single-prompt review of a 14-file PR flags a pattern in one file but approves the same pattern in another. What review structure would have caught this, and which of its two passes is responsible for catching it?","standards":["ccaf:d1"],"sources":[{"label":"Guide §8.3 Multi-pass review for large pull requests","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1130-L1149"}],"centrality":0,"layer":9,"x":2010,"y":46,"knowledgePoints":[{"id":"kp_multipass_review_1","name":"Per-file, then integration","keyPrerequisites":["mt_decomposition_strategies"],"teach":"<p class='kp-key'>For a pull request with 10+ files, pass 1 analyzes each file individually for local issues; pass 2 is a separate integration pass looking specifically at relationships between files (inconsistent types, circular dependencies). Reviewing all 14 files in one prompt instead produces attention dilution, inconsistent flagging of the same pattern, and missed bugs from cognitive overload.</p>","questions":[{"id":"q_multipass_review_1a","q":"A single-prompt review of a 14-file PR flags a pattern in one file but approves the identical pattern in another. Which review structure would have caught this consistently, and which pass is responsible?","options":["Multi-pass review — but the integration pass specifically, since it's the one that examines files jointly rather than alone.","Multi-pass review — the per-file pass, since each file gets isolated, focused attention rather than being diluted across 14 at once.","A single, longer prompt with more explicit criteria added, without changing the number of passes involved.","The 14-file scope itself is the sole problem; splitting the PR into two separate 7-file pull requests would fix it on its own."],"correct":1,"explain":"Consistent flagging of the same pattern within individual files is what a focused per-file pass delivers — each file gets full attention on its own rather than competing for it against 13 others. The integration pass looks at cross-file relationships, not intra-file consistency; more criteria in a single prompt doesn't solve the attention-dilution root cause; and splitting the PR by file count doesn't guarantee each half still gets focused per-file attention."},{"id":"q_multipass_review_1b","q":"What does a multi-pass review's integration pass specifically look for that the per-file passes cannot?","options":["Local issues within one file, such as an unhandled edge case in a single function's logic.","Stylistic formatting issues, like inconsistent indentation, within any single file under review.","Relationships between files — such as inconsistent types across modules or circular dependencies.","Nothing distinct — the integration pass simply repeats the per-file passes a second time as a sanity check."],"correct":2,"explain":"The integration pass is specifically scoped to cross-file relationships — type inconsistencies between modules, circular dependencies — the kind of issue that can't be seen by looking at any one file in isolation. Local issues and per-file style are exactly what the per-file passes already cover, and the integration pass isn't a repeat of that same work."}]}],"reviewQuestions":[{"id":"r_multipass_review_1","q":"Why is reviewing 14 files in a single prompt described as risking 'missed bugs,' beyond just inconsistent flagging?","options":["Because a single prompt technically cannot include more than a handful of files at all, causing some to be silently dropped.","Because bugs are missed only when the files are unrelated to each other; related files are reviewed reliably even in one pass.","Because the risk is purely theoretical in nature — in practice, a single-pass review of many files performs just as well as a multi-pass one.","Because cognitive overload from handling many files at once can cause obvious errors to get skipped entirely, not just judged inconsistently."],"correct":3,"explain":"Beyond flagging the same pattern inconsistently, the guide specifically calls out that overload from juggling many files at once can cause otherwise-obvious bugs to be skipped entirely — a distinct, more severe failure than inconsistency. A single prompt can technically include many files (nothing silently drops them), the risk isn't limited to unrelated-file sets, and the guide treats this as a real, not purely theoretical, risk."}]},{"id":"mt_escalation_triggers","type":"CONCEPTUAL","cluster":"cl_escalation","name":"Reliable escalation triggers vs. unreliable ones","description":"An explicit request for a human, a policy gap, stalled progress, and a financial threshold (ideally hook-enforced) are clear triggers; sentiment analysis, self-rated model confidence, and a bolted-on classifier are not, because none of them reliably correlates with whether the case actually needs a human.","core":true,"estMinutes":12,"evidence":["List two reliable escalation triggers and two unreliable ones.","Explain specifically why model self-rated confidence fails as an escalation signal."],"assessmentPrompt":"A team proposes escalating whenever the model rates its own confidence below 7/10. Why does the guide call this an unreliable trigger, and what would be a more reliable signal for the same underlying concern?","standards":["ccaf:d5"],"sources":[{"label":"Guide §9.1 Reliable escalation triggers vs. unreliable ones","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1152-L1171"}],"centrality":0.06,"layer":5,"x":1170,"y":480,"knowledgePoints":[{"id":"kp_escalation_triggers_1","name":"Reliable triggers are rules; unreliable ones are proxies","keyPrerequisites":["mt_hooks_vs_prompts_guide"],"teach":"<p class='kp-key'>Reliable triggers: an explicit request for a human, a policy gap (the policy is simply silent), the agent making no progress after reasonable attempts, and a financial threshold — ideally hook-enforced, not prompt-requested. Multiple ambiguous customer matches should prompt for another identifier, never a guess.</p><p>Unreliable: sentiment analysis (mood doesn't track case complexity), the model's own 1–10 self-rated confidence (it can be confidently wrong), and a bolted-on classifier (overengineering, and it needs training data you may not have).</p>","questions":[{"id":"q_escalation_triggers_1a","q":"A team proposes escalating whenever the model rates its own confidence below 7 out of 10. Why does the guide call this an unreliable trigger?","options":["Because self-rated confidence scores are technically impossible for a language model to produce in a structured format.","Because the model's own confidence rating is poorly calibrated — it can be confidently wrong, so the score doesn't track actual risk.","Because a 7-out-of-10 threshold is too low; the same mechanism becomes reliable once the threshold is raised to 9 or above.","Because self-rated confidence scores only meaningfully apply to text generation tasks, not to the kind of decisions escalation triggers are meant to govern."],"correct":1,"explain":"The problem is calibration, not format or threshold — a model can be confidently wrong, so its own numeric self-rating doesn't reliably track whether a case actually needs a human. The score is easy to produce technically, no threshold adjustment fixes a calibration problem, and the concern isn't specific to text-generation-only tasks."},{"id":"q_escalation_triggers_1b","q":"A customer search returns three possible matches for the name and email provided, with no clearly correct single match. What should the agent do?","options":["Pick the most recently active of the three accounts, since recency is the best available proxy for correctness here.","Proceed with the first match returned by the lookup, since taking action quickly outweighs a small risk of misidentification.","Ask the customer for an additional identifier (like an order number) rather than guessing among the ambiguous matches.","Escalate to sentiment analysis first, to determine which of the three matching customers seems most likely to be upset."],"correct":2,"explain":"The guide's explicit rule for multiple ambiguous matches is to ask for an additional identifier rather than guess — recency, arbitrary ordering, or any other proxy risks acting on the wrong account. Sentiment analysis has nothing to do with resolving which of several accounts is the correct one."}]}],"reviewQuestions":[{"id":"r_escalation_triggers_1","q":"Why is a policy gap (the policy simply doesn't address the situation) considered a reliable escalation trigger?","options":["It isn't reliable — policy gaps are rare enough that building a trigger around them isn't worth the engineering effort.","Because a policy gap always indicates the customer is being deceptive about the situation they're describing.","Because policy gaps only arise for financial requests, so this trigger is really just a restatement of the financial-threshold trigger.","Because the agent has no authorized answer to give in this case at all, which is a clear, checkable condition rather than a judgment call."],"correct":3,"explain":"A genuine policy gap means there is no authorized answer for the agent to give — that's a clear, checkable state rather than something requiring subjective judgment, which is exactly what makes it a reliable trigger. It isn't rare enough to dismiss, it says nothing about customer honesty, and it isn't limited to financial situations or a restatement of the threshold trigger."}]},{"id":"mt_escalation_patterns","type":"PROCEDURAL","cluster":"cl_escalation","name":"Immediate, attempted, and nuanced escalation","description":"An explicit request for a manager escalates immediately with no attempt to resolve first; a solvable complaint gets an attempt (and escalates only if that fails); a frustrated-but-not-yet-explicit customer gets acknowledgment and a proposed resolution first, escalating only if they reiterate the request for a human afterward.","core":false,"estMinutes":10,"evidence":["Distinguish which of the three escalation patterns fits a customer who has not yet asked for a human.","Explain why acknowledging frustration before proposing a resolution is not the same as ignoring an escalation request."],"assessmentPrompt":"A customer writes 'This is outrageous, I'm very unhappy with the quality!' without asking for a manager. Which escalation pattern applies, and what should happen before any decision to escalate?","standards":["ccaf:d5"],"sources":[{"label":"Guide §9.2 Immediate, attempted, and nuanced escalation","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1172-L1209"}],"centrality":0.02,"layer":6,"x":1380,"y":232,"knowledgePoints":[{"id":"kp_escalation_patterns_1","name":"Immediate, attempted, or acknowledge-then-resolve","keyPrerequisites":["mt_escalation_triggers"],"teach":"<p class='kp-key'>An explicit 'get me a manager' escalates immediately, no attempt first. A solvable complaint gets an attempt (offer a warranty replacement), escalating only if that doesn't satisfy the customer. A frustrated-but-not-yet-explicit customer gets acknowledgment plus a proposed resolution first — escalating only if they reiterate the request for a human afterward. Expressing dissatisfaction is not, on its own, the same as requesting escalation.</p>","questions":[{"id":"q_escalation_patterns_1a","q":"A customer writes 'This is outrageous, I'm very unhappy with the quality!' without asking for a manager. Which pattern applies, and what should happen first?","options":["Acknowledge-then-resolve — acknowledge the frustration and propose a concrete resolution before any decision to escalate.","Immediate escalation — expressing strong dissatisfaction is functionally equivalent to an explicit request for a human.","Attempted resolution only — skip acknowledgment entirely and go straight to offering a replacement or refund.","No escalation pattern applies at all — dissatisfaction expressed this way should simply be logged without a response."],"correct":0,"explain":"This is exactly the acknowledge-then-resolve case: frustration is expressed but a human hasn't been explicitly requested, so the right first move is acknowledging the feeling and proposing a concrete fix — escalating only if the customer reiterates wanting a person after that. Treating dissatisfaction as equivalent to an explicit request skips a step the guide is careful to keep separate, skipping acknowledgment entirely misses half the pattern, and ignoring the message outright isn't a response option the guide describes."},{"id":"q_escalation_patterns_1b","q":"A customer says 'I want to speak to a manager' with no prior interaction in the conversation. Which escalation pattern applies?","options":["Attempted resolution — offer a solution first, and only escalate afterward if the offer doesn't resolve the request.","Immediate escalation — this is an explicit request for a human, so the agent escalates without attempting to solve it first.","Acknowledge-then-resolve — acknowledge the request, then propose a solution before deciding whether to escalate.","No standard pattern applies; explicit requests for a manager fall outside the escalation patterns the guide describes."],"correct":1,"explain":"An explicit request for a human is exactly the immediate-escalation case — the guide is specific that the agent should not attempt to resolve first when the customer has already asked for a person. Attempting a fix first, or acknowledging before resolving, are the patterns for a case where escalation hasn't explicitly been requested yet, not this one."}]}],"reviewQuestions":[{"id":"r_escalation_patterns_1","q":"A customer's refrigerator broke two days after purchase. The agent checks the order and offers a warranty replacement, but the customer says that's not acceptable and repeats they want it refunded, not replaced, or they'll go elsewhere. What should happen next?","options":["Offer the exact same warranty replacement a second time, since repeating the offer more clearly usually resolves the disagreement.","Ignore the pushback and close the case as resolved, since a warranty replacement was already offered per policy.","Escalate — an attempted resolution was offered and rejected, which is the trigger point for the attempted-resolution pattern.","Immediately involve sentiment analysis to determine how upset the customer actually is before deciding whether to escalate."],"correct":2,"explain":"This is the attempted-resolution pattern completing as designed: an offer was made, the customer wasn't satisfied by it, and that dissatisfaction after a genuine attempt is exactly the trigger to escalate. Repeating the identical rejected offer or closing the case ignores that the attempt already failed, and sentiment analysis isn't part of how this decision gets made."}]},{"id":"mt_structured_handoff","type":"PROCEDURAL","cluster":"cl_escalation","name":"A self-contained structured handoff","description":"A human operator handling an escalation typically never sees the full transcript — only the structured summary the agent hands off — so it must independently carry the customer, the issue, what was already tried, and a recommended action.","core":false,"estMinutes":8,"evidence":["List what a structured handoff must include, given that the human won't see the transcript.","Explain why 'actions_taken' matters even though the human wasn't there for them."],"assessmentPrompt":"A handoff to a human operator contains only `{\"escalation_reason\": \"customer requested manager\"}`. Given the human has no transcript access, what critical information is missing, and why does that matter?","standards":["ccaf:d5"],"sources":[{"label":"Guide §9.3 A self-contained structured handoff","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1210-L1233"}],"centrality":0,"layer":7,"x":1590,"y":170,"knowledgePoints":[{"id":"kp_structured_handoff_1","name":"The human sees the summary, not the transcript","keyPrerequisites":["mt_escalation_patterns"],"teach":"<p class='kp-key'>The human operator typically never sees the full conversation — only the structured handoff. It needs to independently carry: who the customer is, the issue, the order/context, the root cause, the actions already taken, any amount involved, and a recommended action with the escalation reason. Missing any of these means the human is re-doing work the agent already did, or worse, missing it entirely.</p>","questions":[{"id":"q_structured_handoff_1a","q":"A handoff to a human operator contains only {\"escalation_reason\": \"customer requested manager\"}. Given the human has no transcript access, what's missing?","options":["Nothing critical — the escalation_reason field alone is sufficient for a human to pick up the case from where the agent left off.","Only the refund_amount field is missing; every other piece of context is optional metadata rather than something the human needs.","The transcript itself should be attached instead of a structured summary, since transcripts are more complete than any summary.","Who the customer is, what the issue actually is, and what the agent already tried — the human has no way to reconstruct any of it."],"correct":3,"explain":"With no transcript access, the human depends entirely on the structured handoff — and a bare escalation_reason leaves out who the customer is, what happened, and what's already been tried, forcing the human to start from nothing. It's far from sufficient on its own, other fields like actions_taken aren't merely optional, and the guide's whole point is that a self-contained summary is what's needed, not the raw transcript."},{"id":"q_structured_handoff_1b","q":"Why does a structured handoff include actions_taken (what the agent already tried) even though the human wasn't present for any of it?","options":["So the human doesn't waste time repeating steps already tried, and can see what's already been ruled out as a solution.","It doesn't need to — actions_taken is purely for internal agent logging and isn't meant to inform the human's next step.","Because actions_taken is a required schema field with no functional purpose beyond satisfying the handoff's format.","Only to support later auditing of the agent's performance, not to help the human resolve the case at hand."],"correct":0,"explain":"Seeing what's already been tried (verified identity, confirmed order, offered a replacement) tells the human what's already ruled out, so they don't repeat those same steps and can move straight to the next decision. It's meant to inform the human's actual next step, not just sit as internal logging, a formality, or purely after-the-fact auditing."}]}],"reviewQuestions":[{"id":"r_structured_handoff_1","q":"A structured handoff includes a recommended_action field ('Approve a full refund') in addition to the raw facts of the case. What role does this field play?","options":["It's redundant with the other fields and could be removed without the human losing anything useful.","It gives the human a starting recommendation grounded in what the agent found, without removing the human's own final decision.","It automatically executes the recommended action without requiring the human to review or approve it first.","It's only ever included when the case happens to involve a monetary amount; purely non-financial escalations never carry this particular field."],"correct":1,"explain":"recommended_action gives the human a concrete starting point grounded in what the agent already determined, while leaving the actual decision with the human — that's additive, not redundant, and it doesn't bypass human judgment by auto-executing anything. Nothing restricts this field to monetary cases specifically."}]},{"id":"mt_confidence_calibration","type":"CONCEPTUAL","cluster":"cl_escalation","name":"Confidence-based routing and stratified auditing","description":"Field-level confidence scores, calibrated against a labeled validation set rather than trusted raw, route high-confidence extractions to automation and the rest to human review — and even high-confidence output needs a stratified random audit by document type and field, because a 97% aggregate accuracy can hide a document type sitting at 40%.","core":false,"estMinutes":10,"evidence":["Explain what calibration against a labeled set adds that a raw confidence score alone does not.","Explain why an aggregate accuracy figure can hide a much worse per-segment accuracy."],"assessmentPrompt":"A pipeline reports 97% aggregate extraction accuracy and nobody audits further. What could that number be hiding, and what auditing approach surfaces it?","standards":["ccaf:d5"],"sources":[{"label":"Guide §9.4 Confidence-based routing and stratified auditing","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1234-L1250"}],"centrality":0,"layer":6,"x":1380,"y":170,"knowledgePoints":[{"id":"kp_confidence_calibration_1","name":"Calibrated routing, and auditing beneath the aggregate","keyPrerequisites":["mt_escalation_triggers"],"teach":"<p class='kp-key'>Field-level confidence scores route high-confidence, stable-accuracy extractions to automation and the rest to human review — but only once those scores are calibrated against a labeled validation set, not trusted raw. Even high-confidence output needs a stratified random audit by document type and field, because a 97% aggregate accuracy can hide a document type sitting at 40%.</p>","questions":[{"id":"q_confidence_calibration_1a","q":"A pipeline reports 97% aggregate extraction accuracy, and nobody audits further. What could that number be hiding?","options":["Nothing meaningful — a 97% aggregate figure already accounts for variation across every document type and field.","The aggregate figure could only be hidden if it were below 90%; anything at 97% or above is inherently trustworthy.","A specific document type or field performing far worse (e.g., 40%) while other segments pull the overall average up.","The 97% figure is misleading only if it was computed from fewer than 100 total documents in the validation set."],"correct":2,"explain":"This is exactly the scenario the guide warns about: a strong overall average can mask one segment — a document type or a specific field — performing much worse, with other segments pulling the aggregate up. The aggregate doesn't automatically account for that variation, no accuracy threshold makes a number immune to this masking effect, and the issue isn't about sample size specifically."},{"id":"q_confidence_calibration_1b","q":"Why does the guide say a raw, uncalibrated confidence score shouldn't be trusted directly for routing decisions?","options":["Because raw confidence scores are always higher than they should be, so a fixed downward adjustment always corrects them.","Because confidence scores are only meaningful for numeric fields, not for text or categorical extraction fields.","Because raw scores change unpredictably between runs of the identical model on the identical input document.","Because a score only reflects reality once it's been checked against a labeled validation set to see what it actually predicts."],"correct":3,"explain":"Calibration means checking what a given confidence score actually predicts about real accuracy, using labeled data — without that check, there's no basis for trusting what a raw number means in practice. It isn't a simple fixed-direction bias, it isn't limited to numeric fields, and the concern isn't about run-to-run inconsistency."}]}],"reviewQuestions":[{"id":"r_confidence_calibration_1","q":"What does a stratified random sample specifically add, compared to auditing an equal-sized random sample drawn without regard to document type?","options":["It ensures every document type and field gets meaningfully represented in the audit, rather than being drowned out by the most common type.","Nothing extra — a plain random sample of the same size provides identical visibility into per-segment accuracy either way.","It reduces the total number of documents that ultimately need to be reviewed, compared to a plain random sample drawn at that same overall confidence level.","It replaces the need for confidence scores entirely, since stratified sampling alone is sufficient to route documents correctly."],"correct":0,"explain":"Stratifying by document type and field guarantees each segment gets real representation in the audit, so a rare-but-poorly-performing type doesn't get drowned out by the volume of a common, well-performing one — that's exactly the visibility a plain random sample can miss. It isn't about sample-size efficiency, and it doesn't replace confidence-based routing; the two work together."}]},{"id":"mt_error_categories","type":"FACTUAL","cluster":"cl_error_handling_guide","name":"Transient, validation, business, and permission errors","description":"A transient error (timeout, 503) is retryable with backoff; a validation error needs the request fixed, not retried as-is; a business error (policy violation) needs explanation and an alternative offered to the user; a permission error needs escalation — conflating any two changes what the agent should do next.","core":true,"estMinutes":8,"evidence":["Match each error category to whether it's retryable and to the correct next action.","Explain why treating a validation error like a transient one (retrying unchanged) wastes calls without fixing anything."],"assessmentPrompt":"An MCP tool call fails with 'missing required field: customer_id.' Which error category is this, is it retryable as-is, and what should the agent do instead of retrying the identical call?","standards":["ccaf:d5"],"sources":[{"label":"Guide §10.1 Transient, validation, business, and permission errors","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1253-L1261"}],"centrality":0.1,"layer":6,"x":1380,"y":294,"knowledgePoints":[{"id":"kp_error_categories_1","name":"Four categories, four different next actions","keyPrerequisites":["mt_mcp_iserror"],"teach":"<p class='kp-key'>Transient (timeout, 503) is retryable with exponential backoff. Validation (missing field, wrong format) is not retryable as-is — fix the request first. Business (policy violation, threshold exceeded) is not retryable — explain to the user and propose an alternative. Permission (access denied) is not retryable — escalate. Conflating any two means retrying something that will never succeed, or escalating something a retry would have fixed.</p>","questions":[{"id":"q_error_categories_1a","q":"An MCP tool call fails with 'missing required field: customer_id.' Which category is this, and what should the agent do instead of retrying the identical call?","options":["Validation — fix the request by supplying customer_id, since retrying the exact same malformed call will fail again identically.","Transient — retry the identical call with exponential backoff, since the field may populate itself on a later attempt.","Business — explain the missing-field situation to the user as a policy limitation and offer an alternative.","Permission — escalate to a human immediately, since a missing required field like this one indicates the agent lacks authorization for the call."],"correct":0,"explain":"A missing required field is a validation error — the request itself is malformed, so retrying it unchanged will fail identically every time; the fix is supplying the missing field before calling again. It isn't transient (nothing external is flaking), it isn't a policy/business issue, and it has nothing to do with permission."},{"id":"q_error_categories_1b","q":"A tool call fails with a 503 Service Unavailable response. Which category does this fall under, and is it retryable as-is?","options":["Validation — not retryable as-is; the request's structure needs to be corrected before trying again.","Transient — retryable, ideally with exponential backoff, since the failure is likely to resolve on its own shortly.","Permission — not retryable; this specific failure requires escalation rather than any form of retry.","Business — not retryable at all; a 503 response typically indicates some kind of policy constraint actively being enforced by the service."],"correct":1,"explain":"503 Service Unavailable is a canonical transient failure — the service itself is temporarily down, and retrying (ideally with backoff) is the appropriate response since nothing about the request itself is wrong. It's not a structural request problem (validation), not an authorization problem (permission), and not a policy decision (business)."}]}],"reviewQuestions":[{"id":"r_error_categories_1","q":"Why does treating a validation error the same way as a transient one (retrying it unchanged) fail to actually resolve anything?","options":["It doesn't fail — validation errors resolve on retry roughly as often as transient ones do, given enough attempts.","Because validation errors are always permanent regardless of what changes are made, so no retry strategy of any kind helps.","Because a validation error stems from something wrong in the request itself, which retrying without changing anything can't fix.","Because exponential backoff specifically triggers a hidden fix for malformed requests that immediate retries do not."],"correct":2,"explain":"A validation error is a problem with the request's own content or structure — retrying the identical, still-malformed request wastes an attempt without addressing the actual cause. It won't resolve given more attempts if nothing changes, it isn't unfixable in general (correcting the request does fix it), and backoff timing has no bearing on a structural request problem."}]},{"id":"mt_error_antipatterns","type":"CONCEPTUAL","cluster":"cl_error_handling_guide","name":"Four error-handling anti-patterns","description":"A generic 'search unavailable' status, silent suppression (an empty result reported as success), aborting an entire workflow on one subagent's failure, and infinite in-subagent retries all deny the coordinator the information or the partial progress it needs to recover well.","core":false,"estMinutes":10,"evidence":["Name the anti-pattern where an empty result set gets reported the same way as a genuine zero-match success.","Explain what a coordinator loses when a workflow aborts entirely on one subagent's failure."],"assessmentPrompt":"A search subagent times out and returns an empty results array with no failure flag, so the coordinator logs it as '0 relevant results found.' Which anti-pattern is this, and what should the response have signaled instead?","standards":["ccaf:d5"],"sources":[{"label":"Guide §10.2 Four error-handling anti-patterns","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1262-L1270"}],"centrality":0.08,"layer":7,"x":1590,"y":232,"knowledgePoints":[{"id":"kp_error_antipatterns_1","name":"Four ways a response can starve the coordinator","keyPrerequisites":["mt_error_categories"],"teach":"<p class='kp-key'>A generic 'search unavailable' status gives the coordinator nothing to decide with. Silent suppression — an empty result reported as success — makes 'no matches' indistinguishable from 'the search failed.' Aborting a whole workflow on one subagent's failure throws away every partial result. Infinite in-subagent retries burn latency and resources instead of surfacing the problem. Each one either hides information the coordinator needs, or denies it partial progress it could have used.</p>","questions":[{"id":"q_error_antipatterns_1a","q":"A search subagent times out and returns an empty results array with no failure flag, so the coordinator logs it as '0 relevant results found.' Which anti-pattern is this?","options":["Infinite retries — the subagent should have kept retrying internally rather than giving up and returning an empty array.","Aborting the whole workflow — the coordinator should have stopped the entire pipeline the moment this subagent returned.","A generic error status — the response should have said 'search unavailable' instead of returning an empty array.","Silent suppression — a failure is being reported identically to a genuine zero-match success, hiding that anything went wrong."],"correct":3,"explain":"This is silent suppression exactly as the guide describes it: a real failure (a timeout) gets reported in the same shape as a legitimate zero-match success, so the coordinator can't tell the two apart. Nothing here involves excessive internal retries, aborting the whole workflow isn't what happened, and a generic status string isn't the specific problem — the missing failure signal is."},{"id":"q_error_antipatterns_1b","q":"A coordinator aborts an entire three-subagent research workflow the moment one of the three subagents fails, discarding the other two subagents' completed results. What does this cost?","options":["It loses every bit of value the two successful subagents already produced, when a partial result with an annotated gap would have preserved it.","Nothing significant is lost — restarting the entire workflow from scratch is generally considered cheaper than trying to salvage any partial results.","It only matters if the failed subagent was the first one to run; failures late in the workflow carry no such cost.","It's the correct behavior specifically to guarantee consistency across the final report's sections."],"correct":0,"explain":"Discarding two subagents' completed work over one unrelated failure throws away real, usable output — the guide's fix is continuing with partial results and annotating the gap, not aborting everything. Restarting from scratch isn't generally cheaper than salvaging what already succeeded, the cost doesn't depend on which subagent happened to fail first, and this abort-on-failure behavior is exactly the anti-pattern being described, not a best practice."}]}],"reviewQuestions":[{"id":"r_error_antipatterns_1","q":"Why is unlimited retrying inside a single subagent, with no cap, listed as an anti-pattern rather than a safety measure?","options":["Because retries of any number are always ineffective, so even a single retry attempt should be avoided entirely.","Because it wastes latency and resources without ever escalating to the coordinator, which could have decided on a better recovery path.","Because retrying is only actually an anti-pattern when the underlying error turns out to be a validation error rather than a transient one.","It isn't actually an anti-pattern; unlimited retries inside a subagent are the guide's recommended default behavior."],"correct":1,"explain":"The problem with unbounded internal retries is that they burn time and resources while never surfacing the issue to the coordinator, which might have a better recovery option (a different approach, partial results, escalation). The guide doesn't say retries are always ineffective — a bounded local retry (1–2 attempts) is fine — and the concern isn't specific to one error category; it's the unboundedness itself."}]},{"id":"mt_structured_error_coverage","type":"PROCEDURAL","cluster":"cl_error_handling_guide","name":"Structured errors and coverage annotations","description":"A subagent error should carry status, failure type, the attempted input, any partial results, and alternative approaches, so the coordinator can decide to retry, use what's there, or route elsewhere — and the final synthesis should mark each section FULL COVERAGE or PARTIAL COVERAGE (with the reason) rather than presenting gapped research as uniformly complete.","core":false,"estMinutes":10,"evidence":["List the fields a structured subagent error needs beyond a bare failure status.","Explain what a coverage annotation communicates that a silently-merged final report would hide."],"assessmentPrompt":"A synthesis report on 'AI in creative industries' covers visual art in depth but is thin on music because a search agent timed out there. What should the final report's structure do to represent that honestly?","standards":["ccaf:d5"],"sources":[{"label":"Guide §10.3-10.4 Structured errors and coverage annotations","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1271-L1312"}],"centrality":0.06,"layer":8,"x":1800,"y":108,"knowledgePoints":[{"id":"kp_structured_error_coverage_1","name":"Enough for the coordinator to actually decide","keyPrerequisites":["mt_error_antipatterns"],"teach":"<p class='kp-key'>A useful subagent error carries status, failure_type, the attempted input, any partial_results, and alternative_approaches — giving the coordinator what it needs to retry differently, use what's there, or route elsewhere. The same honesty extends to the final synthesis: mark each section FULL COVERAGE or PARTIAL COVERAGE with the reason, rather than presenting gapped research as uniformly complete.</p>","questions":[{"id":"q_structured_error_coverage_1a","q":"A synthesis report on 'AI in creative industries' covers visual art in depth but is thin on music because a search agent timed out there. What should the final report's structure do to represent that honestly?","options":["Present all three sections in identical formatting with no indication that the music section is based on less data.","Omit the music section entirely from the report, since incomplete coverage shouldn't be presented to the reader at all.","Mark the music section PARTIAL COVERAGE with the reason (the search agent's timeout), while marking visual art FULL COVERAGE.","Merge the thin music findings into the visual art section, since visual art has the depth to compensate for the gap."],"correct":2,"explain":"This is exactly what coverage annotation is for — labeling the music section as PARTIAL COVERAGE with the specific reason lets a reader correctly weigh it against the FULL COVERAGE visual art section, rather than reading both as equally reliable. Presenting them identically hides the gap, omitting the section discards real (if partial) research, and merging it into an unrelated section obscures rather than clarifies the gap."},{"id":"q_structured_error_coverage_1b","q":"Which field in a structured subagent error most directly helps the coordinator recover with a modified approach, rather than simply retrying unchanged?","options":["status alone, since knowing that something failed is sufficient information to construct a better retry.","failure_type alone, without any of the other fields, since categorizing the failure is the whole of what's needed.","None of the individual fields matter on their own; only the presence of isError:true is relevant to recovery.","alternative_approaches, since it suggests concrete different strategies the coordinator could try next."],"correct":3,"explain":"alternative_approaches specifically proposes concrete different strategies (a narrower query, a different data source) — that's what turns a failure report into an actionable recovery path rather than just a status update. status or failure_type alone tell the coordinator something failed and how, but not what to try differently, and a bare isError flag carries even less."}]}],"reviewQuestions":[{"id":"r_structured_error_coverage_1","q":"Why does a structured subagent error include partial_results even when the overall call is reported as a failure?","options":["So the coordinator can choose to use what was actually produced instead of treating the entire call as a total loss.","It's purely for logging purposes and has no bearing on what the coordinator does with the failed call's outcome.","Because partial_results is required by the JSON schema regardless of whether it's actually useful in a given failure.","Only to support the eventual coverage annotation in the final report, with no other use during the workflow itself."],"correct":0,"explain":"Preserving whatever was actually produced before the failure gives the coordinator a real option — using that partial output rather than discarding everything — which is the whole point of including it alongside the failure status. It's meant to inform an active decision, not just sit as a log entry, isn't an arbitrary schema requirement, and its usefulness isn't limited to feeding the later coverage annotation."}]},{"id":"mt_fact_extraction_block","type":"PROCEDURAL","cluster":"cl_context_mgmt_guide","name":"A dedicated, continuously updated facts block","description":"Instead of trusting a long-running conversation's history (which degrades under summarization), key facts get pulled into a structured block — customer ID, order ID, amount, status — that's included verbatim in every prompt regardless of how much of the rest gets compacted.","core":false,"estMinutes":8,"evidence":["Explain why a facts block survives summarization when relying on conversation history does not.","List the kind of information that belongs in a facts block versus what can stay in free-form history."],"assessmentPrompt":"After several rounds of /compact, a support agent starts asking the customer to re-confirm their order number, which was established six turns ago. What pattern would have prevented this, and how does it differ from just relying on history?","standards":["ccaf:d5"],"sources":[{"label":"Guide §11.1 A dedicated, continuously updated facts block","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1315-L1332"}],"centrality":0.08,"layer":3,"x":750,"y":480,"knowledgePoints":[{"id":"kp_fact_extraction_1","name":"A block that survives compaction because it's separate from history","keyPrerequisites":["mt_context_window_problems"],"teach":"<p class='kp-key'>Key facts (customer ID, order ID, amount, status) get pulled into a structured block that's included verbatim in every prompt, updated whenever a new fact appears — regardless of how much of the surrounding history gets compacted away.</p><p>It works because it isn't part of the history being summarized; it's a standing block re-included fresh on every turn, so it never degrades the way a fact buried in turn 6 of a compacted transcript can.</p>","questions":[{"id":"q_fact_extraction_1a","q":"After several rounds of /compact, a support agent asks the customer to re-confirm their order number, which was established six turns ago. What pattern would have prevented this?","options":["Running /compact less frequently, so the order number would have had more turns to remain in uncompacted history.","Increasing max_tokens on each request, so the model has more room to reference details from further back in history.","Switching from /compact to /memory, since /memory is the command meant to preserve conversation-specific details.","A dedicated, continuously updated facts block included in every prompt, separate from the summarized conversation history."],"correct":3,"explain":"A facts block sidesteps the problem entirely by not depending on history survival at all — it's re-included fresh on every turn regardless of what compaction does elsewhere. Running /compact less often only delays the same risk, more max_tokens doesn't prevent a summarization step from dropping a detail, and /memory persists notes into CLAUDE.md across sessions rather than tracking a single case's live facts."},{"id":"q_fact_extraction_1b","q":"Which kind of information most belongs in a dedicated facts block, versus being left in free-form conversation history?","options":["Concrete, load-bearing values like customer ID, order ID, amount, and current case status.","The customer's exact wording and tone throughout the conversation, to preserve emotional context for later reference.","General small talk exchanged early in the conversation before the actual issue was raised.","The agent's own internal reasoning about which tool to call next at each step of the conversation."],"correct":0,"explain":"The guide's own example block is exactly this: identifiers and status values that the rest of the conversation depends on and that must not degrade. Tone, small talk, and step-by-step internal reasoning are the kind of content that can reasonably live in ordinary history rather than a dedicated, always-included block."}]}],"reviewQuestions":[{"id":"r_fact_extraction_1","q":"Why is a facts block included in every prompt 'regardless of how history is summarized,' rather than being summarized along with everything else?","options":["Because summarization only ever affects text longer than a few sentences, and facts blocks are deliberately kept short.","Because it's treated as separate, always-included content rather than part of the history a compaction step is allowed to compress.","Because /compact is specifically configured to skip any text that appears inside HTML-style tags or delimiters.","It isn't actually separate at all — a facts block, in practice, is just as likely to be summarized away as any other ordinary part of the conversation history."],"correct":1,"explain":"The whole design is that the facts block sits outside what compaction operates on — it's re-supplied fresh every turn rather than being part of the history that gets compressed, which is exactly why it survives. It isn't a length-based exemption, it isn't about tag-based parsing tricks, and it is meaningfully protected, not equally at risk."}]},{"id":"mt_trim_tool_results","type":"PROCEDURAL","cluster":"cl_context_mgmt_guide","name":"Trimming tool results with a PostToolUse hook","description":"When a tool returns 40+ fields but a task only needs 5, a PostToolUse hook that keeps only the relevant fields before the result reaches the model conserves context and cuts the noise the model has to read past.","core":false,"estMinutes":8,"evidence":["Explain which hook event is the right place to trim an oversized tool result.","State the trade-off a trimming hook is making, and why it's usually worth it."],"assessmentPrompt":"`lookup_order` returns 40+ fields, but the current task only needs 5 of them. Where should the trimming logic live, and what does leaving it untrimmed cost?","standards":["ccaf:d5"],"sources":[{"label":"Guide §11.2 Trimming tool results with a PostToolUse hook","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1333-L1351"}],"centrality":0.06,"layer":4,"x":960,"y":480,"knowledgePoints":[{"id":"kp_trim_tool_results_1","name":"A PostToolUse hook keeps only what's load-bearing","keyPrerequisites":["mt_fact_extraction_block"],"teach":"<p class='kp-key'>When <code>lookup_order</code> returns 40+ fields but a task needs 5, a PostToolUse hook filters the result down to just those before it reaches the model — conserving context and cutting the noise the model has to read past on every turn that result is referenced.</p>","questions":[{"id":"q_trim_tool_results_1a","q":"lookup_order returns 40+ fields, but the current task only needs 5 of them. Where should the trimming logic live, and what does leaving it untrimmed cost?","options":["In a PreToolUse hook, since trimming should happen before the tool call is made rather than after its result returns.","In the system prompt itself, by instructing the model to simply ignore the extra 35 fields whenever it happens to encounter them in a tool result.","In a PostToolUse hook filtering the result down to the 5 needed fields; leaving it untrimmed wastes context on every referencing turn.","Nowhere specific — tool result size has no measurable effect on context as long as max_tokens is set generously."],"correct":2,"explain":"PostToolUse is the event that fires after the tool result comes back, which is exactly where filtering it down to the needed 5 fields belongs; skipping this wastes context budget every time that bloated result is referenced. A PreToolUse hook runs before the call, so it can't filter a result that hasn't returned yet, telling the model to 'ignore' extra fields in the prompt doesn't reclaim the context they consume, and result size does affect the budget regardless of the token ceiling."},{"id":"q_trim_tool_results_1b","q":"What is the direct benefit of trimming an oversized tool result before it reaches the model?","options":["It guarantees the model's downstream extraction will be more accurate, independent of how noisy the untrimmed data was.","It prevents the tool call from failing outright, since oversized results otherwise cause the call itself to error.","It removes the need for a facts block, since a trimmed tool result already carries everything a facts block would.","It conserves context and reduces the noise the model has to process alongside the fields that actually matter."],"correct":3,"explain":"The stated benefit is conserving context and reducing noise — a more focused result is easier for the model to work with. It doesn't guarantee improved accuracy by itself, an oversized result doesn't inherently make the call fail, and it isn't a substitute for a facts block, which tracks case-level state rather than a single tool response's fields."}]}],"reviewQuestions":[{"id":"r_trim_tool_results_1","q":"Trimming a tool result and building a dedicated facts block are both examples of what general discipline?","options":["Keeping only what's load-bearing in context, applied first to derived facts, then to raw tool output.","Reducing the number of distinct tools available to an agent, to simplify its overall decision-making.","Replacing structured JSON output with plain prose wherever context conservation is a concern.","Delaying tool calls until as late as possible in a conversation to minimize their footprint on context."],"correct":0,"explain":"Both are instances of the same underlying discipline — keep only what actually matters in context, whether that's a curated set of facts or a filtered tool response. Neither is about reducing the number of available tools, replacing structured output with prose, or delaying when calls happen."}]},{"id":"mt_position_aware_input","type":"PROCEDURAL","cluster":"cl_context_mgmt_guide","name":"Placing content with lost-in-the-middle in mind","description":"Key findings belong at the top, action items at the end, and the bulk of detailed (lower-priority-to-recall) content in the middle — the layout is a direct countermeasure for the effect where models process the start and end of a long input more reliably than its middle.","core":false,"estMinutes":8,"evidence":["Explain why a summary placed at the very top of a long report is more reliably read than one buried mid-document.","Describe where action items should sit in a long report, and why."],"assessmentPrompt":"A long vulnerability report buries its 'fix before merge' priority note in the middle, after pages of per-file detail. Given the lost-in-the-middle effect, what's wrong with that layout, and where should the priority note move?","standards":["ccaf:d5"],"sources":[{"label":"Guide §11.3 Placing content with lost-in-the-middle in mind","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1352-L1369"}],"centrality":0.04,"layer":5,"x":1170,"y":542,"knowledgePoints":[{"id":"kp_position_aware_1","name":"Findings at the top, actions at the end","keyPrerequisites":["mt_trim_tool_results"],"teach":"<p class='kp-key'>Given the lost-in-the-middle effect, place key findings at the top of a long report, detailed results in the middle, and action items at the end — the two positions models read most reliably carry the content that matters most.</p>","questions":[{"id":"q_position_aware_1a","q":"A long vulnerability report buries its 'fix before merge' priority note in the middle, after pages of per-file detail. What's wrong with that layout?","options":["Nothing structural is wrong; priority notes are read just as reliably regardless of where they sit in a long document.","The priority note sits in exactly the position a model reads least reliably; it should move to the top (or the end).","The problem is the report's total length, not the note's position — shortening the report is the only real fix.","The note should move to the very middle intentionally, since that's where the most careful, focused reading happens."],"correct":1,"explain":"Mid-document placement is precisely the position the lost-in-the-middle effect makes least reliable — moving the priority note to the start (or repeating it at the end) puts it where it's actually likely to be read and acted on. Position matters independent of overall length, and the middle is the least, not most, reliably read position."},{"id":"q_position_aware_1b","q":"Where should action items go in a long, position-aware research report, and why?","options":["In the middle, interleaved with the detailed findings that support each specific action item.","Distributed evenly throughout the report so no single position carries disproportionate weight.","At the end, since that's one of the two positions a model processes most reliably in a long input.","Action items shouldn't appear in the report body at all; they belong only in a separate follow-up document."],"correct":2,"explain":"The end of the document is one of the two reliably-read positions (alongside the start), which is exactly why action items belong there. Interleaving them into the middle or spreading them evenly places at least some of them in the least reliable zone, and there's no reason from this pattern to exclude them from the report itself."}]}],"reviewQuestions":[{"id":"r_position_aware_1","q":"Why does position-aware layout count as a direct countermeasure for lost-in-the-middle, rather than a separate, unrelated best practice?","options":["It doesn't counter the effect at all; it's a purely stylistic convention for making long reports easier to skim visually.","Because it eliminates the lost-in-the-middle effect entirely, so content placed anywhere in the document is read equally well.","Because it only applies to reports containing tables, and has no relevance to prose-heavy or narrative-style reports.","Because it deliberately places the highest-priority content at the two positions the effect itself identifies as most reliably read."],"correct":3,"explain":"The layout choice is a direct response to the effect's own shape — it puts the content you most need read at exactly the two positions the effect says are read most reliably. It isn't merely stylistic, it doesn't eliminate the underlying effect (content in the middle is still read less reliably), and it applies to prose reports just as much as tabular ones."}]},{"id":"mt_scratchpad_delegation","type":"PROCEDURAL","cluster":"cl_context_mgmt_guide","name":"Scratchpad files and delegating discovery to protect context","description":"A scratchpad file records key findings so a degraded or new session can consult it instead of re-running discovery, and delegating a 15-file investigation to an Explore subagent means the main agent keeps one summary line instead of 15 files' worth of raw content in its own window.","core":false,"estMinutes":10,"evidence":["Explain what a scratchpad file lets a new session skip re-doing.","Explain how delegating a file-heavy investigation to a subagent protects the coordinator's context budget."],"assessmentPrompt":"A long investigation into `PaymentProcessor`'s dependents is about to be resumed in a new session because context has degraded. What artifact would let the new session skip re-discovering what the old one already found, and what belongs in it?","standards":["ccaf:d5"],"sources":[{"label":"Guide §11.4-11.5 Scratchpad files and delegating discovery to protect context","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1370-L1402"}],"centrality":0.02,"layer":6,"x":1380,"y":356,"knowledgePoints":[{"id":"kp_scratchpad_delegation_1","name":"Write it down, or hand it off — either way, keep it out of the main window","keyPrerequisites":["mt_position_aware_input"],"teach":"<p class='kp-key'>A scratchpad file records key findings during a long investigation, so a degraded or brand-new session can consult it instead of re-running discovery from scratch. Delegating a file-heavy investigation to an Explore subagent achieves the same protection a different way: the subagent reads 15 files and returns one summary line, so the main agent's context holds one line instead of 15 files.</p><p>The coordinator acts as a separate context layer in a multi-agent system — aggregating outputs, holding global state, allocating context — which is what prevents 'context leakage' where one agent's irrelevant detail crowds another's window.</p>","questions":[{"id":"q_scratchpad_delegation_1a","q":"A long investigation into PaymentProcessor's dependents needs to resume in a new session because context has degraded. What artifact lets the new session skip re-discovering what the old one already found?","options":["A scratchpad file recording the key findings from the original investigation, which the new session can read instead of re-discovering them.","Nothing is needed — a new session automatically inherits everything the prior session's context window contained.","Re-running /compact inside the brand-new session, which supposedly reconstructs the old session's findings purely from its own freshly summarized history.","The new session should simply re-run the entire original investigation from scratch to guarantee accuracy."],"correct":0,"explain":"A scratchpad file is exactly the artifact meant for this — key findings written to disk so a new or degraded session can consult them directly instead of re-running discovery. A new session inherits nothing automatically, /compact operates within a session's own history rather than reconstructing a different session's, and re-running the whole investigation is the expensive outcome the scratchpad is meant to avoid."},{"id":"q_scratchpad_delegation_1b","q":"A main agent delegates a 15-file dependency investigation to an Explore subagent, which returns a one-line summary. How does the main agent's context compare to reading all 15 files directly?","options":["It's identical either way, since the subagent's context and the main agent's context are effectively merged into one shared window.","It holds one summary line instead of 15 files' worth of content, protecting the main agent's own context budget.","It's worse, since delegating adds the overhead of a subagent call on top of whatever the main agent would have read directly.","It depends entirely on which built-in tool the subagent uses internally, not on the fact that delegation happened at all."],"correct":1,"explain":"This is the whole point of delegating: the subagent absorbs the verbose 15-file investigation into its own isolated context, and the main agent's window only holds the compact summary that comes back. The two contexts are isolated, not merged; the summary is a net context savings despite the call overhead; and the benefit comes from the delegation pattern itself, not from which specific tool the subagent happens to use."}]}],"reviewQuestions":[{"id":"r_scratchpad_delegation_1","q":"What does the guide mean by the coordinator acting as a 'separate context layer' that prevents context leakage between subagents?","options":["It means every subagent automatically shares one single merged context window between them, which the coordinator then simply displays back to the user.","It means the coordinator deletes a subagent's context entirely the moment that subagent's task is marked complete.","It means the coordinator aggregates outputs and allocates context to each subagent, so one agent's irrelevant detail doesn't crowd another's window.","It means only the coordinator is allowed to call tools; subagents exist purely to receive and forward its instructions."],"correct":2,"explain":"The coordinator's role as a separate layer is specifically to aggregate results and decide what each subagent receives, which keeps one agent's irrelevant detail from leaking into and crowding another's isolated window. Subagents don't share one merged window, the coordinator doesn't need to delete anything to achieve this, and subagents themselves do call tools — they aren't reduced to pure forwarding."}]},{"id":"mt_state_persistence","type":"PROCEDURAL","cluster":"cl_context_mgmt_guide","name":"Structured state persistence for crash recovery","description":"Each agent exports its own status/progress/findings to a known location, and a coordinator-level manifest tracks which agents are completed, in progress, or not started — so a crash mid-run can resume from where each agent actually was instead of restarting the whole system.","core":false,"estMinutes":8,"evidence":["Explain what a per-agent state file and a coordinator manifest each track.","Describe what a coordinator does with the manifest on resume after a crash."],"assessmentPrompt":"A three-agent research pipeline crashes after the web-search agent finishes but before synthesis starts. What would let the coordinator resume without re-running the search, and what file tells it which agents to skip?","standards":["ccaf:d5"],"sources":[{"label":"Guide §11.6 Structured state persistence for crash recovery","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1403-L1431"}],"centrality":0,"layer":7,"x":1590,"y":294,"knowledgePoints":[{"id":"kp_state_persistence_1","name":"Per-agent state plus a coordinator manifest","keyPrerequisites":["mt_scratchpad_delegation"],"teach":"<p class='kp-key'>Each agent exports its own status, progress, and findings to a known location (<code>agent-state/web-search-agent.json</code>). A coordinator-level manifest (<code>agent-state/manifest.json</code>) tracks which agents are completed, in progress, or not started — so a crash mid-run resumes from where each agent actually was, instead of restarting the whole system.</p>","questions":[{"id":"q_state_persistence_1a","q":"A three-agent research pipeline crashes after the web-search agent finishes but before synthesis starts. What lets the coordinator resume without re-running the completed search?","options":["Nothing specific is needed at all — the coordinator is assumed to automatically remember what already completed before a crash, with no persisted state required.","The synthesis agent's own state file, since it's the one that needs to start next after the crash occurs.","A scratchpad file is sufficient on its own; a coordinator-level manifest adds no additional information in this scenario.","A per-agent state file for the web-search agent plus a manifest marking it \"completed\", telling the coordinator to skip re-running it."],"correct":3,"explain":"The web-search agent's own persisted state plus a manifest entry marking it completed is exactly what lets the coordinator know, after a crash, that this step doesn't need to be redone. Nothing is remembered automatically without persisted state, the synthesis agent's own file (which hasn't started) doesn't help resume the search step, and a manifest specifically adds the cross-agent 'who's done' view a single scratchpad doesn't provide."},{"id":"q_state_persistence_1b","q":"What specifically does the manifest.json file track, as distinct from what an individual agent's own state file tracks?","options":["Which agents are completed, in progress, or not started — a cross-agent status view the coordinator reads on resume.","The detailed findings and raw output produced by each individual agent during its own execution.","The exact prompts each agent was given, for auditing and reproducibility purposes after a crash.","The billing cost attributed to each individual agent's own execution, aggregated together across the whole multi-agent pipeline run."],"correct":0,"explain":"The manifest's specific job is a status roster across agents — completed, in progress, not started — which is what the coordinator reads to decide what to skip on resume. Detailed findings live in each agent's own state file, not the manifest, and neither prompt auditing nor cost tracking is what this file is described as doing."}]}],"reviewQuestions":[{"id":"r_state_persistence_1","q":"Why does structured state persistence require both a per-agent state file and a separate coordinator-level manifest, rather than just one or the other?","options":["It doesn't — either one alone provides everything needed to resume a crashed multi-agent pipeline correctly.","Because the per-agent file holds that agent's own detailed progress, while the manifest holds the cross-agent status view the coordinator needs to decide what to resume.","Because manifest.json is only used for billing purposes and has no functional role in resuming a crashed pipeline.","Because per-agent state files are considered deprecated in favor of the coordinator's manifest, which is intended to fully replace them over time as the system matures."],"correct":1,"explain":"The two serve different, complementary purposes: an agent's own file carries its detailed progress and findings, while the manifest gives the coordinator the cross-agent overview of who's done, who's mid-run, and who hasn't started — resuming correctly needs both. Neither one alone covers what the other provides, the manifest isn't a billing artifact, and there's no indication per-agent files are being phased out."}]},{"id":"mt_attribution_loss","type":"CONCEPTUAL","cluster":"cl_provenance","name":"The attribution loss problem","description":"A summarized claim like 'the market is $3.2B' with no source, name, date, or confidence has lost everything a reader would need to judge it — provenance means keeping the claim bound to its source_url, source_name, publication_date, and a confidence score all the way through synthesis.","core":false,"estMinutes":8,"evidence":["List the fields a claim needs to preserve its provenance through a multi-agent pipeline.","Explain what's lost when a claim is stated as a bare fact with no attribution."],"assessmentPrompt":"A synthesis report states 'The AI music market is estimated at $3.2B' with nothing else attached. What specifically has been lost in getting to that sentence, and what fields would restore it?","standards":["ccaf:d5"],"sources":[{"label":"Guide §12.1 The attribution loss problem","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1434-L1450"}],"centrality":0.04,"layer":9,"x":2010,"y":108,"knowledgePoints":[{"id":"kp_attribution_loss_1","name":"A claim needs its source, date, and confidence attached","keyPrerequisites":["mt_structured_error_coverage"],"teach":"<p class='kp-key'>'The market is estimated at $3.2B' with nothing else attached has lost everything a reader needs to judge it. Provenance means keeping <code>source_url</code>, <code>source_name</code>, <code>publication_date</code>, and a <code>confidence</code> score bound to the claim all the way through synthesis — not just in the first agent that found it.</p>","questions":[{"id":"q_attribution_loss_1a","q":"A synthesis report states 'The AI music market is estimated at $3.2B' with nothing else attached. What has been lost in getting to that sentence?","options":["The source, date, and confidence that would let a reader judge how much to trust the figure and how current it is.","Nothing important — a market-size figure is self-evidently reliable regardless of whether a source is cited alongside it.","Only the exact currency conversion used, since $3.2B could theoretically refer to a non-USD figure without clarification.","The claim itself is likely fabricated whenever attribution is missing, so the number should be assumed to be invented."],"correct":0,"explain":"Provenance is exactly source_url, source_name, publication_date, and confidence — without them, a reader has no way to judge how trustworthy or current the figure actually is, even if the number itself is accurate. It isn't self-evidently reliable without that context, the currency framing isn't the guide's concern here, and missing attribution doesn't mean the claim is necessarily fabricated — it means it's unverifiable as stated."},{"id":"q_attribution_loss_1b","q":"Which set of fields does the guide give as what a well-attributed claim should carry through a synthesis pipeline?","options":["claim and confidence only — source information is optional as long as a confidence score is present.","claim, source_url, source_name, publication_date, and confidence.","claim, author's personal opinion, and a general topic tag describing what the claim relates to.","claim and a boolean flag indicating simply whether any source exists at all, with no further detail."],"correct":1,"explain":"The guide's own worked example pairs the claim with source_url, source_name, publication_date, and confidence — that specific set of fields is what preserves provenance through synthesis. A bare confidence score without an actual source, a subjective opinion field, or a plain existence flag all fall well short of that."}]}],"reviewQuestions":[{"id":"r_attribution_loss_1","q":"Why does the guide treat attribution loss as a distinct problem from a claim simply being factually wrong?","options":["It doesn't, really — attribution loss and factual inaccuracy are essentially the same underlying problem, just described using two different names for it.","Because attribution loss only happens when a claim is already known to be false at the time it's synthesized.","Because a claim can be perfectly accurate and still be unattributed, leaving a reader unable to verify or contextualize it regardless of its truth.","Because attribution only matters for numeric claims; qualitative claims don't suffer from the same kind of loss."],"correct":2,"explain":"A claim's truth and its attribution are independent — a correct figure with no source or date is exactly as unverifiable to the reader as an incorrect one presented the same way, which is why the guide treats attribution as its own concern. It isn't the same problem restated, it isn't tied to a claim already being known false, and it isn't limited to numeric claims."}]},{"id":"mt_conflicting_data","type":"PROCEDURAL","cluster":"cl_provenance","name":"Preserving conflicting values instead of picking one","description":"When two sources disagree, the right move is to keep both values with their sources, dates, and methodology attached and flag `conflict_detected`, letting the coordinator reconcile — not to silently choose the more 'credible-looking' number and footnote the discrepancy.","core":false,"estMinutes":10,"evidence":["Explain why silently picking one of two conflicting values and footnoting it is worse than preserving both.","List what should accompany each of two conflicting values so the coordinator can actually reconcile them."],"assessmentPrompt":"Two sources give different market-share figures. An analysis agent picks the one from the source it judges more credible and adds a footnote about the other. What does the guide say to do instead, and why does that serve the coordinator better?","standards":["ccaf:d5"],"sources":[{"label":"Guide §12.2 Preserving conflicting values instead of picking one","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1451-L1478"}],"centrality":0.02,"layer":10,"x":2220,"y":46,"knowledgePoints":[{"id":"kp_conflicting_data_1","name":"Preserve both values; don't pick a winner silently","keyPrerequisites":["mt_attribution_loss"],"teach":"<p class='kp-key'>When two sources disagree, the right move is keeping both values with their source, date, and methodology attached, flagging <code>conflict_detected</code>, and letting the coordinator reconcile — not silently choosing the 'more credible-looking' number and footnoting the other.</p>","questions":[{"id":"q_conflicting_data_1a","q":"Two sources give different market-share figures for the same metric. An analysis agent picks the one it judges more credible and adds a footnote about the other. What does the guide say to do instead?","options":["Average the two figures numerically and report the average as the single value, without noting a conflict occurred.","Discard both values entirely and report that the metric is unknown, since two disagreeing sources cancel each other out.","Keep only the more recently published of the two figures, treating recency as the deciding factor without further annotation.","Preserve both values with their sources, dates, and methodology, flag conflict_detected, and let the coordinator reconcile them."],"correct":3,"explain":"The guide is explicit that both values should be preserved with their full context and flagged as conflicting, with reconciliation left to the coordinator, which has broader context than the analysis agent does. Averaging invents a number neither source actually reported, discarding both throws away real information, and picking purely by recency is still a silent, single-agent judgment call the guide argues against."},{"id":"q_conflicting_data_1b","q":"Why does silently picking one of two conflicting values and footnoting the other serve the coordinator worse than preserving both explicitly?","options":["Because the coordinator, with broader context, may be better positioned to reconcile the conflict than the analysis agent was.","It doesn't serve worse — footnoting a discrepancy is functionally identical to flagging conflict_detected on both values.","Because footnotes are technically unsupported in most structured JSON output formats used by extraction pipelines.","Because silently picking a value is only a problem when the two figures differ by more than 50 percent."],"correct":0,"explain":"The coordinator sits above individual analysis agents with more context to weigh which source is more likely right — pre-emptively picking one value at the analysis stage takes that decision out of the hands of whoever is best positioned to make it. A footnote buried under a chosen value is not equivalent to an explicit conflict flag on both, this isn't a JSON formatting limitation, and the guide draws no percentage threshold for when preservation matters."}]}],"reviewQuestions":[{"id":"r_conflicting_data_1","q":"What information should accompany each of two conflicting values so a coordinator can actually attempt reconciliation, rather than just seeing that a conflict exists?","options":["Nothing beyond the conflict_detected flag itself; the two raw numbers alone are sufficient for reconciliation.","Each value's source, publication date, and methodology, so differences in those can explain the discrepancy.","Only the more recent of the two values needs any additional detail attached; the older one can stand alone.","The personal opinion of whichever agent first discovered each of the two conflicting values."],"correct":1,"explain":"Source, date, and methodology are exactly what let a reconciler figure out whether the discrepancy comes from measuring different time periods, different definitions, or different data collection approaches — a bare conflict flag with no supporting detail gives no way to actually resolve anything. Attaching detail to only one of the two values leaves the comparison lopsided, and an agent's personal opinion isn't the kind of evidence reconciliation depends on."}]},{"id":"mt_dates_render_content","type":"FACTUAL","cluster":"cl_provenance","name":"Dates resolve conflicts; format follows content","description":"Two figures that look contradictory are often just measured a year apart — attaching dates can turn 'contradiction' into 'plausible growth trend' — and the final rendering should match content type: tables for financial data, prose for analysis, structured lists for technical findings, chronological order for time series.","core":false,"estMinutes":8,"evidence":["Explain how attaching a date to each of two conflicting figures can resolve the apparent contradiction.","Match a content type (financial data, time series, technical findings) to the rendering format that suits it."],"assessmentPrompt":"Source A (dated 2023) says 10% and source B (dated 2024) says 15%, and a draft report calls this a contradiction. What does adding the dates change about that framing?","standards":["ccaf:d5"],"sources":[{"label":"Guide §12.3-12.4 Dates resolve conflicts; format follows content","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1479-L1497"}],"centrality":0,"layer":11,"x":2430,"y":46,"knowledgePoints":[{"id":"kp_dates_render_1","name":"A date can turn a contradiction into a trend","keyPrerequisites":["mt_conflicting_data"],"teach":"<p class='kp-key'>'Source A says 10%, source B says 15%. Contradiction' becomes 'Source A (2023) says 10%, source B (2024) says 15%. Likely +5% growth over a year' once dates are attached — the same numbers, radically different interpretation.</p><p>Rendering should also match content type: tables for financial data, prose for analysis, structured lists for technical findings, chronological order for time series — forcing everything into one format loses the structure that made the content legible in the first place.</p>","questions":[{"id":"q_dates_render_1a","q":"Source A (dated 2023) says 10% and source B (dated 2024) says 15%, and a draft report calls this a contradiction. What does adding the dates change about that framing?","options":["Nothing changes — a numeric disagreement between two sources is a contradiction regardless of when each was published.","It proves source B is more accurate than source A, since more recent data is always the more reliable of the two.","It reframes the disagreement as a plausible year-over-year growth trend rather than two sources disputing the same fact.","It means one of the two figures must be an error, and the dates only help identify which one to discard."],"correct":2,"explain":"With the dates attached, 10% in 2023 and 15% in 2024 reads as a plausible growth trend across a year rather than two sources disagreeing about the same point in time — that's the reframing the guide's example is built around. It doesn't automatically make the newer source more accurate in general, and it doesn't mean one figure is simply wrong — both can be correct for their own respective dates."},{"id":"q_dates_render_1b","q":"A final report presents financial figures, a time series, and technical findings all as the same block of continuous prose. What does the guide say this loses?","options":["Nothing is lost; prose is treated as the universally preferred format for every kind of content in a synthesis report.","Only the time series loses meaningful structure; financial and technical content read equally well as prose.","The loss only really matters for reports intended for a non-technical audience; expert readers are assumed to be unaffected by whatever format is chosen.","The structure suited to each content type — tables for financial data, chronological order for time series, lists for technical findings."],"correct":3,"explain":"The guide's rule is to render by content type — tables, chronological ordering, and structured lists each fit a different kind of content, and flattening everything into prose sacrifices exactly that fit. It isn't lossless, the loss isn't limited to just the time series, and it isn't an audience-specific concern — the mismatch between content and format applies regardless of who's reading."}]}],"reviewQuestions":[{"id":"r_dates_render_1","q":"Why does the guide pair 'include dates' with 'render by content type' as related ideas rather than two unrelated tips?","options":["Both are about presenting synthesized information in a way that preserves the structure needed to interpret it correctly.","They aren't actually related; they happen to appear in the same chapter purely by coincidence of the guide's organization.","Both apply exclusively to time-series data and have no bearing on any other kind of content in a report.","Both are really about attribution specifically, with content-type rendering being a restatement of source-tracking."],"correct":0,"explain":"Both ideas are about preserving the structure a reader needs to interpret content correctly — a date resolves an apparent numeric contradiction, and a fitting format (table, list, chronological order) preserves the shape financial, technical, or time-series content actually has. They aren't a coincidental pairing, they aren't limited to time-series data alone, and content-type rendering is a distinct idea from source attribution, not a restatement of it."}]},{"id":"mt_tool_selection_investigation","type":"PROCEDURAL","cluster":"cl_builtin_tools_guide","name":"Choosing the right built-in tool, incrementally","description":"Glob finds files by pattern, Grep searches within them, Read loads one fully, Write creates new files, Edit changes an existing one precisely, and Bash runs shell commands — and a good investigation alternates Grep (find entry points, then usages) with Read rather than reading everything up front.","core":false,"estMinutes":10,"evidence":["Match a task (find files by pattern, search within files, run tests) to the built-in tool that handles it.","Describe the Grep-then-Read cycle an incremental investigation follows."],"assessmentPrompt":"An investigation needs to find every place a function is called before reading those call sites. Which tool finds the call sites, which tool reads them, and in what order should they run?","standards":["ccaf:d2"],"sources":[{"label":"Guide §13.1-13.2 Choosing the right built-in tool, incrementally","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1500-L1522"}],"centrality":0.02,"layer":5,"x":1170,"y":604,"knowledgePoints":[{"id":"kp_tool_selection_1","name":"Glob finds, Grep searches, Read loads, and the Grep-then-Read cycle","keyPrerequisites":["mt_planning_mode_guide"],"teach":"<p class='kp-key'>Glob finds files by name/pattern (<code>**/*.test.tsx</code>). Grep searches within files (a function name, an error message, an import). Read loads one file in full. Write creates a new file; Edit changes an existing one precisely; Bash runs shell commands (git, npm, tests, builds).</p><p>An incremental investigation alternates them rather than reading everything up front: Grep for entry points, Read the files found, Grep for usages, Read the consumers, repeat until the picture is complete.</p>","questions":[{"id":"q_tool_selection_1a","q":"An investigation needs to find every place a function is called before reading those call sites in full. Which tool finds the call sites, which reads them, and in what order should they run?","options":["Grep first to locate the call sites by searching for the function's usage, then Read each of those files afterward.","Read first to load the whole codebase, then Grep within the loaded content to locate the specific call sites afterward.","Glob first to find every file in the project by pattern, then Bash to grep through them via a shell command instead.","Edit first to mark the call sites for review, then Read only the files that were successfully marked by that step."],"correct":0,"explain":"This is exactly the incremental pattern: Grep first to search within files for where the function is actually called, then Read those specific files once they're identified — searching before loading avoids reading the whole codebase up front. Loading everything with Read before searching defeats the purpose of incremental investigation, routing through Bash for grepping bypasses the tool built for it, and Edit is for making changes, not for locating anything."},{"id":"q_tool_selection_1b","q":"Which built-in tool is the right choice for running the project's test suite as part of an investigation?","options":["Grep, since running tests is fundamentally a search operation across the codebase's test files.","Bash, since running a shell command like the test runner is exactly what it's for.","Glob, since finding the test files by pattern is equivalent to actually executing them.","Write, since running the test suite typically requires generating a new output file first."],"correct":1,"explain":"Bash is explicitly the tool for running shell commands — git, npm, builds, and test runs all fall under that category. Grep and Glob are search and file-discovery tools, not execution tools, and Write creates file content rather than running anything."}]}],"reviewQuestions":[{"id":"r_tool_selection_1","q":"Why does the guide recommend an incremental Grep-then-Read cycle over reading every potentially relevant file up front?","options":["Because Read is technically incapable of loading more than one file within a single investigation session.","Because Grep is faster than Read in absolute terms, making the ordering purely a matter of execution speed.","Because reading everything up front wastes context on files that later turn out to be irrelevant to the actual investigation.","There's no real reason given for the ordering; it's presented as a stylistic preference with no meaningful bearing on context usage or outcome quality."],"correct":2,"explain":"Reading broadly before knowing what's actually relevant risks filling context with files that turn out not to matter — searching first narrows down to what's worth reading, which is the whole rationale for going incrementally. Read can load multiple files across an investigation, the reasoning isn't about raw execution speed, and the ordering is presented as a real strategy, not an arbitrary stylistic choice."}]},{"id":"mt_edit_fallback_pattern","type":"PROCEDURAL","cluster":"cl_builtin_tools_guide","name":"Read + Write as Edit's fallback","description":"When Edit fails because its target text isn't unique in the file, the fallback is to Read the full file, modify the content programmatically, and Write the whole updated version back — trading precision for reliability when a unique match can't be found.","core":false,"estMinutes":6,"evidence":["State the three-step fallback used when Edit fails on a non-unique match.","Explain what this fallback trades away compared to a successful Edit."],"assessmentPrompt":"An Edit call fails because its target snippet appears four times in the file. What's the fallback sequence, and what does it give up compared to Edit succeeding on the first try?","standards":["ccaf:d2"],"sources":[{"label":"Guide §13.3 Read + Write as Edit's fallback","href":"https://github.com/paullarionov/claude-certified-architect/blob/main/guide_en.md#L1523-L1530"}],"centrality":0,"layer":6,"x":1380,"y":418,"knowledgePoints":[{"id":"kp_edit_fallback_1","name":"Read, modify, Write — when Edit can't find a unique match","keyPrerequisites":["mt_tool_selection_investigation"],"teach":"<p class='kp-key'>Edit fails when its target text isn't unique in the file. The fallback: Read the full file content, modify it programmatically, and Write the entire updated version back — trading Edit's surgical precision for a reliable path forward when a unique match can't be found.</p>","questions":[{"id":"q_edit_fallback_1a","q":"An Edit call fails because its target snippet appears four times in the file. What's the fallback sequence, and what does it give up compared to Edit succeeding on the first try?","options":["Retry the same Edit call repeatedly until one of the four matches happens to be selected at random by the tool.","Switch to Bash and use a shell command like sed to perform the replacement, bypassing Read and Write entirely.","Delete the file entirely with a Bash shell command and recreate it from scratch using Write, discarding everything in it that isn't directly part of the intended change.","Read the full file, modify the content programmatically to target the correct occurrence, then Write the whole file back — trading precision for reliability."],"correct":3,"explain":"The guide's fallback is specifically Read the full file, make the change in the loaded content, then Write the whole file back — a broader, less surgical operation than a successful Edit, but one that doesn't depend on a unique text match. Retrying the identical Edit call doesn't change the underlying non-uniqueness problem, routing through a shell command bypasses the pattern the guide actually describes, and recreating the file from scratch risks losing unrelated content Edit would have left untouched."},{"id":"q_edit_fallback_1b","q":"Why does Edit specifically fail when its target text isn't unique within the file, rather than just picking the first occurrence?","options":["Because Edit is designed to make a precise, unambiguous change, and a non-unique match means it can't be sure which occurrence was intended.","Edit doesn't actually fail in that case; it always applies the change to the first matching occurrence by default.","Because Edit only supports files under a certain length limit, and non-unique matches of this kind typically only ever occur in longer files.","Because Edit requires the target text to be specified as a line number rather than as a text snippet in every case."],"correct":0,"explain":"Edit's whole value is precise, unambiguous replacement — when the target text matches more than one location, it has no reliable way to know which one was actually intended, so it fails rather than silently guessing. It doesn't default to the first match, its failure mode has nothing to do with file length, and it isn't restricted to line-number-based targeting."}]}],"reviewQuestions":[{"id":"r_edit_fallback_1","q":"What does the Read-then-Write fallback trade away compared to a successful Edit call?","options":["It trades away nothing; the fallback is strictly equivalent in every respect to a successful Edit call on the same file.","It trades away Edit's surgical precision — a full file rewrite is a broader operation than an in-place, uniquely targeted change.","It trades away the ability to make any further changes to that same file for the remainder of the session.","It trades away the file's version-control history entirely, since Write is treated by the underlying system as creating a brand-new, untracked file."],"correct":1,"explain":"Rewriting the whole file via Read-then-Write is a broader operation than Edit's precise, uniquely targeted change — that's the real trade-off the fallback makes to get around a non-unique match. It isn't equivalent to a successful Edit, it doesn't block further edits to the file afterward, and it doesn't affect version-control history any differently than Edit would."}]}],"dependencies":[{"topicId":"mt_message_roles","prerequisiteId":"mt_api_request_structure","strength":"hard","reason":"The roles are what populate the messages array this topic's request shape defines."},{"topicId":"mt_stop_reason","prerequisiteId":"mt_message_roles","strength":"hard","reason":"stop_reason describes how an assistant turn ends, so reading it presupposes knowing which role produces that turn."},{"topicId":"mt_context_window_problems","prerequisiteId":"mt_message_roles","strength":"hard","reason":"The context window is built from the same messages array; its failure modes only make sense once you know what's accumulating inside it."},{"topicId":"mt_tool_use_basics","prerequisiteId":"mt_stop_reason","strength":"hard","reason":"tool_use is one of the four stop_reason values — the mechanism can't be explained without the signal that announces it."},{"topicId":"mt_tool_description_selection","prerequisiteId":"mt_tool_use_basics","strength":"hard","reason":"Description-driven selection only matters once a tool call is understood as a structured request the model chooses to emit."},{"topicId":"mt_tool_choice_modes","prerequisiteId":"mt_tool_use_basics","strength":"hard","reason":"tool_choice controls whether and which tool_use happens, so it presupposes what tool_use is."},{"topicId":"mt_schema_design_errors","prerequisiteId":"mt_tool_use_basics","strength":"hard","reason":"Schema design shapes a tool_use call's input_schema, and syntax/semantic errors are properties of that same call."},{"topicId":"mt_agentic_loop_guide","prerequisiteId":"mt_stop_reason","strength":"hard","reason":"The loop's only branch point is checking stop_reason for tool_use versus end_turn."},{"topicId":"mt_agent_definition","prerequisiteId":"mt_agentic_loop_guide","strength":"hard","reason":"AgentDefinition configures which loop an agent runs — system_prompt and allowed_tools only matter against that loop."},{"topicId":"mt_hub_and_spoke_guide","prerequisiteId":"mt_agent_definition","strength":"hard","reason":"A coordinator and its subagents are each just an AgentDefinition with a different allowed_tools list."},{"topicId":"mt_task_tool_context","prerequisiteId":"mt_hub_and_spoke_guide","strength":"hard","reason":"The Task tool is how the hub actually spawns a spoke — it only makes sense against that topology."},{"topicId":"mt_hooks_vs_prompts_guide","prerequisiteId":"mt_agentic_loop_guide","strength":"hard","reason":"Hooks intercept specific points in the same loop — PreToolUse and PostToolUse are loop steps."},{"topicId":"mt_mcp_overview","prerequisiteId":"mt_tool_use_basics","strength":"hard","reason":"MCP tools reach the model through the same tool_use mechanism; MCP is a way of supplying more of them."},{"topicId":"mt_mcp_server_config","prerequisiteId":"mt_mcp_overview","strength":"hard","reason":"Configuring a server presupposes knowing what a server provides."},{"topicId":"mt_mcp_iserror","prerequisiteId":"mt_mcp_overview","strength":"hard","reason":"isError is a field on a specific MCP tool's response, so the tool/resource/server model comes first."},{"topicId":"mt_mcp_resources","prerequisiteId":"mt_mcp_overview","strength":"hard","reason":"Resources are one of the three primitives the MCP overview introduces."},{"topicId":"mt_claudemd_hierarchy_guide","prerequisiteId":"mt_message_roles","strength":"hard","reason":"CLAUDE.md behaves like a persistent, layered system prompt — the system role has to be understood first."},{"topicId":"mt_path_imports","prerequisiteId":"mt_claudemd_hierarchy_guide","strength":"hard","reason":"@path exists only to keep the CLAUDE.md hierarchy from duplicating content across its levels."},{"topicId":"mt_claude_rules_guide","prerequisiteId":"mt_claudemd_hierarchy_guide","strength":"hard","reason":".claude/rules/ is presented as an alternative organization to the same hierarchy."},{"topicId":"mt_commands_skills_guide","prerequisiteId":"mt_claudemd_hierarchy_guide","strength":"hard","reason":"Commands and skills are contrasted against CLAUDE.md's always-loaded model — on-demand versus always-on is the point."},{"topicId":"mt_planning_mode_guide","prerequisiteId":"mt_agentic_loop_guide","strength":"hard","reason":"Planning mode changes what the agentic loop is allowed to do — investigate only, or investigate and act."},{"topicId":"mt_compact_memory","prerequisiteId":"mt_context_window_problems","strength":"hard","reason":"/compact is the built-in mitigation for the exact summarization-loss problem that topic describes."},{"topicId":"mt_cli_ci_sessions","prerequisiteId":"mt_planning_mode_guide","strength":"hard","reason":"Headless CI execution is direct execution taken to its extreme — no human ever sees a plan to approve."},{"topicId":"mt_few_shot_patterns","prerequisiteId":"mt_schema_design_errors","strength":"hard","reason":"The format-normalization rules here are the free-text extension of the same nullable/enum discipline schema design teaches."},{"topicId":"mt_explicit_criteria","prerequisiteId":"mt_few_shot_patterns","strength":"hard","reason":"Explicit criteria make explicit the rule an example was only demonstrating implicitly."},{"topicId":"mt_prompt_chaining_vs_dynamic","prerequisiteId":"mt_explicit_criteria","strength":"hard","reason":"Each link in a chain is only reliable if it's given the same explicit, non-vague criteria this topic argues for."},{"topicId":"mt_interview_pattern_guide","prerequisiteId":"mt_explicit_criteria","strength":"hard","reason":"Asking clarifying questions is how you obtain explicit criteria when the requester hasn't supplied them yet."},{"topicId":"mt_validation_retry_selfcorrect","prerequisiteId":"mt_schema_design_errors","strength":"hard","reason":"Retry-with-feedback is the runtime backstop for exactly the semantic errors schema design cannot prevent."},{"topicId":"mt_batches_overview","prerequisiteId":"mt_api_request_structure","strength":"hard","reason":"A batch request is a list of the same params object Chapter 1 defines, submitted asynchronously instead of one at a time."},{"topicId":"mt_batches_custom_id_failures","prerequisiteId":"mt_batches_overview","strength":"hard","reason":"custom_id and failure handling are mechanics of the same batch object the overview introduces."},{"topicId":"mt_batches_sla","prerequisiteId":"mt_batches_custom_id_failures","strength":"hard","reason":"Planning a submission window presupposes knowing how failures get identified and resubmitted within it."},{"topicId":"mt_decomposition_strategies","prerequisiteId":"mt_prompt_chaining_vs_dynamic","strength":"hard","reason":"Fixed pipelines and dynamic decomposition are the same fixed-vs-adaptive split, generalized from a prompt chain to a whole task plan."},{"topicId":"mt_multipass_review","prerequisiteId":"mt_decomposition_strategies","strength":"hard","reason":"Multi-pass review is a fixed pipeline — per-file passes then an integration pass — worked through as a concrete example."},{"topicId":"mt_escalation_triggers","prerequisiteId":"mt_hooks_vs_prompts_guide","strength":"hard","reason":"Reliable triggers are the deterministic-rule side of the hooks-vs-prompts split, applied to when a human takes over."},{"topicId":"mt_escalation_patterns","prerequisiteId":"mt_escalation_triggers","strength":"hard","reason":"Deciding when and how to escalate only makes sense once what counts as a trigger is settled."},{"topicId":"mt_structured_handoff","prerequisiteId":"mt_escalation_patterns","strength":"hard","reason":"The handoff protocol is what the agent hands the human once one of these escalation patterns has fired."},{"topicId":"mt_confidence_calibration","prerequisiteId":"mt_escalation_triggers","strength":"hard","reason":"Confidence-based routing is the alternative the triggers topic offers in place of the unreliable self-rated-confidence trigger it rules out."},{"topicId":"mt_error_categories","prerequisiteId":"mt_mcp_iserror","strength":"hard","reason":"The error taxonomy generalizes the single isError flag into the transient/validation/business/permission split multi-agent systems need."},{"topicId":"mt_error_antipatterns","prerequisiteId":"mt_error_categories","strength":"hard","reason":"Each anti-pattern is a specific mishandling of one of the categories just defined."},{"topicId":"mt_structured_error_coverage","prerequisiteId":"mt_error_antipatterns","strength":"hard","reason":"The structured-error shape is what the anti-patterns topic argues you replace a generic status with."},{"topicId":"mt_fact_extraction_block","prerequisiteId":"mt_context_window_problems","strength":"hard","reason":"A dedicated facts block is a direct countermeasure for the progressive-summarization data loss this topic describes."},{"topicId":"mt_trim_tool_results","prerequisiteId":"mt_fact_extraction_block","strength":"hard","reason":"Both apply the same discipline — keep only what's load-bearing in context — first to derived facts, then to raw tool output."},{"topicId":"mt_position_aware_input","prerequisiteId":"mt_trim_tool_results","strength":"hard","reason":"Once results are trimmed to what matters, where that surviving content sits in the prompt is the next lever."},{"topicId":"mt_scratchpad_delegation","prerequisiteId":"mt_position_aware_input","strength":"hard","reason":"A scratchpad is where position-aware, load-bearing findings get written down so they survive outside the prompt entirely."},{"topicId":"mt_state_persistence","prerequisiteId":"mt_scratchpad_delegation","strength":"hard","reason":"Structured state files generalize a single scratchpad into a manifest coordinating several agents' saved progress."},{"topicId":"mt_attribution_loss","prerequisiteId":"mt_structured_error_coverage","strength":"hard","reason":"Coverage annotations already track which parts of a synthesis are trustworthy; provenance tracks where each individual claim inside it came from."},{"topicId":"mt_conflicting_data","prerequisiteId":"mt_attribution_loss","strength":"hard","reason":"You can only detect a conflict between two sources if each value already carries the attribution the previous topic argues for."},{"topicId":"mt_dates_render_content","prerequisiteId":"mt_conflicting_data","strength":"hard","reason":"A date is often the fact that resolves an apparent conflict into an explainable trend instead of a contradiction."},{"topicId":"mt_tool_selection_investigation","prerequisiteId":"mt_planning_mode_guide","strength":"hard","reason":"Incremental Grep-then-Read investigation is what planning mode's read-only phase actually does, tool by tool."},{"topicId":"mt_edit_fallback_pattern","prerequisiteId":"mt_tool_selection_investigation","strength":"hard","reason":"The fallback only comes up once Edit has been chosen and attempted as part of that same investigation-driven workflow."}],"encompassings":[{"topicId":"mt_message_roles","encompassedId":"mt_api_request_structure","coverage":0.5,"reason":"Constructing a roled message presupposes the request envelope it sits inside."},{"topicId":"mt_stop_reason","encompassedId":"mt_message_roles","coverage":0.35,"reason":"Reading a response's stop_reason means reading which role produced the turn it's attached to."},{"topicId":"mt_tool_use_basics","encompassedId":"mt_stop_reason","coverage":0.7,"reason":"Every tool_use exercise re-triggers reading the stop_reason value that announces it."},{"topicId":"mt_tool_choice_modes","encompassedId":"mt_tool_use_basics","coverage":0.55,"reason":"Choosing among tool_choice values requires re-exercising what a tool_use call is."},{"topicId":"mt_schema_design_errors","encompassedId":"mt_tool_use_basics","coverage":0.45,"reason":"Designing a schema is designing the input_schema of a tool_use call."},{"topicId":"mt_agentic_loop_guide","encompassedId":"mt_stop_reason","coverage":0.75,"reason":"The loop's single branch point is the stop_reason check, practiced every time the loop is traced."},{"topicId":"mt_agent_definition","encompassedId":"mt_agentic_loop_guide","coverage":0.4,"reason":"Configuring an agent means configuring which loop it runs."},{"topicId":"mt_hub_and_spoke_guide","encompassedId":"mt_agent_definition","coverage":0.5,"reason":"A coordinator and a subagent are both AgentDefinitions, re-exercising the same fields."},{"topicId":"mt_task_tool_context","encompassedId":"mt_hub_and_spoke_guide","coverage":0.65,"reason":"Spawning a subagent via Task is the hub-and-spoke topology in action."},{"topicId":"mt_hooks_vs_prompts_guide","encompassedId":"mt_agentic_loop_guide","coverage":0.4,"reason":"A hook intercepts a specific step of the same loop this topic re-exercises."},{"topicId":"mt_mcp_server_config","encompassedId":"mt_mcp_overview","coverage":0.5,"reason":"Configuring a server re-exercises what tools/resources/prompts a server provides."},{"topicId":"mt_mcp_iserror","encompassedId":"mt_mcp_overview","coverage":0.4,"reason":"isError is read on the same tool-call shape the overview introduces."},{"topicId":"mt_mcp_resources","encompassedId":"mt_mcp_overview","coverage":0.35,"reason":"Resources are one of the three primitives being re-exercised."},{"topicId":"mt_path_imports","encompassedId":"mt_claudemd_hierarchy_guide","coverage":0.5,"reason":"Using @path means placing content correctly within the same three-level hierarchy."},{"topicId":"mt_claude_rules_guide","encompassedId":"mt_claudemd_hierarchy_guide","coverage":0.6,"reason":"Choosing .claude/rules/ over a hierarchy level requires knowing the hierarchy it's an alternative to."},{"topicId":"mt_commands_skills_guide","encompassedId":"mt_claudemd_hierarchy_guide","coverage":0.35,"reason":"The always-on/on-demand contrast re-exercises what CLAUDE.md's always-loaded model actually is."},{"topicId":"mt_planning_mode_guide","encompassedId":"mt_agentic_loop_guide","coverage":0.35,"reason":"Deciding what the loop may do re-exercises the loop's own step structure."},{"topicId":"mt_cli_ci_sessions","encompassedId":"mt_planning_mode_guide","coverage":0.5,"reason":"Running headless is a specific point on the same direct-execution-vs-planning spectrum."},{"topicId":"mt_explicit_criteria","encompassedId":"mt_few_shot_patterns","coverage":0.45,"reason":"An explicit criterion is what a well-chosen example was already implicitly demonstrating."},{"topicId":"mt_prompt_chaining_vs_dynamic","encompassedId":"mt_explicit_criteria","coverage":0.35,"reason":"Each chained step still needs the same explicit, falsifiable criteria."},{"topicId":"mt_validation_retry_selfcorrect","encompassedId":"mt_schema_design_errors","coverage":0.5,"reason":"A retry loop is exercised against the same nullable/required schema discipline."},{"topicId":"mt_batches_custom_id_failures","encompassedId":"mt_batches_overview","coverage":0.6,"reason":"custom_id and failure handling are mechanics of the same batch shape the overview introduces."},{"topicId":"mt_batches_sla","encompassedId":"mt_batches_custom_id_failures","coverage":0.45,"reason":"Planning a window re-exercises how failures are identified and resubmitted within it."},{"topicId":"mt_decomposition_strategies","encompassedId":"mt_prompt_chaining_vs_dynamic","coverage":0.55,"reason":"Fixed-vs-adaptive is the same distinction, generalized from a prompt chain to a task plan."},{"topicId":"mt_multipass_review","encompassedId":"mt_decomposition_strategies","coverage":0.6,"reason":"Multi-pass review is a worked instance of the fixed-pipeline half of that distinction."},{"topicId":"mt_escalation_patterns","encompassedId":"mt_escalation_triggers","coverage":0.6,"reason":"Applying an escalation pattern re-exercises which trigger fired."},{"topicId":"mt_structured_handoff","encompassedId":"mt_escalation_patterns","coverage":0.4,"reason":"Building a handoff re-exercises which escalation pattern produced it."},{"topicId":"mt_confidence_calibration","encompassedId":"mt_escalation_triggers","coverage":0.4,"reason":"Confidence routing re-exercises why self-rated confidence was ruled out as a trigger."},{"topicId":"mt_error_antipatterns","encompassedId":"mt_error_categories","coverage":0.5,"reason":"Spotting an anti-pattern requires re-classifying which error category is being mishandled."},{"topicId":"mt_structured_error_coverage","encompassedId":"mt_error_antipatterns","coverage":0.55,"reason":"Building a structured error re-exercises exactly what the anti-patterns were missing."},{"topicId":"mt_trim_tool_results","encompassedId":"mt_fact_extraction_block","coverage":0.3,"reason":"Both re-exercise the same keep-only-what's-load-bearing discipline."},{"topicId":"mt_position_aware_input","encompassedId":"mt_trim_tool_results","coverage":0.3,"reason":"Positioning trimmed content still assumes the trimming step already happened."},{"topicId":"mt_scratchpad_delegation","encompassedId":"mt_position_aware_input","coverage":0.35,"reason":"Writing a scratchpad re-exercises which findings are worth surfacing prominently."},{"topicId":"mt_state_persistence","encompassedId":"mt_scratchpad_delegation","coverage":0.5,"reason":"A state manifest is a scratchpad's structure applied across several agents."},{"topicId":"mt_conflicting_data","encompassedId":"mt_attribution_loss","coverage":0.6,"reason":"Detecting a conflict re-exercises the same source/date/confidence attribution fields."},{"topicId":"mt_dates_render_content","encompassedId":"mt_conflicting_data","coverage":0.45,"reason":"Resolving a conflict with a date re-exercises how conflicting values are represented."},{"topicId":"mt_edit_fallback_pattern","encompassedId":"mt_tool_selection_investigation","coverage":0.5,"reason":"The fallback is reached only after the same Grep/Read investigation this topic covers has been run."}],"layout":{"width":2550,"height":650},"practiceTest":{"storageKey":"ccaf_quiz_guide_practice_test_v1","questionTopics":{"pt001":"mt_conflicting_data","pt002":"mt_hub_and_spoke_guide","pt003":"mt_structured_error_coverage","pt004":"mt_hub_and_spoke_guide","pt005":"mt_structured_error_coverage","pt006":"mt_error_antipatterns","pt007":"mt_tool_description_selection","pt008":"mt_hub_and_spoke_guide","pt009":"mt_structured_error_coverage","pt010":"mt_tool_description_selection","pt011":"mt_hub_and_spoke_guide","pt012":"mt_error_categories","pt013":"mt_position_aware_input","pt014":"mt_context_window_problems","pt015":"mt_agent_definition","pt016":"mt_cli_ci_sessions","pt017":"mt_multipass_review","pt018":"mt_batches_overview","pt019":"mt_batches_overview","pt020":"mt_few_shot_patterns","pt021":"mt_batches_overview","pt022":"mt_explicit_criteria","pt023":"mt_explicit_criteria","pt026":"mt_cli_ci_sessions","pt027":"mt_multipass_review","pt029":"mt_explicit_criteria","pt030":"mt_batches_overview","pt031":"mt_few_shot_patterns","pt032":"mt_planning_mode_guide","pt033":"mt_commands_skills_guide","pt034":"mt_planning_mode_guide","pt035":"mt_planning_mode_guide","pt036":"mt_commands_skills_guide","pt037":"mt_claudemd_hierarchy_guide","pt038":"mt_commands_skills_guide","pt039":"mt_commands_skills_guide","pt040":"mt_claude_rules_guide","pt041":"mt_commands_skills_guide","pt042":"mt_claude_rules_guide","pt043":"mt_planning_mode_guide","pt044":"mt_mcp_server_config","pt045":"mt_planning_mode_guide","pt046":"mt_tool_description_selection","pt047":"mt_few_shot_patterns","pt048":"mt_decomposition_strategies","pt049":"mt_escalation_triggers","pt050":"mt_escalation_triggers","pt051":"mt_hooks_vs_prompts_guide","pt052":"mt_validation_retry_selfcorrect","pt054":"mt_fact_extraction_block","pt055":"mt_interview_pattern_guide","pt056":"mt_tool_description_selection","pt057":"mt_tool_description_selection","pt058":"mt_agentic_loop_guide","pt059":"mt_trim_tool_results","pt060":"mt_few_shot_patterns","pt061":"mt_hooks_vs_prompts_guide","pt062":"mt_error_categories","pt063":"mt_conflicting_data","pt064":"mt_api_request_structure","pt065":"mt_fact_extraction_block","pt066":"mt_fact_extraction_block","pt067":"mt_api_request_structure","pt069":"mt_hooks_vs_prompts_guide","pt070":"mt_few_shot_patterns","pt071":"mt_message_roles","pt074":"mt_interview_pattern_guide","pt075":"mt_hooks_vs_prompts_guide","pt076":"mt_interview_pattern_guide"}}};
